# Dentobot Logbook — 2026-08-25

## 15:36 IST — Embedded Slicer tests extracted

- Mechanically moved the 6,920-line `DENTOWorkflowTest` implementation into
  `dentobot_workflow/slicer_tests.py`. The public
  `DENTOWorkflow.DENTOWorkflowTest` remains a thin multiple-inheritance proxy,
  so named direct tests and Slicer's generic test discovery keep the same API.
- The test module is installed with the scripted extension but imported only
  at the compatibility boundary. Production Widget/Logic method bodies were
  not edited. The main entrypoint fell from 39,889 to 32,976 lines.
- Verification: AST parsing and `git diff --check` passed; the host suite
  returned `94 passed`; a containerized Slicer direct test printed
  `DENTOBOT_EXTERNAL_TEST_PROXY_PASS`. No robot or hardware action ran.

## 15:31 IST — Modularization baseline checkpoint

- Began the approved economical decomposition of the 39,889-line scripted
  module. The compatibility boundary is fixed: public Widget/Logic/Test imports,
  MRML parameter roles, `.dentocase`, ROS topics, geometry algorithms, and
  simulation-only behavior must remain unchanged.
- Audited the starting tree before relocation: 18,434 Widget lines/494 methods,
  14,171 Logic lines/355 methods, 6,920 embedded Slicer-test lines, and 94 host
  tests. The current Viewer, Step 6, launcher, case-schema compatibility, and
  documentation changes remain part of the baseline rather than being
  discarded or silently mixed with extraction logic.
- Baseline verification: `pytest -q -p no:cacheprovider Testing` returned
  `94 passed`; `git diff --check` passed. No ROS controller, hardware motion,
  drilling, or patient-facing operation was started.

## 15:23 IST — False schema-v1 ROI mismatch diagnosed and fixed

- Inspected the user-saved
  `SampleStudy1/dentobot-case-step6.dentocase` read-only inside the container;
  did not load it into the active ROS-enabled Slicer process. The archive has
  the required six members, reports no serialized ROS runtime/active flag, and
  had no Step 6 freshness issue at save.
- Confirmed the saved `targetToothBoundsRoi` world-RAS center exactly matches
  the embedded Markups ROI JSON after LPS-to-RAS interpretation. The package
  was internally consistent rather than geometrically damaged.
- Root cause: schema-v1 node records recently gained append-only target/jaw
  lineage attributes. The prior comparator required identical dictionary key
  sets, so a package created before those fields existed failed against a
  newer reconstructed MRML record at the first ROI with additional metadata.
- Added pure `lineage_snapshot_matches` and mismatch-path helpers in
  `DENTOCaseBundle.py`; routed loaded-scene validation through them. Saved
  fields remain mandatory, numbers remain within `1e-6`, lists remain exact,
  additional reconstructed dictionary fields are accepted, and a real
  mismatch now identifies its nested path. Added old-package/new-reader,
  missing-field, and changed-coordinate regressions.
- Verification and diagnostics executed:
  - the initial default `python3 -m pytest` attempt did not start because that
    Miniconda interpreter has no `pytest`;
  - the first system-Python focused run exposed only mismatch-path formatting
    (`.[0]`), which was corrected; its bytecode compile attempt also met an
    existing unwritable `__pycache__`, so subsequent syntax verification used
    read-only AST parsing;
  - `/usr/bin/python3 -m pytest Testing/test_case_bundle.py -q` returned
    `8 passed`;
  - `/usr/bin/python3 -m pytest Testing -q` returned `94 passed`;
  - AST parsing and `git diff --check` passed;
  - the exact saved ROI record returned old exact-key match `False` and new
    schema-v1 compatible match `True` with its coordinates unchanged.
- No Slicer process, ROS connection, motion, Git commit/push, or Drive sync was
  started. Clean-restart operator reopen of the package remains pending.

## 14:41 IST — Composable viewer and Step 6 interaction guards implemented

- Replaced the 3×3 teeth grid with a shared, UI-independent
  `ViewComposition` value: anatomy scope × 2D/3D dimension × CBCT mode ×
  overlay set. Added metadata-first upper/lower/full-anatomy classification and
  a fresh hierarchical Advanced tree, avoiding the former reparented-list
  lifecycle/blank-panel problem.
- Kept recommendations display-only. A recommendation never creates geometry
  or a renderer. Only an explicit CBCT 3D choice lazily uses Slicer's Volume
  Rendering logic; the viewer records its own display/property nodes and
  removes them on Restore. Slice composites, pre-existing renderers, segment
  state, and model state are snapshot/restored.
- Added a Step 6 robot-workspace context card reporting selected case/phantom,
  MRML/ROS robot authority, TMJ-opening readiness, and base lock. The view-key
  filter and Advanced tree both enforce case/phantom XOR and MRML/ROS XOR.
- Added stage-owned markup interaction: non-owning DENTOBOT markups become
  locked and non-selectable, with exact prior state restored on return. Step 6
  permits only the currently required mouth-opening landmarks or an unlocked
  MRML mount plane; the Step 5A patient-shell boundary can no longer be dragged
  there accidentally.
- Files changed for this increment: `DENTOViewPresets.py`,
  `DENTOWorkflow.py`, `test_view_presets.py`, and
  `run_dentobot_view_composition_smoke.py`, plus controlled documentation.
- Verification: Python compilation passed with a temporary bytecode cache;
  `Testing/test_view_presets.py` returned 8 passed; full host `Testing/`
  returned 92 passed; `git diff --check` passed. The focused containerized
  Slicer test printed `DENTOBOT_COMPOSABLE_VIEWS_PASS`, verified explicit
  intensity-render creation/removal, upper/lower visibility, Step 6 lock and
  Step 5A restore, and produced
  `dentobot-composable-views-advanced.png` and
  `dentobot-robot-workspace-view.png`. Both screenshots were visually checked.
  The visible six-workspace shell printed `DENTOBOT_APPLICATION_SHELL_PASS`;
  focused in-module regressions printed `DENTOBOT_VIEW_WIDGET_PASS` and
  `DENTOBOT_ROBOT_WIDGET_PASS`.
- Observed teardown-only issue: after the focused viewer PASS and exit 0, some
  no-main-window logs contain late `_updateTemplateModeling` calls targeting a
  destroyed `QLabel`. Recorded a lifecycle-cleanup follow-up; the messages
  occurred after the verified assertions and are not treated as a clean reload
  acceptance claim.
- Boundaries: no ROS controller/hardware execution, drilling, patient-facing
  operation, Git publication, or Google Drive sync was performed. A real
  54-label case and normal-window Step 6 operator session remain required.

## 13:46 IST — Compact/composable Views implementation audit

- Reconciled the pasted redesign plan against current source, Git history,
  controlled documents, the August 24–25 logbooks, and the Daily Compass.
  The workbook contains no unreconciled Views update and was last reconciled
  on 24 August.
- Static source evidence confirms only the earlier grouped-Views checkpoint is
  present: shared Legacy/New-GUI palette, all 11 stage recommendations,
  FDI-based upper/lower/all teeth shortcuts, exact display snapshot/restore,
  and non-creating volume-render inventory. Commit `81a4bff` includes that
  checkpoint.
- The later compact/composable redesign is not implemented. The UI still
  constructs the 3 x 3 teeth-button grid and reparents a flat
  `QListWidget` into a collapsed Advanced section; no `ViewComposition`,
  compact anatomy/CBCT/overlay selectors, tri-state hierarchy, comparison
  opacity, or view-session-owned lazy renderer exists. The current uncommitted
  August 25 diff contains no material Views implementation.
- Verification: focused pure view/shell tests returned `14 passed`; `git diff
  --check` passed. This is static/pure-test evidence only; no graphical Slicer
  acceptance was run. Recorded the missing redesign as an active task without
  changing application source or runtime state.

## 13:45 IST — Launcher Docker daemon auto-start

- Failure mode: inactive `docker.service` left `/var/run/docker.sock` missing and
  Compose failed before the image could load.
- `Workspace/scripts/launch-dentoworkflow.bash` now ensures the daemon before
  any Compose call: start socket/service on demand, wait for readiness, attempt
  boot enable when rights allow, otherwise print the one-time enable command.
- Verified: daemon stop → `--check-only` restarted Docker and passed; focused
  launcher regressions 6 passed. Permanent enable on this machine still needs
  one interactive: `systemctl enable --now docker`.

## 13:34 IST — Step 6.0A required case mouth opening

### Request and boundary

- Insert a required case mouth-opening step at the start of Step 6 after
  importing the Steps 0–5 package. Reuse existing TMJ hinge / 4-landmark logic
  from the draft skull phantom so collision-aware planning has intra-oral
  space. No clinical jaw-kinematics claim; source CBCT/masks must not be
  destructively resampled.

### Design

- Non-destructive: preserve source volume and segmentation; place four case
  landmarks; build a derived opened lower-jaw planning surface via
  `solve_hinge_rotation_for_gap`; add mandibular display proxies for opened
  trajectory/target-attached geometry when needed.
- Gate 6.1+ (load robot, lock, ROS connect, planning-scene sync, related UI)
  until opening freshness is current. Phantom remains alternate XOR scene and
  does not require 6.0A.

### Implementation and verification

- UI group `6.0A`, parameter-node fields, freshness/fingerprint helpers,
  façade `_scene_preparation_issue`, collision/planning consumers, and
  `test_DENTOWorkflowStep6CaseJawOpening` were implemented in-session.
- Container test printed `DENTOBOT_CASE_JAW_OPENING_PASS`. Host `Testing/` and
  focused façade/env suites passed before the context limit interrupted the
  session. The interrupted UI `scene_prepared` gating patch was already present
  on recovery.
- Controlled docs (`DECISIONS`, `TASKS`, `changelog`, `ARCHITECTURE`, dated
  logbook) updated after limit recovery. Representative graphical acceptance
  after extension reload remains operator follow-up.

## 00:52:52 IST — DENTOWorkflow launcher diagnosis and repair

### Request and boundary

- Reproduce and fix the reported Ubuntu DENTOWorkflow launch issue.
- Launch only the existing simulation-preview stack. No Execute action,
  hardware connection, robot motion, drilling, or patient-facing operation was
  authorized or performed.

### Diagnosis

1. `bash -n Workspace/scripts/launch-dentoworkflow.bash` passed.
2. The first `--check-only` attempt failed because both `docker.service` and
   `docker.socket` were disabled/inactive and `/var/run/docker.sock` did not
   exist. `systemctl start docker.service` succeeded through host service
   policy; no sudo credential was recorded.
3. The complete preflight then passed: external CPU backend health was `ok`,
   both ROS packages built, render device and runtime safeguards matched, and
   Compose configuration was valid.
4. The first full launch reached the final Docker call but failed with
   `cannot attach stdin to a TTY-enabled container because stdin is not a
   terminal`. The launcher had hard-coded `docker exec -it`.

### Fix and verification

- Updated `Workspace/scripts/launch-dentoworkflow.bash` to pass `-it` only when
  both stdin and stdout are terminals; non-TTY callers remain attached without
  requesting a pseudo-terminal.
- Added focused coverage in `Testing/test_ros2_cli_slicer_env.py`.
- Bash syntax passed, the focused test file returned `4 passed`, and the final
  host suite returned `96 passed`.
- The repaired full launch remained live on CRD `DISPLAY=:20.0`, started
  Slicer/SlicerApp-real with `slicer.util.selectModule("DENTOWorkflow")`, and
  reported `ready:true`, `planning_ready:true`, and exactly one simulated
  joint-state publisher.
- `xdotool` and `wmctrl` were unavailable, so no host window-title query was
  claimed. Process arguments and the ROS readiness contract are the recorded
  runtime evidence; operator visual acceptance remains separate.

### 01:53 IST follow-up — bounded shutdown repair

- The developer closed Slicer after the successful run. The pinned SlicerROS2
  build emitted its known VTK leak report and exited 1, then the launcher
  remained blocked while the ROS/MoveIt stack continued running.
- Inspection showed the cleanup had signalled only the top-level `ros2 launch`
  PID. Terminating that PID orphaned its five simulation children under
  container PID 1. Sent TERM only to those exact simulation PIDs and verified
  that no robot-state publisher, joint-state source, MoveIt, collision guard,
  or readiness publisher remained.
- Updated the launcher to start the simulation stack under `setsid`, test the
  complete process group, and escalate bounded INT → TERM → KILL before
  waiting. Added a fifth focused static regression.
- Bash syntax and all five focused tests passed. A container-side disposable
  process-group probe deliberately ignored INT, was terminated by the bounded
  TERM stage, and printed `bounded process-group cleanup passed`.
- The final host suite returned `97 passed`; `git diff --check` passed. The
  container process audit found only its idle process and ROS CLI daemon—no
  Slicer, simulation launcher, robot-state publisher, joint-state source,
  MoveIt, collision guard, or readiness publisher.

## 03:18 IST — Representative Step 5B disconnected-fusion repair

### Request and reproduction

- Loaded the de-identified development scene
  `SampleStudy1/test1_5b2.mrb` in a disposable headless Slicer process. The
  saved scene truthfully reopened with a stale patient shell and a
  stale/Draft Step 4C assembly, so the first direct final-fusion attempt was
  rejected before geometry work.
- Regenerated the stale directional blockout, patient shell, and Step 4C draft
  in memory, then marked the dock orientation confirmed to reproduce the
  operator's already-confirmed live state. No source MRB or runtime data was
  saved or changed.
- The final build then reproduced exactly:
  `Final shell/docking fusion contains 4 disconnected occupied volumes with
  voxel sizes [85852, 3, 1, 1]`.

### Diagnosis and fix

- All four independent 3.5 mm dock-to-shell branches existed. Each had 2.0 mm
  endpoint overlap, one watertight surface, and a measured shell gap of
  0.3104, 0.1839, 0.0030, or 0.1529 mm. The main 85,852-voxel component
  therefore contained the printable assembly; the three extra regions totalled
  only five sampled voxels.
- The fusion guard already removed isolated one-voxel artifacts but treated
  the three-voxel sampling island as a substantive disconnected component.
  Generalized the cleanup to discard extra occupied regions only when exactly
  one printable region remains and every extra region is at most 0.1 mm³
  (with the pre-existing one-voxel compatibility floor). Raw, retained, and
  removed-region metrics are persisted. Any larger second component still
  fails closed.
- Added deterministic Slicer-native coverage for the exact `[100, 3, 1, 1]`
  cleanup pattern and a `[100, 4]` rejection at 0.3 mm spacing.

### Verification and limits

- The repaired representative build succeeded with one retained 85,852-voxel
  occupied volume, 89,524 triangles, zero invalid edges, four recorded
  shell-contact branches, and raw regions `[85852, 3, 1, 1]`; five artifact
  voxels (approximately 0.133 mm³ total across three individually bounded
  regions) were reported as removed.
- The focused artifact test printed `TINY_FUSION_ARTIFACT_TEST_OK`; the complete
  synthetic support/shell/four-dock/fusion/MRB regression printed
  `VISIBLE_TEMPLATE_SUPPORT_TEST_OK`; Python AST parsing passed; and the host
  suite returned `97 passed`.
- One initial focused-test assertion exposed an incorrectly retained synthetic
  label in the test fixture and timed out after the assertion; the fixture was
  reset correctly and the rerun passed. A first host-suite command used the
  base Python without pytest; rerunning with the documented `dentobot`
  environment passed.
- This verifies software connectivity and topology only. Visual inspection,
  channel continuity, fit, mechanical strength, printing, and clinical or
  robot use remain unverified and unauthorized.

## 03:55 IST — Connected case-package save sanitization repair

### Reproduction and cause

- Reproduced the operator error by loading `test1_6_FD14.mrb`, setting its
  persistent robot base to the same `DENTOBOT.Ros2MotionControlActive=true`
  state used by a live connection, and calling the real `_createCaseBundle`
  path in a separate Slicer process. Package audit rejected the generated MRB
  with the same `ROS-active flag` message.
- The package creator correctly remained fail-closed. The defect was upstream:
  programmatic package snapshot creation relied only on the module's general
  MRML StartSave/EndSave observer to suspend the process-only flag. That
  callback boundary did not reliably cover this programmatic MRB snapshot, so
  the archive audit caught the leaked flag.

### Fix and verification

- Added one shared active-flag suspend/restore pair. Ordinary scene saves still
  use it through StartSave/EndSave. `_saveSceneSnapshotToMrb` now also invokes
  it explicitly around `slicer.util.saveScene`, marks SlicerROS2 runtime nodes
  transient, restores the live scene URL/root state, and restores the running
  scene's active flag in nested `finally` blocks.
- Extended `run_dentobot_case_bundle_smoke.py` so the Step 6 source deliberately
  starts with an active flag, verifies the live flag is restored after save,
  opens the resulting package, verifies no ROS-active flag or live ROS robot is
  restored, and compares geometry at the existing `1e-6` tolerance.
- Before the fix, the sourced SlicerROS2 run reproduced the package rejection.
  After the fix it printed
  `DENTOBOT_CONNECTED_CASE_SAVE_PASS test1_6_FD14.mrb 131323229`.
  The later historical-package portion could not run because the external
  fixture `dentobot-step6.dentocase` is no longer present; this is recorded as
  a fixture limitation, not a connected-save failure.
- Two earlier attempts omitted either SlicerROS2 module paths or sourced ROS
  runtime libraries and failed before the target test. The corrected sourced
  command loaded the runtime and produced the reproduction/PASS evidence.
- The first final host run returned `96 passed, 1 failed` because a static
  lifecycle regression requires the transient-node call to remain explicit in
  `onSceneStartSave`. Kept that direct defense-in-depth call as well as the new
  explicit package-snapshot call; the rerun returned `97 passed`. AST parsing
  and `git diff --check` also passed.
- The already-running graphical Slicer/ROS/MoveIt session was inspected but
  not stopped, modified, or driven. The operator must reload DENTOWorkflow to
  use the source fix; module reload intentionally disconnects transient
  Slicer-side ROS state, so reconnect explicitly afterward if needed.
