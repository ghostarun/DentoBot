# DENTOBOT Engineering Log — 2026-09-12

## Multi-workstation development and saved-case exchange boundary

- **Operator statement:** The operator wants script development committed to
  the repository while large important local files stay out of Git because
  they involve medical data, with a personal Google Drive workflow for smooth
  multi-workstation development. The operator then narrowed the intended
  exchange to saved Slicer `.dentocase` files under `Slicer_Saved`.
- **Existing-plan lookup:** Searched all of `docs/backlog.md` for
  multi-workstation, Drive, sync, repository, local-data, medical-data,
  platform, and workstation terms. No applicable pending task existed. The
  existing Git-tracked overlay, docs-only `active-development-ubuntu` mirror,
  and local `data/` boundary were reviewed in `CONTEXT_SYNC.md`, `SETUP.md`,
  `PROJECT_CONTEXT.md`, `TASKS.md`, `DEVELOPMENT_PLAN.md`, and `DECISIONS.md`.
- **Drive grounding:** Read-only search found the existing
  `active-development-ubuntu` folder (`1YsnPmedJPOu4ZT3xGx_yD87ZYpm5OPOV`) and
  its controlled-document children. No engineer-owned folder or record was
  opened, no case-data Drive location was searched, and no Drive write was
  performed.
- **Evidence inspection:** The local `data/Slicer_Saved/` tree is 3.4 GiB and
  contains `.mrb`, `.stl`, `.nrrd`, and `.dentocase` files. Archive member-name
  inspection of representative `.dentocase` bundles showed that the outer
  bundle contains an embedded MRB and that the MRB can contain
  `PreDentalSurgery_1.nrrd`, `PostDentalSurgery_1.nrrd`, and a segmentation
  NRRD. No medical-image payload was opened or copied.
- **Engineering interpretation:** The operator's file-type instinct is the
  smallest useful scope, but a `.dentocase` is a portable medical-data archive,
  not metadata-only. Exchange only individual synthetic or explicitly
  approved de-identified bundles; do not sync the whole `Slicer_Saved` folder,
  raw/non-anonymized bundles, or adjacent `.mrb`/`.stl`/`.nrrd` files to a
  personal Drive account by default.
- **Work performed:** Added `PLAT-U-07` to `backlog.md`, its detailed contract
  to `TASKS.md`, its acceptance entry to `DEVELOPMENT_PLAN.md`, and the
  narrowed source/data policy to `CONTEXT_SYNC.md`, `SETUP.md`, and
  `DECISIONS.md`. No source code, data artifact, environment file, or
  engineer-owned record was changed.
- **Verification:** `git -C ros2_ws/src/DentoBot diff --check` exited 0. A
  targeted `rg` search found `PLAT-U-07` in `backlog.md`, `TASKS.md`, and
  `DEVELOPMENT_PLAN.md`, and found the corresponding policy in `CONTEXT_SYNC.md`,
  `SETUP.md`, `DECISIONS.md`, and this logbook. No runtime, Slicer, ROS,
  network, or Drive-write check was run.
- **Evidence boundary and limitation:** The policy is recorded, but no exact
  large-data Drive location, permitted bundle class, second workstation, or
  pilot bundle has been selected. No clinical, privacy, or governance approval
  is inferred.
- **Next bounded action:** The operator must name the separate Drive location
  and approve the allowed bundle class. Then exchange one synthetic or
  de-identified `.dentocase` with checksum/readback verification and record the
  exact result without patient identifiers.

## S6-REUSABLE-CASE-SETUP targeted continuation

- **Operator statement:** The operator approved continuation of the S6
  implementation and targeted verification, with a stop before broad or full
  workflow testing. No robot, ROS/MoveIt motion, hardware, patient-facing, or
  normal-window acceptance activity was authorized.
- **Implementation state:** The focused correction adds the native
  `startStep6CaseJawLandmarkPlacement` compatibility entrypoint, uses supported
  MRML parent-transform accessors, rounds persisted foundation matrix elements
  for reload-stable fingerprints, and gives the existing visible-support Slicer
  fixture a reviewed synthetic Case Foundation (120^3 source grid, reviewed
  upper/lower incisor and mandible surfaces, four source-surface-snapped
  landmarks, committed opening). The API contract includes the compatibility
  entrypoint. These edits remain uncommitted so the failing runtime boundary is
  inspectable and unrelated dirty work stays untouched.
- **Targeted static evidence:**
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-case-foundation-pycache-20260912b \
  python3 -m py_compile DENTOWorkflow/Resources/Python/dentobot_workflow/logic_case_foundation.py \
  DENTOWorkflow/Resources/Python/dentobot_workflow/slicer_tests.py \
  Testing/run_dentobot_reusable_case_smoke.py` exited 0;
  `python3 -m pytest -q -p no:cacheprovider
  Testing/test_modular_structure.py::test_modularized_public_api_matches_verified_baseline`
  reported `1 passed`; and `git diff --check` exited 0.
- **Runtime evidence:** The approved S6 matrix command was attempted after
  the fixture correction. It exited 1 and emitted no
  `DENTOBOT_REUSABLE_CASE_PASS` marker. The retained first causal message is
  `DENTOBOT_REUSABLE_CASE_FAILED: unsupported operand type(s) for -:
  'float' and 'NoneType'`. Earlier retained Slicer logs show the prior fixture
  failures (landmark surface residual, then the expected collision-surface
  count); the current count/fixture edits are present. The repeated Qt
  `destroyed QPushButton`/`destroyed qMRMLNodeComboBox` traces occur during
  teardown and are not treated as the first causal failure.
- **Evidence locations:** Full command output and marker capture remain under
  `/tmp/dentobot-verification/s6-reusable-case-20260912T-continuation/`; the
  latest container Slicer log is
  `/tmp/Slicer-/Slicer_5.10.0-2025-11-10_3838_20260912_125231_533.log`.
- **Protocol boundary:** The S6 runtime blocker has reached the
  `AGENTIC_VERIFICATION_PROTOCOL.md` three-failure ceiling. A further Slicer
  sequence/traceback attempt was rejected by the protocol; no additional
  runtime, Docker rebuild, full test suite, or normal-window workflow test was
  run after that stop. Static inspection did not identify a safe, evidence-backed
  one-line source fix for the `float`/`None` subtraction, so no speculative
  patch was applied.
- **Evidence boundary and status:** Implemented and targeted static evidence
  are recorded. Focused Slicer runtime verification is **not** passed,
  integration/normal-window operator verification is open, and
  `S6-REUSABLE-CASE-SETUP` remains open in the backlog. No task-specific
  checkpoint or foundation commit/push was created from this failing state.
- **Post-entry hygiene:** A fresh `git diff --check` after this evidence entry
  exited 0. `git status --short` confirms the three task-specific source/API/test
  files, the new logbook, and the previously dirty controlled records remain
  visible; no unrelated hunk was discarded.
- **Next bounded action:** Obtain an explicitly approved revised diagnostic
  plan (one traceback-producing Slicer run, or an operator-provided failure
  location) before resuming the blocked runtime path; otherwise leave the
  current uncommitted fixture correction and evidence available for review.


## S6-REUSABLE-CASE-SETUP revised diagnostic — approved continuation

- **Operator statement:** The operator approved one revised traceback-producing
  Slicer diagnostic and asked to continue, while the existing boundary against
  broad/full workflow testing, ROS/MoveIt execution, robot motion, hardware,
  and normal-window acceptance remained in force.
- **Diagnostic command:** Copied the temporary script
  `/tmp/dentobot_s6_sequence_diag.py` into the active container and ran:
  `docker exec dentobot-slicerros2 bash -lc 'timeout 180s xvfb-run -a
  /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash
  --additional-module-paths
  /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script
  /tmp/dentobot_s6_sequence_diag.py'`. The script ran the Case Foundation
  test and the focused visible-support test in sequence with traceback output.
- **Runtime result:** The latest Slicer log
  `/tmp/Slicer-/Slicer_5.10.0-2025-11-10_3838_20260912_134709_756.log`
  records `SEQ_BEGIN foundation`, `SEQ_PASS foundation`, `SEQ_BEGIN support`,
  and `SEQ_PASS support`. It also records a non-fatal early-refresh traceback
  in `widget_robot_scene.py`: `_updateStep6CaseJawOpeningStatus` attempted
  `GetAttribute` on a missing lower-jaw model before the foundation proxy was
  available. The focused sequence still reached both PASS markers; it did not
  emit the matrix runner's `DENTOBOT_REUSABLE_CASE_PASS` marker and is not a
  matrix-profile pass.
- **Implementation correction:** Added the smallest scoped guard so the status
  callback reports zero moving surfaces while the lower-jaw model is absent.
  This change is limited to
  `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_robot_scene.py`;
  no geometry, gate, persistence, or runtime policy was changed.
- **Post-correction static evidence:** A writable-cache `py_compile` over the
  changed workflow/test scripts exited 0, and `git diff --check` exited 0.
  No additional Slicer process, full suite, Docker rebuild, ROS/MoveIt action,
  or normal-window test was run after the guard was applied.
- **Protocol boundary and status:** This was the one approved revised runtime
  diagnostic after the three-failure ceiling. The exact matrix runner remains
  unresolved, focused runtime verification is not marked passed, and
  `S6-REUSABLE-CASE-SETUP` remains open pending an approved next runtime plan
  and the ten-point operator review. No task-specific commit or push was made;
  all unrelated dirty hunks remain preserved.
- **Next bounded action:** Pause for review of this evidence and obtain fresh
  explicit approval before any further Slicer/runtime attempt; otherwise keep
  the current uncommitted correction available for inspection.

## S6-REUSABLE-CASE-SETUP exact runner — targeted runtime pass

- **Operator statement:** The operator said “Continue” after the requested
  bounded-plan approval. This authorized one exact `runtime.s6_reusable_case`
  attempt after the harness-only event-pumping correction. The existing limits
  remained in force: no full workflow suite, Docker rebuild, ROS/MoveIt
  execution, robot motion, hardware, or normal-window acceptance.
- **Harness correction:** The focused runner now follows the passing sequence
  diagnostic directly: it selects the module, runs the two synthetic tests with
  `setUp()` between them, and avoids event-loop pumping immediately after scene
  clears and a final teardown clear. This removes a test-harness teardown/event
  path that had been producing the earlier opaque `float`/`None` failure; the
  production callback also retains the separate missing-model guard recorded
  above.
- **Command:**
  `docker exec dentobot-slicerros2 bash -lc "timeout 300s xvfb-run -a
  /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash
  --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow
  --python-script
  /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_reusable_case_smoke.py"`
- **Result:** The command exited 0 and emitted
  `DENTOBOT_REUSABLE_CASE_PASS`. The final container log is
  `/tmp/Slicer-/Slicer_5.10.0-2025-11-10_3838_20260912_144611_131.log`;
  its marker appears at line 365. No `DENTOBOT_REUSABLE_CASE_FAILED`,
  `Traceback`, or `NoneType` marker appears in that final log excerpt.
- **Targeted post-run hygiene:** The focused changed-file `py_compile` exited
  0, and `git diff --check` exited 0. No additional runtime path was started.
- **Graph evidence:** `graphify update .` completed with AST extraction of 16/16
  uncached files and rebuilt the local graph (6,113 nodes, 9,708 edges, 457
  communities). The command reported that the code graph was updated; no
  semantic-label refresh was requested.
- **Acceptance boundary:** This closes the automated focused Slicer/runtime
  evidence for the S6 matrix profile, but does not establish integration or
  normal-window operator acceptance. The task remains open for the recorded
  ten-observation Case Foundation review and any separately authorized
  integration checks. No task-specific commit or push was made in this turn;
  unrelated dirty files and the mixed 2026-09-12 logbook content remain
  preserved for review.
- **Next bounded action:** Pause for review and obtain explicit authorization
  before any further runtime or operator acceptance work; do not infer
  clinical, hardware, or ROS/MoveIt readiness from this synthetic pass.

## PLAT-U-07 continuation — Drive destination grounding

- **Operator direction:** “Continue” with the multi-workstation saved-case
  exchange work.
- **Read-only Drive checks:** Folder searches for `Slicer_Saved`, `dentocase`,
  `case-bundles`, and `research-data-ubuntu` returned no matches. The only
  grounded destination remains `IITM Dentobot/active-development-ubuntu`,
  which is reserved for controlled Ubuntu documents.
- **Result:** No case-bundle destination was guessed or created. No `.dentocase`
  payload was fetched, inspected, uploaded, replaced, or downloaded, and no
  engineer-owned record was accessed.
- **Next bounded action:** Obtain the exact Drive folder URL/ID and confirm
  that the pilot bundle class is synthetic or explicitly approved
  de-identified data before performing one individual `.dentocase` metadata and
  checksum-verified exchange.
