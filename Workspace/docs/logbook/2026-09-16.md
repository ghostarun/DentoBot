# DentoBot logbook — 2026-09-16

## Campaign 1 P3 r4-only — current-state preflight

**Operator objective:** continue toward the existing P3 success point. No new
explicit P3 runtime authorization was supplied in this turn; the repaired
driver was therefore inspected and verified statically, but no Slicer/ROS P3
endpoint batch was launched.

**Current contract and boundaries:** The active task remains
`S6-REUSABLE-CASE-SETUP` / Campaign 1 P3. Its sole executable input is
`c1-gatea-fdi31-20260915-r4/FDI31-step5c.dentocase` with SHA-256
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`.
The repaired driver preserves the one-batch, at-most-128 J1–J5 native static
IK/guard contract and returns before workspace generation. No historical case
or output was read as an input, and no planner, preview, insertion, motion,
controller/hardware, spindle or patient action occurred.

**Read-only evidence and checks:**

- `docker ps` confirmed `dentobot-slicerros2` is up. A read-only in-container
  process inventory found no matching `ros2 launch`, `dentobot_moveit`,
  `move_group`, Slicer, Xvfb, P3-driver or exact-case-runner process.
- `bash -n Testing/run_c1_p3_r4_batch.bash`, JSON parsing of
  `Testing/verification_matrix.json`, `py_compile` of the exact-case runner,
  and `git diff --check` passed.
- `PYTHONPYCACHEPREFIX=/tmp/dentobot-p3-20260916-pycache python3 -m pytest -q
  -p no:cacheprovider Testing/test_step6_planning.py
  Testing/test_fdi31_saved_trajectory.py` passed: `29 passed in 0.25s`.

**Disposition:** The launcher/readiness repair remains Runtime Verified for
its stack-only scope. P3 endpoint feasibility remains **NOT TESTED /
INCONCLUSIVE** at the prior three-attempt runtime/setup ceiling. The next
bounded action is a new, explicit authorization for exactly one r4-only P3
endpoint batch through `Testing/run_c1_p3_r4_batch.bash`; P4 remains NOT RUN.

## Campaign 1 P3 r4-only — authorized runtime interruption and repair

The operator explicitly authorized one r4-only P3 batch. The fresh output
directory was `c1-p3-fdi31-20260916-r4`; the driver validated the locked r4
SHA-256, reported `C1_P3_STACK_READINESS_PASS`, and Slicer loaded r4. It then
stopped before any native candidate, endpoint JSON or completion marker with
`reconfirm restored task: Generate and review the live workspace-assisted
limits before confirming the task.` The log has no `Planning request` marker.
No preview, insertion, motion, controller/hardware, spindle, patient or
historical-data action occurred.

The cause was a generic post-runtime `confirmTask()` call that requires
workspace-assisted limits. P3 cannot use that production workspace helper
because it submits planning requests. The targeted source repair makes endpoint
mode retain the restored r4 task snapshot and fail closed when it is absent;
the normal path retains task reconfirmation. The owned stack process group was
verified absent after exit, along with Slicer/ROS/MoveIt matching processes.

Post-fix checks passed: launcher `bash -n`, matrix JSON parsing, exact-runner
`py_compile`, `git diff --check`, and the focused source suite
(`29 passed in 0.26s`). `Workspace/scripts/graphify-update.bash` also completed
with `Code graph updated`. P3 endpoint feasibility remains **NOT TESTED /
INCONCLUSIVE**, with no candidate evidence; any next runtime batch needs a new
explicit one-batch r4-only authorization. P4 remains NOT RUN.

## Campaign 1 P3 r4-only — second setup stop and ephemeral snapshot repair

The operator authorized one additional fresh r4-only P3 batch. Run
`c1-p3-fdi31-20260916-r4-r2` again passed the SHA-256 preflight, emitted
`C1_P3_STACK_READINESS_PASS`, and loaded r4 in Slicer. It stopped before a
native candidate, endpoint JSON, completion marker, planner request, preview,
insertion or motion because the r4 offline restore has no persisted confirmed
task snapshot. The owned stack group and all matching runtime processes were
verified absent after exit.

For endpoint-only mode only, the repair permits exactly this missing
confirmation and calls the existing pure `build_task_snapshot` helper with
unchanged r4 trajectory data, live Task Home, base/task-limit/robot fingerprints
and simulation-tool provenance. The in-memory diagnostic record identifies
itself as `ephemeral_r4_p3_snapshot`; every other freshness issue remains
fail-closed. It does not write a normal confirmation or use workspace generation.

Post-fix checks passed: `29 passed in 0.26s`, launcher syntax, matrix JSON,
exact-runner `py_compile`, `git diff --check`, and Graphify (`Code graph
updated`). No P3 candidate evidence exists yet; a new explicit one-batch
r4-only authorization is required for the next runtime attempt. P4 remains
NOT RUN.

## Campaign 1 P3 r4-only — native all-joint topology stop and mapping repair

The operator-authorized fresh run `c1-p3-fdi31-20260916-r4-r3` passed the
locked r4 SHA-256 preflight and `C1_P3_STACK_READINESS_PASS`, then loaded r4
in Slicer. It stopped before any native IK solve, candidate JSON, completion
marker, planner request, preview, insertion, motion, controller/hardware,
spindle or patient action with `P3 active J1-J5 joint-limit API is incomplete
or reordered`. The owned temporary stack and all matching Slicer/ROS/MoveIt
processes were verified absent after teardown.

Read-only native API/source evidence showed that the Slicer ROS robot node
returns its complete KDL chain: J1–J5 plus the known external visual spindle.
The P3 diagnostic incorrectly required the native list to be exactly the five
planning joints. The minimal fail-closed repair now requires that exact known
six-joint topology, reads native joint types, selects only J1–J5 in the bridge
planning order, rejects duplicates, missing limits/types or any unexpected
joint, and uses wrapped comparison only for the native continuous J5. The
spindle is neither seeded nor solved nor guarded as a planning joint.

Source verification passed: driver syntax, matrix JSON parsing, exact-runner
compile, `git diff --check`, and the focused suite (`29 passed in 0.26s`).
Graphify refreshed successfully (`Code graph updated`; existing parser warnings
remain non-fatal). This is a setup/topology interruption, not P3 endpoint
evidence: P3 remains **NOT TESTED/INCONCLUSIVE**, P4 remains NOT RUN, and any
replacement runtime batch requires fresh explicit one-batch r4-only authority.

**Commands recorded:** `bash -n Testing/run_c1_p3_r4_batch.bash`; `python3 -m
json.tool Testing/verification_matrix.json`; `PYTHONPYCACHEPREFIX=/tmp/
dentobot-p3-joint-topology-pycache python3 -m py_compile
Testing/run_dentobot_step65_exact_case_smoke.py`; `PYTHONPYCACHEPREFIX=/tmp/
dentobot-p3-joint-topology-pycache python3 -m pytest -q -p no:cacheprovider
Testing/test_step6_planning.py Testing/test_fdi31_saved_trajectory.py`; `git
diff --check`; and `Workspace/scripts/graphify-update.bash`.

## Campaign 1 P3 r4-only — completed bounded negative endpoint result

**Operator authority and input:** The operator authorized one fresh replacement
batch. `c1-p3-fdi31-20260916-r4-r4` validated the sole active r4 case
(`FDI31-step5c.dentocase`, SHA-256
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`) and
emitted `C1_P3_STACK_READINESS_PASS`. The first direct invocation stopped
before the driver began because its mode was `664`; rerunning the unchanged
driver through `bash` is the authorized batch. No second simulation stack or
additional seed batch was created.

**Completed P3 evidence:** The run emitted `DENTOBOT_ENDPOINT_ONLY_COMPLETE`
and wrote `endpoint_candidates.json` (SHA-256
`8853f5208a87368d2611fd9c0a9737fe6b73b4da43de6851f4a428adee3f734c`). It
used exactly 128 premanifested r4-only J1–J5 seeds, with native runtime types
revolute/prismatic/revolute/prismatic/continuous and the ephemeral r4 P3
snapshot. It recorded 21 position-axis IK convergences, six unique solution
clusters, zero generic-static-valid candidates, zero phase-static-valid
candidates and zero retained P4 representatives. Its terminal result is
`NO_VALID_TARGET_SOLUTION_FOUND_IN_BOUNDED_SEARCH`.

**Boundaries and interpretation:** `Planning request` count was zero. The run
record states no approach/drilling planner invocation, preview/joint command,
controller/hardware motion, spindle/patient action or historical case/output
input. Every converged candidate was rejected by the full-chain phase guard:
the provisional drill tip left the approved Entry-to-Target corridor. Generic
static validation also rejected its recorded Template↔visual-spindle and
target-tooth↔burr contacts. These observations do not establish physical or
global task infeasibility; under the P3 contract they distinguish only this
complete bounded search. No geometry, tool, base, limits, target, guard policy,
seed count or solver was changed, and no visual/geometry correction was
attempted.

**Cleanup and disposition:** The driver’s recorded process group completed its
teardown; post-run inventory found zero Slicer, ROS launch, MoveIt, P3-driver
or exact-runner processes. Slicer logged an optional `ROS2Tests` `psutil`
install-permission failure, but the P3 runner nevertheless loaded r4, produced
the complete marker and complete evidence; it is an environment warning, not a
P3 interruption. P3 is now **COMPLETED — NO VALID TARGET SOLUTION FOUND IN
BOUNDED SEARCH**. Per Campaign 1, stop before more seeds, solver tuning,
geometry changes or P4; **P4 remains NOT RUN** and an architectural review is
required to choose any next campaign. The evidence path is
`data/Slicer_Saved/SampleStudy1/FDI31/c1-p3-fdi31-20260916-r4-r4/`.

**Commands recorded:** failed pre-execution direct driver invocation; authorized
driver execution through `timeout 600s docker exec ... bash
Testing/run_c1_p3_r4_batch.bash`; read-only evidence/process checks; and marker
counts `Planning request=0`, `DENTOBOT_ENDPOINT_ONLY_COMPLETE=1`, residual
processes `0`.

## Presentation screenshot set — weekly update 8–16 September 2026

**Operator request:** Save all available screenshots within the scope of the
weekly progress update in a separate presentation folder.

**Implementation:** Created
`data/presentations/weekly-update-2026-09-08-to-16/` with five labelled
screenshot groups and copied 67 existing PNG artifacts without modifying their
sources. The groups cover Case Foundation/save-reuse, Step 4A–5B target/guide/
template views, FDI31 save/reopen and mask-visibility evidence, FDI31 planner
endpoint/collision evidence, and supplemental FDI21 collision views. Added a
README, TSV source manifest and SHA-256 checksum file. No new Slicer/runtime
capture was started and no source case or diagnostic was changed.

**Verification:** `sha256sum -c SHA256SUMS.txt` passed for all 67 presentation
copies; total copied image size is 18,826,630 bytes. The README explicitly
states that the 128-seed/21-IK/six-cluster result is supported by the P3 JSON
and logs, while screenshots provide visual context only. FDI21 images are
labelled supplemental and are not presented as FDI31 results.

**Next bounded action:** Use the presentation folder as the image source for
the weekly meeting update; retain the canonical diagnostics and logs as the
authoritative numerical and acceptance evidence.

**Completeness extension:** The date-bounded inventory under
`data/dentobot-runs/` and `data/Slicer_Saved/SampleStudy1/` contains 496 image
files from 8–16 September, including repeated retries and historical
comparators. Preserved all 496 in
`data/presentations/weekly-update-2026-09-08-to-16/all-available-screenshots/`
alongside the 67-image curated set. `sha256sum -c RAW_SHA256SUMS.txt` passed;
raw archive size is 274,502,511 bytes. Updated the README and manifest to
separate meeting-ready selection from the complete raw archive.

**Verification correction:** During the follow-up audit, the checksum commands
were first invoked from the presentation-folder root, where the manifest's
archive-relative paths are intentionally unresolved; that produced only
path-not-found output and did not indicate image corruption. Re-running from
the `screenshots/` and `all-available-screenshots/` archive roots passed both
manifests (67 curated files and 496 raw files, exit 0). The source-window
inventory remains 496 files for 2026-09-08 through 2026-09-16; no image or
source case was changed.

## P3 diagnostic-correctness repair and saved-candidate revalidation — 2026-09-16

**Operator authority:** The operator authorized correction through verified
completion within the existing P3 package: routine source/API/launcher/test
repairs and required r4-only saved-candidate static revalidation, but no new IK,
planning, geometry/task/tool/base/limit/tolerance/policy change, applied motion,
hardware, spindle, patient action or P4. The existing causal retry ceiling
remains binding.

**Diagnosis and implementation:** The old r4 P3 JSON (SHA-256
`8853f5208a87368d2611fd9c0a9737fe6b73b4da43de6851f4a428adee3f734c`) is
retained as the sole 21-vector input. Its phase-static label came from a
Home-to-endpoint transition, and generic static collision diagnostics were
incorrectly part of the admission gate. The repaired runner sends direct,
unique/increasing, phase=`drilling`, `validate_only` `static_state` requests;
generic contacts and native FK remain diagnostic. It fails closed on
correlation/attribution gaps, avoids the raw joint stream and avoids Task Home
transition. The last repair permits the guide-ID helper to use only the exact
deferred static audit state, still requiring approved/successfully published
guide records. Normal transition callers retain their behavior.

**Fresh runtime history and evidence boundary:** r1
(`c1-p3-fdi31-20260916-r4-static-revalidation-r1`) stopped before candidates at
normal scene acknowledgement; the legacy helper issued an unchanged-current-
state raw simulation request before 31-versus-zero readback failure. r2 stopped
before candidates because offline r4 has no persisted Task Home. r3 stopped
before candidates because the normal guide-ID lookup rejected the intentionally
deferred static audit. Each temporary stack was torn down; none wrote a new
endpoint JSON or candidate reply. No candidate was displayed/applied, so no new
attributable screenshot exists. Corrected accepted/rejected/unknown counts and
rejection/warning/housing status are not measured.

**Verification:** `python3 -m py_compile` of the runner, bridge, facade, logic
helper and test; `bash -n Testing/run_c1_p3_r4_batch.bash`; matrix JSON parse;
focused `pytest` over `test_step6_planning.py`, `test_ros2_bridge.py`,
`test_moveit_config.py`, `test_fdi31_recovery_diagnostic.py` and
`test_fdi31_saved_trajectory.py`; and `git diff --check` passed: **79 passed in
0.25s**. The first Graphify script invocation used the overlay-root-relative
path and failed to locate it; the canonical
`ros2_ws/src/DentoBot/Workspace/scripts/graphify-update.bash` invocation then
passed with `Code graph updated` (pre-existing parser warnings remain non-fatal).
Final read-only container inventory found `dentobot-slicerros2` up with no
Slicer, `ros2 launch`, MoveIt, P3 launcher or exact-case runner process.
Read-only preflight also reconfirmed the locked r4 case and immutable P3 input
hashes, the exact 128/21 candidate manifest, and the container-visible native
guard source/binary hashes
`8259a062b8c314fedb7a5ac95652a53ea280cfca2b67a6f6eda57b50ff2273b3` /
`f1132ea07d81c39249539c05c8900c817f39d50a95a6574ac3974f17e512fc3d`.
Those are preflight identities only; no candidate static response was created.

**Disposition:** The old bounded-negative classification is superseded only as
a phase-static endpoint result. Corrected P3 is **INCONCLUSIVE**, not PASS or a
new negative. The three-attempt static-runtime causal ceiling is exhausted; do
not run a fourth batch without an explicit operator ceiling exception. P4 is
NOT RUN. The decision requested is narrowly whether to execute one fresh r4-only
21-vector static revalidation using the already repaired guide-ID path.

## P3 fourth r4-only static revalidation — verified completion — 2026-09-16

**Operator authority:** The operator expressly authorized exactly one fourth
fresh r4-only P3 static revalidation batch using the repaired deferred-audit
guide-ID path. Scope was limited to the locked 21 saved vectors and fresh
evidence; it excluded new IK, planning, workspace generation, preview, applied
motion, hardware/controller, spindle, patient, policy/invariant changes and
P4. This consumed the exception; no fifth batch is authorized.

**Runtime evidence retained:** The fourth batch wrote
`data/Slicer_Saved/SampleStudy1/FDI31/c1-p3-fdi31-20260916-r4-static-revalidation-r4/endpoint_candidates.json`
with SHA-256 `d5a3ef8857b518a293d1ed7c8a9c750d0bfe0922110150242c4a4107729acd4f`.
It used locked r4 SHA-256
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83` and the
sole old 21-vector input SHA-256
`8853f5208a87368d2611fd9c0a9737fe6b73b4da43de6851f4a428adee3f734c`. Its raw
responses are 21 command-successful unique/increasing (1–21) drilling
`validate_only` `static_state` requests, all `accepted=true`, with matching
task/policy/session identity, 31/31 scene readback, one requested/starting/
evaluated state and sample count one. Bounds, native static FK and world-RAS
FK pass for all 21. No planner, raw command stream, preview, applied motion,
controller/hardware, spindle/patient or historical-data action is recorded.

**Diagnosed classification defect and implementation:** The raw responses were
initially all classified `INCONCLUSIVE` because the runner required bit-exact
joint echoes, while native `collision_guard.cpp` serializes them with
`std::setprecision(12)`. The maximum observed echo delta is
`4.875266856885219e-13`. Updated
`Testing/run_dentobot_step65_exact_case_smoke.py` to accept only that bounded
status representation (`math.isclose`, relative `1e-12`, absolute `5e-13`) and
to emit a pure retained-evidence correction only after every identity, scene,
policy and one-state check passes. Updated `Testing/test_step6_planning.py`
with the rounded-positive and materially-different/stale-negative regression.
This changes neither native guard behavior nor task/geometry/tool/base/limits/
tolerances/collision policy.

**New evidence and result:** The new companion
`endpoint_candidates.corrected_static_attribution.json` has SHA-256
`ec6ad4febcf202de3469303f53ceb4d8056f14d2d6e03272a467ba5938fce362`. It
references the raw source SHA, preserves all raw request/response/FK/generic
records unchanged, issued zero additional native queries and zero IK calls, and
correctly reports **21 phase-static accepted, 0 rejected, 0 inconclusive and
21 admissible (P3 endpoint PASS)**. The former false-inconclusive fields and
their four original reasons remain embedded for causal traceability.

**Contact interpretation:** Generic static still rejects all 21 only for
`[Step 5C] DENTO Final Printable Template`↔`pneumatic_spindle-Copy` and
target-tooth↔`burr`; these are diagnostic, not admissibility gates. The
unchanged authoritative guard accepts all 21 with a configured non-rotating
template/spindle contact warning of `0.326643468686`–`0.406108573858 mm`, below
the configured `0.500000 mm` simulation limit, and explicit exploratory
burr-to-task suppression. It reports all self, unrelated-world and corridor
checks pass. Therefore the result is not literal guide/housing clearance; it is
phase-static acceptance under the unchanged policy warning. No screenshot is
claimed because static evaluation applied and displayed no candidate state.

**Verification and cleanup:** `PYTHONPYCACHEPREFIX=/tmp/dentobot-p3-static-
revalidation-pycache python3 -m py_compile` of the changed runner/test;
focused `pytest -q -p no:cacheprovider` over P3, bridge, MoveIt config,
recovery and saved-trajectory tests; and `git diff --check` passed: **80 passed
in 0.29s**. `Workspace/scripts/graphify-update.bash` completed with `Code graph
updated`; its known parser warnings remain non-fatal. A first read-only Docker
inventory was sandbox-denied on the socket, then the approved read-only check
reported only the persistent `dentobot-slicerros2` container and an in-container
process check found no P3 launcher, Slicer, launch, MoveIt or guard process.

**Decision/result:** Corrected P3 endpoint evidence is PASS and conditionally
qualifies a separately authorized P4 insertion test. P4 is **NOT RUN**. The
remaining question is whether this permitted phase-static Target can support a
bounded insertion path under independent P4 guard constraints; no action beyond
this report is authorized or performed.

## P4 insertion and P5 approach — bounded verified completion — 2026-09-16

**Operator authority and observation:** After P3 passed, the operator authorized
P4, requested diagnostic screenshots, then authorized the work required to
reach P5. The authority remains simulation-only and excludes physical/hardware
action, powered spindle, patient action, P6, P7, and a full cycle. The operator
later observed that the guide appeared to be out in the open rather than on the
open-mouth anatomy. This record retains that as an observation, not a clinical
conclusion.

**Coordinator interpretation and scene evidence:** A fresh focused P4 scene
audit found the template co-parented with `MovingLower`; the target lies within
the template bounds with signed target-to-template surface distance
`2.029787958812281 mm`. The wide transparent full-head view was therefore
misleading as a registration view. This is simulation-scene evidence only; it
does not establish physical seating, guide fit, housing clearance, or clinical
suitability. The focused registration image is
`data/Slicer_Saved/SampleStudy1/FDI31/c1-p4-fdi31-20260916-r4/screenshots/p4-r4-guide-registration.png`
(SHA-256 `a0c11ff7a87e3fcdd385d16bb65eb1bffd364d89ac91966f4473011426cd501b`).

**Implementation:** Reused the existing P3/P4 diagnostic runner and existing
facade/MoveIt planning and native guard interfaces. P5 adds only an explicit
`APPROACH_ONLY` input/output path: it reads the hashed P4 witness, uses current
monitored `/joint_states` rather than Task Home, keeps the raw stream disabled,
plans bounded Stage 1, then validates a fixed-axis Stage 2 and P4 Entry join
with validate-only guard requests. The façade direct-PreEntry seed path keeps
ordinary callers' defaults; P5 alone suppresses the generic-static diagnostic
veto already corrected in P3. Sequence allocation begins static evidence at 1
and remains uniquely increasing through the final join. Tests cover the normal
façade route plus the P5 direct-seed route and prohibit P3/P4/P5 diagnostic
branches from entering normal Connect/Task-Home/motion paths.

**Focused verification before runtime:**

- `PYTHONPYCACHEPREFIX=/tmp/dentobot-p5-pycache python3 -m py_compile` of the
  changed P5 runner/facade/test sources: PASS.
- `bash -n Testing/run_c1_p3_r4_batch.bash`, JSON parse of
  `Testing/verification_matrix.json`, and `git diff --check`: PASS.
- Focused `pytest -q -p no:cacheprovider Testing/test_step6_planning.py
  Testing/test_robot_workflow_facade.py`: **61 passed**.
- Broader focused matrix slice over `test_step6_planning.py`,
  `test_ros2_bridge.py`, `test_moveit_config.py`,
  `test_fdi31_recovery_diagnostic.py`, `test_fdi31_saved_trajectory.py`, and
  `test_robot_workflow_facade.py`: **115 passed in 1.41 s** on the final
  readback.
- The overlay-only Graphify refresh initially hit a sandbox stream-permission
  limitation, then the approved canonical `Workspace/scripts/graphify-update.bash`
  completed with `Code graph updated`; pre-existing parser warnings were
  non-fatal.

**Fresh P5 runtime and evidence:** One temporary simulation stack loaded only
locked Gate-A r4 SHA-256
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83` and P4
SHA-256 `3cc1759d1dc5042748d729d5e2a1ae784a94a60d33cb7c8350695b321492bc72`.
The authorized runtime command was:

```bash
timeout 960s docker exec dentobot-slicerros2 env \
  DENTOBOT_P5_APPROACH_INPUT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/c1-p4-fdi31-20260916-r4/insertion_branches.json \
  DENTOBOT_P5_OUTPUT_DIR=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/c1-p5-fdi31-20260916-r4 \
  bash /workspace/ros2_ws/src/DentoBot/Testing/run_c1_p3_r4_batch.bash
```

It wrote
`data/Slicer_Saved/SampleStudy1/FDI31/c1-p5-fdi31-20260916-r4/approach_branches.json`
with SHA-256 `6455a52eb170ca304f33bf1b4e76bcb583bc552cc5991971fa3e9593546b7f9b`
and result **`SAMPLED_PASS`**. The fresh monitored J1–J5 start was all zero and
stable at maximum error `0.0`; P4 source candidate 6 was selected. P5 matched
P4 native guard source/binary SHA-256
`e6c368549935abce382ad0d26980b8895fddb0128271e15e56f8ea4c13eb6fe3` /
`204da24a0572209977b9fad8e40aaa3c908ad364c2109dfdd67b4cbabf4ce7b1` and
unchanged `RRTConnectkConfigDefault` configuration SHA-256
`10f6f69a2f40f047b64430d1f408ecec0350ee29cafc27a9758d07821b16c355`.

**P5 acceptance detail:** The start static request was accepted. Stage 1
planned 208 joint waypoints at fraction 1.0 and every Phase-`approach` guard
edge completed. Stage 2 Cartesian interpolation returned no points, then the
existing bounded position-axis continuity fallback completed 12 joint waypoints
at fraction 1.0; every Phase-`terminal_contact` guard edge completed. One P4
Entry join completed. Including static evidence, all 222 request sequences are
unique/increasing and validate-only. Native Stage-2 FK residual to P4 Entry is
`0.0004285879962906579 mm` position and `0.0002018339679783406°` axis. The
artifact P4 task-identity comparison has no issues. It separately retains the
expected offline reminder to confirm an immutable Step 6 snapshot because P5
intentionally does not save, apply, or reconfirm Task Home; this did not replace
the P4 identity gate.

**Boundary, warning, and cleanup:** Raw joint streaming was false before and
after, adapter native positions remained unchanged, and no preview, applied
motion, controller/hardware endpoint, spindle/patient action, workspace
generation, policy/invariant change, P6/P7, insertion preview, Return Home,
playback, or full flow was recorded. The optional Slicer `ROS2Tests` module
attempted to auto-install `psutil` into a protected bundled site-packages path
and failed; that module is independent of the completed DENTOWorkflow/MoveIt
P5 route, so no environment mutation or rerun was made. The temporary stack
was torn down. Final read-only container inspection found no Slicer, ROS
launch, MoveIt, collision guard, or P5-runner process.

**Disposition:** P5 is reached as bounded simulation feasibility evidence;
physical guide seating, housing clearance, executable motion, clinical
acceptance, and P6/P7/full-cycle work remain unproven and unauthorized. The
next action, if desired, requires a new explicit integration scope and separate
physical/clinical review.
