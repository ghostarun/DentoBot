# Dentobot Logbook — 2026-09-01

## Stage-1 fixed drilling-frame planning

### 19:26 IST — Full-chain orientation ownership implemented in source

- Operator clarified that drilling is straight-line motion with one fixed
  drill orientation, which must be finalized at PreEntry and optimized for the
  connected Stage 2/3 task rather than Stage 1 alone.
- Audit found two residual violations: Stage 3 discarded the FK-derived
  PreEntry frame and used canonical `0°`, and diagnostic records also replaced
  the actual frame roll with `0°`. Both are corrected.
- Added policy `stage1-fixed-entry-target-frame-v1`. Entry→Target fixes tool
  +Z; the J6-locked FK branch fixes the remaining 3×3 rotation. Candidate
  diagnostics retain axis, rotation, fingerprint, chain status, stage
  fractions and normalized joints-1–5 post-PreEntry motion.
- Each reachable direct/seeded PreEntry branch is now preflighted through
  strict-axis approach, terminal contact, straight drilling and non-mutating
  guard validation before selection. Complete guarded chains rank first; then
  minimal post-PreEntry joints-1–5 motion and the existing Stage-1 score.
- Goal 2 consumes the retained drilling plan only when its Stage-1 orientation
  commitment is present. J6/collision/safety policy remains unchanged.
- Updated the focused source-contract test so it checks that Stage 3 consumes
  the committed roll and that Stage 1 selects only after full-chain preflight;
  the test was authored but not run.
- Files changed this increment: `DENTOStep6State.py`,
  `DENTORobotWorkflowFacade.py`, `DENTORobotSimulationPanel.py`, controlled
  architecture/decision/development/evidence/task/changelog documents and this
  logbook.
- Commands: read-only `sed`, `rg`, `find`, `git status`, and `date`; patch
  application; then the explicitly requested AST-only `graphify update .`.
  The final Graphify refresh completed with 5,408 nodes, 8,589 edges and 386 communities; it
  reported that semantic community labels are older than the new community
  partition, but no optional LLM label pass was requested. No test, syntax
  check, build, Slicer, ROS, MoveIt, preview, execution, hardware action, Git
  sync or Drive sync. Verification awaits the operator's explicit approval.

## Exact x4 spindle-lock/full-chain runtime

### 16:21 IST — Goal 1 preview accepted; causal Stage-2 block retained

- Reapplied commit `648307f`, then exercised
  `dentobot-case-step6x4.dentocase` in an isolated ROS/MoveIt/Slicer stack.
  No controller, hardware command, powered spindle, drilling or Execute path
  was available.
- First run exposed post-bind lineage mutation: `taskHomeRecord()` persisted a
  migration while the bundle was still being hydrated, invalidating the
  confirmed snapshot. Made hydration reads side-effect-free; explicit 6.2 save
  remains the only persistence action.
- Two abandoned launcher process trees had produced duplicate `/joint_states`
  publishers. Terminated only those identified test PIDs and changed the trial
  wrapper to launch ROS in its own process group and reap that exact group.
- Next run exposed malformed runtime workspace evidence: accepted samples used
  six bare SI values while connectivity expected `(joint name, value)` pairs.
  Corrected the producer and added transition-build normalization plus a pure
  regression.
- Live Stage 2 then rejected a `74.6 deg` orientation bridge. Root cause was
  twofold: J6-locking preserves position but changes irrelevant tool roll, and
  a stale assignment overwrote the selected FK-derived roll with the retired
  `0 deg` constant. Added FK-derived spindle-locked roll and carried the
  selected value into Cartesian planning.
- Goal 1 now probes strict-axis reach for each bounded PreEntry arm branch
  before selecting it. Exact x4 still stops at Stage 2, but now for a real
  collision boundary: MoveIt planned `50.0%` and retained 11 partial axis
  waypoints; the stack reports `link-3` contacting tooth objects. This contact
  is not the allowed burr↔target pair and was not relaxed.
- Exact-case assertions emitted `DENTOBOT_STEP65_EXACT_CASE_PASS`: 55 strict
  Home→PreEntry waypoints, guarded preview complete, one Stage-1 path, J6
  exactly `0 rad`, `full_task_status=Blocked`, Goal 2 deferred, hardware false.
  Slicer later returned process code `1` while printing the known
  VTK/class-loader teardown leaks; the PASS marker and report preceded that
  lifecycle failure, which remains unresolved and is not hidden.
- Verification results: focused Step 6 suite `62 passed, 1 deselected`; the
  deselected existing test assumes the current URDF all-zero pose passes the
  draft AABB guard and fails independently. Earlier collision-guard build and
  `dentobot_moveit_config` build remain green. Final edited-file `py_compile`
  and `git diff --check` passed. The final `graphify update .` retry succeeded:
  5,396 nodes, 8,573 edges and 403 communities were written to `graphify-out`.
- Next: normal-window review of Planner-leg/Stage evidence. Full-chain x4 work
  must change the reviewed base/Home/trajectory or find a genuinely distinct
  collision-free arm route; it must not suppress link-to-anatomy collision or
  promote the 50% partial path.

## Step 6 spindle lock and connected full-task preflight

### 15:29 IST — Source/build checkpoint reapplied after presentation rollback

- Implemented `external-pressure-spindle-locked-v1`: J6 remains represented
  but canonicalizes to zero in Task Home, restore, UI commands, IK/MoveIt
  trajectories, workspace evidence, phase commands and preview. C++ rejects a
  nonzero spindle request. Old Homes preserve joints 1–5 and stale later roll
  evidence/confirmation.
- Removed routine eight-roll planning. The one canonical PreEntry orientation
  may produce distinct direct or workspace-seeded arm IK solutions; failed
  direct routes may still use bounded reviewed 6.3 clearance detours.
- Added non-mutating C++ guard shadow state and Python orchestration to validate
  every returned Home→PreEntry→Entry→Target waypoint before full-task
  acceptance. Goal 2 consumes only the retained complete Stage-3 plan. A later
  failure keeps a provisional approach preview and reports the blocked stage.
- Diagnostics now show Planner leg/Route plus Stage 1/2/3/full-task status;
  visualization renders three transient colored paths.
- Commands/results: focused `pytest` completed `46 passed`; targeted
  `git diff --check` passed; `py_compile` passed with `/tmp` cache; explicit
  container build of `dentobot_moveit_config` passed. The host build was not
  applicable because ROS Jazzy is container-owned. No Slicer runtime trial,
  hardware motion, powered drilling or execution was performed.
- Git checkpoints: `0633214` implemented the increment; `ed6469c` temporarily
  reverted it for the operator's presentation; `648307f` reapplied it afterward.
  Concurrent pressure-analysis and earlier documentation edits were preserved.
- `graphify update .` was attempted after the final source/doc pass; the local
  watcher again failed with `Operation not permitted`, so no graph-refresh
  claim is made. Normal-window x4 full-chain acceptance is the next bounded
  action.

## Pressure analysis full-window traces

### 13:38 IST — Inspector plots are no longer locked to a pancake strip

- The analysis GUI stacked four tables under three linked plots, so each
  trace was a short wide rectangle and micro time zooms were unreadable.
- Source now uses a vertical splitter, one tabbed table panel, **Plots only**
  (F11 / Esc), a **Show** combo (or double-click) to isolate Pressure / ΔP /
  dP/dt, **Auto Y** on the visible window, and per-zoom resampling at CSV
  density instead of a whole-run stride.
- Verification: `pressure-env` `py_compile`; `--no-gui` on
  `run_20260831_181522` (40264 samples). Offscreen Qt: Plots only hid tables
  and grew the plot pane 655→823 px; focusing ΔP hid the other traces; a
  0.2 s window displayed 232 points at stride 1. No physical-display click.
  Sensing-only; no robot motion, Git, or Drive.

## Step 6.3 workspace value for Goal 1/2

### Read-only dependency and necessity review

- Queried Graphify, then inspected the current façade, workspace sampler,
  assisted-limit logic, controlled Step 6 ledger, and reproducibility contract
  to determine whether 6.3 is required or redundant for Goal 1/2.
- Finding: the current workflow makes live-valid 6.3 evidence and reviewed
  assisted limits mandatory for 6.4 confirmation and Goal 1. The direct Goal 1
  planner does not use the workspace cloud when its Task-Home-to-PreEntry plan
  succeeds. If every direct roll branch fails, it may use at most three retained
  `HomeConnected` 6.3 samples as candidate detour waypoints, replanning both
  legs in the current scene. Goal 2 does not directly search or optimize from
  the cloud; it inherits the confirmed task/limits and requires the accepted
  Goal 1 endpoint and guard session.
- Architectural conclusion: a minimal current-scene validity/limit review is a
  defensible gate, but the full generic Halton workspace cloud is not inherently
  necessary for every Goal 1/2 plan and is not a general time or path-quality
  optimizer. The broad cloud is principally design evidence, an assisted-limit
  proposal, and a bounded Goal 1 fallback library. A future simplification may
  separate mandatory 6.3 validation from optional workspace mapping or replace
  generic fallback samples with a task-directed roadmap; no source or workflow
  change was authorized or made in this review.
- No test, build, Slicer, ROS, MoveIt, preview, execution, hardware, Git, Drive,
  or external-write operation was performed.

## SlicerROS2/MRML simulation ownership review

### Repository-grounded architecture and failure analysis

- Reviewed the compact context, controlled Step 6 architecture, development,
  decision, task, setup and traceability evidence, recent 2026-08-28 through
  2026-09-01 logbook entries, and the Daily Compass as unreconciled working
  memory to explain how the in-Slicer VTK/MRML workflow differs from a
  conventional Gazebo or PyBullet simulation.
- Queried the repository Graphify graph before targeted source inspection.
  Inspected the façade/runtime split, explicit-state MoveIt FK/planning wrapper,
  collision-object naming path, guard world-object evidence, and active-ROS
  MRML scene-lifecycle defect. Cross-checked only general simulator capability
  claims against primary SlicerROS2, 3D Slicer, Gazebo and Bullet/PyBullet
  documentation.
- No Slicer, ROS, MoveIt, simulation preview, test, build, robot command,
  hardware action, Git operation or external project write was performed.
- The bundled DOCX renderer could not start because `pdf2image` is absent, and
  `pandoc` is not installed. LibreOffice text/PDF conversion emitted font-cache
  warnings; read-only OOXML extraction supplied the working-memory text. A
  sandbox `/tmp/.git` mount-quota error required a narrowly approved read-only
  extraction outside the sandbox. One later read-only `rg` command placed
  `/joint_states` in backticks inside the shell string, so Bash attempted and
  failed an unintended path substitution; the remaining searches completed
  and no state changed. No project artifact was created or edited from the
  workbook.
- Review conclusion: DENTOBOT is an image-guided planning and evidence system
  with MRML as persistent clinical/research truth and transient ROS/MoveIt as a
  plan-only kinematics/collision service. The VTK viewport is not itself a
  dynamics simulator. This architecture provides direct anatomical review and
  traceability, but makes coordinate, identity, lifecycle, callback, and
  persistence boundaries first-class engineering work that Gazebo/PyBullet
  normally hide behind a simulator-owned world.

## Goal 1 PreEntry preview acceptance

### 04:06 IST — Focused clean-stack runtime pass

- The prior exact-case run reached MoveIt and preview but the smoke harness
  failed while dereferencing an absent legacy immutable snapshot in its report.
  This was a harness/reporting defect, not a planner result. The harness now
  treats an absent restored snapshot as an expected legacy state and reports
  the live reconfirmed fingerprint only when present.
- `planApproachPhase()` now returns a truthful successful PreEntry-only
  `PhasePlan` when Home→PreEntry is fully planned but the strict terminal
  Cartesian axis segment cannot reach Entry. The V2 record is marked
  `DeferredAtPreEntry`/`ReadyForPreEntryPreview`; no partial terminal path is
  promoted and collision checking is not disabled.
- Verification commands/results:
  - `PYTHONPYCACHEPREFIX=/tmp/dentobot-goal1-pycache python3 -m py_compile`
    over the edited Step 6 Python modules: passed.
  - Focused unit suite (`test_robot_workflow_facade.py`, `test_ros2_bridge.py`,
    `test_step6_state.py`, `test_moveit_config.py`): `48 passed`.
  - Direct CMake build/install of `slicer_ros2_module`: completed at 100%.
  - Clean ROS 2/MoveIt stack status reported `planning_ready:true` and one
    joint-state publisher. Slicer/Xvfb exact-case run with
    `DENTOBOT_GOAL1_ONLY=1` emitted `DENTOBOT_STEP65_EXACT_CASE_PASS`:
    55 strict Home→PreEntry waypoints, Cartesian fraction `1.0`, guarded
    preview complete, Goal 2 deferred, hardware execution disabled.
- Runtime emitted non-fatal Slicer warnings while shutting down (missing STL
  coordinate metadata, subject-hierarchy cleanup, and VTK leak diagnostics).
  They did not affect the accepted simulation preview. `graphify update .`
  was attempted after editing but the local watcher returned
  `Operation not permitted`; graph files were not treated as source evidence.
- Remaining blocker is the strict PreEntry→Entry terminal segment; Goal 2 is
  intentionally not claimed until that reachability path is accepted.

## Step 6 saved-Home bootstrap and J2 zero convention

### 03:48 IST — Source implementation completed; verification withheld pending approval

- Operator evidence showed the grey restored MRML robot at saved Home while
  the first cyan ROS robot used default joints. Applying Home in 6.2 and then
  disconnecting/reconnecting only worked because the bridge retained the
  later vector; `DENTORobotWorkflowFacade.connect()` ignored a fresh persistent
  Home on first connection.
- Read-only extraction of retained
  `/workspace/data/Slicer_Saved/SampleStudy1/dentobot-case-step6x4.dentocase`
  found controls `[0 deg, 78 mm, 0 deg, 0 mm, 0 deg, 0 deg]` and the identical
  saved Task Home vector. The package robot profile is
  `a9f904d8778bfd524dfaecc64876f8c1519d6fe85e1ce00e6a8fc5a0801f9581`;
  its URDF SHA-256 is
  `af3862afc95fe4b071827eaf947408102a5baf12444cc44213a86813148acfc0`.
- Connect now uses a base/resource-current saved Home as the transient initial
  vector and reports that bootstrap source. The session validation key is
  still cleared, so 6.2 must establish new MoveIt/FCL evidence. Without a
  fresh Home, Connect explicitly falls back to the visible candidate.
- The tracked URDF now places J2 zero at the former fully extended `0.08 m`
  physical pose by moving its joint origin and reversing its axis. The
  equivalent coordinate is `q_new=0.08-q_old`; x4's former `78 mm` therefore
  becomes `2 mm`. J1/J3/J5/J6 already encode the previously selected Home
  offsets in their URDF origins and remain displayed as zero.
- Added a fail-closed package migration for only that exact former/current
  URDF pair. It converts J2 display/Home/task bounds, preserves physical pose
  and base, rewrites Home for the current profile as `Unreviewed`, and clears
  coordinate-dependent 6.3+ evidence. Other robot-profile mismatches remain
  blocked. The package status explains the migration and required regeneration.
- Files changed in this batch: `dentobot.urdf`, description README, Step 6
  façade, robot logic, case restore/bootstrap/lifecycle widgets, Legacy UI
  label, and controlled architecture/decision/development/evidence/task/
  changelog/context documents.
- Commands were limited to read-only source/package inspection, `sha256sum`,
  and patch application. No syntax check, diff-check, build, Slicer, ROS,
  MoveIt, test, preview, execution, or hardware action was run. Verification
  awaits explicit operator approval.

## Step 6 Goal 1 branch-continuity repair

### 02:47 IST — Operator-approved no-motion static checks passed

- After reporting the completed source changes and intended checks, received
  explicit operator approval for Python syntax compilation and Git patch
  hygiene only.
- `PYTHONPYCACHEPREFIX=/tmp/dentobot-step65-pycache python3 -m py_compile`
  passed for `DENTOROS2Bridge.py` and `DENTORobotWorkflowFacade.py`.
- `git diff --check` returned zero with no patch diagnostic. The shell printed
  the known non-fatal sandbox stream-descriptor warnings before both commands.
- Queried the repository Graphify graph and inspected the authoritative
  `DENTOWorkflow`, `slicer_ros2_module`, `dentobot_moveit_config`, and
  `dentobot_description` CMake/package boundaries to answer the operator's
  build-system questions. No graph update, build, Slicer, ROS, MoveIt plan,
  preview, execution, or hardware action was run.

### 02:43 IST — Bounded axial-roll IK planning and direct-chord diagnosis implemented

- Pushed the operator-requested checkpoint commit `6ca8869` to
  `origin/integration/gui-step6`, then resumed the paused Goal 1 diagnosis. No
  tests were run for that checkpoint.
- Read the authoritative Step 6 lock, routing map, controlled motion/evidence
  contracts, recent Daily Compass context, tracked URDF and current façade,
  bridge, MoveIt/KDL and OMPL source. Graphify was queried before source
  inspection.
- The operator's current-flow trial completed 6.1 through immutable task
  confirmation in 6.4. At 6.5 a collision-aware PreEntry IK endpoint appeared
  valid in the translucent robot, but explicit-start OMPL returned an empty
  trajectory after the old three identical 10 s attempts. The old message
  exposed only raw maximum Home/goal delta `6.25726 rad`, not its joint or a
  collision pair.
- The tracked URDF declares joints 1, 3 and 5 bounded over full-turn spans and
  joint 6 continuous. Source now normalizes `2*pi` representation only for
  joint 6; bounded joints are never wrapped through their limits. Monitored
  state comparison uses the same shortest-angle rule for that continuous
  joint.
- Goal 1 now solves collision-aware PreEntry IK across the existing bounded
  cylindrical-burr axial rolls, ranks endpoints by normalized Task-Home joint
  continuity, and gives MoveIt up to three distinct branches rather than three
  repeats of one branch. The chosen roll is held through strict-axis and
  terminal contact and becomes the drilling-preflight start roll.
- Requested/submitted goals, raw/effective per-joint deltas, named maximum
  joint and continuous wrap adjustments are retained in the action result. If
  all strict branches fail, a read-only guard-resolution direct joint chord
  reports its first exact MoveIt collision pair/fraction. A chord collision
  does not rule out a curved path; a clear chord is never promoted as a plan.
  The translucent robot is explicitly described as endpoint evidence and is
  reset to the selected plan endpoint on success.
- Collision surfaces, 1 mm research margin, non-tool/self/world checks,
  partial-plan rejection, hidden Execute and hardware prohibition were not
  changed. No Slicer/ROS/MoveIt request, build, syntax check, test, preview,
  execution or hardware action was run. Static/build/runtime verification
  awaits the operator's explicit approval.
- `graphify update .` was attempted at both the outer workspace and actual Git
  checkout after editing, but the local watcher failed with `Operation not
  permitted`; no semantic/API pass was attempted.

## Cursor CRD profile auto-share

### 02:35 IST — ΔP / change-point pressure detector

- Replaced the 2 s residual DIP/PEAK detector with `pressure_filter.py`:
  5-sample median, 15 ms fast LPF, 400 ms slow LPF, ΔP, filtered dP/dt.
  Air-off and spinup are not scored. Detection arms only while air is on
  and the annotator marks DRILL IN DENTIN. Events are STEP vs TRANSIENT
  with before/after medians. Live GUI: filtered P, ΔP, dP/dt, p90−p10.
  Raw ADC/pressure still stored. Cues and F1–F4 annotations unchanged.
- Verification: `pressure-env` `py_compile`. Synthetic 0→225 then 225→218
  kPa with DENTIN armed after spinup: one STEP (−6.9 kPa) at 2.26 s, no
  spin-up event. `--no-gui run_20260831_181522`: 40264 samples, 0
  redetected tissue events (unmarked). Live serial/GUI not run.
  Sensing-only. No Git/Drive/secrets/patient data.

### 00:32 IST — `cursor-xfce` prefers host chat profile when free

- Diagnosed missing agent chats on Chrome Remote Desktop: CRD Cursor was
  launched with `--user-data-dir=~/.config/cursor-crd-profile` while host
  chats live in `~/.config/Cursor` (~529 MB vs ~40 MB profile DBs). Chats
  were not deleted.
- Updated `/home/light-tarun/.local/bin/cursor-xfce` so `CURSOR_XFCE_PROFILE`
  defaults to `auto`: use host profile when its `code.lock` PID is dead and
  no host main Cursor process is running; otherwise fall back to
  `cursor-crd-profile`. Overrides: `host` / `crd`.
- Verified detection with only the CRD instance running → selects host
  profile. Documented in `docs/SETUP.md` under CRD.
- Operator must fully quit the current CRD Cursor window and relaunch via
  `cursor-xfce` (or the Remote XFCE menu entry) to pick up host chats.
  Do not run two Cursors against `~/.config/Cursor` at once.

### 03:20 IST — Goal 1 TCP waypoint path and clean-stack acceptance

- Added a transient `[Step 6] Planned TCP Path (Approach)` MRML model. Its
  points are recomputed from the selected MoveIt joint waypoints through the
  SlicerROS2 KDL chain and the locked base transform, in world RAS mm. The
  model is display-only, excluded from collision payloads, saved with the
  scene disabled, and removed whenever the transient phase session is cleared.
- Connected the model to the shared View catalog and trajectory appearance
  controls so the operator can show/hide or adjust opacity without touching
  case geometry. Goal 1 planning now refreshes the catalog immediately and
  reports whether the path was displayed.
- Exact-case clean ROS 2/MoveIt acceptance command completed with
  `DENTOBOT_STEP65_EXACT_CASE_PASS`: 55 strict Home-to-PreEntry waypoints,
  complete guarded PreEntry preview, and the smoke assertion found one path
  model containing the expected polyline. Goal 1 terminal Entry remained
  deferred (`goal1_terminal_points=0`); Goal 2 remains blocked by design.
- Focused host gate remained green (`48 passed`), edited Python modules passed
  `py_compile`, and the prior isolated `slicer_ros2_module` build/install was
  already green. Runtime shutdown still prints known VTK/class-loader leak
  warnings after the explicit PASS; this does not invalidate the planning
  result but remains a lifecycle follow-up.
- `graphify update .` was retried after the edits; the local watcher again
  failed with `Operation not permitted`, so no graph refresh claim is made.

### 04:25 IST — Goal 1 stabilization postmortem and documentation sync

- Goal 1 is now accepted through the collision-free PreEntry milestone on the
  exact x4 saved case: 55 strict Home→PreEntry checkpoints, guarded preview
  complete, and the transient TCP path model present. Entry and Goal 2 remain
  unaccepted because the terminal axis segment is still deferred.
- Root causes of the earlier failures were endpoint IK being confused with a
  reachable path, repeated planning of one distant branch, continuous-angle
  representation jumps, collision-proxy/source-ID disagreement, default ROS
  pose on first connect, and partial Cartesian results being over-promoted.
- Implemented fixes are explicit Home start ownership; continuous J5/J6
  normalization; eight ranked axial-roll candidates; bounded 6.3 detours;
  canonical collision IDs and stale-ID cleanup; V2 per-candidate collision and
  last-valid diagnostics; fail-closed PreEntry fallback; and the KDL-derived
  display-only TCP polyline integrated into Views/opacity controls.
- Evidence recorded: `48 passed`, Python compilation, `git diff --check`, and
  clean-stack `DENTOBOT_STEP65_EXACT_CASE_PASS`. Known VTK/class-loader exit
  leak warnings remain a lifecycle issue only.
- Added reusable keyword `DENTO-POSTMORTEM-SYNC` to `AGENT_CONTEXT.md`. Future
  uses perform this generic controlled-doc/logbook reconciliation and Git
  synchronization; Google Drive, the personal journal, and `TASKS.md` require
  separate approval.

### 20:02 IST — Approved fixed-frame implementation verification

- The approved focused pure suite completed with `45 passed, 1 deselected`;
  the deselected test is the unrelated draft-AABB all-zero-pose assumption.
- Edited Step 6 Python files passed `py_compile`; `git diff --check` passed.
- The isolated exact-x4 ROS/MoveIt/Slicer smoke run emitted
  `DENTOBOT_STEP65_EXACT_CASE_PASS`: 55 guarded Home→PreEntry waypoints,
  one provisional Stage-1 path, J6 exactly `0 rad`, unit tool axis, and a
  non-empty fixed-frame orientation fingerprint.
- Full-chain status remained truthfully `Blocked` at Stage 2: the strict-axis
  segment reached `50.0%` before MoveIt reported forbidden `link-3`↔tooth
  contact. No terminal/drilling path, Goal 2 plan, collision relaxation,
  execution, or hardware action was enabled or promoted.
- The process later printed known VTK/class-loader teardown warnings and exited
  nonzero after the PASS marker; this remains an unresolved lifecycle issue,
  separate from the accepted planner milestone.
- Final `graphify update .` completed after the source/test edits: 5,414 nodes,
  8,600 edges, and 391 communities were written to `graphify-out`. Semantic
  community labels were not refreshed.
- Final rerun used `PYTHONPYCACHEPREFIX=/tmp/dentobot-pycache-final` and
  `--basetemp=/tmp/dentobot-pytest-final` to avoid repository cache permissions;
  the same focused suite again completed `45 passed, 1 deselected`.
- The full pure Python sweep then exposed two stale test expectations: the API
  contract omitted the intentional `runtime_validation` keyword and the robot
  placement test retained the retired J2 zero pose. Both expectations were
  updated; the full sweep now completes `131 passed, 1 deselected`.
- Graphify was refreshed again after those test-contract edits: 5,414 nodes,
  8,600 edges and 391 communities. No semantic label pass was run.

### 20:18 IST — DENTO-NOTE Priority 0: saved Step 6 checkpoint resume

- Triage: active fix. `.dentocase` correctly excludes ROS/MoveIt nodes and
  plans, but the current loader deletes transient local robot nodes and leaves
  the runtime disconnected, so packages saved after 6.2–6.5 require manual
  reconstruction before 6.5/6.6.
- Required contract: after load, best-effort simulation-only reconstruction
  may load the local robot, reconnect ROS/MoveIt, apply/revalidate saved Home,
  replay saved workspace evidence, and reconfirm the task. It must stop at the
  first missing or stale operator-owned prerequisite and report that reason;
  it must never serialize ROS objects or silently promote stale plans.
- Implemented the case-load backend resume callback. After package integrity
  validation it reconstructs the local robot, reconnects simulation ROS/MoveIt,
  reapplies Task Home, replays saved workspace evidence, reconfirms the task,
  and selects 6.5. It stops at the first missing/stale prerequisite when a
  checkpoint is partial or the external stack is unavailable.
- Verification: `widget_case_backend.py` compiled with a temporary bytecode
  prefix; full pure suite completed `131 passed, 1 deselected`; `git diff
  --check` passed. Graphify was refreshed to 5,420 nodes, 8,610 edges and 402
  communities. Normal-window package-resume verification remains pending.
- Documentation reconciliation: `DEVELOPMENT_PLAN.md` now has an explicit
  Priority-0 DENTO-NOTE register for both the fixed drill-frame/full-chain
  requirement and saved Step 6 checkpoint resume. `TASKS.md` carries matching
  separate Priority-0 work rows with their current evidence and next acceptance.

### Controlled plan/task deduplication

- Audited all 24 second-level sections and 1,830 lines of the prior
  `DEVELOPMENT_PLAN.md`, and all 42 second-level sections and 2,382 lines of the
  prior `TASKS.md`. The redundancy was semantic rather than literal: current
  Step 6 work, DENTO-NOTEs, old `Active`/`Next` lists, completed evidence, and
  dated checkpoints repeatedly described the same work under different status
  labels.
- Preserved both complete pre-cleanup documents under
  `Workspace/docs/archive/2026-09-01/` rather than deleting history.
- Replaced the live Development Plan with one implementation-order contract,
  one Step 6 ownership table, bounded work-package contracts, a compact future
  roadmap, and explicit evidence-status rules.
- Replaced the live Tasks file with one stable-ID queue. Merged the repeated
  Goal 1/spindle/full-chain narratives into `S6-P0-01`, checkpoint restore and
  modular continuation into `S6-P0-02`, Step 6A safeguards into `S6-P1-01`,
  and retained the restored-review, slider, progress, and GUI items as distinct
  Priority-2/3 tasks. Kept active-ROS lifecycle and physical mount truth as
  separate unprioritized issues because they are different failure domains.
- Preserved non-Step-6 open work as compact umbrella rows rather than losing it
  in the archive: representative imaging, Step 4/4C/5 acceptance, clean Ubuntu
  image, Windows launcher, workstation stability, aggregate Slicer exit,
  medical-image/transform interoperability, and Template V0/physical closure.
- Marked runtime-first ordering, collision-audit identity, Viewer integrity,
  schema-v1 restore, the old world/base conversion defect, spindle-roll
  planning, and modularization as closed or superseded pointers rather than
  duplicate active workstreams.
- No source code, runtime, test, build, Google Drive, personal journal, or Daily
  Compass action was performed for this documentation-only cleanup. Per the
  operator's approval rule, no verification script or `git diff --check` was
  run; verification remains pending explicit approval.

### `S6-P0-01` implementation resumed — bounded arm-branch expansion

- Root-cause finding from the current source: full-chain ranking was present,
  but PreEntry IK collection stopped at Task Home plus the first three
  Home-connected 6.3 samples. The x4 package contains eight Home-connected
  representatives, so potentially distinct joints-1–5 postures were never
  submitted. Different IK seeds are meaningful; spindle-roll rows are not.
- Updated Stage 1 to consume every bounded Home-connected 6.3 representative
  (up to the existing 13-sample connectivity bound), deduplicate identical
  joints-1–5 solutions, and keep J6 fixed at `0 rad`.
- Added an authoritative MoveIt static-state audit after canonicalizing each IK
  endpoint to J6 zero. A raw collision-aware IK result cannot enter planning if
  the actually submitted spindle-locked state is invalid.
- Added the originating 6.3 sample to each diagnostic record and the normal
  diagnostic table, so direct, seeded, and clearance routes can be distinguished
  without implying that spindle angle is a route degree of freedom.
- Added one focused pure regression case covering Task Home plus all eight
  Home-connected seeds and confirming every audited endpoint has J6 zero.
- No test, build, Slicer runtime, MoveIt trial, `git diff --check`, Graphify,
  commit, push, Drive sync, Daily Compass, or personal-journal action was run.
  The source remains explicitly unverified pending the operator's required
  approval gate.
