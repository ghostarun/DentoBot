# Dentobot Logbook — 2026-09-01

## Cursor CRD profile auto-share

### 02:35 IST — ΔP / change-point pressure detector

- Replaced the 2 s residual DIP/PEAK detector with `pressure_filter.py`:
  5-sample median, 15 ms fast LPF, 400 ms slow LPF, ΔP, filtered dP/dt.
  Air-off and spinup are not scored. Detection arms only while air is on
  and the annotator marks DRILL IN DENTIN. Events are STEP vs TRANSIENT
  with before/after medians. Live GUI: filtered P, ΔP, dP/dt, p90−p10.
  Raw ADC/pressure still stored. Cues and F1–F4 annotations unchanged.
- Verification: `pressure-env` `py_compile`. Synthetic 0→225 then 225→218
  kPa with DENTIN armed after spinup: one STEP (−6.9 kPa) at 2.26 s, no
  spin-up event. `--no-gui run_20260831_181522`: 40264 samples, 0
  redetected tissue events (unmarked). Live serial/GUI not run.
  Sensing-only. No Git/Drive/secrets/patient data.

### 00:32 IST — `cursor-xfce` prefers host chat profile when free

- Diagnosed missing agent chats on Chrome Remote Desktop: CRD Cursor was
  launched with `--user-data-dir=~/.config/cursor-crd-profile` while host
  chats live in `~/.config/Cursor` (~529 MB vs ~40 MB profile DBs). Chats
  were not deleted.
- Updated `/home/light-tarun/.local/bin/cursor-xfce` so `CURSOR_XFCE_PROFILE`
  defaults to `auto`: use host profile when its `code.lock` PID is dead and
  no host main Cursor process is running; otherwise fall back to
  `cursor-crd-profile`. Overrides: `host` / `crd`.
- Verified detection with only the CRD instance running → selects host
  profile. Documented in `docs/SETUP.md` under CRD.
- Operator must fully quit the current CRD Cursor window and relaunch via
  `cursor-xfce` (or the Remote XFCE menu entry) to pick up host chats.
  Do not run two Cursors against `~/.config/Cursor` at once.

