# DENTOBOT Development Log — 2026-08-10

## 15:59 IST — zoomable isolation and ROI-Z-only simple cut

### Report and diagnosis

- The developer reported that the isolated shell could leave the useful
  viewport area while the camera lock prevented zoom recovery. The simple
  horizontal cut also exposed more transform freedom than needed after the
  Step 5B ROI frame had already been locked.
- Source inspection found the camera Modified-event correction repeatedly
  restoring `ParallelScale`. The plane observer corrected only its normal,
  while Markups translation handles remained enabled.
- Read-only inspection of the installed Slicer 5.10 Markups Plane source
  confirmed that Point-Normal mode with `NormalPointRequired(False)` has one
  required/maximum control point; the ROI header also confirmed the world-axis
  getter used for the constraint. A first combined lookup also attempted a
  host search from the wrong working directory and a Docker read without
  socket permission; the bounded commands were retried separately and
  succeeded without changing files. Slicer was not launched.

### Implementation

- Removed parallel-scale correction from the persistent camera lock. Entering
  isolation explicitly still resets yaw and performs the initial ROI Z fit;
  afterward zoom is available with yaw enabled or frozen. Plane/curve actions
  no longer reset the user's scale.
- Added an ROI-frame constraint helper for the simple plane. Each placement or
  edit preserves only signed height along the current Step 5B ROI Z axis,
  snaps the origin onto that axis, fixes the normal to ROI `+Z`, uses one
  origin control point, and hides translation/rotation/scale handles.
- Persisted the plane-to-ROI MRML reference and constraint attribute. Widget
  refresh migrates loaded planes, and finalization re-enforces and validates
  the ROI link, face-parallel normal, and no lateral displacement before use.
- Updated Step 5C labels/tooltips and the controlled architecture, decision,
  plan, task, changelog, and logbook records. No environment or dependency
  changed, so `SETUP.md` was not modified.

### Evidence and remaining acceptance

- Added a synthetic rotated-ROI regression covering Z-axis projection,
  retained signed height, normal, one-point contract, hidden handles, ROI
  reference, and rejection of tilted/laterally displaced planes.
- `python3 -m py_compile`, `xmllint --noout`, `git diff --check`, and the
  repository static gate passed. The Slicer-native test and live interaction
  remain pending because no Slicer launch was authorized.
- Live acceptance must confirm wheel zoom under both lock states, yaw plus
  zoom, orientation-lock plus zoom, one-click/one-point vertical adjustment,
  persistence after save/reopen, the selected kept side, and acceptable FPS.
- No patient data, inference, mesh/STL operation, ROS, robot, motion, drilling,
  Slicer launch, Git commit/push, or Drive sync occurred. Changes remain
  uncommitted on top of local commit `c574094`.

## 15:38 IST — preserve the selected MPR plane during trajectory correction

### Report and diagnosis

- The developer's first live review found the MPR useful but identified a
  fatal correction flow: after choosing an angle, dragging Entry or Target
  caused the MPR and visible points to reorient abruptly instead of allowing
  correction in that selected view.
- Source inspection confirmed every point event called
  `computeTrajectorySliceMatrix()` with the unchanged slider number but a new
  Entry→Target direction. That rebuilt transverse axes from world `+Z`/`+Y`,
  so the numeric angle did not preserve the previously viewed plane.
- Installed Slicer 5.10 headers were inspected read-only in the existing
  container and confirmed `PointStartInteractionEvent` and
  `PointEndInteractionEvent`. The first lookup used `rg`, which was absent in
  the container; the bounded retry used `grep` and succeeded. Slicer was not
  launched.

### Correction

- The selected slice is held unchanged throughout an active Markups drag so
  the control point stays under the pointer. Point and measurement state still
  update; the reslice is scheduled once the interaction ends.
- Added a reusable transport calculation. It projects the previous plane
  normal onto the corrected trajectory's perpendicular space, reconstructs
  slice X and the longitudinal trajectory Y, and applies only the change from
  the last slider angle. Therefore an edit made in the displayed plane keeps
  its exact normal. If the new trajectory is parallel to the previous normal,
  the previous horizontal axis provides a stable finite fallback.
- Selecting another trajectory or enabling the feature starts from the
  deterministic absolute frame. **Reset Orientation** also deliberately
  returns to deterministic world-reference 0°.
- Updated the Step 4A tooltip and the controlled architecture, decision, plan,
  task, changelog, and logbook records. No environment or dependency changed,
  so `SETUP.md` was not modified.

### Evidence and remaining acceptance

- Added math regressions for exact in-plane normal preservation, off-plane
  minimal transport, singular fallback, slider-delta rotation, midpoint,
  endpoint containment, finite axes, orthonormality, handedness, and invalid
  delta rejection.
- `python3 -m py_compile`, `xmllint --noout`, and `git diff --check` passed
  before and after the controlled-record update.
- `Infrastructure/close_day_checks.sh` passed 22 Python AST files, two Qt UI
  XML files, eight Markdown fence checks, and the overall static gate. Backend
  tests were skipped because no backend source or dependency changed.
- Live Slicer acceptance must confirm no cursor chasing during a drag, exact
  circumferential-plane continuity after release, smooth subsequent slider
  motion, deterministic Reset, and acceptable FPS. No patient data, inference,
  mesh/STL operation, ROS, robot, motion, drilling, Slicer launch, Git commit/
  push, or Drive sync occurred in this correction batch. The preceding source
  checkpoint remains the local commit `c574094`; this correction is uncommitted.

## 14:50 IST — trajectory-aligned longitudinal oblique MPR

### Request and architecture inspection

- The developer authorized an out-of-plan Step 4A verification feature:
  display a longitudinal CBCT plane containing the fixed Entry→Target line and
  rotate that plane circumferentially about the trajectory. Perpendicular
  depth cross-sections, safety analysis, bur simulation, optimization, and
  template changes were explicitly excluded.
- Source inspection confirmed `trajectoryLine` is already the parameter-node
  MRML reference; `getTrajectorySummary()` reads both points in world RAS; and
  `_bindPlanningTrajectoryNode()` already observes point definition, movement,
  removal, and generic changes. The authoritative segmentation already points
  to its source CBCT. No parallel trajectory or image model was needed.
- Read the installed Slicer 5.10 source in the existing container. Its
  `vtkMRMLSliceNode` contract confirms `SliceToRAS` columns 0/1/2 are slice X,
  slice Y, and slice normal and that direct matrix `DeepCopy` must be followed
  by `UpdateMatrices()`. The Markups display API confirms native 2D slice
  projection support.

### Implementation

- Added singularity-safe `computeTrajectoryFrame()` and
  `computeTrajectorySliceMatrix()` logic. World `+Z` is the preferred
  reference, switching to world `+Y` near parallel. The right-handed matrix
  uses rotated transverse X, Entry→Target as vertical slice Y,
  `cross(X,Y)` as normal, and the Entry/Target midpoint as origin.
- Added Step 4A enable, integer `-180°..+180°` slider, angle/status display,
  and reset. It selects the first available layout slice view without looking
  up an MRML node by name, sets the referenced source CBCT as that view's
  background, and projects the existing trajectory for a reliable overlay.
- Rotation and point changes are event-loop-coalesced. They update only the
  native slice matrix when it differs. Initial framing uses a target-bounds
  diagonal so all rotation angles fit without per-angle zoom changes.
- Captured/restored slice matrix/FOV, composite volume/link state, and line
  display/projection state. Restoration runs on disable, module exit, scene
  close, cleanup, before Step 5C isolation, and before MRB save; the active
  view resumes after save so transient presentation is not serialized.
- Added focused Slicer-native regression cases for arbitrary and vertical
  axes, orthonormality, handedness, midpoint, zero/90-degree matrices,
  in-plane endpoints, degenerate paths, and non-finite rotation.

### Commands, evidence, and remaining acceptance

- Used `rg`/`sed` for repository inspection and read the relevant Slicer
  headers/source through bounded `docker exec` commands. No Slicer process or
  medical dataset was opened.
- `env PYTHONPYCACHEPREFIX=/tmp/dentobot-pycache-oblique python3 -m py_compile
  DENTOWorkflow/DENTOWorkflow.py`, `xmllint --noout
  DENTOWorkflow/Resources/UI/DENTOWorkflow.ui`, and `git diff --check` passed.
- `bash Infrastructure/close_day_checks.sh` passed 22 Python AST files, two
  Qt UI files, eight Markdown fence checks, and the overall static gate;
  backend tests were skipped because no backend source changed and no backend
  interpreter was supplied.
- At 15:00 IST, the final pass after adding the missing-display-node overlay
  fallback and simplifying one test expression repeated Python compilation,
  UI XML parsing, `git diff --check`, and `Infrastructure/close_day_checks.sh`;
  all passed with the same static-gate counts. An initial `git diff --check`
  was accidentally issued from `/home/light-tarun/dentobot`, which is outside
  the nested Git worktree and returned exit 129 without changing files; it was
  immediately repeated successfully from `ros2_ws/src/DentoBot`.
- Live Slicer acceptance remains required for overlay direction, visible
  circumferential anatomy changes, immediate point updates, exact restoration,
  save/reopen behavior, near-vertical paths, and slider FPS. The new
  Slicer-native tests were added but not executed because no runtime launch was
  authorized.
- No external inference, patient-data inspection, CBCT copy, segmentation,
  mesh operation, STL export, ROS, robot, motion, drilling, fabrication, Git
  commit/push, or Google Drive synchronization occurred.

## 14:11 IST — ROI-aligned Step 5C isolation and immutable bounds

### Request and confirmed interaction contract

- The developer rejected the initial Step 5C isolation UX. The fixed anterior
  camera, simultaneous 2D/3D layout, and camera lock made editing chaotic and
  laggy. The required replacement handles one upper or lower jaw shell at a
  time and uses the Step 5B ROI as a traditional XYZ frame.
- The developer confirmed zero yaw as camera look direction ROI `+Y`, ROI `+X`
  to viewport right, ROI `+Z` to viewport up, and yaw about ROI `+Z`. The ROI
  top and bottom must meet the 3D viewport top/bottom boundaries.
- Translation, zoom, pitch, and roll are locked by default so native 3D motion
  becomes a 360-degree yaw-only turntable. A second checkbox freezes yaw too;
  clearing the first lock deliberately enables a free camera.
- The developer also required the Step 4A target bounds and Step 5B shell ROI
  to be completely workflow-generated and non-customizable in slice/3D views.

### Diagnosis and implementation

- Inspection found Step 5B explicitly called `SetLocked(False)`. Step 4A set
  the node locked and hid handles at creation, but did not disable view
  selection or reapply the policy to all loaded nodes. The old camera observer
  also rewrote orientation on every camera Modified event while keeping the
  existing multi-view layout.
- Replaced isolation with a one-up 3D workspace. It captures the prior Slicer
  layout, 3D camera, crosshair mode, and display visibility, then restores all
  of them on explicit exit, module exit, scene close, or widget cleanup.
  Isolation hides unrelated nodes and does not create a plane/curve or start
  Markups placement; the edit buttons remain separate deliberate actions.
- Derived an orthonormal camera frame from the current Step 5B ROI. The camera
  position is an ROI-Z yaw orbit around the ROI center; parallel scale is
  exactly half the ROI Z size. A live frame label reports XYZ convention,
  current yaw, and yaw-only/frozen/free mode. The isolated viewport also shows
  Slicer's XYZ axes marker and restores the prior marker/axis-label state on
  exit.
- Reworked the camera observer to coalesce correction to one event-loop task,
  capture only the yaw component, and set only camera properties that drifted.
  The default lock restores focal point, orbit distance, ROI-Z view-up,
  parallel projection, and scale while preserving yaw. Yaw lock preserves the
  captured angle too.
- Added one reusable ROI interaction policy: node lock on, view selection off,
  and translation/rotation/scale handles off. It applies to new, reset, and
  loaded Step 4A/5B role-owned ROIs. Automatic Step 5B generation resets the
  ROI to axis-aligned Step 5A bounds before creating the shell. The existing
  `TemplateShellTrimROI` role remains for MRB compatibility even though its
  earlier user-adjustable meaning is superseded.

### Files and verification

- Changed `DENTOWorkflow/DENTOWorkflow.py` and
  `DENTOWorkflow/Resources/UI/DENTOWorkflow.ui`. Updated the controlled active
  and canonical architecture, development plan, decisions/tasks where
  applicable, changelogs, and logbooks.
- Added parameter-default coverage for yaw lock, deterministic zero/90-degree
  camera pose/yaw assertions, locked/non-selectable/no-handle ROI assertions,
  and a regression that deliberately unlocks Step 5B before workflow refresh
  and verifies it is relocked.
- `env PYTHONPYCACHEPREFIX=/tmp/dentobot-pycache-roi-view python3 -m
  py_compile DENTOWorkflow/DENTOWorkflow.py` passed.
- `xmllint --noout DENTOWorkflow/Resources/UI/DENTOWorkflow.ui` and `git diff
  --check` passed.
- `bash Infrastructure/close_day_checks.sh` passed 22 Python AST files, two Qt
  UI XML files, eight Markdown fence checks, and the overall static gate.
  Backend tests were skipped because backend source did not change and no
  backend interpreter was supplied.
- No Slicer process was launched because the request authorized implementation
  but not that separate runtime action. Native mouse yaw direction, exact
  visual top/bottom fit, layout/camera/crosshair/visibility restoration, and
  interaction FPS therefore remain developer-run acceptance items.
- No inference, patient/anatomy inspection, STL export, ROS, robot, motion,
  drilling, fabrication, Git commit/push, or Google Drive synchronization ran.
