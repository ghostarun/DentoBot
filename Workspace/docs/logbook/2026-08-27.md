# DENTOBOT logbook — 2026-08-27

## Step 6 saved-case deadlock and one-card substep UX

- User evidence:
  - retained `dentobot-case-step6.dentocase` loaded through Step 5C, but the
    Step 6 panel reported no active scene and disabled 6.0A and every later
    action while still showing a `ProvisionalLocked` base;
  - the full 6.x control stack rendered in one long scrolling panel;
  - durable `DENTO-NOTE`: split Step 6 into substeps for a smoother, less
    chaotic workflow.
- Root cause:
  - `step6PlanningContextFreshnessIssues` mixed current Steps 0–5 package
    freshness with the post-import case-jaw prerequisite;
  - restore therefore cleared `step6PlanningContextImported` when historical
    packages lacked the newer four-landmark opening, but retained a locked
    base, causing a circular UI gate;
  - the first focused Slicer regression exposed a second race: stale status
    and Boolean unlock were emitted as separate parameter-node events, so the
    UI observed the intermediate `locked + Stale` pair and rewrote status to
    `ProvisionalLocked`.
- Implementation:
  - added a package-only freshness boundary for required planning roles and
    Steps 4A/4C/5C state; the combined readiness API still includes 6.0A after
    import;
  - current packages now remain the active Step 6 case when 6.0A is missing;
    any reviewed pre-opening base is retained as `Stale` and unlocked, with
    typed fields, source revision, MRML lock attribute, plane lock/selectability,
    and transform handles synchronized atomically;
  - task confirmation/workspace state is invalidated; no saved coordinate or
    source geometry is altered and no ROS runtime is restored;
  - added one shared visibility controller for seven cards: 6.0 Case and Task,
    6.1 Robot and Base, 6.2 Task Home, 6.3 Workspace and Limits, 6.4 Runtime and
    Confirmation, 6.5 Goal 1 Approach, and 6.6 Goal 2 Drilling Preview;
  - the normal module now has a compact selector plus Back/Next controls and
    shows one card at a time; the application shell uses the same controller.
    Substep navigation is presentation-only and does not write MRML or create
    ROS objects.
- Files changed in this increment:
  - `logic_robot.py`, `widget_lifecycle.py`, `widget_case_backend.py`,
    `widget_robot_shell.py`, `widget_bootstrap.py`, `runtime.py`,
    `DENTOApplicationShell.py`, `slicer_tests.py`,
    `test_application_shell.py`, and
    `run_dentobot_existing_case_restore_smoke.py`;
  - controlled architecture, decision, persistence, tasks, and this logbook.
- Commands/results:
  - task-specific `compileall` completed successfully;
  - the first host run after the UX test addition returned `99 passed, 1
    failed`; the failure was a static assertion looking for a method call
    instead of its definition and did not indicate a runtime defect;
  - final `pytest -q Testing` returned `100 passed`; `git diff --check` passed;
  - the initial focused restore-gate Slicer test failed because the non-atomic
    stale/unlock transition was rewritten to `ProvisionalLocked`; after the
    transaction fix it printed `DENTOBOT_STEP6_RESTORE_GATE_PASS`;
  - the synthetic restore-gate case printed
    `DENTOBOT_STEP6_RESTORE_GATE_PASS`: its imported Steps 0–5 context remained
    active, 6.0A was enabled, and the old base became unlocked `Stale`;
  - the current retained archive records
    `planningContextImportedAtSave: false`. Its twice-open regression preserves
    that saved false state rather than promoting it, keeps Step 4C/5C current,
    restores no ROS robot, and prints `DENTOBOT_EXISTING_CASE_RESTORE_PASS`;
  - focused legacy/shell card visibility and Back/Next navigation printed
    `DENTOBOT_STEP6_SUBSTEP_NAVIGATOR_PASS` and created no ROS node or case-state
    mutation;
  - Slicer runs emitted ROS-module load warnings because the focused commands
    did not source the full ROS runtime library environment. Those tests were
    deliberately ROS-disconnected and did not claim ROS/MoveIt verification.
- Safety/evidence boundary: no robot motion, drilling, hardware execution,
  patient-facing operation, anatomical validation, or clinical claim was made.
- Remaining operator acceptance: restart or cleanly reload DENTOWorkflow,
  reopen the same retained package without re-saving/exporting, confirm its
  saved false Step 6 import state exposes the explicit Import action, then
  complete 6.0A and walk the seven visible substeps. Review scroll focus,
  wording, viewport composition, and gating in a normal workstation window. No
  Git commit or Google Drive sync was requested or performed.

## DENTO-NOTE — modular saved-case continuation

- **User requirement:** loading a saved package must support modular Step 1–6
  continuation so the operator can select and resume from any workflow step.
- **Triage:** active investigation. The explicit navigation/load behavior and
  retained-package regression were implemented immediately; a package matrix
  saved independently at each coarse Step 1–6 checkpoint remains required for
  full closure.
- **Decision:** preserve the existing whole-case transactional MRB/lineage
  restore. Modularity begins only after integrity commit: every Legacy stage
  and all six New GUI workspaces remain selectable, while each stage's own
  saved prerequisites and Current/Stale lineage gate its actions. No partial
  node import, duplicate geometry store, transform change, or saved ROS runtime
  was introduced.
- **Implementation:** added explicit selectable-stage/workspace wording and
  tooltips to both presentations; package-load status now directs the operator
  to the picker and states that all Steps 1–6 are available; the exact retained
  package smoke now visits every internal Legacy stage and all six New GUI
  workspaces after both restores, requires its card/workspace to activate, and
  rejects a live ROS robot.
- **Files changed for this note:** `widget_navigation.py`,
  `widget_case_backend.py`, `DENTOApplicationShell.py`,
  `run_dentobot_existing_case_restore_smoke.py`,
  `test_application_shell.py`, and the controlled context, architecture,
  decision, plan, task, reproducibility, and logbook documents.
- **Verification:** task-specific `compileall` and `git diff --check` passed;
  `pytest -q Testing` returned `101 passed`. The first Slicer invocation used
  `--no-main-window` and failed before package load because the application
  shell requires a main window. The corrected normal-window Xvfb run then
  exposed an outdated fixture assumption: the archive truthfully records
  `planningContextImportedAtSave: false`, while the test required `true`.
  The test now compares against that saved value instead of silently promoting
  it. The final normal-window run opened the package twice, visited every
  internal Legacy stage and all six New GUI workspaces, preserved current Step
  4C/5C geometry and final verification, created no live ROS robot, and printed
  `DENTOBOT_EXISTING_CASE_RESTORE_PASS` with exit 0.
- **Safety boundary:** no ROS connection, robot motion, drilling, hardware
  action, patient-facing operation, or clinical claim occurred.

## Step 6A anatomy-directed jaw preparation and governed fallback

- **User objective:** finish Step 6A, prove that the retained
  `dentobot-case-step6.dentocase` imports into it, and verify that consecutive
  controls enable in the intended order. If the anatomy-directed opening cannot
  be completed for a valid case/solver reason, provide a simple target-jaw-only
  placement-testing branch without attempting another jaw transform.
- **Implementation:** added a schema-v2 anatomy-directed hinge solver that
  probes and brackets only the gap-increasing direction; exact-source landmark
  association/projection for lower-jaw condylar surfaces and target-side upper/
  lower central-incisor surfaces; bilateral/order/level/FOV gates; immutable
  source anatomy; fixed-upper and moving-lower derived segmentations; and a
  persisted preparation record. Derived segmentations use closed surface as
  their source representation to avoid allocating unintended full-volume binary
  labelmaps during save.
- **Fallback:** a valid recorded primary failure may create one untransformed
  target-jaw segmentation containing its jawbone and all same-jaw teeth as
  separate segments. It is labelled `PlacementTestingOnly` and may unlock local
  robot/base placement, Task Home, assisted-limit review, and coarse workspace
  exploration. It cannot unlock ROS connection, planning-scene sync, task
  confirmation, approach planning, or drilling preview. Missing landmark work,
  inactive package context, locked/ROS-active state, or invalid target anatomy
  cannot authorize it.
- **Restore/gating correction:** importing a saved pre-Step-6A locked base now
  retains its pose but atomically unlocks it as `Stale`, invalidates dependent
  task/workspace state, and allows Step 6A review. Local robot load/base lock use
  the placement-readiness gate; Connect and planning-scene sync continue to use
  the full open-mouth gate.
- **Collision/visualization path:** primary anatomy is exposed as independently
  visible fixed/moving segmentations and synchronized as one validated,
  triangulated, watertight collision surface per retained jaw/tooth segment.
  The fallback exposes only the target jaw for viewing and coarse workspace;
  its planning-scene synchronization is rejected.
- **Files changed for this increment:**
  `DENTOWorkflow/Resources/Python/DENTORobotPlacement.py`,
  `DENTORobotWorkflowFacade.py`, the `logic_step6_scene`, `logic_robot`,
  `logic_robot_scene_sync`, `logic_robot_placement`, `logic_case_bundle`,
  `parameter_state`, `logic_constants`, `runtime`, Step 6 widget/view modules,
  `DENTOWorkflow.ui`, `slicer_tests.py`, `test_robot_placement.py`, and
  `run_dentobot_step6a_package_gate_smoke.py`; controlled architecture,
  decision, development-plan, task, traceability, and this logbook record were
  updated.
- **Commands/results:** Python AST parsing of eight changed implementation/test
  files passed. `pytest -q Testing` returned `103 passed in 0.54s`. Focused
  headless Slicer primary/fallback create-reset-save-reopen checks printed
  `DENTOBOT_STEP6A_SAVE_RESTORE_PASS` with exit 0. The exact retained package
  smoke printed `DENTOBOT_STEP6A_PACKAGE_GATE_PASS ... upper 0` with exit 0:
  package import stayed current, the first source-specific surface placement
  action started on the intended segment, 6.1/local robot load remained gated
  before preparation, missing landmark evidence did not authorize fallback,
  and no local/ROS robot restored. The twice-open retained-package smoke also printed
  `DENTOBOT_EXISTING_CASE_RESTORE_PASS` with exit 0 and restored no ROS robot.
- **Errors resolved during verification:** the first synthetic Slicer run lacked
  an input CBCT; the fixture now supplies one. Derived segmentations initially
  caused `bad_alloc` by implicitly requesting a binary labelmap; closed-surface
  source representation fixed it. Source visibility restoration initially ran
  in the wrong order; reset now restores the saved state. The retained package
  initially disabled the first landmark because of its historical base lock;
  import now converts that lock to reviewed-stale/unlocked state. One mistyped
  test-script path and one terminated wrapper were non-product test harness
  errors; clean reruns passed and left no Slicer/Xvfb process. The final host
  `compileall` attempt could not replace container-owned `__pycache__` entries;
  a write-free AST parse of all 95 implementation/test Python files passed,
  followed by `PYTHONDONTWRITEBYTECODE=1 pytest -q -p no:cacheprovider Testing`
  (`103 passed in 0.55s`) and `git diff --check` (no findings).
- **Remaining acceptance:** perform a normal-window operator placement of all
  four real-anatomy landmarks, orthogonal-MPR review, primary solver/anatomy-
  warning review, and live per-segment MoveIt/contact checks. These are not
  claimed by the automated PASS results. No hardware motion or drilling ran.

## Step 6A exact-package fallback, retry, Views, and robot-persistence correction

- **User evidence reproduced:** `dentobot-case-step6x1.dentocase` restored a
  target-jaw fallback with a provisionally locked base, made the primary 6A
  controls unreachable after progressing to 6.4, kept ROS blocked, and showed
  a cluttered fallback viewport whose manual Views choices were overwritten.
  The older `test1_5C_FD14.mrb` exposed four Markups points but Apply rejected
  them for missing source-specific surface snapping.
- **Recovered archive evidence:** the x1 parameter node records the initiating
  failure verbatim as `All four landmarks must be placed with source-specific
  surface snapping before opening the case jaw.` This is a missing-provenance
  prerequisite, not an anatomical or hinge-solver failure. The earlier
  substring classifier incorrectly authorized fallback. Both retained Step 6
  packages also contained seven saved local MRML robot link models/transforms,
  although those nodes are reconstructible simulation resources.
- **Implementation:** added exact current landmark-evidence validation including
  the recorded snapped world-RAS point, source segment, and source fingerprint;
  legacy/moved/stale evidence now disables Apply and exposes `Replace
  unreviewed landmarks`. Such failures cannot authorize fallback. A restored
  fallback now exposes `Exit Fallback and Retry Primary 6A`; it clears fallback,
  failure and landmark evidence, invalidates downstream task state, and retains
  the base pose as unlocked `Stale`. ROS remains blocked in fallback by design.
- **Views correction:** recommended fallback composition now excludes the final
  template, docks, and target ROI. Recommended presets may still enforce
  source/derived separation, but a user's subsequent simple-list or advanced-
  tree visibility toggle is authoritative and is no longer immediately
  overwritten.
- **Robot persistence correction:** newly reconstructed local robot model,
  display/storage, and link-transform nodes are `SaveWithSceneOff`. Loading an
  older package deletes those transient nodes and clears façade runtime state;
  the persistent base/mount intent is retained separately. `Load / Refresh
  Robot` reconstructs the seven links explicitly.
- **Files changed in this correction:** `logic_step6_scene.py`,
  `widget_robot_scene.py`, `widget_views.py`, `widget_view_controls.py`,
  `widget_view_catalog.py`, `logic_robot_placement.py`, `widget_lifecycle.py`,
  `slicer_tests.py`, the two Step 6A package smokes, and the controlled
  architecture/decision/tasks/traceability/logbook documents.
- **Verification:** write-free host suite returned `103 passed in 0.56s`;
  `git diff --check` passed. Exact original-package normal-window Xvfb printed
  `DENTOBOT_STEP6A_PACKAGE_GATE_PASS ... upper 0`; focused primary/fallback
  geometry and MRB round-trip printed `DENTOBOT_STEP6A_PRIMARY_FALLBACK_PASS`;
  and the exact x1 fallback/retry/view/package-switch run printed
  `DENTOBOT_STEP6A_RETRY_RESTORE_PASS` after proving the explicit local robot
  reconstruction created seven non-persistent links. Slicer emitted expected
  module-registration warnings because these focused tests intentionally ran
  without ROS module paths/runtime.
- **Remaining acceptance:** cleanly reload the module, open x1, use the explicit
  retry transition, replace all four points through guided surface placement,
  and report the next primary failure if a genuinely reviewed anatomy attempt
  fails. Normal-window MPR/anatomy review and live ROS/MoveIt acceptance remain
  pending. No motion, drilling, hardware, patient-facing, or clinical claim was
  performed.

## DENTO-NOTE — Step 5C exported-STL evidence during Step 6 restore

- **Triage:** backlog by explicit user instruction; no behavior implemented.
- **Finding:** Step 6 does not need an exported STL for placement or simulation;
  current MRML segmentation/model geometry is the coordinate/provenance
  authority. STL is a Step 5C manufacturing/handoff artifact.
- **Risk and next action:** do not silently claim a historical STL is verified,
  but do not create a second geometry authority or block valid Step 6 restores.
  Design and test an optional export-artifact manifest/checksum and clear
  `present`/`missing`/`not exported` manufacturing status before considering
  any future manufacturing-specific gate.

## DENTO-NOTE — Priority 1: Step 6A TMJ logic, landmark, and rigid-transform safeguards

- **User confirmation:** prioritize correct condylar landmark safeguards and
  RAS laterality before implementing the live mouth-opening slider. After Left
  TMJ placement, provide a visual contralateral guide and hinge-axis feedback
  so Right TMJ placement is symmetrical and avoids an unintentionally oblique
  axis.
- **Priority/triage:** Priority `1` on the user-defined `0 = highest/immediate`
  scale; active backlog. This blocks the slider increment and representative
  anatomy acceptance, but does not invalidate the existing save/restore and
  source-provenance tests.
- **Code-review evidence:** Slicer RAS is `+X` patient Right, while the current
  validator/fixture require Left-TMJ X greater than Right-TMJ X. Both TMJs are
  associated with the complete lower-jaw segment; incisal points use complete
  central-incisor segments; MPR review is recorded as Pending but not enforced.
- **Accepted scope:** correct laterality and fixtures; add separately
  fingerprinted left/right condylar and upper/lower crown-incisal candidate
  surfaces; enforce exact-source snap and reviewed MPR; add a transient
  contralateral guide, live hinge-axis line, separation, superior-level and
  anterior-posterior offset feedback. Assistance must not force artificial
  symmetry over actual anatomy.
- **Rigidity/solver boundary:** accept only one finite orthonormal world-RAS
  rigid transform about the reviewed hinge axis; preserve both hinge points,
  fixed-upper anatomy, source masks, and CBCT IJK-to-RAS; transform only moving-
  lower anatomy and explicitly attached proxies. Fail closed for degenerate,
  inverted, oblique/non-homologous, ambiguous, unreachable, non-monotonic, or
  non-rigid results.
- **Verification due:** pure laterality/guide tests, wrong-subregion rejection,
  Slicer pickability and MPR gates, save/reopen, and a representative normal-
  window CBCT trial. No implementation or clinical claim was made this turn.

## DENTO-NOTE — Priority 2: realtime incisor-gap preview and explicit lock

- **User-confirmed behavior:** dragging the gap slider displays only a smooth
  projected jaw pose. `Lock / Accept Opening` alone commits the new transform,
  derived positions and collision-ready geometry.
- **Priority/dependency:** Priority `2` on the user-defined
  `0 = highest/immediate` scale; sequenced backlog gated on the Priority 1 TMJ
  logic, landmark, laterality, and rigid-transform safeguards above.
- **Architecture decision:** use a transient `SaveWithSceneOff` preview
  transform or display-only equivalent while dragging. Do not regenerate
  segmentations, write lineage, synchronize MoveIt, or repeatedly stale the
  base/task. On explicit lock, run the full validation once, commit the
  persistent transform/preparation record and derived positions, validate
  per-segment MRML collision surfaces, and invalidate downstream state once.
  Actual MoveIt collision objects remain transient and are created later by
  explicit Connect/Sync, not by the lock button.
- **Planned verification:** reachable/monotonic gap mapping, solver equivalence,
  smooth coalesced rendering, no source-coordinate mutation during preview,
  single lock invalidation, preview discard/save/reopen, and no ROS object
  creation. Documentation only; no code, robot motion, or collision runtime was
  changed this turn. `git diff --check` passed after the controlled-document
  update.

## DENTO-NOTE — Priority 2: progressive Restored Case Review dashboard

- **User requirement:** after atomic `.dentocase` load, provide a separate
  semi-manual review panel that walks Steps 1–6, combines automated integrity/
  freshness evidence with visual inspection, exposes details and relevant
  viewport framing, and lets the operator stop at an affected step and redo the
  downstream chain.
- **Illustrative behavior:** Steps 1–3 may show all automated checks passed plus
  visual confirmation; a stale Step 4 trajectory reports `Needs attention`
  with details and a revalidate/regenerate recommendation; Step 5 and Step 6
  report `Blocked upstream by Step 4` rather than being mislabeled as corrupt.
- **Priority/triage:** Priority `2` on the user-defined `0 = highest/immediate`
  scale; backlog by explicit request. No popup, state schema, or restore behavior
  was implemented in this capture.
- **Accepted boundary:** preserve whole-package transactional loading and saved
  stale geometry for comparison. Separate archive integrity, automatic
  Current/Stale lineage, and operator visual review. Manual review cannot
  override hard geometry/provenance/safety failures, and the panel cannot create
  ROS/robot runtime state.
- **Next verification:** dependency-status aggregation, exact detail/action
  messages, review-state persistence/invalidation design, normal-window visual
  trial, and Steps 1–6 saved-checkpoint matrix.

## Project instruction — DENTO-NOTE priority ordering

- **User requirement:** make the numeric DENTO-NOTE priority scheme part of the
  repository instructions and use it to order backlog work.
- **Policy added:** `Priority 0` is highest/immediate, followed by Priority 1,
  Priority 2, and increasing numbers. The lowest-numbered safe, authorized,
  actionable backlog item is selected first; explicit prerequisite sequencing
  is preserved and documented. Unprioritized notes remain `Unprioritized`
  instead of receiving an inferred number.
- **Boundary:** priority changes work order, not authority. It cannot bypass
  safety, verification, scope, external-write approval, or robot/hardware
  restrictions. Triage state remains separate from numeric priority.
- **Files changed:** `Workspace/AGENTS.md`, `Workspace/docs/TASKS.md`, and this
  logbook. No application/runtime code, ROS state, robot motion, Drive sync, or
  Git commit was performed.

## DENTO-NOTE — Priority 3: New GUI aesthetic upgrades and optimization

- **User instruction:** plan the New GUI aesthetic/optimization work now and
  implement it later at Priority 3.
- **Triage:** planned backlog. It follows actionable Priority 1 and Priority 2
  work unless explicitly reprioritized; no GUI/runtime implementation occurred
  in this capture.
- **Plan recorded:** baseline all six workspaces and substeps across window
  sizes/themes/scaling; establish shared accessible design tokens/components;
  improve progressive disclosure, status hierarchy, navigation consistency,
  persistent Views access, and viewport allocation; then optimize lazy hidden-
  workspace updates, coalesced MRML/UI refreshes, reused models/controllers, and
  duplicate callback/widget prevention.
- **Acceptance boundary:** require Legacy/New action and state parity, keyboard/
  focus/contrast/scaling review, normal-window screenshot comparison,
  save/restore/reload/New Case trials, bounded update/widget/callback behavior,
  and proof of no geometry, coordinate, lineage, renderer, ROS, or safety-gate
  side effects. Legacy remains the fallback and New GUI remains opt-in until
  that evidence passes.
- **Files changed:** `Workspace/docs/TASKS.md`,
  `Workspace/docs/DEVELOPMENT_PLAN.md`, and this logbook only. No application
  code, robot motion, Drive sync, or Git commit was performed.

## DENTO-NOTE — Priority 0: Step 6 Viewer integrity and native crash

- **User evidence:** after loading `dentobot-case-step6x1.dentocase`, Step 6
  displayed internal dental anatomy instead of the enclosing teeth/jaw;
  changing Viewer elements then crashed Slicer.
- **Triage:** fix now at user Priority 0. No ROS, motion, drilling, hardware, or
  patient-facing operation was authorized or performed.
- **Reproduction and root cause:** a normal-window exact-package smoke first
  failed because Recommended retained source internal anatomy. Repeated changes
  through the real Advanced `QTreeWidget` then ended in Slicer's abnormal
  native exit without a Python exception. Code inspection confirmed that the
  fallback composition selected source `full_anatomy`, separation hid only
  source jaw/teeth, and `itemChanged` synchronously cleared/rebuilt its own
  tree. After deferring the rebuild, the test exposed a further logical race:
  Qt-generated `PartiallyChecked` ancestor notifications were treated as hide
  requests. A later controlled assertion showed source per-dimension flags also
  needed explicit clearing after a custom internal-anatomy toggle.
- **Implementation:** classified current Step 6 fixed/moving/fallback
  segmentations as `case_jaw_opening`; changed fallback Recommended to derived
  jaw/teeth + CBCT slices + trajectory + robot context with no source anatomy;
  made node-level re-show restore every derived segment; synchronized general,
  2D, and 3D per-segment display flags; moved separation after all segmentation
  selection; deferred/coalesced Viewer refresh; ignored automatic partial
  states; and routed the legacy list through the same visibility setter.
- **Files changed:** `logic_display.py`, `widget_bootstrap.py`,
  `widget_view_catalog.py`, `widget_view_composition.py`,
  `widget_view_controls.py`, `widget_views.py`, `widget_lifecycle.py`,
  `Testing/run_dentobot_step6_view_integrity_smoke.py`,
  `Testing/test_module_reload.py`, and the controlled
  context/architecture/decision/tasks/traceability/logbook documents.
- **Verification commands/results:**
  - exact normal-window x1 Viewer stress:
    `DENTOBOT_STEP6_VIEW_INTEGRITY_PASS 16 54`, exit `0`;
  - exact x1 fallback/retry/package-switch regression:
    `DENTOBOT_STEP6A_RETRY_RESTORE_PASS`, exit `0`;
  - composable Views regression: `DENTOBOT_COMPOSABLE_VIEWS_PASS`, exit `0`;
  - five internal/helper reload cycles:
    `DENTOBOT_FIVE_RELOAD_CYCLES_PASS`, exit `0`;
  - `pytest -q Testing`: `104 passed`;
  - `git diff --check`: passed with no output.
- **Recorded non-product test errors:** the first accidental `--no-main-window`
  run could not construct the palette and returned `Could not find main
  window`. An unscoped host `pytest -q` also collected Inference tests outside
  this change's configured environment and failed on missing
  `dentobot_inference`/`nibabel`; the repository-prescribed `pytest -q Testing`
  gate passed. Two Docker calls initially hit sandbox socket permission and
  passed when rerun through the approved container-test path.
- **Evidence boundary:** display-node presentation and callback-lifecycle
  evidence only. No mask voxels/surfaces, CBCT geometry, IJK-to-RAS/world-RAS,
  transforms, lineage, save-package content, ROS state, planning, or motion
  were changed or clinically validated.

## Step 6A exact x1 landmark review, transient-state normalization, and opened-case Views

- **User evidence/triage:** the operator could see four apparently correct
  landmarks but could not apply 6A. This continued the Priority-1 TMJ logic
  note as an active partial fix and the Priority-0 Viewer note as fix-now. No
  ROS connection, motion, drilling, hardware, or patient-facing action ran.
- **Read-only exact-package diagnosis:** on
  `dentobot-case-step6x1.dentocase`, Left TMJ, Right TMJ, upper incisor and
  lower incisor were respectively within `0.000042512`, `0.000000429`,
  `0.000882611`, and `0.000000708 mm` of the four currently intended surfaces.
  Left X `-132.735245 mm` was correctly lower than Right X `-47.763081 mm` in
  patient RAS; separation was `85.013973 mm` and superior offset
  `2.656853 mm`. The points were therefore not rejected for bad placement.
- **Root causes:** the package lacked current source-evidence records and also
  retained `PendingLandmarkIndex=3`, so the UI believed lower-incisor placement
  was still active and kept review disabled. The anatomy validator separately
  used the wrong RAS laterality inequality. After review/apply, generic Markups
  lock/selectability `ModifiedEvent`s falsely staled the unchanged transform.
  Finally, opened-case Recommended still selected source `full_anatomy`, which
  reintroduced closed-pose internal masks around the derived jaws.
- **Implementation:** corrected laterality to anatomical `Left X < Right X`;
  added an explicit, atomic four-point review/reprojection path with a `5 mm`
  maximum residual; canceled pending Place/source/visibility-snapshot state
  before save and after validated restore while retaining raw points; improved
  one-point-at-a-time UI guidance; changed stale detection to compare the
  coordinate fingerprint; and made current opened-case Recommended use only
  the derived fixed-upper/moving-lower jaw-and-teeth groups. Source internal
  anatomy remains available as an explicit custom view.
- **Files changed in this continuation:**
  `DENTORobotPlacement.py`, `logic_step6_landmark_review.py`,
  `logic_step6_scene.py`, `widget_robot_scene.py`, `widget_case_backend.py`,
  `widget_lifecycle.py`, `widget_view_composition.py`, `runtime.py`,
  `slicer_tests.py`, `test_robot_placement.py`, both Step 6A/Views exact-case
  smoke scripts, CMake/routing metadata, and controlled documents.
- **Verification commands/results:**
  - exact x1 diagnostic after review: no evidence issues; maximum reprojection
    residual `0.000882611 mm`; the pre-direction-check solver numerically
    reached `39.996773 mm` at `+40.048828°`, later rejected after the operator
    observed that it moved the mandible superiorly;
  - normal-window exact x1 review/open/reset/package-switch regression:
    `DENTOBOT_STEP6A_RETRY_RESTORE_PASS`, exit `0`;
  - normal-window real Viewer tree stress after opening:
    `DENTOBOT_STEP6_VIEW_INTEGRITY_PASS 31 54`, exit `0`;
  - focused `test_DENTOWorkflowStep6CaseJawOpening`: exit `0`;
  - final `pytest -q Testing`: `105 passed in 0.58s`;
  - final `git diff --check`: passed with no output.
- **Errors encountered and resolved:** the first retry smoke expected an older
  saved fallback state, but inspection proved x1 now truthfully stores
  `ClosedSource`; the smoke was updated to test its current saved state. The
  next two exact runs exposed and then fixed the stale pending-placement flag
  and non-geometric Markups invalidation. The first updated Viewer stress
  failed because source internal anatomy remained enabled; the opened-case
  composition fix made the rerun pass.
- **Remaining Priority-1 acceptance:** complete lower-jaw/central-tooth
  membership is not condylar-lateral-pole/incisal-edge evidence. Dedicated
  source-fingerprinted candidate regions, enforced orthogonal MPR review,
  contralateral guide/hinge feedback, wrong-subregion rejection, save/reopen of
  that stronger evidence, and a representative operator trial remain due. No
  anatomical, clinical, registration, collision, or hardware claim is made.
- **External state:** no Google Drive sync and no Git commit were performed.

## Step 6A inferior opening-direction correction

- **Operator evidence:** the 40 mm transform completed, but the mandibular
  anatomy rotated above the maxilla. The user requested the opposite signed
  hinge branch (`-Z` / negative theta).
- **Root cause:** branch selection maximized the infinitesimal increase in
  straight-line incisor distance. On x1, `+0.25°` increased the gap while
  moving the lower incisor `+0.2221 mm` superiorly; the anatomically inferior
  `-0.25°` branch initially reduced the gap slightly, so the old monotonic
  bracketing discarded it.
- **Implementation:** case Step 6A now selects the unique probe branch with
  negative lower-incisor RAS-Z displacement, samples it until the first target-
  gap crossing, bisects that crossing, and rejects any final solution that does
  not retain inferior displacement. The draft phantom solver is unchanged.
- **Exact x1 result:** angle `-45.781250°`, gap `39.996424 mm`, closed lower-
  incisor Z `51.003372 mm`, opened Z `21.760961 mm`, and
  `delta Z = -29.242411 mm`.
- **Verification:** focused pure geometry tests `9 passed`; the exact normal-
  window review/open/reset/package-switch smoke, now asserting negative angle
  and inferior gap-line endpoint, printed
  `DENTOBOT_STEP6A_RETRY_RESTORE_PASS`; final repository gate returned
  `106 passed`; `git diff --check` passed.
- **Boundary:** this is a provisional pure-hinge direction constraint, not a
  clinical TMJ motion model. Condylar/incisal subregions, MPR review, soft-
  tissue/contact behavior, translation, and representative operator acceptance
  remain outside this correction. No ROS, motion, drilling, hardware, Drive
  sync, or Git commit was performed.

## Step 6.5 Goal 1 partial Cartesian-path diagnosis

- **Operator evidence:** after completing through 6.4, **Plan Guarded
  Approach** displayed the light-blue goal robot but failed with `MoveIt
  planned only 0.9% of the requested path.` The task was diagnosed only; no
  runtime/product code, joint command, preview, execution, drilling, or
  hardware action was authorized or performed.
- **Stage isolation:** live `/tmp/dentobot-simulation-stack.log` proves the
  collision-aware RRTConnect current-to-pre-entry joint plan completed
  successfully. The subsequent collision-disabled `/compute_cartesian_path`
  request attempted six waypoints with 1 mm interpolation and returned
  `0.858283%`. Jump checking was disabled. This excludes target contact,
  ordinary MoveIt collision checking, and jump threshold as the cause of this
  planning percentage.
- **Root cause:** `tool_pose_matrices_world_mm()` returns raw world-RAS
  matrices. `PlanMoveItCartesianTrajectoryFromPoseMarkers()` honors
  `relativeToNode` for MRML transform nodes, but its raw-matrix branch deep
  copies without conversion. `plan_moveit_cartesian_path()` supplies raw
  matrices plus the base, so world poses are mislabeled as `base_link` poses.
  The blue robot is the preceding correctly framed pre-entry IK result, not
  proof that the differently framed Cartesian request is valid.
- **Exact-case correlation:** live guard fingerprint
  `39201d8f79a4a9ebee2290dfe7f2f37415123187b8b654aee038dba27584c27c`
  matches `dentobot-case-step6x4.dentocase`. Its locked base and task give a
  correct pre-entry `[-75.128281, 16.324510, 119.832905] mm` in `base_link`;
  current code sends world `[-74.433660, -66.834729, 47.057526] mm` as local,
  producing a `110.5088 mm` position mismatch plus orientation mismatch.
- **Planning-only A/B verification:** a temporary ROS service diagnostic made
  no publisher/joint/preview calls. With the same x4 geometry, corrected
  world-to-base poses returned Cartesian fraction `1.0` and 18 trajectory
  points; the current raw-world-as-base poses reproduced fraction
  `0.008582834331337325` and 33 points. This establishes the primary defect and
  shows that the exact 5 mm terminal pose chain is kinematically solvable.
- **Commands/evidence reviewed:** inspected the façade, DENTOROS2 bridge,
  SlicerROS2 pose-marker builder/C++ service adapter, URDF/TCP and joint limits;
  filtered the live simulation log; read x4 manifest/MRML state without
  extraction; computed the exact rigid-frame discrepancy; and ran
  `/tmp/diagnose_step65_frames.py` through the active simulation stack with a
  30-second bound. A first host calculation failed because host NumPy is not
  installed and was repeated with standard-library arithmetic. One Docker
  inspection initially hit sandbox socket permission and succeeded through the
  approved read-only container path.
- **Required next implementation:** convert DENTOBOT world matrices explicitly
  to base-local matrices before the upstream call, pass no ambiguous relative
  node for those already-local matrices, cover Goal 1 and Goal 2, and add an FK
  first-waypoint continuity assertion plus stage-specific UI diagnostics. Then
  rerun exact x4 planning, guarded preview, focused Slicer/ROS smokes,
  `pytest -q Testing`, and `git diff --check`. Full Step 6.5 remains unverified
  until that implementation and guarded-preview acceptance are complete.

## graphify install for Cursor and Codex (economic agentic coding)

### 22:32 IST — Request

- Install **graphify** locally so Cursor and Codex agents can query a codebase
  knowledge graph instead of repeatedly grepping/reading raw files (lower token
  use for architecture questions).

### 22:35 IST — Installation

- **Important package name:** PyPI package is `graphifyy` (two y's). CLI
  command is `graphify`. Do **not** `pip install graphify` (unrelated package).
- Installed with pipx on host Python 3.14.6:
  - `python3 -m pip install --user pipx`
  - `pipx install graphifyy` → **graphify 0.9.50**
  - Binary: `~/.local/bin/graphify` (already on PATH)
- Project integration (from `/home/light-tarun/dentobot`):
  - `graphify cursor install` → `.cursor/rules/graphify.mdc` (`alwaysApply: true`)
  - `graphify codex install` → graphify section in `Workspace/AGENTS.md`
    (root `AGENTS.md` is symlinked) + `.codex/hooks.json` PreToolUse no-op hook
- Initial index (AST-only, no LLM cost):
  - `graphify update .` in 4.8 s
  - **7374 nodes**, 11396 edges, 578 communities
  - Outputs: `graphify-out/graph.json`, `graphify-out/graph.html` (aggregated
    view because >5000 nodes), `graphify-out/GRAPH_REPORT.md`
  - 9 C++ files in `slicer_ros2_module` had partial syntax-extraction warnings
- Added `graphify-out/` to `.gitignore` (local index; regenerate locally).
- Test query:
  - `graphify query "DENTOWorkflow Step 6 ROS motion control" --budget 800`
    returned 381 scoped nodes (truncated by budget).
- **Not done:** `graphify hook install` failed with
  `No git repository found at or above /home/light-tarun/dentobot` on this
  workstation snapshot. Re-run after git is initialized if auto-update on
  commit is desired.

### 22:36 IST — Operator usage

**Rebuild after code changes (free, local AST):**

```bash
cd /home/light-tarun/dentobot
graphify update .
```

**Query from terminal:**

```bash
graphify query "<architecture question>"
graphify path "<symbol A>" "<symbol B>"
graphify explain "<concept>"
```

**In Cursor:** rule auto-applies; agents should prefer `graphify query` before
broad grep/read. First-time full semantic pass: type `/graphify .` in chat
(uses assistant model for docs — costs tokens; code AST pass is local).

**In Codex:** guidance is in `AGENTS.md`; hook is intentional no-op on Codex
Desktop — graph instructions come from AGENTS.md.

### Problems / limits

| Issue | Notes |
|-------|-------|
| Large graph (>5000 nodes) | `graph.html` is aggregated; use CLI `query`/`path`/`explain` |
| No git hooks | Install failed — no `.git` at repo root in this environment |
| C++ parse warnings | Some SlicerROS2 headers partially extracted; Python/docs dominate |
| Semantic doc pass | Optional; needs API key only if running full `/graphify` with docs |

No robot motion, patient data, credentials, Git commit, or Google Drive sync.
