# DENTOBOT Engineering Log — 2026-09-11

## W5-U-04 FDI31 component-localization continuation

- **Operator authorization:** “Continue” after the 2026-09-10 FDI31 first
  causal failure and the blocked diagnostic-retry boundary. This authorizes one
  headless, read-only FDI31 rebuild solely to emit the retained fusion
  components' RAS centre/bounds.
- **Scope:** `runtime.pulp_shell_fdi31` against
  `data/Slicer_Saved/SampleStudy1/FDI31/run2-dentobot-case-step6x5.dentocase`.
  No parameter change, artifact-threshold change, saved-package write, ROS,
  MoveIt, robot motion, hardware action or normal-window review is included.
- **Preflight:** `dentobot-slicerros2` was running and no Slicer, ROS, MoveIt
  or DENTOBOT process was present. Runtime ownership is exclusive for this
  diagnostic attempt.

### Result and evidence boundary

- The approved FDI31 rebuild reached final fusion after the shell pass and
  retained `[45966, 5, 2, 2, 1, 1, 1, 1]` occupied regions. The four-voxel
  ceiling remains three at this spacing, so the one-solid gate correctly
  rejected the 5-voxel fragment; no threshold or geometry parameter changed.
- The largest retained region is centred at RAS
  `(-93.572, -34.898, 56.827)` with bounds
  `x=[-94.170, -92.973], y=-34.898, z=56.827`. It is approximately 1.83 mm
  radially from the FDI31 trajectory centreline and at least 7.66 mm from any
  dock-bore axis. This is evidence that it is likely a central
  trajectory-guide/sleeve-envelope remnant rather than a dock attachment;
  source-mask contribution diagnostics are still required before declaring it
  independent or changing construction.

## W5-U-04 FDI11 final-fusion visual evidence

- **Operator request and scope:** the operator asked to see screenshots of the
  test-run final fusion dock. A headless, read-only FDI11 saved-case rebuild
  exported one fused-template overview and one bore-axis view for each of the
  four docks. No package was saved or changed; no ROS, MoveIt, robot, hardware
  or normal-window operation was performed.
- **Capture hygiene:** the first capture included planning overlays, and the
  first isolation retry hid the final model itself. Those images are not used
  as evidence. The capture helper now hides all scene display nodes, then
  explicitly enables only the fused final model before image export.
- **Final result:** the corrected rebuild exited 0 with
  `DENTOBOT_PULP_SHELL_PASS`; it produced 71,620 triangles, zero
  boundary/non-manifold edges, one occupied region `[62258]`, and zero
  residual occupied samples in the 6,086 final-channel samples. The four
  bore-axis images visibly show open apertures. Evidence files are
  `/tmp/dentobot-verification/w5u04-fdi11-final-capture-re8vgq/`.
- **Static checks:** source compilation without bytecode output and
  `git diff --check` both passed. Direct `py_compile` could not write its
  repository `__pycache__` in this environment, so the no-write compile check
  was used instead; this is an environment limitation, not a code result.

## Commit readiness verification

- **Host regression:**
  `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q -p no:cacheprovider
  Testing/test_case_bundle.py Testing/test_robot_workflow_facade.py
  Testing/test_step6_state.py` passed `58 passed in 0.70s` (exit 0).
- **Static/package checks:** all changed Python files compiled with
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-verification/commit-static-2gAlqy/pycache
  python3 -m py_compile ...`; `python3 -m json.tool
  Testing/verification_matrix.json` and `git diff --check` passed. The
  generated bytecode stayed under `/tmp` and no repository cache was changed.
- **Publication boundary:** the current branch is `main`; the working tree
  contains the complete current development state since `a09beb5`. The commit
  will include project code, tests, controlled `Workspace/docs` records and the
  new reusable-case runner/logbook only. Engineer-owned records and generated
  verification PNGs remain outside Git.
- **Local publication:** commit `3040d00` (`fix(workflow): harden unified
  template fusion diagnostics`) was created with a clean tree. The first
  `git push origin main` attempt failed in the sandbox because GitHub DNS was
  unavailable. The escalated retry was safety-blocked because `origin` resolves
  to the external destination `https://github.com/ghostarun/DentoBot.git` and
  the complete source/internal-doc payload requires explicit destination
  confirmation. No remote state changed; the local commit remains one commit
  ahead of `origin/main`.
- **Confirmed publication:** the operator explicitly confirmed that exact
  destination and payload. Commit `8ba971c0c66375ba1ec0e9e851e295ef6183b38b`
  was then accepted by GitHub as `origin/main`; `git ls-remote` returned the
  same commit and local/remote ancestry is synchronized (`0 0`).

## Pending-only backlog and project-tracker reconciliation

### Operator request and interpretation

- **Operator statement:** Create `backlog.md`, summarize pending planned tasks
  and backlog distinctly from the Project Tracker, identify overlaps, require
  every future plan to check this file, and remove accomplished work after its
  evidence is recorded in the logbook and TASKS/DEVELOPMENT_PLAN as applicable.
  The stated purpose is to prevent lost backlog work, repeated tasks and
  redundant implementation during new major plans.
- **Engineering interpretation:** Make backlog.md the only pending queue while
  retaining detailed contracts/history in TASKS.md, milestone/acceptance design
  in DEVELOPMENT_PLAN.md, decisions in DECISIONS.md and chronology here. A task
  remains pending through required acceptance, even if source code is complete.

### Sources and reconciliation result

- Read the controlled routing, project/setup, task, development-plan, decision,
  context-sync, verification-protocol and current logbook records. No
  engineer-owned local holding file was opened.
- The current message explicitly named and requested summarization of `IITM
  Dental Drilling Robot — Project Tracker`. Google Drive read-only inspection
  identified spreadsheet ID
  `1W118Z6oDqfA6IOBXg3llCo6-IowoSAif1HxuTFK7NHU`, seven tabs, and a tracker
  status reconciled through 3 September. Read bounded ranges from Dashboard,
  Priority Focus, Master Work Register and Decisions/Risks; no Drive write was
  performed.
- The Master Work Register contains 38 action rows: 30 unfinished and eight
  marked complete. Every unfinished ID is now retained in backlog.md directly,
  as an alias, or through an explicit overlap mapping to a current local owner.
  The eight source-complete rows were excluded from the pending queue; follow-up
  work embedded in A-033/A-034 was mapped to existing platform items.
- Newer local evidence supersedes the tracker's old Step-6 91.7%/x4 status and
  older broad software umbrellas. Conflicting source priorities for A-006 versus
  A-024 and A-036 register versus dashboard are preserved for later resolution.
  No priority was invented or changed.

### Controlled documentation changes

- Added `docs/backlog.md` with distinct active/planned, queued-milestone,
  DENTO-NOTE/design/verification backlog, tracker-only obligation and overlap
  sections.
- Updated AGENTS.md with a mandatory backlog-first gate and completion-removal
  protocol. Updated AGENT_CONTEXT.md, PROJECT_CONTEXT.md and CONTEXT_SYNC.md to
  route pending work through backlog.md. Updated
  REPRODUCIBILITY_AND_TRACEABILITY.md status ownership accordingly.
- Retired the duplicate TASKS.md front-page queue while preserving its detailed
  task contracts and completion records. Added the tracker migration crosswalk
  and DENTO-NOTE lifecycle.
- Updated DEVELOPMENT_PLAN.md ownership language and removed stale F0/F1/F2
  rows in favor of the already accepted DCP/DSS study roadmap. Recorded this
  policy in DECISIONS.md.

### Verification and evidence boundary

- Documentation-only verification: `git diff --check`; targeted searches for
  stale TASKS-first/active-queue ownership and obsolete F0/F1/F2 rows; manual
  ID accounting against the 38 tracker action rows; and final diff inspection.
- Final results: `git diff --check` exited 0; the stale-ownership/roadmap search
  returned no matches; and a direct lookup for all 30 unfinished tracker IDs in
  backlog.md reported no missing IDs. The final heading check found the two
  planned-work sections, two backlog sections, overlap section and protected
  live parallel lane.
- No source implementation, runtime, ROS, MoveIt, Slicer, robot, drilling,
  patient-facing, hardware, Git publication or Drive mutation was authorized or
  performed by this documentation task.

### Next bounded action

- Before the next plan, read/search all of backlog.md, compare matches and
  overlaps, then open only the linked detailed contracts and current evidence.
  Maintain the queue in the same turn whenever an item's state or acceptance
  changes.

### Parallel implementation coordination update

- **Operator statement:** Current active implementation is proceeding in
  parallel on the reusable-case foundation and Step 6 repositioning. It must be
  considered now and must not be repeated later, to avoid chaos.
- **Triage:** Active implementation under the existing
  `S6-REUSABLE-CASE-SETUP` ID; no new task or plan created.
- **Coordination decision:** Mark this scope as in progress elsewhere in
  backlog.md and TASKS.md. Do not overlap code edits, replanning, refactoring,
  worker assignment or verification while its files are changing. Preserve the
  working tree. At handoff, inspect its actual diff and evidence, reconcile the
  Step 6 reposition/legacy restore/branch-activation outcome, and plan only the
  residual acceptance work. An undocumented dirty tree is neither permission
  to redo the change nor completion evidence.

## DENTO-NOTE — guided testing and verification learning

- **Operator statement:** The operator needs to learn once how DENTOBOT testing
  and verification is performed, including pytest, colcon builds and the wider
  workflow, so they understand it rather than treating commands as opaque.
- **Existing-plan lookup:** Searched the complete backlog for learning,
  training, onboarding, pytest, colcon and guided-verification synonyms. No
  applicable pending task existed. Related commands/evidence occur in historical
  logbooks, but no operator-learning contract exists.
- **Triage:** Backlog as new `VERIFY-LEARN-01`, Priority `Unprioritized`; no
  priority was inferred. This is a one-session learning outcome, not a new test
  framework.
- **Routed evidence:** Read the canonical AGENTIC_VERIFICATION_PROTOCOL, relevant
  `Testing/verification_matrix.json` static/pure/build entries, and SETUP.md
  focused colcon examples. The protocol requires cheapest-sufficient selection,
  explicit execution approval, evidence-level separation, a three-failure
  ceiling, exclusive `colcon_install`/runtime resources and no checks against
  files being actively edited.
- **Sequencing:** Wait for the active reusable-case foundation/Step 6 reposition
  implementation handoff. Use its stable result or another stable bounded
  matrix profile for the lesson; do not collide with the active implementation
  or later repeat it.
- **Proposed learning arc:** inspect diff/matrix and define the question; run or
  read static `py_compile`/`git diff --check`; run one focused pytest; explain
  and, when applicable and approved, run one focused colcon package build; read
  logs/PASS markers/first failure; explain when a Slicer/ROS/runtime or manual
  gate is required; produce a short checklist.
- **Errors/limitations:** An initial combined read was launched from the
  checkout root with the wrong `docs/...` prefix, so the protocol/SETUP paths
  were not found there; corrected paths are `Workspace/docs/...`. The shell also
  emitted non-fatal `Failed to create stream fd: Operation not permitted`
  warnings while the requested files still read successfully. No test, build,
  runtime or hardware action was executed in this capture turn.
- **Next bounded action:** After the active handoff, select one real matrix
  profile and present its exact teaching/run plan for operator approval before
  executing any command.

## S6 reusable Case Foundation implementation handoff and targeted verification

- **Operator direction:** Continue the previously stated bounded cleanup and
  use targeted verification only; stop before a major refactor or full Slicer
  workflow test. This superseded the earlier implementation pause without
  authorizing Docker rebuilds, ROS/MoveIt execution or operator acceptance.
- **Implementation:** Completed the schema-3 DentoCase/PreparedBranch and
  schema-2 Case Foundation state path; centralized pose/base eligibility;
  planning-pose-bound Step 5C activation; fixed-upper/moving-lower transient
  CBCT displays; jaw-owned downstream nodes with world-pose-preserving adoption;
  pre-Step-4 Case Foundation controls; branchless offline robot/base setup;
  foundation-only persistence; session-only foundation reuse; precise source,
  opening, branch, base and robot-profile invalidation; and offline-only load.
- **Retirement:** Removed the draft phantom from production composition, CMake,
  UI/presets and active Slicer tests. Archived its logic and explanatory smoke
  under `Testing/archive/draft_open_mouth_phantom/`. Removed the obsolete
  target-jaw fallback action and target-specific display-proxy builder; retained
  only compatibility fields and cleanup needed to hide/remove legacy scene data.
- **Focused test assets:** Extended `Testing/test_step6_state.py`, refreshed the
  modular API contract, updated the existing `s6-reusable-case` matrix profile,
  and changed `run_dentobot_reusable_case_smoke.py` to exercise Case Foundation
  ownership before the existing PreparedBranch gate. The Slicer runner was not
  executed in this turn.
- **Commands and results:** Changed-Python compilation with
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-case-foundation-pycache` exited 0.
  `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_state.py` passed
  23/23. The first complete `Testing/test_modular_structure.py` run produced
  four passes and one unrelated failure: `widget_template_build.py` is 1,511
  lines against the 1,500-line context ceiling. The four relevant API, CMake,
  import and process/network-boundary checks then passed 4/4. The matrix's
  production phantom-surface command exited 0.
- **Evidence boundary:** Current status is Implemented and Unit/Static Verified.
  Runtime, Integration and Operator Verified are not claimed. No Slicer,
  Docker rebuild, ROS, MoveIt, robot, hardware, patient-facing or engineer-owned
  action ran. The earlier 31-test intermediate result predates later cleanup and
  is not substituted for the current 23-test result.
- **Git boundary:** The requested historical PreparedBranch-only checkpoint
  cannot be recreated without rewriting an intervening broad commit made by
  another process. No reset or history rewrite was attempted. The current Case
  Foundation checkpoint must exclude unrelated dirty `Workspace/AGENTS.md`,
  `CONTEXT_SYNC.md`, `PROJECT_CONTEXT.md` and any non-task hunks.
- **Next:** Refresh Graphify and run diff hygiene, then inspect/stage only the
  Case Foundation and controlled-record boundary. Stop before
  `runtime.s6_reusable_case` or normal-window acceptance unless explicitly
  approved.
- **Final targeted maintenance:** The first sandboxed `graphify update .` failed
  with `Operation not permitted`; the approved repository-local retry completed,
  rebuilding 6,099 nodes, 9,695 edges and 477 communities. It reported that
  community labels may be refreshed later; no semantic/LLM labeling was run.
  `git diff --check` then exited 0, and `python3 -m json.tool
  Testing/verification_matrix.json` parsed the updated matrix successfully.

## S6 reusable Case Foundation focused runtime attempts after approval

- **Operator direction:** Approval was given to continue with the recorded
  `runtime.s6_reusable_case` gate while keeping verification targeted and
  stopping before a major refactor or full workflow test.
- **Question and hypothesis:** On source revision `3e022140a7f9b6aef50524b7c0f5f64a17a894c5`,
  does the focused headless runner complete the Case Foundation
  ownership/save-reopen and PreparedBranch activation scenarios and emit
  `DENTOBOT_REUSABLE_CASE_PASS`? The container was
  `dentobot/slicerros2:jazzy-moveit-sim-20260909` (image
  `sha256:544c5b759ccef7ce6c41157bbd7bd8b602657de367f1f6b71352de054c81b019`);
  no matching Slicer/ROS worker process existed before or after each attempt.
- **Execution:** The matrix command was run three times, serially, with the
  existing `docker:dentobot-slicerros2`, `slicer_process`, `mrml_scene` and
  `display` resources. Each run used:

  ```text
  docker exec dentobot-slicerros2 sh -lc 'timeout 300s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_reusable_case_smoke.py'
  ```

  | Attempt | UTC interval | Exit / marker | First causal failure | Evidence |
  |---|---|---|---|---|
  | 1 | 13:15:30–13:15:38 | 1 / no PASS marker | `DENTOWorkflowLogic` lacked `startStep6CaseJawLandmarkPlacement` | `/tmp/dentobot-verification/s6-reusable-case-20260911T131415Z/runtime.log` |
  | 2 | 13:18:42–13:18:49 | 1 / no PASS marker | Static compatibility call resolved through `CaseFoundationLogicMixin`, which has no `STEP6_CASE_JAW_LANDMARKS_ROLE` | `/tmp/dentobot-verification/s6-reusable-case-20260911T131415Z/attempt-2/runtime.log` |
  | 3 | 13:19:58–13:20:06 | 1 / no PASS marker | `vtkMRMLMarkupsLineNode` has no `GetParentTransformNodeID` | `/tmp/dentobot-verification/s6-reusable-case-20260911T131415Z/attempt-3/runtime.log` |

- **Bounded corrections:** Restored the missing Case Foundation landmark
  placement compatibility entrypoint, then made it a concrete-MRO classmethod
  and refreshed `Testing/contracts/dentoworkflow_api.json`. The changed source
  compiles and the focused modular API contract check passes (`1 passed`).
  These two corrective edits remain uncommitted until runtime evidence is
  obtained.
- **Protocol boundary:** The third attempt is the final retry for this focused
  gate. The runner never emitted `DENTOBOT_REUSABLE_CASE_PASS`; runtime and
  integration verification therefore remain open. The repeated destroyed-Qt
  warnings and missing ROS2 registration messages occur during teardown after
  the first causal failure and are not treated as the root cause. No Docker
  rebuild, ROS/MoveIt execution, robot/hardware action, manual acceptance or
  full workflow test was performed.
- **Next bounded action:** Obtain approval to inspect and correct the
  `GetParentTransformNodeID` ownership helper against the available MRML API,
  run its smallest static/API check, and then propose a fresh bounded runtime
  attempt. Do not retry the current runner without that correction or expand
  beyond `runtime.s6_reusable_case`.
