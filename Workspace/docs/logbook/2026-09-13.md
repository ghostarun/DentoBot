# DENTOBOT Engineering Log — 2026-09-13

## PLAT-U-07 — approved Data folder and partial saved-case upload

- **Operator authorization:** The operator requested a folder named `Data`
  inside `IITM Dentobot` and approved anonymous `.dentocase` exchange.
- **Backlog and contract lookup:** Re-read the complete `docs/backlog.md` and
  the linked `PLAT-U-07` sections in `TASKS.md`, `DEVELOPMENT_PLAN.md`,
  `DECISIONS.md`, and the prior logbook. The recorded contract permits only
  individual synthetic or explicitly approved de-identified `.dentocase`
  bundles and excludes the rest of `Slicer_Saved`.
- **Drive grounding:** Read-only metadata confirmed the parent folder
  `IITM Dentobot` (`1e7AWrEeVLctMKfWaBGiqv0Ck8p0AbFWh`) and no existing `Data`
  child. The existing `Engineer-owned — manual only` folder was identified by
  name but not opened.
- **Drive writes:** Created `IITM Dentobot/Data`
  (`1u1K2EVeBe3-U1-JeWxKhOKslEW-zFnkd`), then created
  `SampleStudy1/FDI11`, `FDI14`, `FDI31`, and `FDI44`. Uploaded 7 of 17 local
  `*.dentocase` bundles: the root `dentobot-case-13sept.dentocase`, all three
  FDI11 bundles, and all three FDI31 bundles. The preserved relative layout
  avoids collisions between repeated filenames.
- **Connector limit:** FDI14 and FDI44 uploads were rejected before action
  because each file exceeds the connector's 100 MiB input limit. The initial
  FDI14 batch attempt therefore did not create files; the destination folder
  was read back empty before the later successful uploads. No standalone
  `.mrb`, `.stl`, `.nrrd`, screenshot, run record, or engineer-owned record was
  uploaded.
- **Browser fallback:** Opened the Drive browser destination, but it is
  currently at Google sign-in. No password was entered or recorded; the tab was
  left available for user sign-in before the remaining uploads.
- **Verification:** Folder-creation and upload results returned success for
  the 7 completed files with their Drive IDs and parents. The local source
  inventory is 17 bundles totaling 1,924,391,255 bytes. Full checksum and
  Drive metadata readback for the completed set, and upload of the remaining
  10 files, remain open.
- **Readback:** Direct folder listings confirmed the Data root contains the
  root bundle and `SampleStudy1`; `FDI11` contains exactly its 3 uploaded
  bundles; `FDI31` contains exactly its 3 uploaded bundles; and `FDI14` and
  `FDI44` are empty. `git diff --check` passed. One initial logbook-tail
  command used the overlay-relative path instead of the checkout path and
  returned `No such file or directory`; no repository file was changed by
  that error.
- **Evidence boundary:** The operator's anonymous-data approval is recorded,
  but no independent clinical/governance approval is inferred. `.dentocase`
  bundles can embed CBCT-derived NRRD volumes; only the operator-approved
  bundle class was transmitted.
- **Next bounded action:** User signs into Google Drive in the handed-off
  browser tab, then continue the 10 large individual `.dentocase` uploads and
  verify names, parents, sizes, and SHA-256 checksums. Do not sync the rest of
  `Slicer_Saved`.

## S6 — dock-default migration and target-generation preflight

- **Operator change:** The operator requested that new Step 4C dock inputs use
  the latest saved FDI31 case values, with the trajectory-guide hole kept at a
  minimum of 2.0 mm. The existing 2.0 mm guide floor remains distinct from the
  1.5 mm robot-dock bore.
- **Implementation:** Added the offline-only
  `Testing/run_dentobot_stage6_target_generation.py` harness. It reuses the
  production 4A assisted trajectory, 4B support, 5A boundary, 5B shell, 4C
  docking, 5C fusion/verification, PreparedBranch activation, case save, and
  case reopen APIs. It does not connect ROS or invoke hardware execution.
- **First runtime result:** In the approved Slicer simulation container
  (`dentobot-slicerros2`, image `dentobot/slicerros2:jazzy-moveit-sim-20260909`),
  the FDI31-only run reached the Step 5A geometry gate and stopped with:
  `The insertion-aligned support plane does not intersect any selected tooth
  anatomy. Move the plane toward the crowns and try again.` No target package
  was promoted. The run report is under
  `data/dentobot-runs/stage6-target-generation-fdi31-20260913/`.
- **Evidence-bound adjustment:** The latest saved FDI31 package records
  `templateSupportPlaneDepthMm=4.0`; the harness initially inherited the new
  node default of 3.0 mm. The harness now applies the recorded 4.0 mm support
  plane depth while retaining the requested 2.0 mm trajectory-guide inner
  diameter. This is a bounded geometry correction, not a collision-policy
  relaxation.
- **Runtime notes:** Slicer emitted a non-fatal `psutil` install attempt from
  the optional ROS2Tests module because the container's Slicer site-packages
  are not writable; DENTOWorkflow itself initialized and the geometry gate
  executed. Scene-reload callbacks also logged destroyed-Qt-widget warnings
  during teardown after the failed run; no package was written.
- **Verification boundary:** Python source compilation by `compile(...)` and
  `git diff --check` passed. `graphify update .` was attempted after the new
  harness change but the graph rebuild was refused with `Operation not
  permitted`; graph refresh remains unresolved and is not treated as code
  acceptance.
- **Frame diagnosis:** The 4.0 mm rerun failed at the same Step 5A gate. The
  FDI31 source segmentation bounds were `Z=38.09..56.10 mm`, while the
  assisted trajectory and opened planning geometry were `Z=23.12..34.02 mm`.
  The Case Foundation lower-jaw matrix includes a substantial translation and
  rotation, so the boundary plane was in opened planning space while the Step
  4B/5A anatomy remained in the closed source frame. This is the W5-U-04 root
  cause; plane-depth tuning cannot correct it.
- **Implementation:** Added
  `caseFoundationPlanningSurfaceCopy(...)` in `logic_case_foundation.py`.
  It copies source anatomy without mutation, returns fixed-upper anatomy
  unchanged, and applies the existing fresh Case Foundation transform only to
  current moving-lower anatomy. Step 4B, the Step 5A plane/boundary/visible
  surface paths, Step 4C dock collision inputs, and Step 5B collision anatomy
  now consume that shared planning-frame copy. Source lineage and checksum
  callers retain closed/source-frame behavior.
- **Focused verification:** The existing Slicer visible-support test exercised
  the new lower/upper frame assertions successfully, then later stopped in an
  unrelated test save/reload fixture because reopened Case Foundation display
  volumes were not reconstructed. The direct FDI31 harness advanced past the
  former Step 5A intersection gate to Step 5B, where it correctly rejected a
  stale visible-support surface. That stale state was traced to the harness:
  it bypassed the normal boundary-restoration guard and used
  `templateSamplingSpacingMm` instead of the UI's
  `templateSupportCurveSamplingSpacingMm` for visible Step 5A generation.
- **Harness correction:** The runner now uses the normal widget restoration
  guard while applying its deterministic plane boundary, processes queued UI
  events, explicitly checks that the generated visible support remains
  `Current`, and uses the normal-window boundary sampling parameter. This is
  test orchestration only; it does not relax a production geometry or safety
  gate.
- **Verification:** Source `compile(...)` and `git diff --check` passed after
  each edit. `graphify update .` was retried after the production and runner
  changes; it remains blocked by `Operation not permitted`. The first FDI31
  post-frame report is preserved at
  `stage6-target-generation-fdi31-framefix-20260913/`; the observer and
  spacing reports are preserved separately under the corresponding
  `framefix-*` directories. No `.dentocase` was promoted.
- **Next bounded action:** Rerun FDI31 with the UI-consistent Step 5A runner.
  Stop again on any W5/5C/package gate; do not promote partial cases or bypass
  the one-solid/four-open-bore/zero-residual-channel gate.

## S6 — FDI31 package reload identity repair

- **End-to-end evidence:** The UI-consistent FDI31 run at
  `data/dentobot-runs/stage6-target-generation-fdi31-framefix-ui-spacing-20260913/`
  completed the current 4A → 4B → 4C → 5A → 5B → 5C path and wrote
  `FDI31-generated.dentocase` (87,587,921 bytes), but its mandatory reopen
  rejected the package before activation. It remains preserved as failed
  evidence and was not promoted.
- **Root cause:** Package node IDs and attributes survived scene reload. The
  failure instead came from approximately `1e-14 mm` coordinate changes when
  MRML serializes transformed trajectory/insertion points: the raw registry
  geometry hash changed, producing a different PreparedBranch revision after
  reopen. The separately observed absent opened-CBCT display volumes occur
  during failure recovery and remain a pending reload-fixture issue; they are
  not assumed to be the registry mismatch cause.
- **Implementation:** Registry identity hashing now rounds world trajectory
  and insertion points to 9 decimal places (1 nanometre) before canonical
  hashing. This does not alter stored geometry, collision calculations, or
  the existing `1e-6 mm` geometric validator. It only removes lossless MRML
  serialization noise from package identity.
- **Focused verification:** The headless Slicer test
  `test_DENTOWorkflowCaseBundleRegistryNodeRecord` initially exposed an
  unrelated fixture dependency because `createTrajectoryNode` correctly
  requires a reviewed Case Foundation. The test now uses a bare markups-line
  node, and passes: a `1e-12 mm` perturbation keeps its registry fingerprint;
  a `1e-7 mm` edit changes it. The run used a strict 60-second bound and
  completed with exit status 0. Source `compile(...)` and `git diff --check`
  remain clean. `graphify update .` remains blocked by `Operation not
  permitted`.
- **Next bounded action:** Run a new FDI31 package generation/reopen cycle
  under `stage6-target-generation-fdi31-framefix-registry-stable-20260913/`.
  Promote nothing unless its final save/reopen integrity report and active
  PreparedBranch both verify current.

## S6 — FDI31 reopen follow-up and retry boundary

- **Second bounded package cycle:** The registry-normalized FDI31 package at
  `data/dentobot-runs/stage6-target-generation-fdi31-framefix-registry-stable-20260913/`
  no longer reported a PreparedBranch revision mismatch. It failed only while
  binding the reopened UI because `setParameterNode(...)` attempted to lock
  the robot base before the intentionally non-persistent opened-CBCT display
  volumes were rebuilt. The failure report and 87,587,929-byte package remain
  preserved; neither was promoted.
- **Implementation:** After pre-bind package validation and before UI binding,
  `widget_case_backend.py` now calls the existing
  `rebuildCaseFoundationDisplayVolumes(...)` only for a
  `CaseFoundationCurrent` case. Post-bind package validation remains in place;
  no integrity comparison, geometry gate, or stale-state check was relaxed.
- **Third bounded package cycle:** The restore-order case at
  `data/dentobot-runs/stage6-target-generation-fdi31-restore-order-20260913/`
  advanced through that UI-bind boundary and stopped at post-bind lineage
  validation: `targetToothBoundsRoi.controlPointsWorldRasMm[0][0]`. This is
  the same `~1e-14 mm` MRML world-coordinate round-trip behavior found in the
  trajectory registry, now at the general Markups lineage record. Its failed
  report/package are preserved and not promoted.
- **Implementation and focused check:** `_caseBundleNodeRecord(...)` now uses
  the same 9-decimal-world-coordinate canonicalization already used for
  registry and insertion identity. The extended
  `test_DENTOWorkflowCaseBundleRegistryNodeRecord` passes in headless Slicer
  (exit status 0): a `1e-12 mm` Markups perturbation preserves both registry
  and case-record identity, while a `1e-7 mm` edit changes both. Static
  compilation and `git diff --check` passed after the edits.
- **Verification limit and unresolved acceptance:** This completed the three
  bounded FDI31 post-5C package/reopen attempts for this diagnosis cycle
  (registry identity, transient-display restore ordering, then generic Markups
  identity). Per the verification protocol, no fourth expensive FDI31 run was
  started. Exact package integrity, current Step 5C evidence, active
  PreparedBranch acceptance, and manual-reload delivery remain open.
- **Tooling boundary:** `graphify update .` was retried after each production
  change; it remains blocked by `Operation not permitted`. One initial static
  compile invocation had a shell quoting error and made no changes; the
  corrected compile check passed.
- **Next bounded action:** Review the package-lineage comparison policy with
  this evidence, then authorize a fresh FDI31 end-to-end retry only after the
  remaining canonicalization scope is accepted. Do not run the FDI32/upper
  target matrix or Stage 6 live planner while FDI31 package integrity is open.

## S6 — read-only package hydration audit

- **Protocol recheck:** `AGENTIC_VERIFICATION_PROTOCOL.md` requires an
  operator decision before runtime continuation beyond the three-failure
  ceiling. The earlier offline-runtime approval does not cover a fourth full
  FDI31 package/reopen cycle. No Slicer, ROS, or robot-hardware action was
  started in this audit.
- **Root cause refinement:** The failed post-bind ROI was not a mere floating
  point comparison issue: package hydration called `_updatePlanning()`, which
  unconditionally regenerated `targetToothBoundsRoi` and could also enforce
  trajectory bounds. That mutates persistent geometry during a method whose
  contract is read-only GUI hydration, explaining the post-bind lineage
  difference despite successful pre-bind validation.
- **Implementation:** During `_caseBundleRestoreDepth`, `_updatePlanning()`
  now displays the saved, target-associated ROI bounds without invoking
  `createOrUpdateTargetBoundsRoi(...)`, and skips automatic trajectory-bounds
  enforcement. Normal interactive authoring retains the original refresh and
  enforcement behavior after restoration completes. This does not tolerate a
  missing/wrong target ROI; it records a planning warning and leaves package
  validation authoritative.
- **Static verification:** `compile(...)` of the changed workflow modules and
  `git diff --check` passed. The applicable matrix rows are
  `pure.step6_restore`, `pure.step6_planning`, and the eventual serialized
  Stage 6 target-matrix gate. They are not run here because their
  `explain_then_approve` / explicit-runtime approval class, together with the
  retry-ceiling rule, requires a fresh operator decision.
- **Proposed bounded resume:** First run `pure.step6_restore` and
  `pure.step6_planning` in the existing simulation container; if both pass,
  run one FDI31-only offline package/save/reopen check using the fixed source
  and a new evidence directory. Success requires schema-3 package validation,
  preserved current Step 5C evidence, one eligible active PreparedBranch, and
  successful reopen. Stop on the first failure; do not alter bore, depth,
  support, dock, guard, or collision parameters in that run.

## S6 — approved FDI31 retry and docking-hydration repair

- **Operator authorization:** The operator explicitly approved the bounded
  resume. The runtime remains offline Slicer/ROS simulation only; no hardware,
  motion, drilling, or patient-facing action was authorized or performed.
- **Pure verification:** The approved preflight checks passed in the existing
  `dentobot-slicerros2` container: `pure.step6_restore` reported `42 passed in
  0.28s`, and `pure.step6_planning` reported `75 passed in 0.29s`.
  Operator-owned interactive Slicer and ROS processes were preserved; only two
  stale, exact-match test processes left by a previous completed test were
  stopped before the bounded run.
- **Single FDI31 result:** The FDI31-only generator used the saved FDI31 dock
  values and the requested 2.0 mm trajectory-guide channel. It completed the
  current 4A → 4B → 4C → 5A → 5B → 5C path, wrote the 87,589,122-byte
  `FDI31-generated.dentocase` under
  `data/dentobot-runs/stage6-target-generation-fdi31-readonly-hydration-approve-20260913/`,
  and stopped at mandatory post-bind package validation. The package remains
  failed evidence and was not promoted.
- **Primary blocker:** The exact mismatch is
  `targetDockingReferencePlane.attributes.DENTOBOT.OrientationState`.
  `widget_docking._updateTargetDocking()` recomputed stale status while
  `_caseBundleRestoreDepth` was active and changed persistent docking/template
  state. The later `Recovery scene restoration failed: Reconstruct the opened
  Case Foundation CBCT displays.` text is a secondary recovery-path error,
  not the package-integrity cause.
- **Implementation:** `widget_docking.py` now treats docking refresh as
  read-only during package hydration: it does not mark docking/template state
  stale, clear stale attributes, or rewrite `targetDockingYawConfirmed` until
  package post-bind identity validation has finished. Ordinary interactive
  stale detection is unchanged. `widget_case_backend.py` also rebuilds the
  explicitly transient Case Foundation display volumes before recovery-scene
  UI rebinding, matching the primary package-load path.
- **Shared-root-cause correction:** The audit found that the primary package
  loader called `setParameterNode(...)` before its second identity comparison.
  That executes every normal GUI refresh, including template/registry/base
  refreshes, and can therefore mutate any persisted workflow state during the
  integrity window. The loader now completes both MRML comparisons before it
  binds the parameter node and processes GUI events. Normal GUI hydration,
  compatibility migration, and Step 6 freshness review run only afterward.
  This is a single package-load lifecycle boundary rather than a collection of
  widget-specific exemptions; the planning and docking guards remain defensive
  for any exceptional restore-time callback.
- **Static verification:** A direct `py_compile` attempt could not create a
  package-owned `__pycache__` file (`Permission denied`), so it was replaced
  by a non-writing `compile(...)` source check for both modified modules; that
  check passed after the shared loader change. `git diff --check` passed.
  `graphify update .` was attempted again after the source change and remains
  blocked by `Operation not permitted`.
- **Lifecycle regression check:** Added the smallest ordinary-Python contract
  check in `Testing/test_case_bundle.py`: it requires exactly two package
  identity checks before the GUI bind, and requires GUI bind to occur only
  after `_endCaseBundleRestore(...)` and before post-load freshness hydration.
  `python3 -m pytest -q -p no:cacheprovider Testing/test_case_bundle.py`
  passed with `10 passed in 0.04s`; the final `git diff --check` passed.
  Graphify was retried after the test edit and remains environment-blocked by
  `Operation not permitted`.
- **Non-mutating runtime preflight:** `dentobot-slicerros2` is healthy on the
  pinned `dentobot/slicerros2:jazzy-moveit-sim-20260909` image. A process
  inspection found no live `run_dentobot_stage6_target_generation` process;
  no operator-owned Slicer or ROS process was changed.
- **Guide-bore invariant:** The distinct 1.5 mm docking bore remains the
  saved FDI31 dock setting. The runner now explicitly rejects any target run
  whose trajectory-guide channel or sleeve inner bore falls below 2.0 mm
  before Step 5B shell/Step 5C fusion generation. This makes the requested
  burr-clearance floor enforceable rather than relying only on stored defaults.
  The existing pure policy test now checks both runner assertions as well as
  parameter and UI minimums. `python3 -m pytest -q -p no:cacheprovider
  Testing/test_case_bundle.py Testing/test_step6_planning.py` passed with
  `31 passed in 0.37s`. Non-writing compilation and `git diff --check` passed.
  Graphify was retried after these edits and remains environment-blocked by
  `Operation not permitted`.
- **Report completeness:** The per-target generation report now carries a
  `guide_bore` object with the effective channel diameter, sleeve inner bore,
  and required 2.0 mm floor. It is emitted with the existing source/package
  checksums; current foundation; 4B selection; 4A trajectory/bounds; 5A;
  5B shell; 4C dock; 5C verification; PreparedBranch activation-preservation;
  and reopen evidence. The later serialized Stage 6 matrix remains the
  separate consumer of only valid saved packages for Task Home → PreEntry →
  Entry → Target → guarded Return Home proof. The updated source-policy suite
  passed with `31 passed in 0.15s`; non-writing compilation and `git diff
  --check` passed. Graphify was retried and remains environment-blocked by
  `Operation not permitted`.
- **Downstream bore enforcement:** The exact saved-case Stage 6 smoke used by
  the six-target matrix previously verified the guarded Task Home → PreEntry
  → Entry → Target → Return Home loop but did not independently reject an
  undersized saved trajectory guide. It now rejects a loaded case before local
  robot/ROS simulation if either channel or sleeve bore is below 2.0 mm,
  records that `guideBore` evidence in the planning report, and rechecks it
  after the smoke's saved-case reopen. The source-level bore-policy test
  requires all generator and downstream-smoke checks. The pure suite passed
  with `31 passed in 0.16s`; non-writing compilation and `git diff --check`
  passed. Graphify was retried and remains environment-blocked by `Operation
  not permitted`.
- **Matrix evidence gate:** `run_dentobot_stage6_target_matrix.py` no longer
  treats any non-empty stdout JSON as acceptance. For each FDI it now fails
  closed unless the published smoke report proves the matching target FDI,
  channel and sleeve bore at least 2.0 mm, guarded preview, guarded Return
  Home, repeat preview, finite original/repeat target residuals, reopened
  saved-case identity, persisted locked route, and disabled hardware
  execution. Per-target report failures retain the exact missing-evidence
  list. A new pure matrix-contract check covers both a complete report and an
  undersized-bore rejection. `python3 -m pytest -q -p no:cacheprovider
  Testing/test_case_bundle.py Testing/test_step6_planning.py` passed with
  `32 passed in 0.16s`; non-writing compilation and `git diff --check` passed.
  Graphify was retried and remains environment-blocked by `Operation not
  permitted`.
- **Next bounded action:** Obtain a fresh explicit decision before one new
  FDI31-only offline save/reopen attempt. It must stop on the first remaining
  integrity mismatch and must not alter bore, support, dock, guard, collision,
  or trajectory parameters. Do not start the remaining FDI target campaign or
  Stage 6 planner/runtime acceptance while this FDI31 package remains
  unaccepted.

## S6 — FDI31 target-generator startup containment

- **Operator authorization:** The operator's explicit `Continue` approved one
  bounded, offline FDI31 target-generation/save/reopen attempt only. It did
  not authorize physical robot motion, drilling, patient-facing operation, a
  full target campaign, or a second runtime attempt.
- **Execution and result:** The run used the approved FDI31 source package and
  output root
  `/workspace/data/dentobot-runs/stage6-target-generation-fdi31-post-hydration-20260913`
  in `dentobot-slicerros2`. It completed in about 31 seconds, but the output
  root is empty: neither `generation-report.json` nor `FDI31-report.json`
  exists. No `.dentocase` was saved or promoted, and no geometry, PreparedBranch,
  base-lock, planner, homing, or Stage 6 acceptance claim follows from this
  failed harness startup.
- **Observed evidence and diagnosis:** The Slicer log
  `/tmp/Slicer-/Slicer_5.10.0-2025-11-10_3838_20260913_135358_533.log`
  repeatedly says `The module "ROS2" has not been registered.` The launch
  included DENTOWorkflow but omitted the installed SlicerROS2 loadable and
  scripted module paths. This is a runtime-launch configuration defect, not a
  dock, bore, trajectory, geometry, collision, or package-identity change.
- **Implementation:** The shared FDI generator now checks
  `slicer.util.getModuleLogic("ROS2")` before initializing DENTOWorkflow and
  fails closed with an explicit SlicerROS2-path diagnostic. Its top-level
  exception path now writes a per-target `FDIxx-report.json` and a
  `generation-report.json` containing `startup_error`, source path, targets,
  exception type/message, and traceback. This prevents an empty evidence
  directory when initialization fails before the normal per-target loop.
- **Verification:** The source-level policy test asserts the ROS2 fail-fast
  guard and startup-report path. `python3 -m pytest -q -p no:cacheprovider
  Testing/test_case_bundle.py Testing/test_step6_planning.py` passed with
  `32 passed in 0.15s`; a non-writing Python `compile(...)` check of the runner
  passed; and `git diff --check` passed. `graphify update .` was attempted after
  the code edit but remains blocked by `Operation not permitted`.
- **Next bounded action:** With fresh explicit runtime approval, repeat only
  FDI31 with the installed SlicerROS2 module paths (and the established module
  library path) supplied to Slicer. Stop at the first remaining failure and
  preserve the generated report; do not begin the other FDI targets or planner
  evidence campaign until the FDI31 package gate passes.
- **Authorization boundary:** The proposed corrected FDI31 remediation-cycle-1
  command was rejected by the runtime approval gate because the prior explicit
  approval had been consumed by the failed launch. The command did not start;
  no container process, case save, geometry change, planner playback, robot
  motion, or hardware action occurred. The updated completion objective does
  not replace the project requirement for a fresh, command-specific runtime
  approval.
- **Diagnostic evidence preparation:** The target generator now captures a
  Slicer UI image and a true 3D-viewport image after a successful package
  reopen or at the exact target failure point. It records both image paths and
  capture errors in `diagnostic_evidence` in the per-target report. DENTOBOT
  case bundles deliberately reject unexpected archive members, so the PNGs are
  stored as SHA-linked sidecars in `FDIxx-evidence/` beside the valid
  `.dentocase`, not embedded into it and thereby invalidating its manifest or
  checksum identity. This is the evidence mechanism for the requested
  screenshot-backed final diagnostic report if the three permitted remediation
  cycles fail.
- **Verification:** The source policy test now requires the evidence capture
  helper and report field. `python3 -m pytest -q -p no:cacheprovider
  Testing/test_case_bundle.py Testing/test_step6_planning.py` passed with
  `32 passed in 0.15s`; a non-writing `compile(...)` check and `git diff
  --check` passed. `graphify update .` was attempted after this code edit and
  remains blocked by `Operation not permitted`.

## S6 — FDI31 remediation cycle 1 and self-contained evidence layout

- **Operator authorization:** The operator explicitly approved one offline,
  FDI31-only remediation test. It ran in `dentobot-slicerros2` with the
  installed SlicerROS2 loadable/scripted module paths and ROS runtime
  environment. No physical robot motion, drilling, or patient-facing action
  was requested or performed.
- **Launch result:** The corrected launch passed the earlier ROS2-registration
  boundary, generated the current 4A→4B→5A→5B→4C→5C geometry, saved a
  87,589,120-byte case, and entered the mandatory reopen path. The optional
  ROS2Tests `psutil` installation still fails because Slicer site-packages are
  read-only, but DENTOWorkflow and the registered ROS2 module initialized; it
  was not the test failure.
- **Cycle-1 failure:** The restored PreparedBranch failed fail-closed with
  `STALE_4C`: the saved Final Printable Template verifies branch revision
  `8b7539110fb7056a127cff1a98d74b548816ead55ab28864ab713811902b7307`,
  while the reopened registry recomputed
  `5be6af3776faa29a8f38202b0158f6ea8231959d8b1348d9908c734e467228f0`.
  The saved MRML itself proves the dock persisted as `GeometryState=Current`
  and `OrientationState=Confirmed`; all selected 4B/5B/5C dependent models
  are likewise current. Thus this is restore-time provenance drift, not a
  saved geometry, bore, or dock-parameter failure. The generated case is
  failed evidence only and was not promoted.
- **Artifact organization and integrity:** Per the operator's request, the
  cycle artifacts were moved into the self-contained folder
  `data/dentobot-runs/stage6-target-generation-fdi31-registered-ros2-cycle1-20260913/FDI31/`:
  `case.dentocase`, `report.json`, and `evidence/failure-ui.png` plus
  `evidence/failure-viewport.png`. The root `generation-report.json` now acts
  only as an artifact index. The case SHA-256 before/after move is identical:
  `d4ff58075ffa17baefdc91bc35756eddc427e2130c93219f1066d4b8a77e4e2f`.
  Independent bundle validation passed: case schema `2.0`, workflow schema
  `3.0`, and `ros2Serialized=false`. Screenshots are sidecars because the
  valid bundle schema rejects untracked archive members.
- **Root-cause correction:** Step 4C and Step 5C persisted raw world-RAS
  trajectory JSON and later compared it byte-for-byte after MRML reload. The
  established package rule already canonicalizes world coordinates to nine
  decimals. `canonicalTrajectoryGeometry(...)` now reuses that rule for only
  persisted Step 4C/5C provenance and UI freshness comparisons; full-precision
  values remain the inputs to actual geometry construction. This prevents
  reload noise from invalidating a current branch while retaining stale
  detection for meaningful trajectory changes. Failure/success evidence now
  also frames the 3D camera around the final printable template or dock rather
  than the large target ROI.
- **Verification:** The focused pure suite passed with `32 passed in 0.16s`;
  non-writing compilation of all modified production modules and the target
  runner passed; and `git diff --check` passed. `graphify update .` was
  attempted after the code changes and remains blocked by `Operation not
  permitted`.
- **Next bounded action:** With fresh explicit runtime authorization, run one
  FDI31-only offline generation/save/reopen cycle using the canonical
  provenance fix and self-contained FDI31 subfolder. This is remediation
  cycle 2 of at most 3; stop on the first failure. If all three cycles fail,
  prepare the requested screenshot-backed diagnostic report from their
  self-contained artifacts and stop the goal instead of making a fourth
  runtime attempt.

## S6 — FDI31 remediation cycle 2: persisted orientation demotion

- **Operator authorization and boundary:** The operator explicitly approved
  one FDI31-only offline generation/save/reopen test. It used the registered
  SlicerROS2 module paths and the existing simulation environment. No
  physical robot motion, drilling, patient-facing operation, second target,
  or Stage 6 playback was started.
- **Execution result:** The generator completed the current FDI31
  4A → 4B → 5A → 5B → 4C → 5C construction and wrote
  `data/dentobot-runs/stage6-target-generation-fdi31-registered-ros2-cycle2-20260913/FDI31/case.dentocase`.
  It then fail-closed during the mandatory pre-bind reopen comparison. The
  optional `ROS2Tests` `psutil` installation again could not write into the
  Slicer site-packages directory; DENTOWorkflow and the ROS2 planner module
  still initialized, so that warning is not the planner/package failure.
- **Exact failure:** The retained report records
  `Pre-bind package validation failed: Loaded MRML geometry/lineage differs
  from the package: targetDockingReferencePlane.attributes.DENTOBOT.OrientationState`.
  The saved workflow lineage says both Step 4C nodes are `Confirmed`, while a
  read-only raw-MRB inspection (before DENTOWorkflow hydration) found both the
  target docking plane and docking assembly persisted as `Draft`. Therefore
  the MRB and manifest disagree at save time; this is not a GUI-hydration
  tolerance or a later PreparedBranch decision.
- **Root cause and correction:**
  `_onSelectedGuideTrajectoryModified(...)` in
  `widget_guide_support_setup.py` was the remaining provenance consumer that
  compared raw world-RAS floats with the canonical Step 4C JSON. Save/reload
  representation jitter was consequently treated as a real source edit and
  called `markTargetDockingAssemblyStale(...)`, which demotes both orientation
  states to `Draft` after the lineage summary had already been captured.
  The observer now compares `canonicalTrajectoryGeometry(...)`, the shared
  nine-decimal package representation, and ignores callbacks while the
  case-bundle restore barrier is active. This retains ordinary operator-edit
  invalidation while removing the false save/restore invalidation path.
- **Evidence organization and integrity:** The failed package and report stay
  together under the FDI31 folder, with sidecars in `FDI31/evidence/`:
  `failure-ui.png` and `failure-viewport.png`. SHA-256 values are respectively
  `0e84a3694ec2f6f60ceb4d3c41ab893849c1ab0d813a448cd396a35eda36202f`,
  `77590f1ebd209291aea65c1f1df63bd646e68136e994945c152929653e64c6a5`,
  and `a4a09e76b748e217450f68c399be27933ada224cca9d0a94771e60d1d8d87354`.
  Static package validation passed: bundle schema `2.0`, workflow schema
  `3.0`, and `ros2Serialized=false`. The UI sidecar shows the FDI31 dock/ROI
  failure state. The direct viewport capture was blank despite a successful
  write, so the runner now captures the rendered 3D-pane rectangle from the
  visible main window; the next approved cycle will verify that replacement
  evidence path.
- **Verification:**
  `python3 -m pytest -q -p no:cacheprovider Testing/test_case_bundle.py
  Testing/test_step6_planning.py` passed with `32 passed in 0.15s`.
  Non-writing `compile(...)` checks of the target runner and affected workflow
  modules passed; `git diff --check` passed. `graphify update .` was run after
  the edits but remains blocked by `Operation not permitted` in this workspace.
- **Next bounded action:** This is remediation cycle 2 of at most 3. A fresh
  explicit runtime authorization is required for exactly one final FDI31-only
  offline generation/save/reopen cycle with the canonical observer correction
  and repaired viewport evidence. Stop on its first failure. If it fails,
  publish the requested screenshot-backed diagnostic report from the
  self-contained cycle artifacts and stop instead of attempting a fourth run.

## S6 — FDI31 remediation cycle 3: terminal PreparedBranch restore failure

- **Operator authorization and execution boundary:** The operator explicitly
  approved one final, FDI31-only offline generation/save/reopen cycle. It used
  the saved FDI31 dock configuration and 2.0 mm trajectory-guide channel and
  sleeve floor. No physical robot motion, drilling, patient-facing action,
  playback, or second target was started. A stale agent-created read-only
  inspection process (container PIDs 8127/8147) from an earlier failed
  diagnostic invocation was identified by its exact command and stopped; the
  active FDI31 runner and the user Slicer/ROS session were preserved.
- **Terminal result:** The runner wrote
  `data/dentobot-runs/stage6-target-generation-fdi31-registered-ros2-cycle3-20260913/FDI31/case.dentocase`,
  then failed after reopen with `UPSTREAM_CHANGED: Patient shell or insertion
  state changed.` The saved schema-3 lineage records one selected `Single`
  FDI31 branch, state `Current`, and a non-empty verification revision; the
  reopened registry recomputed the same branch as `Stale` with no verification
  revision. Thus cycle 3 passed the earlier package-orientation integrity
  boundary but still failed the mandatory reopened PreparedBranch eligibility
  gate. It is failed evidence only and was not promoted.
- **Artifact integrity:** The final case SHA-256 is
  `c8053112e15f43c77c3eb3b1a7229a7fbfdcff6ca003fd409789fae599efe1c6`;
  the FDI31 UI screenshot SHA-256 is
  `4495352374dee8f34ef62b76ca323f93a80b2fc8f0b53791b4fea165ba6cab6a`.
  Static validation passed: bundle schema `2.0`, workflow schema `3.0`,
  `ros2Serialized=false`, and saved case classification `PlanningPackage`.
  The FDI31 report, case, and screenshot remain together in that cycle's
  `FDI31/` folder. The new viewport-crop path raised
  `TypeError: 'QSize' object is not callable`, so only the useful UI screenshot
  was recorded; this evidence limitation is explicit in the report.
- **Three-cycle stop:** The operator's stated ceiling has been reached. No
  further implementation, Slicer/ROS run, FDI target, route/planner test,
  homing attempt, geometry tuning, or parameter workaround was performed.
  The terminal, screenshot-backed diagnostic report was written beside the
  final case at
  `data/dentobot-runs/stage6-target-generation-fdi31-registered-ros2-cycle3-20260913/FDI31/diagnostic-report.md`.
  It links the self-contained cycle-1/2/3 reports, cases, and screenshots;
  explains the acceptance boundary; and enumerates every unaccepted P0/P1
  gate. Future work requires a new explicit investigation/approval rather than
  a fourth retry.

## S6 — source constraint blockers and exact-target planner boundary

### Operator statement

The operator approved implementation of the Stage 6 planner workflow and
required constraint blockers so the implementation stays on the defined plan.
The existing three-cycle FDI31 runtime ceiling and the outstanding package,
geometry, PreparedBranch, and normal-window gates remain in force.

### Agent interpretation

Implement the smallest shared guards that prevent the planner from waving off
an upstream failure: current package/branch evidence is mandatory, target
depth is exact, route activation is atomic, post-hydration identity is audited,
and Step 5C geometry must describe one current-frame printable target dock.
Do not turn source checks into runtime acceptance and do not reopen the stopped
FDI31 campaign in this turn.

### Implementation

- Removed the former simulation target-depth cap and preserved the confirmed
  target through `Task Home → PreEntry → Entry → Target` planning.
- Made provisional insertion-envelope excess diagnostic-only while retaining
  hard IK, FK, joint-limit, guard, collision, and endpoint failures.
- Added current PreparedBranch/Step 5C freshness to the shared confirmed-task
  gate, so an old task snapshot cannot bypass stale or missing branch evidence.
- Kept alternate-route selection/locking transactional and preserved the prior
  diagnostic plan and route intent when a candidate activation fails.
- Added the post-hydration package/lineage audit and recovery-scene rollback;
  hydration-only transient state is excluded from the identity comparison.
- Persisted the Step 4C preparation mode and made Step 5C reject stale or
  duplicate closed-jaw target docks, while retaining the four open bores, zero
  residual occupancy, one connected printable solid, and watertight checks.
- Reused the shared nine-decimal insertion provenance helper for all affected
  shell/template/restore comparisons; the read-only insertion getter no longer
  mutates MRML labels.

### Files and controlled records

Implementation touched the existing Stage 6 state/facade, case-bundle,
patient-shell, docking, guide, robot, template-build, restore-backend, and
focused runner/test paths. Controlled records updated are
`Workspace/docs/DECISIONS.md` and `Workspace/docs/TASKS.md`; no engineer-owned
record was read or changed.

### Verification

- `88 passed in 0.30s` for the focused Stage 6 state, façade, case-bundle,
  and planning contract tests.
- Non-writing `compile(...)` checks passed for every touched production and
  Stage 6 runner module.
- `git diff --check` passed.
- `graphify update .` was attempted after the edits but remains blocked by the
  workspace `Operation not permitted` error during rebuild.
- No Slicer/ROS runtime, robot motion, drilling, playback, or normal-window
  acceptance was run in this source-only checkpoint.

### Evidence boundary and unresolved blockers

The source constraints are implemented, but the plan is not accepted. The
remaining blockers are the terminal FDI31 three-cycle stop, exact package
integrity/reopen identity, regenerated Step 5B/5C geometry, current eligible
PreparedBranch and schema-3 Step 5C evidence, the ten normal-window
observations, and separately approved S6-LIVE-01 through S6-LIVE-04 runtime
checks. The viewport-crop `QSize` diagnostic limitation remains historical
failed-run evidence and is not silently reclassified.

### Next bounded action

Keep this implementation at the source/runtime boundary. Before any live
planner campaign, obtain a new explicit runtime approval and resolve the
upstream package/geometry/PreparedBranch/normal-window gates in their recorded
order. Do not tune depth, switch target, synthesize a missing package, or use
the planner as a workaround for those gates.

## S6 — final validation campaign and compiled diagnostics

### Operator request and execution boundary

The operator requested final tests and validation with complete success
metrics, diagnostics, and clearly attached screenshots. The campaign was
limited to source/static checks, pure contract checks, retained synthetic
case evidence, and one approved synthetic Slicer reusable-case validation.
No robot motion, drilling, patient-facing operation, hardware action, or
clinical acceptance was performed.

### Checks executed

- Host focused Stage 6 suite: `88 passed in 0.29s`.
- Container planning suite: `78 passed in 0.50s`.
- Container restore/package suite: `44 passed in 0.51s`.
- Non-writing compile: 25 touched production/runner Python files, exit 0.
- Retired phantom production-surface check: exit 0.
- `git diff --check`: exit 0.
- Matrix-routed synthetic reusable-case Slicer test: exit 0 with
  `DENTOBOT_REUSABLE_CASE_PASS`.
- Post-run process inspection: no Slicer, inference, bridge, or nnUNet worker
  remained in the container.

### Retained runtime diagnostics reviewed

- FDI32 repaired17 visual/trajectory evidence: valid Case Foundation pose,
  valid 5.487854799733802 mm assisted trajectory, 24 screenshots, but
  `base_locked=false` and `prepared_branch_count=0`; classified PARTIAL.
- FDI31 cycle 1: `STALE_4C` after reopen; two screenshots.
- FDI31 cycle 2: pre-bind `OrientationState` package/MRML mismatch; two
  screenshots, with a blank viewport capture explicitly marked unusable.
- FDI31 cycle 3: `UPSTREAM_CHANGED` shell/insertion state after reopen; one UI
  screenshot; viewport capture failed with `TypeError: 'QSize' object is not
  callable`.
- The three-cycle FDI31 ceiling remains reached; no fourth attempt was made.

### Report and evidence

The compiled report with metrics, first causal failures, evidence levels,
artifact paths, SHA-256 hashes, and screenshot captions is:
`Workspace/docs/diagnostics/S6-FINAL-VALIDATION-2026-09-13.md`.

The report intentionally keeps PASS, PARTIAL, FAIL, BLOCKED, and NOT VERIFIED
separate. It does not close `W5-U-04`, `S6-REUSABLE-CASE-SETUP`,
`S6-LIVE-01..05`, or the ten normal-window observations.

### Environment note

The first attempt to invoke the synthetic Slicer check from the restricted
shell was rejected by Docker-socket permissions. The exact command was then
rerun under the approved elevated execution boundary and passed. The earlier
`graphify update .` rebuild remains blocked by the workspace
`Operation not permitted` error; the graph query itself completed.
