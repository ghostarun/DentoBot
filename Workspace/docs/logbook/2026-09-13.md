# DENTOBOT Engineering Log — 2026-09-13

## S6-REUSABLE-CASE-SETUP status and visual-evidence handoff

- **Operator request:** Summarize the completed Case Foundation/reusable-base
  implementation, remaining acceptance work, bypassable checks, and screenshot
  requirements for goal approval.
- **Read-only grounding:** Reviewed the S6 entries in `backlog.md`, `TASKS.md`,
  `DEVELOPMENT_PLAN.md`, `DECISIONS.md`, `AGENT_CONTEXT.md`, and the recorded
  2026-09-12 S6 evidence. No source, case data, engineer-owned record, ROS,
  MoveIt, hardware, or external service was accessed or changed.
- **Current evidence:** The recorded targeted static/pure checks pass, and the
  focused `runtime.s6_reusable_case` run exited 0 with
  `DENTOBOT_REUSABLE_CASE_PASS`. Integration and ten-point normal-window
  operator acceptance remain open; this task is not closed.
- **Visual evidence boundary:** The available local Step 6 images under
  `data/dentobot-open-mouth-*.png` are dated 2026-08-17 and visibly contain the
  retired Draft Open-Mouth Phantom controls. They are historical context only,
  not current Case Foundation evidence, and must not be used for approval.
  No current normal-window screenshot was captured in this read-only turn.
- **Next bounded action:** On an anonymized/synthetic case, capture the ten
  required UI/state observations with timestamps, fingerprints, branch/base
  revisions, and no patient identifiers; then review the task-only diff boundary
  before any commit/push or further runtime work.

## Visibility and Step 4A gate follow-up

- **Operator observation:** After the committed mouth opening, Step 4A target
  selection stayed disabled until robot-base placement was locked; assisted
  trajectory generation also appeared locked while manual placement was active.
  The attached screenshots show intermittent early-step “full anatomy” views
  where only pulp/support masks remain, and a Step 6 lower jaw rendered as a
  beige fallback instead of the reviewed segmentation colors.
- **Implementation:** Moved the workflow continuation boundary to Stage 2
  “Segmentation and Review” and added dedicated Stage 3 Case Foundation
  navigation/view handling. Early inspection now normalizes aggregate, 2D,
  3D, and every per-segment visibility flag. Segment checkbox/isolate helpers
  use the same normalization, including legacy selected runs without the
  transient bridge marker. The opened-jaw recommended view uses colored fixed
  upper/moving lower derived anatomy and hides the beige fallback when the
  moving proxy exists. The Case Foundation gate now restores the enabled state
  captured before the pose was incomplete, and the opening callback refreshes
  planning controls immediately so Step 4A does not depend on base locking.
- **Focused test harness:** Extended
  `Testing/run_dentobot_view_composition_smoke.py` with a seeded pulp-only
  regression, colored synthetic reviewed segmentation metadata, Case
  Foundation proxy assertions, and UI/3D evidence capture. The latest bounded
  Slicer attempt printed `STAGE2_VISIBILITY` with all five source segments
  visible (aggregate and per-segment flags), then stopped only at the off-screen
  Qt 3D screenshot save. The evidence helper now uses a direct VTK render-window
  PNG fallback; a final rerun is still approval-gated by the three-failure
  runtime retry ceiling.
- **Static evidence:** `git diff --check` and writable-cache `py_compile` pass
  for all changed production/test Python files. `graphify update .` completed
  successfully (6,119 nodes / 9,718 edges). No ROS, MoveIt, robot motion, or
  hardware operation was started.
- **Evidence boundary / unresolved:** The host currently contains only the
  Stage 2 UI screenshot from the failed capture at
  `/home/light-tarun/dentobot/data/dentobot-runs/dentobot-case-views-stage2-full-anatomy-ui.png`;
  it is diagnostic (the synthetic inspection selector was not yet bound), not
  acceptance evidence. Stage 3 and viewport screenshots require one explicitly
  approved bounded rerun after the retry ceiling.
- **Follow-up hardening:** The smoke fixture now binds the synthetic reviewed
  run through `selectInspectionContext` before capture, and the comparison
  shortcut text points to Step 3. The shared checkbox/isolate helpers and
  selected-run inspection path now update every available base/2D/3D segment
  flag, so stale pulp-only state cannot be resurrected by a later view action.

## Case13sept FDI32 focused offline workflow verification

- **Operator request:** Verify the supplied `dentobot-case-13sept.dentocase`
  from Case Foundation mouth opening through offline Step 6 Manual Simulation
  Base setup, confirm that Step 4A is usable before base locking, automate one
  assisted trajectory for FDI32 near the crown centre, and capture visual
  evidence. ROS/MoveIt, motion, hardware, and patient-facing execution were
  explicitly out of scope.
- **Source package and integrity:** The original host package is
  `/home/light-tarun/dentobot/data/Slicer_Saved/SampleStudy1/dentobot-case-13sept.dentocase`.
  Its host/container SHA-256 is
  `14ed8f0f61e0c791d31b9582af7969ce712dd80d8cfd3a33319da63d0a996a15` and it
  was not overwritten. Production loading correctly stopped before workflow
  actions because MRML `step6BasePlacementRevision 1` disagreed with the
  package lineage/environment revision `0` (`step6.basePlacement.fingerprint`).
  This is a saved-package integrity blocker, not evidence to weaken the
  fail-closed loader.
- **Diagnostic copy boundary:** For focused runtime evidence only, the runner
  created
  `data/dentobot-runs/case13sept-fdi32-repaired4/diagnostic-integrity-repaired-case.dentocase`.
  The opt-in repair changed exactly one inner MRML revision token from `1` to
  `0`, then rebuilt the inner MRB, manifest scene hash/size, and checksum list.
  The source package remained byte-for-byte unchanged; the repaired copy is
  not acceptance of the original package and must not be treated as a product
  migration.
- **Implementation produced during this verification:** Restored the shared
  `RobotSceneSyncLogicMixin._modelRasBounds`, `combinedRasBounds`, and
  `step6ResearchWorkspaceRasBounds` API used by production Step 6 framing and
  the existing focused Slicer tests. This was the causal runtime failure after
  the repaired package loaded; no unrelated workflow algorithm was changed.
- **Commands and static evidence:** The focused runner and bounds helper were
  compiled with writable caches:
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-case13sept-pycache3/4 python3 -m py_compile Testing/run_dentobot_case13sept_fdi32_workflow_smoke.py DENTOWorkflow/Resources/Python/dentobot_workflow/logic_robot_scene_sync.py`.
  Targeted `git diff --check` passed for those files, and `graphify update .`
  completed after the code edit (about 6.1k nodes / 9.8k edges). No Docker
  rebuild or broad test suite was run.
- **Runtime command:** Seven serialized attempts were kept within the runtime
  retry ceiling: the original package (failed at the integrity gate), one
  repaired copy (exposed the missing bounds API), and five repaired-copy passes
  after the helper and view fixes. The final evidence run wrote its direct log
  to
  `/home/light-tarun/dentobot/data/dentobot-runs/case13sept-fdi32-repaired7/run.log`;
  it emitted `CASE13_LOADED`, `STAGE3_FOUNDATION_PASS`,
  `STEP6_OFFLINE_BASE_PASS`, `STEP4A_FDI32_ENTRY_PASS`,
  `DENTOBOT_CASE13_FDI32_ASSISTED_PASS`, and
  `DENTOBOT_CASE13_FDI32_WORKFLOW_PASS`.
- **Runtime-verified result (diagnostic repaired copy):**
  `result.json` reports source volume `PostDentalSurgery`, reviewed
  segmentation `Post_surgery_seg`, pose `VALID`, planning-pose fingerprint
  `2f6006ce3404580a20a0be7a417799772bd78313ed2f8685915e66b079593e5a`, seven
  reconstructed local robot links, base status `Unlocked`/lock `false`, and
  `ros_active: false`. Stage 3 displayed both fixed-upper and moving-lower
  colored Case Foundation proxies with all segments visible and the beige
  fallback hidden. Step 4A target selection was enabled while the base was
  still unlocked; FDI32 was selected; Assisted mode and one-trajectory entry
  placement were enabled. The automated crown-cap entry was
  `[-100.73516082763672, -37.3179931640625, 54.4561653137207]` RAS, and the
  generated FDI32 line was valid with two points, length `5.487854799733803`
  mm, target RAS `[-99.88500855710342, -39.27584089733821,
  49.400413769867406]`, creation method `EntryDirectedRootSurfaceCapV1`, and
  assisted provenance present. Manual mode remained enabled afterward.
  Stages 4B, 4C, 5A, 5B, and 5C remained navigable and pose-eligible. No
  PreparedBranch was created or activated, so the explicit ROS gate correctly
  stayed inactive.
- **Visual evidence:** Twenty-four PNGs are under
  `/home/light-tarun/dentobot/data/dentobot-runs/case13sept-fdi32-repaired7/`.
  The strongest current UI/viewport evidence is `stage3-case-foundation-*`,
  `step6-offline-base-*`, `step4a-fdi32-assisted-ready-ui.png`,
  `step4a-fdi32-assisted-generated-ui.png`, `stage5-ui.png`, and
  `stage9-ui.png`. The entry/generated direct-viewport PNGs are uniformly
  purple/blank even though their UI captures, runner assertions, log markers,
  and JSON state pass; therefore this run does not claim a visually confirmed
  trajectory line in those two viewport images. This is an evidence-quality
  limitation to investigate separately, not a reason to alter trajectory
  state or the loader during this run.
- **View and framing follow-up:** The initial repaired-copy passes exposed two
  distinct visibility defects. First, the opened derived segmentation could
  have aggregate or per-segment 3D visibility disabled after a recommended-view
  preset, producing a blank/pulp-only early-step view. The production view
  enforcement now explicitly enables the derived aggregate and every derived
  segment while hiding the beige lower-jaw fallback. Second, Step 4A framing
  used closed-source target bounds after the pose was current; the planning
  focus and workflow framing paths now use the jaw-owner-aware fixed-upper or
  moving-lower proxy bounds. Repaired7 assertions report aggregate/3D/all-
  segment visibility `true` for both proxies, and the Stage 3 and Step 6 UI/
  viewport captures show the full vivid reviewed anatomy. The direct VTK Step
  4A target/entry/generated viewport captures remain uniformly purple/blank,
  so visual trajectory-line evidence is intentionally withheld pending a
  separate render-timing/Step-4A viewport investigation.
- **Current final evidence path:** The repaired7 package copy has SHA-256
  `9dbf6275224d22cde45f19d6e56c6ba3d703066c2893f09931d2c4c8740e30b9`; the
  original package checksum remains unchanged. Its `result.json` records the
  FDI32 entry, generated assisted line, pose/base/robot state, and 4B–5C
  navigation results. The repaired copy is diagnostic runtime evidence only;
  it is not a migration or replacement of the supplied package.
- **Post-fix static evidence:** After the aggregate/per-segment visibility and
  jaw-owner-aware framing edits, writable-cache compilation passed for
  `Testing/run_dentobot_case13sept_fdi32_workflow_smoke.py`,
  `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_views.py`, and
  `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_planning_focus.py`.
  Targeted `git diff --check` also passed for the updated runner, widgets, and
  this logbook. `graphify update .` was rerun after the source edits. These are
  static checks only; no broad suite or additional Slicer runtime was started
  after repaired7.
- **Evidence classification:** Implemented — shared Step 6 bounds API restore;
  Unit/Static Verified — writable-cache compile, targeted diff check, Graphify;
  Runtime Verified — diagnostic repaired-copy Stage 3, offline Step 6, FDI32
  target/entry/assisted generation, and 4B–5C navigation; Integration Verified
  — not complete (original package integrity must be repaired by an explicit
  current save or reviewed migration, and full 4B–5C geometry/Step 5C PASS was
  intentionally not built); Operator Verified — not complete. Keep
  `S6-REUSABLE-CASE-SETUP` open until the original package loads cleanly and
  normal-window acceptance records the remaining ten-point workflow evidence.

## Case13sept opened-frame trajectory follow-up

- **Operator continuation:** Continue the approved targeted verification after
  the Step 4A viewport remained empty in the earlier evidence run. The required
  scope stayed offline: no ROS/MoveIt connection, robot motion, hardware, or
  patient-facing action.
- **Root cause and implementation:** The assisted-entry UI point was collected
  in the opened Case Foundation world, while `generateAssistedTrajectories`
  analyzed it against the closed source segmentation and emitted closed-frame
  trajectory points. Added one shared planning-bounds helper and a single
  source/planning coordinate conversion at the workflow generation boundary.
  Lower-jaw entry/target points are now analyzed in immutable source RAS and
  published in opened Case Foundation world RAS; upper-jaw and no-foundation
  callers retain the identity path. Target bounds ROI, trajectory bounds
  validation, planning focus, and workflow framing use the jaw-owner-aware
  opened proxy bounds when the pose is current.
- **Static evidence:** Compiled the changed production and focused-runner
  Python files with writable bytecode caches using
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-case13sept-pycache4 python3 -m py_compile
  DENTOWorkflow/Resources/Python/dentobot_workflow/logic_planning_dependencies.py
  DENTOWorkflow/Resources/Python/dentobot_workflow/logic_workflow.py
  DENTOWorkflow/Resources/Python/dentobot_workflow/widget_planning_focus.py
  Testing/run_dentobot_case13sept_fdi32_workflow_smoke.py`; the command passed.
  Targeted `git diff --check` passed. `graphify update .` completed after the
  source edit. No broad suite, Docker rebuild, or full Slicer verification was
  run.
- **Runtime evidence:** A bounded serialized run against the diagnostic copy
  `data/dentobot-runs/case13sept-fdi32-repaired18/diagnostic-integrity-repaired-case.dentocase`
  emitted `CASE13_LOADED`, `STAGE3_FOUNDATION_PASS`,
  `STEP6_OFFLINE_BASE_PASS`, `STEP4A_FDI32_ENTRY_PASS`,
  `DENTOBOT_CASE13_FDI32_ASSISTED_PASS`, and
  `DENTOBOT_CASE13_FDI32_WORKFLOW_PASS`. The source package was not modified;
  its SHA-256 remains
  `14ed8f0f61e0c791d31b9582af7969ce712dd80d8cfd3a33319da63d0a996a15`.
  The focused copy repaired exactly the known inner MRML base-revision token
  (`1` to saved lineage revision `0`) and rebuilt its manifest/checksums.
- **Measured workflow result:** Case Foundation pose was `VALID` with planning
  pose fingerprint
  `2f6006ce3404580a20a0be7a417799772bd78313ed2f8685915e66b079593e5a`;
  seven local robot links reconstructed offline; base remained `Unlocked` and
  ROS remained inactive. Step 4A target selection, Assisted mode, and one-entry
  placement were enabled before base locking. FDI32 was selected and the
  automated crown-cap entry was transformed into the opened lower-jaw frame.
  The generated line is valid with two points, length `5.487854799733803 mm`,
  target FDI `32`, creation method `EntryDirectedRootSurfaceCapV1`, and
  assisted provenance. Its entry and target both lie within the opened target
  bounds. Stages 4B, 4C, 5A, 5B, and 5C remained navigable and the runner did
  not create a PreparedBranch or enable ROS.
- **Visual evidence:** The current 24-image evidence set is under
  `/home/light-tarun/dentobot/data/dentobot-runs/case13sept-fdi32-repaired18/`.
  `stage3-case-foundation-viewport.png` and
  `step6-offline-base-viewport.png` show full vivid reviewed upper/lower
  anatomy with the beige fallback hidden. `step4a-fdi32-assisted-generated-ui.png`
  and `step4a-fdi32-assisted-generated-viewport.png` show the FDI32 target,
  Entry 1, Target, and the generated trajectory line inside the opened target
  bounds. The earlier blank Step 4A viewport limitation is resolved for this
  corrected run.
- **Non-causal teardown noise:** After the pass marker, Slicer emitted Qt
  teardown callbacks attempting to update destroyed planning widgets. These
  occurred after `DENTOBOT_CASE13_FDI32_WORKFLOW_PASS`, did not change the
  saved result, and are not treated as workflow failures in this bounded run.
- **Evidence status:** Implemented and Runtime Verified for the shared opened
  planning-frame fix and the requested offline FDI32 smoke. Integration and
  Operator Verified remain open: the original package still fails its
  fail-closed integrity check until an explicit current save/reviewed migration,
  and normal-window acceptance must still cover the full ten-point contract.
- **Post-run targeted check:** Writable-cache compilation and `git diff --check`
  passed again. `python3 -m pytest -q Testing/test_modular_structure.py
  Testing/test_view_presets.py` produced 12 passed and one unrelated existing
  failure: `test_active_workflow_modules_stay_within_context_budget` reports
  `widget_template_build.py` at 1,511 lines (limit 1,500). No unrelated module
  was refactored for this workflow fix.
- **Evidence file check:** Re-read `case13sept-fdi32-repaired18/result.json`
  and verified all 24 referenced PNGs exist on the host. The result still
  reports a valid two-point trajectory and `ros_active: false`. No further
  runtime was started after this check.

## Release documentation and backlog reconciliation

- **Operator request:** Wrap up the implemented Case Foundation/opened-frame
  correction, preserve the diagnostic evidence for user review, reconcile the
  current task/backlog status, and publish only the relevant working state.
- **Documentation produced:** Added
  `Workspace/docs/diagnostics/S6-REUSABLE-CASE-SETUP-2026-09-13-FDI32.md` with
  the workflow timeline, source-versus-diagnostic package boundary, success and
  error parameter comparison, fingerprints, screenshot links, verification
  levels, controlled task crosswalk, and a dependency-ordered Stage 3 planner
  completion checklist. Updated `Workspace/docs/backlog.md` to record the
  repaired18 diagnostic pass while retaining the exact-package, current
  PreparedBranch, and normal-window acceptance blockers.
- **Commit boundary:** The intended release commit includes the S6 display,
  planning-frame, gate, and focused-runner changes plus the new diagnostic,
  backlog, and 2026-09-13 logbook. Existing dirty policy/workstation records,
  unrelated daily logs, and any other non-S6 hunks remain unstaged and are not
  included.
- **Next publication action:** Run a staged diff review and `git diff --cached
  --check`, create the S6 release commit, push it to `origin/main`, then append
  the exact commit/push result to this logbook in a follow-up docs-only commit.

## Publication result

- **Staged review:** `git diff --cached --check` passed and writable-cache
  compilation completed for every staged Python file before commit.
- **Commit:** Created `1f8c55a` with message
  `fix(workflow): finish reusable case foundation verification`. The commit
  contains 23 scoped S6 source/test/documentation files, including the FDI32
  runner, diagnostic report, backlog reconciliation, and this dated evidence.
- **Push:** `git push origin main` completed successfully; `origin/main` moved
  from `3e02214` to `1f8c55a`.
- **Preservation:** The unrelated dirty policy/workstation records and the
  untracked `Workspace/docs/logbook/2026-09-12.md` remain outside the release
  commit. No reset, checkout, deletion, Drive write, or engineer-owned record
  access was performed.
- **Post-publication state:** This logbook append is intentionally a separate
  docs-only follow-up commit so the already-pushed implementation commit is not
  rewritten. The S6 task remains open for the exact-package integrity repair,
  current PreparedBranch/Step 5C acceptance, and normal-window operator review.
