# DENTOBOT Logbook — 2026-09-02

## Shared Codex/Cursor/Claude verification practice

- Accepted one tool-neutral subagent verification contract for Codex, Cursor,
  and Claude. Added `Workspace/docs/AGENTIC_VERIFICATION_PROTOCOL.md` with
  approval keywords, coordinator/read-only-worker ownership, parallel-safe
  gates, one exclusive runtime lane, evidence/output limits, teardown, failure
  routing, and the required three-stage MoveIt diagnostic report.
- Added `Testing/verification_matrix.json` as a resource/dependency inventory
  of the current Priority-0, restore, and lifecycle gates. It references
  existing pytest/smoke entrypoints and introduces no new runtime harness.
- Added one pure matrix contract check covering unique IDs, closed dependency
  and profile references, and exclusive runtime classification.
- Added tracked thin entrypoints for Codex (`AGENTS.md` and
  `Workspace/AGENTS.md`), Cursor
  (`.cursor/rules/dentobot-verification.mdc`), and Claude (`CLAUDE.md`). No
  tool-specific worker policy was duplicated.
- Extended the non-overwriting workspace bootstrap to link the tracked Claude
  entrypoint and Cursor rule into `~/dentobot`; ran the bootstrap successfully.
- Verified `Testing/test_verification_matrix.py` with host `py_compile` (exit
  `0`) and the scoped container pytest (`1 passed in 0.09s`). This checked only
  JSON parsing, unique IDs, closed references, and non-parallel runtime/build
  resource declarations.
- Refreshed the local AST-only Graphify index: 5,201 nodes, 8,469 edges, and
  395 communities. No semantic/LLM labeling pass was run.
- No verification agent was spawned and no Slicer, ROS, MoveIt, colcon, robot,
  drilling, Git publication, or Drive synchronization action was performed.

## Windows lab WSL2 + Linux SlicerROS2 export

- Added `Workspace/LAB_RELEASE` pinning `lab/2026-09-02`,
  `slicer_ros2_module` `4ef3d5b427ff`, local image
  `dentobot/slicerros2:jazzy-moveit-sim-20260821`, and GHCR name
  `ghcr.io/ghostarun/dentobot/slicerros2:jazzy-moveit-sim-20260821`.
- Added `install-lab-wsl.bash`, `update-lab-release.bash`,
  `publish-lab-image.bash`, and Windows `.bat` wrappers that call WSL.
  Launch still uses `launch-dentoworkflow.bash`. Native PowerShell launcher
  is not this profile.
- Install/update fail closed until git tag `lab/2026-09-02` exists on origin.
  They refuse the maintainer `integration/gui-step6` worktree. No password in
  scripts; access is GitHub collaborators + GHCR.
- Did not commit, tag, push, or `docker push`. First lab tag waits for
  explicit Git authorization after an Ubuntu freeze of the current tree.
- Verification: `bash -n`; `install-lab-wsl.bash --check-only` parsed the pin;
  `update-lab-release.bash --check-only` exited 2 on `integration/gui-step6`
  as designed; `launch-dentoworkflow.bash --check-only` still passed (GUI
  skipped). Overlay `graphify update .`: 6,682 nodes. No Windows lab PC, no
  robot motion, no Drive sync, no git tag.

## Drive/MCP scratch, nested graphify, unused-script scan

- Deleted leftover overlay Drive/MCP temps at `~/dentobot/` (`.tmp_*`,
  `.mcp_*`, `.upload_*`, `.drive_upload_*`, `.cursor_setup_upload.json`,
  `.decisions_*`, `.dec_meta.json`) and overlay `.pytest_cache`. Kept
  `.dentobot.env`.
- Deleted untracked DentoBot `.drive_upload_b64_*.txt` and
  `.drive_upload_text/`.
- Removed nested `ros2_ws/src/DentoBot/graphify-out/` (~37 MB). Untracked
  `graphify-out/.graphify_python` and `.graphify_root`. Live graph remains
  `~/dentobot/graphify-out/` (~93 MB). DentoBot `.gitignore` now covers
  `graphify-out/` and Drive dumps.
- A broad `find` pycache sweep failed on Docker-owned
  `Testing/`, `Inference/`, and `DENTOWorkflow/` caches (`Permission
  denied`). Left those regenerable files; did not escalate to root delete.
- Script scan: all `Workspace/scripts/` launchers remain in use
  (`bootstrap-workspace.bash`, `git-dentobot.bash`,
  `launch-dentoworkflow.bash/.ps1`, description/RViz launchers, health,
  host ROS 2 source, MRML roundtrip, image probe, Record3D viewer).
  `tools/arduino-pressure/run_*.sh/.bat` stay. `Infrastructure/close_day_checks.sh`
  and `install_ubuntu_backend.sh` stay (SETUP/close-day). Archive copies
  under `archive/DentoBot-demo-aff8b2e/` were not touched.
  `extract_workflow_mixin.py` has no current callers; it is a one-shot
  mixin extractor, not a bash leftover, so it was kept.
- Verification: overlay temps gone; nested graphify path gone;
  `git -C ros2_ws/src/DentoBot ls-files graphify-out` empty. Overlay
  `graphify update .` rebuilt 6,659 nodes / 10,539 edges / 626 communities.
  No Git commit or Drive sync.

## Host overlay and Arduino bench in the main git tree

- Moved the live pressure scripts from `ros2_ws/src/Arduino/` into
  `ros2_ws/src/DentoBot/tools/arduino-pressure/` and added a
  `~/dentobot/tools` symlink. Deleted the standalone overlay package
  `~/dentobot/arduino-pressure/` and the old `src/Arduino` tree.
  Recordings stayed with the bench under gitignored `pressure_runs/`.
- Renamed stale `DentoBot/docs/` to `docs-legacy/`. Moved
  `DentoBot-demo-aff8b2e` to `~/dentobot/archive/`. Added
  `Workspace/HOST_LAYOUT.md` and a `tools` bootstrap link.
- Wired `--port` / `--list-ports` into the in-repo monitor. Cursor launch
  configs now use `tools/arduino-pressure`.
- Verification: `pressure-env` `python -m py_compile` on the moved
  modules; `pressure_monitor.py --help`. Did not open serial or flash the
  board. Sensing-only. No Git commit, Drive sync, secrets, or patient data.

## Pressure sampling rate and pipeline Config tab

### 18:10 IST — fs is paced, displayed, and editable

- Replaced the stale live `pressure_monitor.ino` (10-bit, 115200, delay 10 ms)
  with 14-bit `seq,micros,raw` at 460800, micros-paced default 1000 Hz, and
  host `RATE <hz>` (200–1500). Default 1 kHz is from NXP MPX5700 Rev 10
  tR=1.0 ms (~350 Hz analog BW), not a hidden constant.
- Live GUI: always-on strip with set vs measured fs, ADC bits, sensor, taus;
  **Config** tab to edit rate/scale/filters (Apply disabled while recording).
  Each run writes `pipeline.json`. Analysis shows measured fs and can redetect
  with new filter taus; it cannot change past `fs`.
- Verification this turn: `py_compile` and `--no-gui` / offscreen analysis.
  Did not flash the board or open serial. Sensing-only; no robot motion.

## Priority-0 Step 6 full-chain continuation

### Scope

- Continued `S6-P0-01` only.
- Preserved simulation/preview-only scope; no hardware, controller, powered
  spindle, or drilling execution was exposed.

### Approved checks completed before planner-v4 edit

- Ran the focused Home-connected-seed/J6 regression:
  `pytest -q Testing/test_robot_workflow_facade.py -k stage1_uses_every_bounded_home_connected_seed_with_j6_locked`.
  Result: `1 passed, 20 deselected`.
- Compiled the edited façade, simulation panel, and focused test with
  `python3 -m py_compile`. Result: exit `0`.
- Inspected the container for an existing Slicer/MoveIt owner; none was active.
- The first exact-x4 launch attempt was rejected by the sandbox before Docker
  access. Re-ran the same approved simulation-only command with the required
  Docker permission.
- Ran `Testing/run_dentobot_step65_exact_case_smoke.py` against
  `dentobot-case-step6x4.dentocase` with `DENTOBOT_GOAL1_ONLY=1`.
  The script emitted `DENTOBOT_STEP65_EXACT_CASE_PASS`: Stage 1 previewed 55
  strict waypoints, J6 stayed `0 rad`, and one Stage-1 path was rendered. Full
  task remained `Blocked` at the then-current guessed Stage-2 split: `50.0%`,
  11 retained partial waypoints. The wrapper exited `1` only after the PASS
  marker during the known Slicer VTK/class-loader teardown diagnostics.

### Source changes after the exact trial — unverified

- `DENTORobotWorkflowFacade.py`
  - retained every bounded Home-connected 6.3 sample as a meaningful arm IK
    seed and audited each canonical J6-zero endpoint;
  - replaced the guessed terminal-distance split with one fixed-frame
    PreEntry→Entry Cartesian path;
  - delegated selective configured burr contact and all authoritative
    collision/corridor checks to the independent phase guard;
  - created a fresh validate-only guard session for each candidate so sequence
    and corridor history cannot leak between branches;
  - preserved Stage-1-only preview when Stage 2 is rejected and exposed the
    first composed invalid waypoint/cause;
  - advanced the route-planning identity to v4 with
    `phase_guard_evidence_based` Stage-2 policy.
- `DENTOStep6State.py`
  - advanced motion diagnostics to schema 2.1 with
    `stage2_fixed_axis_terminal`;
  - retained schema-2.0 parsing for historical strict-axis evidence.
- `dentobot_workflow/logic_robot.py` and the façade diagnostic review path
  preserve the record's original schema when marking old evidence stale or
  reviewed, instead of silently rewriting it as schema 2.1. Freshness now
  explicitly marks pre-2.1 motion evidence stale for planning while retaining
  it as readable history; `dentobot_workflow/runtime.py` routes the schema
  constant to that check.
- `DENTORobotSimulationPanel.py`
  - updated Stage-2 wording and schema-2.1 display mapping;
  - retained seed/frame/chain diagnostic columns.
- `dentobot_workflow/parameter_state.py`
  - retained the old terminal-tolerance field only for package compatibility;
    planner v4 no longer uses it as contact authority.
- Added focused regression definitions for the fixed-axis phase-guard policy
  and diagnostic schema 2.0/2.1 round trips. They have not been run.
- Updated the controlled Development Plan, Tasks, Agent/Project Context,
  Decisions, and Reproducibility documents to mark the v4 increment as source
  implemented and unverified.

### Open gate

- No verification was run after the planner-v4/schema-2.1 edits.
- Next proposed bounded gate, requiring operator approval: Python compilation;
  focused seed/contact-policy/schema regressions; then the exact x4
  simulation-only trial. Acceptance requires either a complete guard-valid
  Home→PreEntry→Entry→Target chain or a Stage-1-only provisional result naming
  the exact first forbidden Stage-2/3 waypoint and collision pair.

### Repository graph refresh

- Initial `graphify update .` was blocked by the sandbox with
  `Operation not permitted`; the identical AST-only local refresh succeeded
  after approved workspace-process escalation.
- Result: 8,936 nodes, 14,008 edges, and 805 communities. `graph.json`,
  `graph.html`, and `GRAPH_REPORT.md` were updated.
- Graphify reported nine partial-parser warnings in existing SlicerROS2 C++
  headers and retained six fail-closed nodes after scan-filter changes. No
  semantic/LLM labeling pass was run.

## Priority-0 three-stage failure feedback

### Operator boundary

- The operator withdrew approval for Slicer/ROS/MoveIt smoke, exact-case,
  headless, GUI-automation, and other runtime verification and will perform the
  normal-window application trial manually.
- The operator then explicitly approved scoped `pytest` in addition to
  `py_compile`. A SlicerROS build remains permitted only when production C++
  changes require one; no C++ changed in this increment, so no build was run.

### Production changes

- `DENTORobotWorkflowFacade.py`
  - retains Stage 1/2/3 waypoint counts, Cartesian fractions, composed and
    stage-local first-invalid indices, failure stage, guard message, and bounded
    cause for every evaluated full-chain route;
  - persists those fields into diagnostic schema 2.1 stage/full-task outcomes;
  - infers truthful Stage-1 failure identity for routes that never reach a
    full-chain evaluation;
  - clears a failed Stage-3 preflight object after an exception so it cannot be
    stored or reported as a complete drilling preflight.
- `DENTORobotSimulationPanel.py`
  - extends the existing inspector rather than adding another framework;
  - reports each stage's status, progress, waypoint count, first invalid
    location, retained cause, and a stage-specific next operator action;
  - keeps all feedback display-only and non-authorizing.
- `dentobot_workflow/widget_robot_shell.py`
  - automatically opens the current diagnostic inspector only when the just-
    completed Goal 1 action produced a diagnostic fingerprint and failed or
    returned a blocked full-task result; unrelated stale evidence is not opened.
- Corrected one focused source-contract assertion to inspect
  `_configure_phase_guard`, which owns the policy, rather than its thin
  `_prepare_phase_guard` synchronization wrapper.

### Approved checks and results

- Initial `python3 -m py_compile ...` could not write the source-tree
  `__pycache__` (`Permission denied`); reran with
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-step6-pycache`. Result: exit `0`.
- Host `python3 -m pytest ...` did not run because the host environment has no
  `pytest` module.
- Container scoped run initially produced `32 passed, 1 failed`; the failure was
  the stale helper-name assertion described above, not a production failure.
- Reran
  `python3 -m pytest -q Testing/test_step6_state.py Testing/test_robot_workflow_facade.py`
  inside `dentobot-slicerros2`. Result: `33 passed in 0.08s`.
- No Slicer window, ROS/MoveIt stack, exact-case smoke, GUI automation, hardware
  motion, or drilling execution was run.

### Manual next action

- In normal-window Slicer, run Goal 1 on the current case. Confirm that a
  blocked result opens the inspector and names the failed stage, progress,
  first invalid waypoint/cause, and next action. A complete result must retain
  one frame fingerprint through Home→PreEntry→Entry→Target and enable only the
  guarded simulation previews.

### Graph maintenance

- The first local `graphify update .` attempt was blocked by the workspace
  process sandbox. The identical AST-only refresh succeeded with the required
  permission: 5,474 nodes, 8,676 edges, and 400 communities. No semantic/LLM
  labeling pass was run.

## Step 6.5 diagnostics Close-button DENTO-NOTE

### 19:58 IST — source fix; operator acceptance pending

- Recorded unprioritized item `S6-U-04` and triaged it as **fix now** because it
  is a narrow operator-facing defect in the existing Goal 1 diagnostics dialog.
- Operator evidence: the bottom **Close** button did not dismiss the diagnostics
  window either before or after evidence confirmation; the title-bar X remained
  the only working exit.
- `DENTORobotSimulationPanel.py` was the only local control that connected a
  `clicked(bool)` button directly to a native no-argument dialog method. It now
  follows the panel's established bool-consuming callback pattern and accepts
  the dialog explicitly.
- Impact is limited to diagnostics-window dismissal; planning evidence, review
  state, MRML, ROS, MoveIt, guards, and motion policy are unchanged.
- Verification: `python3 -m py_compile` passed with bytecode redirected to
  `/tmp`; scoped `git diff --check` passed. No Slicer, ROS, MoveIt, GUI
  automation, hardware motion, or drilling operation was run.
- Normal-window acceptance remains operator-led: close before review, reopen,
  review, close again, and confirm a subsequent reopen has no stale dialog
  behavior.
- Refreshed the local AST-only graph with `graphify update .`: 6,705 nodes,
  10,592 edges, and 619 communities. Existing partial C++ parser and six
  fail-closed-node warnings remain; no semantic/LLM labeling pass was run.

## Git main tracking and lab publication state

### 20:26 IST — local tracking repaired; controlled references reconciled

- Confirmed remote `HEAD` points to `main`; remote `main` and the retained
  `integration/gui-step6` alias both resolve to
  `17af3d8a7790f86e7a85965693107e92b0d2165b`.
- Replaced the checkout's narrow origin fetch list with the standard all-branch
  mapping, fetched/pruned origin, attached local `main` to `origin/main`, and
  aligned `origin/HEAD`. Before documentation edits, main was clean and
  exactly `0` ahead / `0` behind.
- Confirmed local and remote tag `lab/2026-09-02` resolve to the same frozen
  commit. The GHCR image is still unpublished; no image push was attempted.
- Updated only current branch/release authority text plus the lab helper's
  error wording. Preserved dated historical evidence about the earlier
  integration-branch and pre-tag state.
- No remote branch was deleted, no Git tag was created, no Slicer/ROS test was
  run, and no hardware motion, drilling, or Google Drive sync occurred.
- After explicit operator authorization, inspected the pinned local image:
  Linux/amd64, 4,489,970,463 bytes, image ID
  `sha256:035968f28f84fda0ce8a31471c329a5752a04474c0459ea716ec6f4ae906f929`.
- `Workspace/scripts/publish-lab-image.bash --push` applied the GHCR tag but
  the registry rejected upload with `denied` before any layer was published.
  `gh auth status` reported the configured maintainer token invalid. A proposed
  cross-use of the Git credential helper was rejected on credential-safety
  grounds and was not executed. Publication now waits for standard GHCR login
  with `write:packages`; no secret was printed or recorded.
