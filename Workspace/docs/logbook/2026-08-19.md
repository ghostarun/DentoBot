# 2026-08-19

## Step 6 Connect: RSP without Slicer joint publisher

### 19:55 IST

- Observed dialog: `/dentobot_robot_state_publisher is running without
  /dentobot_slicer_joint_state_publisher. Stop that launch and start
  joint_state_mode:=slicer.`
- Live check at the same time: both description nodes plus `/slicer` were
  already running (healthy stack from 19:50). `/joint_states` had 1 publisher.
- Two causes: (1) the graphical Slicer still had the pre-PATH-fix module in
  memory, so Connect treated an existing RSP as a leftover and refused;
  (2) even the new Connect path called `slicer_motion_stack_ready()` without
  `force=True`, so a 1.5 s cache from the failed half-stack could report
  the publisher missing after it was up.
- Connect now forces a fresh node list after start/stop. Reload the DENTOBOT
  extension, dismiss the dialog, and press Connect again.
- Verification: host 12 passed / 4 skipped; container 16 passed; live node
  list still complete. Hardware motion was not run.

## Step 6 Connect: joint-state stack did not appear in 8 s

### 19:50 IST

- Observed: “Description launch was started but the Slicer joint-state stack
  did not appear within 8 s.” Disconnect stayed disabled.
- Live leftover launch pid 2066: `ros2 launch dentobot_description
  description.launch.py use_rviz:=false joint_state_mode:=slicer` with only
  `/dentobot_robot_state_publisher` and `/slicer`. No
  `/dentobot_slicer_joint_state_publisher`. `/joint_states` had 0 publishers.
- Root cause: Slicer PATH puts SuperBuild `python-install/bin` first.
  `slicer_joint_state_publisher` is `#!/usr/bin/env python3`, so it started
  Slicer Python and crashed with `ModuleNotFoundError: No module named
  'yaml'`. C++ `robot_state_publisher` kept running.
- Bridge now exports a Jazzy-plus-system PATH before sourcing overlays,
  stops an incomplete slicer-mode leftover, and keeps the launch log.
- Verification:
  - host `test_ros2_bridge.py` + `test_ros2_cli_slicer_env.py` +
    `test_step6_planning.py` → 23 passed, 5 skipped.
  - container those ROS tests → 14 passed, including yaml/rclpy under a
    Slicer-first PATH after the sanitized setup.
  - Stopped leftover pid 2066. Clean launch with sanitized PATH brought up
    both description nodes plus `/slicer` in 1 s (`STACK_START_PASS`).
    That stack was left running for an immediate Connect retry.
- Reload the DENTOBOT extension so the next Slicer-started launch also uses
  the PATH reset. Hardware motion was not run.

## Step 6.0 case into view and 6.2 merged joint rows

### 19:45 IST

- Next two slices after Connect: 6.0 import now frames the case package;
  6.2 is one min/value/max row per joint. Plan/Preview re-check `/slicer`
  when ROS is active (`ensure_slicer_ros2_runtime`, forced node list).
- Case-into-view bug found during finish: `onFrameStep6CaseScene` had
  overwritten `onFrameStep6ResearchWorkspace` and still framed phantom
  bounds after the case. Restored the phantom/robot helper; import only
  frames case RAS bounds and sets the CBCT slice background.
- Nested `robotJointControlGroupBox` removed. Widget names unchanged.
- Commits before/after the UI restructure:
  `c849f45` case view, `6ce7c5f` merged limits.
- Verification:
  - host `/usr/bin/python3 -m pytest -q Testing/test_step6_planning.py
    Testing/test_ros2_bridge.py Testing/test_robot_placement.py` → 28
    passed, 1 skipped (`scipy` absent on host).
  - container `python3 -m pytest -q Testing/test_ros2_bridge.py
    Testing/test_ros2_cli_slicer_env.py Testing/test_step6_planning.py
    Testing/test_robot_placement.py` → 32 passed. Live `SlicerApp-real`
    pid 1323 was running; `ros2 node list` required `/slicer`.
- Interactive Connect/Import/limits after DENTOBOT extension reload is
  still operator-run in the graphical window. Hardware motion was not run.
- Untracked leftover not committed: `Workspace/scripts/view_record3d_scan.py`.

## Step 6 Connect: ros2 CLI not available

### 19:35 IST

- Observed Slicer error after the module-import fix: “ros2 CLI is not
  available in this Slicer process.”
- Live `SlicerApp-real` (pid 521) PATH includes `/opt/ros/jazzy/bin` and
  `ROS_DOMAIN_ID=73`. `PYTHONHOME=/opt/slicer/Slicer-SuperBuild/python-install`.
- Direct `ros2 --help` under that env exits 1 (`librcl_action.so` / rclpy).
  The same command after `unset PYTHONHOME PYTHONPATH` and sourcing Jazzy plus
  `/workspace/ros2_ws/install/setup.bash` exits 0; `ros2 node list` returned
  `/slicer`.
- Bridge now uses `run_ros2_cli` (sanitized child env + sourced overlay) for
  help, node list, pkg prefix, and description launch.
- Verification:
  - host `/usr/bin/python3 -m pytest -q Testing/test_ros2_bridge.py
    Testing/test_ros2_cli_slicer_env.py` → 8 passed, 3 skipped (no Jazzy).
  - container `python3 -m pytest -q Testing/test_ros2_bridge.py
    Testing/test_ros2_cli_slicer_env.py` → 11 passed.
  - live SlicerApp-real pid 521 env (`PYTHONHOME` SuperBuild python,
    `ROS_DOMAIN_ID=73`): `ros2_cli_available()` True, `ros2 node list`
    returned `/slicer` (`LIVE_SLICER_ENV_PROBE_PASS`).
- Interactive Connect after DENTOBOT extension reload is still operator-run
  in the graphical window. Hardware motion was not run.

## Step 6 Connect: ROS2 module missing

### 19:20 IST

- Observed Slicer error: “The ROS2 Slicer module is not available. Use the
  dentobot SlicerROS2 container.” Connect never reached the description stack.
- Live `SlicerApp-real` args already included
  `slicer_ros2_module/.../qt-loadable-modules`,
  `qt-scripted-modules`, DENTO Workflow, and EndoPlanner. The second
  `--additional-module-paths` did not drop SlicerROS2 in this process.
- Root cause in the bridge: `get_ros2_logic()` used a global `slicer` name
  without importing it; the `NameError` was caught and shown as a missing
  ROS2 module. The helper now imports `slicer` locally and can register
  installed SlicerROS2 paths. Connect offers MRML **Load / Refresh Robot** if
  ROS2 is still absent.
- Launcher hardening: append DENTO Workflow to `SLICER_ROS2_MODULE_PATHS`
  after sourcing the workspace overlay; `slicer_args` only selects DENTO
  Workflow.
- Verification: `bash -n` on the launcher; host
  `python3 -m pytest -q Testing/test_ros2_bridge.py` → 6 passed. Container
  `slicer_ros2_module_search_paths()` found
  `libqSlicerROS2Module.so` and `ROS2MotionControl.py`.
- Unrelated host pytest miss: `test_plan_trajectory_motion_reports_self_collision_with_strict_clearance`
  failed with `ModuleNotFoundError: scipy` under `/usr/bin/python3`; not
  introduced by this slice.
- Not verified this session: pressing Connect after reloading the DENTOBOT
  extension in the graphical Slicer window.
- Hardware motion was not run.

## Step 6 panel order, gating, and Elements recommended view

### 18:45 IST

- Reordered Step 6 UI to a robotic sequence:
  - **6.0 Choose scene:** import case package XOR load draft phantom.
  - **6.1 Load ROS robot, place, and lock:** ROS connect is the main path;
    MRML load/frame remain under fallback.
  - **6.2 Task joint limits** (includes joint spinboxes).
  - **6.3 Plan / preview.**
- Gating: 6.1 until a scene is active; place/lock until a robot is present
  (ROS or MRML); 6.3 until case import + lock. Keyboard nudges ignore locked
  mounts.
- Case vs phantom are mutually exclusive on operator action (confirm, then
  delete phantom or clear the imported flag).
- View Controls Elements now lists Stage 6 nodes only (not the full 4A–5C
  dump). Recommended view: case package vs phantom. Applied on scene/robot
  load and when auto-recommended is on.
- `_updateRobotPlacement` no longer force-shows every robot/phantom/plane
  display node.
- Verification: host pytest 22 passed (`test_step6_planning`,
  `test_robot_placement`, `test_ros2_bridge`, `test_description`).
- Slicer `test_DENTOWorkflowRobotPlacementWidget` updated to load phantom
  before the MRML robot fallback. Not executed in this session — reload the
  DENTOBOT extension and run the widget test / live Step 6 in the graphical
  session.
- Planner, import-as-true-package, and collision upgrades were not part of
  this slice.
