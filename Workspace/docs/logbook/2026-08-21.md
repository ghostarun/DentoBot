# Dentobot Logbook — 2026-08-21

## Documentation checkpoint — detailed Step 6 reconciliation

### 15:17 IST — Request

- Update logbook, changelog, and controlled docs in detail: timestamps, what
  worked, what failed, fixes, algorithms implemented, whether logic works or
  should be fixed/replaced, alignment with `DEVELOPMENT_PLAN.md`, next steps,
  and lower-priority backlog.

### 15:17 IST — Actions taken

- Expanded this logbook with Step 6 slice checklist, 6.3 algorithm notes,
  fix-vs-replace matrix, and evidence labels.
- Updated `docs/changelog.md`, `docs/DEVELOPMENT_PLAN.md` (Step 6 status),
  and `docs/TASKS.md` (slice checklist and priorities).
- No new code commits in this batch. Branch:
  `cursor/step6-runtime-case-limits-06d8` (last code commit **2026-08-19
  19:49:53 IST**, `f93b7e9`). No Git push or Google Drive sync unless
  separately approved.

---

## Step 6 development-plan checklist (2026-08-21)

Branch `cursor/step6-runtime-case-limits-06d8`. Evidence is **Synthetic**
unless noted.

| Slice | Plan item | Status | Last verified (IST) | Notes |
|-------|-----------|--------|---------------------|-------|
| Foundation | `dentobot_description` URDF/TF/manual RViz | Done | 2026-08-14 | Draft q=0, J4 reversed, 5 mm AABB advisory |
| MRML placement | Seven-link STL chain, mount plane, keyboard nudges | Done | 2026-08-17 | Disposable open-mouth phantom co-located |
| 6.0 import | Planning-package validation + import flag | Done | 2026-08-18 | Requires Steps 0–5 roles on parameter node |
| 6.0 view | Frame **case RAS bounds** + CBCT slice background | Done | 2026-08-19 19:45 | Bug fixed: had framed phantom origin |
| 6.1 bridge | `DENTOROS2Bridge.py`, Connect/Disconnect UI | Done | 2026-08-18 | `joint_state_mode:=slicer` |
| 6.1 runtime | `ensure_slicer_ros2_runtime` on Plan/Preview | Done | 2026-08-19 19:45 | Fails closed if `/slicer` or stack missing |
| 6.1 Connect fixes | SlicerROS2 import, PYTHONHOME, PATH, node cache | Done | 2026-08-19 19:55 | Container pytest; **Developer-live GUI pending** |
| 6.2 limits | Task min/max clamped to URDF | Done | 2026-08-18 | Persisted on parameter node |
| 6.2 UI | One min / value / max row per joint | Done | 2026-08-19 19:45 | Min/max change updates value spin range |
| 6.3 plan | Linear Entry→Target sampling + position IK | Done | 2026-08-18 | SciPy L-BFGS-B; host skips if no scipy |
| 6.3 collision | Coarse AABB self + subsampled env clearance | Done | 2026-08-18 | 5 mm default can reject neutral pose |
| 6.3 preview | MRML joint animation along waypoints | Done | 2026-08-18 | Does not consistently drive `/joint_states` when ROS active |
| UX gating | 6.0 scene → 6.1 ROS/place/lock → 6.2 → 6.3 | Done | 2026-08-19 18:45 | Case XOR phantom; Elements Stage 6 view |
| **Operator** | Extension reload + Connect + sliders + import on real MRB | **Not verified** | — | Required before next code slice |
| Preview → ROS | Plan preview publishes joint path to live TF robot | **Partial** | — | MRML path works; ROS streaming gap |
| Axis alignment | Bur axis aligned to trajectory segment | **Not started** | — | Position-only IK today |
| Body vs env collision | Separate self vs anatomy/template gates | **Partial** | — | Both exist but share conservative models |
| MoveIt / `move_group` | IK gizmo, Plan, Execute in SlicerROS2 | **Not started** | — | Unauthorized for hardware |
| Navigation metrics | Virtual bur, error scalars, playback | **Not started** | — | DEVELOPMENT_PLAN future scope |
| Registration bridge | Physical template → robot base TF | **Not started** | — | Blocked on work package 4 |

---

## Step 6.3 planning algorithm (implemented 2026-08-18)

Implementation: `DENTOWorkflow/Resources/Python/DENTOStep6Planning.py`. Simulation
only — no MoveIt, no hardware command, no clinical safety claim.

### Pipeline

1. **Context import (6.0):** `validate_planning_context()` requires parameter-node
   roles: `inputVolume`, `teethSegmentation`, `trajectoryLine`,
   `targetDockingAssemblyModel`, `finalPrintableTemplateModel`. Optional roles
   include support models and target bounds ROI. Import sets a flag; framing uses
   `combine_ras_bounds()` over case-view roles (ignores degenerate boxes).

2. **Mount lock (6.1):** Base transform must be locked before 6.3. Robot pose
   comes from locked MRML base matrix or ROS TF under the same parent.

3. **Task limits (6.2):** `default_task_joint_limits_from_urdf()` seeds from URDF;
   operator min/max clamped via `apply_task_joint_limits_to_display_ranges()`.
   Merged UI uses `apply_task_limit_range_to_value()` per joint.

4. **Trajectory sampling:** `sample_trajectory_world_mm()` linearly interpolates
   Entry→Target Markups in world RAS mm (`sample_count` ≥ 2).

5. **Per-waypoint IK:** `_solve_position_ik_display()` minimizes squared
   burr-origin error in base frame with **SciPy `L-BFGS-B`**, seed = previous
   waypoint, bounds = task joint limits. **Position only** — no bur-axis alignment
   to trajectory tangent.

6. **Self-collision:** Reuses `CoarseKinematicModel` from
   `dentobot_description/scripts/manual_joint_state_publisher.py` — non-adjacent
   link AABBs with configurable clearance (default 5 mm). Reports draft overlap,
   not exact mesh or swept volume.

7. **Environment clearance:** Subsampled closed-surface points from teeth
   segmentation, template shell, docking assembly, and support anatomy.
   `evaluate_motion_configuration()` checks minimum point-to-burr-origin distance
   (default clearance parameter). Point cloud, not continuous surface.

8. **Output:** `MotionPlanResult` with joint waypoints, collision indices, burr
   world path. Preview animates MRML link transforms; Plan may reject on first
   failing waypoint.

### Does the logic work?

| Component | Works (synthetic) | Known limitation | Fix or replace? |
|-----------|-------------------|------------------|-----------------|
| Context validation | Yes — pytest | Needs real Steps 0–5 MRB import | **Keep**; verify Developer-live |
| Case framing | Yes — pytest + widget test | Requires extension reload in GUI | **Keep** |
| L-BFGS-B position IK | Yes — when scipy present | No axis constraint; local minima | **Keep** for research gate; add axis cost later |
| AABB self-collision | Yes — detects overlaps | Neutral pose warns (conservative boxes) | **Keep** as tunable gate; tune clearance before replace |
| Env point clearance | Yes — subsampled | Undersampling can miss thin features | **Keep**; increase samples / add meshes later |
| Connect / ros2 CLI | Yes — container pytest | GUI not re-run after 19:55 fixes | **Fix verified in code**; operator reload |
| Preview on ROS robot | MRML only reliable | Joint path not streamed to `/joint_states` during preview | **Fix next** after Connect GUI pass |

Default 5 mm self-clearance rejecting the neutral configuration is **expected**
with conservative AABBs — not a regression. Lower clearance or reviewed collision
meshes before treating as a bug.

---

## Connect failure chain — fix vs replace (2026-08-19)

| Time (IST) | Symptom | Root cause | Fix applied | Replace? |
|------------|---------|------------|-------------|----------|
| 19:20 | “ROS2 Slicer module is not available” | `get_ros2_logic()` used global `slicer` without import | Local `import slicer`; path registration | **Fixed** — do not replace bridge |
| 19:35 | “ros2 CLI is not available” | `PYTHONHOME` → Slicer stdlib breaks `rclpy` | `run_ros2_cli` unsets Python isolation | **Fixed** — standard pattern for Slicer+ROS |
| 19:50 | 8 s stack timeout; only RSP | SuperBuild `python-install/bin` first in PATH; `yaml` missing | `CONTAINER_SAFE_PATH` for ROS children | **Fixed** — do not run description nodes under Slicer Python |
| 19:55 | “RSP without slicer publisher” (healthy stack) | 1.5 s node-list cache + old in-memory module | `_invalidate_node_list_cache()`, `force=True` | **Fixed** — operator must still reload extension |

Nothing in this chain warrants replacing SlicerROS2 or the description launch
architecture. Replace only if a future requirement needs MoveIt trajectory
execution or hardware `ros2_control` (unauthorized today).

---

## Documentation checkpoint — logbook reconciliation (earlier)

### 14:41 IST — Request

- Update logbooks with all latest changes through today, problems faced (with
  date and time), and pending tasks in detail relative to
  `docs/DEVELOPMENT_PLAN.md`.

### 14:45 IST — Actions taken

- Reconciled dated logbooks for 2026-08-18 through 2026-08-21.
- Added session summaries and problem tables to 2026-08-18 and 2026-08-19.
- Created 2026-08-20 (no development recorded) and the first checkpoint entry.
- Source of truth for engineering detail: `docs/changelog.md`.

---

## Changes recorded since 2026-08-17 evening (by date)

### 2026-08-18

| Time (IST) | Area | What changed | Verification |
|------------|------|--------------|--------------|
| 15:46–16:10 | Record3D scan | `scripts/view_record3d_scan.py`; inspected `data/3dscan_iphone.zip` (195 coloured OBJ point clouds, camera metres) | Self-test; 195-frame report; GUI smoke on `DISPLAY=:0` |
| 17:45 | SlicerROS2 bridge | `DENTOROS2Bridge.py`, Step 6 connect/disconnect UI, `launch-dentobot-description-for-slicer.bash` | pytest 7 passed; container `ros2 node list` |
| 18:10 | Joint streaming | `slicer_joint_state_publisher`, `joint_state_mode:=slicer`, Connect refuses neutral/manual publishers | pytest 15 passed; isolated launch + Float64MultiArray probe |
| 19:30 | Planning 6.0–6.3 | `DENTOStep6Planning.py`; import/lock/limits/plan/preview; env obstacles include teeth segmentation | pytest 22 passed |

**Problems / limits (2026-08-18):**

- Record3D zip is extraoral lip/skin surface in a walking camera frame — not
  intraoral cavity volume, not Slicer RAS, not millimetre incisor-gap authority.
- Duplicate `dentobot_robot_state_publisher` names if an old launch was still
  running when a second description launch started.
- Interactive Slicer Connect not exercised in the engineering session.

### 2026-08-19

| Time (IST) | Area | What changed | Verification |
|------------|------|--------------|--------------|
| 18:45 | Step 6 UX | Gated sequence 6.0 scene → 6.1 ROS/place → 6.2 limits → 6.3 plan; Elements Stage 6 recommended view; case XOR phantom | pytest 22 passed |
| 19:20 | Connect | Fix `NameError` for `slicer` in bridge; launcher module-path merge | pytest 6 passed; container path probe |
| 19:35 | Connect | `run_ros2_cli` unset `PYTHONHOME`/`PYTHONPATH` for `ros2` child | pytest 8+11 passed; live Slicer env probe |
| 19:45 | 6.0 / 6.2 | Import frames case RAS + CBCT background; merged min/value/max rows; Plan/Preview call `ensure_slicer_ros2_runtime` | pytest 28 host / 32 container |
| 19:50 | Connect | Sanitized PATH for `slicer_joint_state_publisher` (`yaml` crash under Slicer Python) | Clean launch in 1 s; 14 container tests |
| 19:55 | Connect | Force fresh `ros2 node list` after start/stop; clearer incomplete-stack message | 12 host / 16 container tests |

**Problems faced (2026-08-19)** — full table in `docs/logbook/2026-08-19.md`.

### 2026-08-20

- No repository-recorded development, tests, or documentation updates.

### 2026-08-21 (this entry)

- Logbook reconciliation only. No Git publication or Google Drive sync.

---

## Pending tasks mapped to `DEVELOPMENT_PLAN.md` fast-track work packages

Evidence labels from the plan: **Static**, **Synthetic**, **Developer-live**,
**Representative anatomy**, **Printed phantom**, **Clinician/expert**. Current
Step 6 ROS/planning work is mostly **Synthetic**; several items need
**Developer-live** or **Representative anatomy**.

### Work package 1 — Freeze narrow clinical task and Template V0

| Pending item | Detail | Blocks / depends on |
|--------------|--------|---------------------|
| One-sentence PoC procedure boundary | Exact autonomous/bounded drilling task, out-of-scope list, clinician approval point, re-plan/re-register/abandon triggers, permitted motion, failure-safe state | Clinical team |
| Template V0 parameters | Support surfaces, insertion direction, undercut engagement, clearance, wall thickness, margin, sleeve/dock role, rocking/gap, repeat seating, print/material assumptions | Clinician/expert review |
| Mark UI dimensions as research defaults | Until evidence supports numeric clinical thresholds | Documentation discipline |

**Step 6 relevance:** phantom jaw opening and robot placement are **disposable
design checks**, not clinical jaw simulation or registration.

### Work package 2 — Representative end-to-end software acceptance

| Pending item | Detail | Current evidence |
|--------------|--------|------------------|
| One governed case Steps 0–5 → STL | Trajectory review, 4B docks, supports, undercut shell, 5B fusion, 5C gate, MRB save/reopen, stale/backtracking | Partial synthetic; not one recorded representative run |
| Step 6 **6.0 Import Planning Package** on real scene | Import completed Steps 0–5 MRB; frame case; CBCT slice background | **Developer-live** pending after extension reload |
| Step 6 **6.1 Connect ROS 2** | Start stack (`joint_state_mode:=slicer`), CreateAndAddRobotNode, base alignment, Motion Control sliders → `/joint_states` | Synthetic + container CLI; **Developer-live** Connect pending |
| Step 6 **6.2 task limits** | Merged min/value/max per joint; limits feed plan | Synthetic pytest |
| Step 6 **6.3 Plan / Preview** | SciPy IK trajectory along approved path; coarse AABB self-collision; subsampled env clearance (template, docks, segmentation) | Synthetic pytest; **Developer-live** preview pending |
| Reload DENTOBOT extension | Required after every Step 6 code change in 2026-08-18/19 sessions | Operator action |
| Slicer widget tests | `test_DENTOWorkflowRobotPlacementWidget` updated for phantom-before-robot; not re-run in session | Extension reload |
| Live-test Steps 0–5 navigator, 4A–5C | See `TASKS.md` immediate Step 4/5 workload (8 ordered items) | Physical Intel graphical session |

### Work package 3 — Print and seat Template V0

| Pending item | Detail |
|--------------|--------|
| Print one representative guide | Record printer, material, orientation, post-processing |
| Physical seating protocol | Terminal seating, removal, rocking/gap, repeat pose, flex under load, dock rigidity, CAD vs print dimensions |
| Not started | No printed phantom evidence for template fit |

**Step 6 relevance:** MRML/ROS robot placement does not substitute for printed
template seating.

### Work package 4 — Formalize registration before robot motion

| Pending item | Detail |
|--------------|--------|
| Full transform graph | CBCT RAS → tooth/template → physical dock → robot base → EE → bur, with sources and invalidation events |
| Rigid target-point phantom | Independent known targets; TRE at drilling target as primary metric |
| Repeat protocol | Reseating, redocking, re-registration — mean/std/worst case |
| No inference from Step 6 base snap | Mount plane snap is scene-local simulation, not physical registration |

**Step 6 relevance:** ROS robot parented under Step 6 base transform follows
**MRML placement**, not measured registration.

### Work package 5 — Robot, tool, sensing, metrology lanes

| Pending item | Detail | Evidence |
|--------------|--------|----------|
| `dentobot_description` physical RViz session | One joint at a time; direction, scale, draft zero, J4 negative-base-X | Synthetic kinematics probe; **Developer-live** pending |
| Manual AABB / burr origin | Draft workspace only; link-3/link-5/spindle warnings may be box overlap | Synthetic |
| Open-mouth + robot physical trial | Deliberate landmarks, mount plane, all joint controls, saved poses | Synthetic graphical 2026-08-17; physical pending |
| Engineering authority for URDF | Zeros, axes, limits, masses, inertias, TCP frames | Unverified CAD export |
| Simplified collision geometry | Before MoveIt or strong collision claims | Not done |
| `RobotAdapter` / `ros2_control` / command topics | Transport and safety ownership | **Unauthorized** — powered motion forbidden |
| MoveIt `move_group` package | IK gizmo, Plan, Execute in SlicerROS2 | Not implemented |
| Pressure monitor | Host `pressure-env` GUI on `/dev/ttyACM0` | Sensing bench only (2026-08-17) |

**Step 6 ROS path status:** visualization + simulated `/joint_states` via
`slicer_joint_state_publisher`; not hardware command.

### Work package 6 — Total-system error budget

| Pending item | Detail |
|--------------|--------|
| Per-contributor mm/deg tracking | Image geometry, segmentation, planning, manufacturing, seating, docking, registration TRE, kinematics, TCP calibration |
| Replace assumptions with measurements | Dominant uncertainty unknown |
| Step 6 metrics | Depth/lateral/angular/target error, sequence recording, fault simulation — **planned** in DEVELOPMENT_PLAN Step 6, **not implemented** |

---

## What is next (priority order)

1. **Operator — Developer-live Connect (highest Step 6 gate):** Reload DENTOBOT
   extension → stop stale description launches → **Start Stack & Connect Motion
   Control** → `CreateAndAddRobotNode` → Motion Control sliders move live TF →
   Disconnect restores MRML meshes. Without this, later slices lack evidence.
2. **Developer-live 6.0–6.3 on representative MRB:** Complete Steps 0–5 scene →
   Import Planning Package (case framed, CBCT background) → lock mount → set
   task limits → Plan/Preview; record collision messages and joint vectors.
3. **Fix preview → `/joint_states` when ROS connected:** After Connect passes,
   stream planned waypoints through existing `dentobot/slicer_joint_positions`
   topic so preview matches TF robot (small targeted bridge change).
4. **PoC closure work packages 1–2** (`DEVELOPMENT_PLAN.md`): freeze clinical
   boundary + one representative Steps 0–5 → STL run — **higher priority than
   Step 6 feature polish**.
5. **Physical RViz session:** `launch-dentobot-manual-rviz.bash`; confirm draft
   zero, J4 direction, one measured dimension.
6. **Planner upgrades (after GUI pass):** bur-axis alignment cost; reviewed
   collision meshes; optional swept-path — see backlog.

## Lower-priority backlog

- MoveIt / `ros2_control` / `RobotAdapter` / hardware motion (Steps 7–9,
  **unauthorized**)
- Reviewed collision meshes vs visual-mesh AABB reuse
- Engineering authority for URDF zeros, TCP frames, masses/inertias
- Navigation metrics, stale-transform simulation, sequence recording
- Bridge B regression, MRB reopen, aggregate Slicer test-runner exit code
- Record3D viewer interactive acceptance (`view_record3d_scan.py` untracked)
- Google Drive sync (needs auth; batched approval rule)
- Host `/usr/bin/python3` missing scipy — use `dentobot` conda or container for
  full planning pytest

---

## Operator checklist (immediate)

1. Ensure container `dentobot-slicerros2` is running.
2. Stop any stale `description.launch.py` (neutral or half-failed slicer mode).
3. `./scripts/launch-dentoworkflow.bash`
4. **Module → Reload DENTOBOT extension** (or restart Slicer).
5. Step 6: optional phantom → **Start Stack & Connect Motion Control**.
6. If dialog persists after fixes: dismiss, reload extension, Connect again.
7. Complete Steps 0–5 on a representative case → **Import Planning Package** →
   lock mount → limits → **Plan / Preview**.

---

## Unresolved issues

| Issue | Status |
|-------|--------|
| Interactive CreateAndAddRobotNode after 2026-08-19 fixes | Not verified in GUI |
| Host `/usr/bin/python3` pytest missing `scipy` for some planning tests | Use `dentobot` conda env or container |
| Duplicate ROS node names if multiple description launches | Operator must stop old launch |
| `view_record3d_scan.py` untracked path note (2026-08-19) | Confirm if committed or stays local tool |
| Git push / Google Drive sync | Awaiting batched approval per project rule |

No passwords, credentials, patient identifiers, or non-anonymized medical data
were recorded in this checkpoint.
