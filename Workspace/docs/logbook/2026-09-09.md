# DENTOBOT logbook — 2026-09-09

## Operator correction — Sol restored and visual debugging confirmed

- **Operator statement:** Restore Sol wherever recommended; about 90% use up
  to High and 10% Extra High when needed. Operator reports Sol is significantly
  better for their development due to Astra's token burn. Also asks whether
  the new verification rules require screenshots/visual evidence and user expertise.
- **Interpretation:** Prefer `gpt-5.6-sol` with default `high`; lower effort
  for routine work, bounded justified `xhigh` as a minority. 90/10 is an
  approximate allocation guideline, not measured quota enforcement. No cost
  benchmark was requested or performed. Preserve Luna Max/Terra High auxiliary
  options and existing selective delegation, retry and approval rules.
- **Implementation:** Replaced the active avoid-Sol/Astra-coordinator section
  of Workspace/AGENTS.md and updated the protocol's coordinator references,
  compact context, DECISIONS and TASKS. Historical dated policy remains as history.
  The screenshot-first collision rule already requires context/close-up imagery
  and a narrow operator question; made UI screenshots explicit under the same
  runtime ownership/approval boundary.
- **Saved settings:** Authorized escalated edit changed only the two top-level
  model fields in `/home/light-tarun/.codex/config.toml` to `gpt-5.6-sol` and
  `high`. Actual file readback confirms both; service tier and remaining settings
  preserved. This config edit does not demonstrate a switch of the current task.
- **Commands/results:** `rg -n` inspected active model/visual rules; `cat` read
  config. A bounded Python text replacement and `apply_patch` updated the named
  records; the config edit completed with exit 0. `sed` read back actual saved
  defaults, AGENTS model section and protocol visual/ownership rules. No tests,
  builds, screenshots, simulation, benchmark, hardware or external sync ran.
- **Changed files:** Workspace/AGENTS.md; docs/AGENTIC_VERIFICATION_PROTOCOL.md,
  AGENT_CONTEXT.md, DECISIONS.md, TASKS.md and this logbook; plus the authorized
  personal Codex config. No production code or case data changed.
- **Result/next:** Instruction and saved-default update complete. Apply Sol
  routing to subsequent model selection and use visual evidence/operator expertise
  on the next relevant debugging blocker. Current numerical guard authority and
  three-attempt ceiling remain in force.

## Adopt efficient SlicerROS verification policy and current Astra guidance

### Operator request and source boundary

Operator supplied `/home/light-tarun/Downloads/CODEX_TESTING_VERIFICATION_POLICY.md`
and requested updated agent development rules for SlicerROS testing/verification,
especially current OpenAI guidance after Astra's release. This authorizes policy
integration, not execution of the attachment's example tests, parameter changes,
or priority labels. The attachment remains unchanged; SHA-256 is
`f6acb1c66ad22e4b52bec53d22fa3032cad2920ab41c6621297ecb14eac10177`.

### Interpretation and implementation

Integrated the attachment's operational requirements into the existing canonical
AGENTIC_VERIFICATION_PROTOCOL instead of creating duplicate instruction files.
Added cheapest-sufficient evidence selection; three-failure ceiling across
resumes/workers; early collision/UI visual escalation; compact blocker evidence;
bounded package builds and affected regressions; explicit simulation assumptions;
and separate implementation/unit/runtime/integration/full-cycle/operator claims.
The ladder selects applicable checks and retains dependencies; it is not five
mandatory executions. Screenshot collection respects scene/runtime ownership.
Unknown dimensions and example base/tool changes do not permit tuning.

Added compact task-contract and conflict-handling guidance to AGENTS.md, routed
the compact context to the canonical policy, and removed its broad-suite default.
Clarified matrix resolution from the real checkout. Existing Astra-low, Luna-Max,
Terra-High policy, selective delegation, runtime serialization and execution
approval remain. Reuse approval within its scope; stop only the blocked path and
continue independent authorized work. Source attachment policy priorities do
not reorder TASKS. Existing scoped contact decisions and full/repeat-cycle
acceptance remain unchanged.

### Sources, commands and results

- Read the supplied policy and OpenAI Docs SKILL.md with `cat`, then scoped
  `sed`/`tail` recovered the attachment's ending after output truncation. Read
  the skill's model-migration reference for the named Astra prompting route;
  no resolver or model migration was needed.
- Read active overlay and checkout-root AGENTS.md, canonical verification
  protocol and the actual `ros2_ws/src/DentoBot/Testing/verification_matrix.json`.
  Initial `cat ../Testing/verification_matrix.json` failed: that relative path
  is valid from physical Workspace, not the overlay cwd. `readlink -f` and
  scoped `rg --files` located the real files; the corrected read succeeded.
- Used official-domain web search and fetched OpenAI Astra model guidance,
  Codex best practices and AGENTS.md discovery. DECISIONS.md retains links and
  separates general OpenAI advice from project-specific retry/model policies.
  Some pages returned large navigation sections; `find` retrieved the relevant
  prompting/testing and instruction-discovery sections. No general blog advice
  was used as authority.
- Read scoped architecture/traceability/decisions context with `sed` and `rg`;
  current project/setup/development-plan/task routing was also read in this
  task's earlier backlog work. No environment or product architecture change.
- `sha256sum` recorded attachment identity; `git -C ros2_ws/src/DentoBot status
  --short` showed extensive existing changes. Preserved those changes. Scoped
  `git diff -- <edited-doc-paths>` exited 0 but included historical work and
  was truncated; final `sed` readback inspected the newly edited policy,
  instruction and record sections directly. No `git diff --check` or matrix
  check was run; diff display/readback is documentation inspection only.
- `apply_patch` updated seven files: Workspace/AGENTS.md,
  docs/AGENTIC_VERIFICATION_PROTOCOL.md, docs/AGENT_CONTEXT.md,
  docs/DECISIONS.md, docs/REPRODUCIBILITY_AND_TRACEABILITY.md, docs/TASKS.md,
  and this logbook (docs paths relative to Workspace).

### Acceptance and next action

Document readback confirms the rules, checkout-relative matrix path, retained
approval/model limits, and source/interpretation distinction. `AGENT-VERIFY-ECONOMY`
is complete as instruction integration only. No production source, test harness,
matrix, container, saved case, model config or engineer-owned record was changed;
no test/build/runtime or external publication was performed. Efficiency gains
and future agent adherence are unmeasured. Apply the new question/check/stop
contract on the next authorized Step 6 restore/workspace investigation.

## DENTO-NOTE capture — Step 6 restore, workspace purpose and backlog ordering

### Operator statements

- Step 6.3 Generate Workspace needs updating for the latest developments;
  operator reports that 5 mm clearance leaves no TCP workspace intraorally and
  requests discussion/revamp of this step's purpose before the Studio revamp.
- Loading a Step 6 DentoCase connects ROS without loading the robot; operator
  must manually load the robot and connect ROS again to proceed.
- Operator asks to continue, compare backlog DENTO-NOTEs, and arrange these into
  tasks with recommended priority. Lowercase “dentonote” is treated as durable
  note capture, consistent with earlier entries.

### Triage and agent interpretation

Created `S6-RESTORE-ROBOT-ROS` and `S6-WORKSPACE-PURPOSE` as backlog tasks ready
for scoped investigation/discussion. Both remain Unprioritized, with recommended
P0 clearly separate from assigned priority. Workspace purpose and a demonstrated
current defect are proposed P0; broader Studio presentation stays P1. No numeric
operator priority was supplied for either new note.

Recommended restore reliability, then existing P0 shared setup retention,
workspace contract, and the dependent multi-target registry. Workspace discussion
and independent geometry review can proceed earlier. The existing P0 Track-A
full/repeat-cycle gate remains binding; FDI11's latest adjacent-FDI21 collision is
the operative recorded blocker, not the historical guide-only failure. Recommend
P0 for that existing unprioritized blocker without changing its assigned value.
Remaining Studio work stays P1; UI polish P3 and non-blocking shutdown hygiene P4.
Completed endpoint/collar fixes are not reopened. Changed-target geometry checks
are an acceptance dependency of retention/selection, not a duplicate feature.

The current checkpoint auto-reconnect contract differs from planned schema-2
offline Studio loading. The reported 5 mm setting has not been traced: no claim
is made about its implementation or a safe replacement. Workspace samples,
Home connectivity and full oriented guarded-path feasibility require distinct
evidence. No architecture or clearance-policy decision is made by this triage.

### Actions, commands and evidence boundary

- Read `docs/AGENT_CONTEXT.md`, current `docs/TASKS.md` and today's logbook with
  `cat`; used `rg -n` for DENTO/priorities/workspace/restore/roadmap references.
  Initial combined output was truncated; subsequent scoped reads recovered the
  relevant task and roadmap sections.
- Ran `wc -l` on PROJECT_CONTEXT/SETUP/DECISIONS/DEVELOPMENT_PLAN and used `sed`
  and `tail` for scoped context: DEVELOPMENT_PLAN lines 1–65 and 342–484,
  TASKS lines 110–126, DECISIONS final 90 lines, PROJECT_CONTEXT first 55 lines,
  SETUP first 45 lines. Read commands exited 0; no source-code investigation
  or runtime diagnosis was undertaken.
- Used `apply_patch` to add the two stable task records and a cross-referenced
  recommendation table to TASKS.md, and this entry to the logbook. Changed only
  these two development-controlled records; preserved earlier entries.
- Documentation readback confirms the two IDs, explicit Unprioritized versus
  recommended P0 fields, observations, evidence limits and next verification
  actions. No tests, builds, Slicer/ROS operations, case changes, external sync,
  or engineer-owned-record access. No behavioral fix is claimed.

### Next bounded action

Audit the current Step 6 post-load reconstruction/reconnect ordering and shared
setup invalidation callers; separately prepare the 6.3 purpose/5 mm filter
discussion from source evidence. Implement only after the relevant contract is
settled, and propose the scoped checks under the execution-approval protocol.

## DENTO-NOTE Priority 0 — reusable Step 6 setup and multi-target case

### Operator statement

Operator wants to test more target-tooth trajectories on the same CBCT without
repeating Steps 0–5 and Step 6 mouth opening, robot base placement and Task Home.
Requests save/load of repeatable Step 6 setup, multiple target teeth with their
trajectories and respective shells in one DentoCase, selection/import in Step 6,
and package reset retaining mouth opening including landmarks, base placement,
and Task Home configuration. Operator describes the repetition as tiring and
absurd. Interpreted “Dentonote p0” as a durable DENTO-NOTE with Priority 0.

### Triage and agent interpretation

Recorded `S6-REUSABLE-CASE-SETUP` as **active investigation**, Priority 0, in
TASKS.md. Retention and round-trip persistence precede the dependent multi-target
registry. Both requested increments precede unrelated P1 work; the whole Studio
roadmap is not promoted. Shared configuration should survive target-only changes
when its actual dependencies match; live planning validity must be re-established.
This is a requirement/triage record, not an accepted persistence design or fix.

### Actions and evidence

- Read `docs/AGENT_CONTEXT.md`, TASKS.md, selected DEVELOPMENT_PLAN.md routing,
  `AGENTIC_VERIFICATION_PROTOCOL.md`, internal package README, Ponytail and
  Graphify skills. Today's logbook did not exist; created it here.
- Ran `graphify query "Step 6 package reset jaw opening landmarks robot base Task Home persistence" --budget 1600`
  from `ros2_ws/src/DentoBot`: exit 0; returned bounded source routing with
  stream-fd warnings and truncated graph output. This is navigation only.
- Used `rg` for reset, clear and Task Home ownership: located
  `widget_planning.py::_resetStep6CaseJawOpeningBeforeInputChange`,
  `logic_step6_scene.py::resetStep6CaseJawOpening`, and Task Home serialization
  in `logic_robot.py`. One guessed `logic_robot_package.py` path was absent;
  subsequent directory-scoped search located actual owners. Full caller and
  persistence audit remains next.
- Existing TASKS changed-target record documents deliberate transient geometry
  cleanup and cross-target lineage checks. Retention must preserve that protection.
- Changed files: `Workspace/docs/TASKS.md` and this logbook only. No production
  code, case data, engineer-owned records or runtime state changed.

### Verification boundary and next action

Patch application confirms durable issue capture. No test, build, simulation or
hardware command was run; no behavioral fix is claimed. Next: scoped reset and
save/load caller audit, minimal retention implementation specification, then
the focused reset/round-trip/staleness/legacy compatibility verification plan
under the existing execution-approval protocol. Multi-target storage follows
the shared-setup boundary rather than duplicating it per trajectory.

## Operator clarification — 96-trajectory Studio foundation

- **Operator statement:** The requested feature must be implemented as framework
  foundation for the major Robotics Simulation Studio revamp, running planning
  simulation for 96 trajectories in a single click.
- **Requirement captured:** Extended the same P0 `S6-REUSABLE-CASE-SETUP` item;
  do not deliver an isolated reset-saving workaround. Shared environment and
  persistent target/trajectory/shell identity must serve both current Step 6
  selection and the future batch runner.
- **Agent interpretation:** Existing DEVELOPMENT_PLAN.md defines three stable
  slots per tooth, environment/attempt identity and serialized non-moving
  studies. 32 × 3 gives the requested 96-record capacity, without requiring
  all teeth or trajectories to exist in an actual case. Batch eligibility,
  per-record results, cancellation and fingerprinted provenance must be
  supported; saving a shell does not silently change study collision policy.
- **Sequencing:** P0 foundation first, batch runner consumes it; unrelated P1
  work remains behind P0. This clarification does not claim full Track A
  acceptance or authorize simulation execution.
- **Actions/evidence:** Read DEVELOPMENT_PLAN.md lines 342–475, current task
  entry and today's logbook using `sed`, `head`, and `tail`; applied the
  TASKS.md and logbook requirement update. No code or runtime change, no tests
  executed. Implementation and 96-record round-trip verification remain open.

## FDI11 full-cycle scope scan and endpoint clarification

### Operator statement

Operator clarified that the missing FDI11 pulp label is a segmentation fault
in the input result. Because the current spindle length and guide interaction
constrain this simulation, the current run intentionally bypasses the rule that
the drilling endpoint must be the pulp boundary. Special-case handling for an
unlabelled pulp region will be added later.

### Agent interpretation and decision

The existing Step 6 policy is correct for this temporary scope: it caps the
saved 7.977207 mm manual trajectory at 6.000 mm and explicitly reports
`effectiveTargetIsPulpClaim=False`. No target-selection source change is
required. This clarification does not authorize relabelling or inferring a pulp
mask, and it does not broaden the guide-only collision exception to adjacent
anatomy.

### Commands, evidence and result

- Queried Graphify for the endpoint, guide-contact and exact-cycle owners;
  query exited 0 with stream-fd warnings and a truncated bounded result.
- Inspected the saved `.dentocase`, nested MRB, trajectory markup and compressed
  segmentation NRRD read-only with Python/NumPy. The NRRD has FDI11 tooth label
  11 and no FDI11 pulp segment. Exact ray/voxel-box intervals place the first
  bounded internal FDI11-label gap at 7.798621 mm, beyond the temporary 6 mm
  target. This is geometric evidence only, not an inferred pulp label.
- Read `/tmp/dentobot-exact-case-diagnostic.json` from the pinned container.
  It records the 6.000 + 0.500 mm insertion envelope, configured guide warnings,
  and a hard Stage-3 final-waypoint collision between the spindle housing and
  adjacent FDI21. The full task remains `Blocked`; no retraction, Home or repeat
  cycle was executed or claimed.
- Reviewed the production target cap, full-chain candidate preflight, phase
  guard and exact-cycle harness. The harness validates approach, Entry, the
  temporary capped Target, endpoint FK, axial retraction, Home and a repeat
  cycle. The current failure is a real adjacent-anatomy gate, not a false target
  expectation or a test-only mismatch.
- Changed controlled records only: `DECISIONS.md`,
  `REPRODUCIBILITY_AND_TRACEABILITY.md`, `TASKS.md`, and this logbook. No source,
  tests, saved case, runtime state or engineer-owned record changed. No new
  verification run was needed for documentation-only recording.

### Remaining boundary

The implementation is aligned with the clarified temporary endpoint. Full-cycle
acceptance remains blocked by the adjacent FDI21 contact at the 6 mm endpoint.
Resolving it requires a collision-free reviewed trajectory/base/tool geometry,
or an explicit later safety classification supported by the physical model;
the current guide-only warning must not be generalized to anatomy.

## Windows machine 2 — Docker backend comparison

### Operator statement

The second Windows installation encountered a faulty Docker Desktop setup and
was completed using Docker Engine inside the WSL distribution. The operator
requested comparison with the latest commit and an ideal future deployment
path. No exact command transcript, versions, or acceptance markers were supplied
for the second machine in this task.

### Repository and source evidence

- `origin/main` is `f4c50b24457eb5ab490eb54f93bb15604bb033d4`
  (`merge: integrate Windows WSLg installation profile`, 2026-09-09). The
  current primary checkout remains at local `529626d`, is five commits ahead
  and eleven behind `origin/main`, and has extensive unrelated dirty changes.
  No checkout, merge, reset, fetch, commit, or push was performed.
- The published lab manifest still selects `lab/2026-09-03` at `760a8ce` with
  the pinned `slicer_ros2_module` and GHCR image. Therefore latest `main`, the
  current dirty development tree, and the supported lab release are three
  distinct identities. A `git ls-tree`/`git show` comparison confirmed that
  this tag has the systemd Docker-daemon helper but does not contain the newer
  `compose.wslg.yaml`, `compose.cuda.yaml`, model-cache installer, or explicit
  WSLg/CUDA launcher selection. A new install that detaches to this tag cannot
  be assumed to retain the latest Windows integration behavior.
- Latest `origin/main` adds the primary Windows WSLg/CUDA profile, WSLg and
  CUDA Compose overlays, model-cache installer, and backend-aware launcher.
  The launcher consumes the Docker API rather than a Docker Desktop API and
  already attempts `systemctl` startup/enablement of `docker.socket` and
  `docker.service`; this is compatible with a systemd-managed engine in WSL.
  The written Windows procedure still names Docker Desktop as the prerequisite.
- Graphify query routed the comparison to `SETUP.md`, the launcher,
  `lab-release-lib.bash`, and the Windows installation log. Targeted `git show`,
  `git diff`, `rg`, and `sed` inspections then established the identities and
  behavior above. Repeated `Failed to create stream fd: Operation not permitted`
  diagnostics accompanied local read-only Git/Graphify commands but did not
  prevent their expected outputs.

### External guidance and analysis

- Current Docker documentation recommends Docker Desktop with its WSL2 backend
  for most Windows users and warns against running Docker Desktop integration
  and a separately installed Docker Engine/CLI together because they can
  conflict. It also recommends keeping source and bind-mounted data in the WSL
  Linux filesystem. Docker's Ubuntu instructions support Docker Engine from the
  official apt repository with the Compose plugin and systemd management.
  Microsoft documents systemd support in WSL.
- For DENTOBOT, both backends can satisfy the same runtime contract: Docker API,
  Compose v2, the pinned Linux image, Linux-filesystem bind mounts, WSLg mounts,
  and optional NVIDIA container access. Docker Desktop is the vendor-supported
  Windows convenience path. Direct WSL Engine removes the Desktop GUI,
  `docker-desktop` distribution, and integration startup from the critical path,
  but makes Docker package, daemon, group/socket, NVIDIA toolkit, and upgrade
  maintenance an explicit DENTOBOT workstation responsibility.

### Recommended future path and evidence boundary

Keep one backend-neutral launcher and Compose stack. Document two mutually
exclusive one-time host bootstraps: Docker Desktop/WSL integration and direct
Docker Engine in WSL. Prefer direct WSL Engine provisionally for controlled lab
machines after the second machine passes the same full platform gate; retain
Docker Desktop as the officially supported convenience alternative. Never run
both daemon paths in the same distro. Capability checks should decide readiness,
not the product name.

Before changing the canonical profile, record `wsl --version`, distro/version,
systemd state, Docker client/server and Compose versions, active context/socket,
GPU/driver/toolkit identity, image digest, source tag/SHA, `docker info`, a CUDA
container probe when applicable, launcher `--check-only`, WSLg GUI startup,
one segmentation, and the simulation-only ROS/MoveIt readiness gate. Then cut a
new pinned `lab/*` tag and matching labeled/digested GHCR image. Do not make lab
machines follow `main` or reconstruct from the current dirty checkout.

This task changed `TASKS.md` and this logbook only. It ran no installation,
Docker daemon, container, GUI, test, simulation, or robot command. The second
machine's direct-Engine success remains an operator observation until its exact
evidence is captured; no hardware or clinical acceptance is implied.

## FDI21 blocker screenshot capture

### Operator request and boundary

- **Operator request:** Provide screenshots of the current FDI21 collision so
  the operator can inspect and intervene.
- **Interpretation:** Reproduce or restore the retained simulation-only
  first-invalid state, distinguish FDI21/FDI11/spindle visually, and preserve
  the hard collision result. No hardware execution or new collision exemption
  was requested or applied.

### Implementation and commands

- Temporarily added an opt-in diagnostic capture path to
  `Testing/run_dentobot_step65_exact_case_smoke.py`. It reused the existing
  goal-robot display and collision-audit proxies, accepts a retained diagnostic
  record, isolates the relevant models, saves oblique/opposite/axial 3D views,
  wrote a JSON legend, emitted a distinct capture marker, and exited before
  preview. The one-off hook was removed after the artifacts were verified, so
  the acceptance harness has no lasting screenshot-specific behavior.
- Static checks: `PYTHONPYCACHEPREFIX=/tmp/dentobot-pycache python3 -m
  py_compile Testing/run_dentobot_step65_exact_case_smoke.py` and `git diff
  --check -- Testing/run_dentobot_step65_exact_case_smoke.py` passed. An
  initial compile without `PYTHONPYCACHEPREFIX` could not write the existing
  `Testing/__pycache__`; no source state changed.
- Runtime used container `dentobot-slicerros2`, image
  `dentobot/slicerros2:jazzy-moveit-sim-20260903`, ROS domain 73, source HEAD
  `529626df3db483531bf477e884dc8b5d4a1ff8a8`, exact case
  `data/Slicer_Saved/SampleStudy1/FDI11/dentobot-case-step6x4x2.dentocase`
  SHA-256 `ffc0940899d2396a8ae0c942f0b9d8e9d30a059dc8995807e4998e2f45677f0b`,
  and in-memory `DENTOBOT_BASE_LOCAL_Z_OFFSET_MM=-4`.
- The first live attempt planned successfully and therefore did not enter the
  original failure-only capture branch; its later Goal-1 preview failed
  monitored-state convergence. A retry exposed duplicate ROS children left by
  the first two launch wrappers; their exact PIDs were terminated, and later
  runs used a separate process group with descendant-aware teardown. One retry
  found no nondeterministic FDI21 rejection and correctly refused capture.
- The final render restored the already-recorded authoritative diagnostic from
  `artifacts/verification/guide-contact-20260908/exact-diagnostic.json`, copied
  byte-for-byte into the mounted run directory with SHA-256
  `4424d010d44a0ecbab42e8075a470a5bd5c1410cce879a998f890e8c73ac66a1`.
  No candidate was reinterpreted or fabricated.

### Result and evidence

- Capture marker: `DENTOBOT_FDI21_BLOCKER_CAPTURE_PASS`.
- Collision pair: FDI21 object
  `dentobot_tooth_2.25.156727832881888777359082987559036393993_71ddde60`
  versus `pneumatic_spindle-Copy` at retained candidate 3. First-invalid TCP
  world RAS is `[-88.5273125290, -37.1403976492, 60.1710240282]` mm.
- Evidence directory:
  `data/dentobot-runs/fdi21-blocker-20260909/`. Final image hashes are:
  oblique `9ddef5d8a71246f448eeaffe7b614720c4db43df414d6d1e91107e042e1957c4`,
  opposite `5239e1ea84fb4f8ee9140714616b0ebd969990c9862437fca998b2413ded47c3`,
  axial `43f36c905a3b5de174e2bf0f33646ad866220df8dfb027b92867adac232577df`.
  The views show orange spindle housing entering the red FDI21 proxy beside
  green FDI11; the visible overlap is on the lower crown-side portion of FDI21
  in the rendered orientation.
- Slicer emitted the capture PASS marker and wrote all artifacts, then returned
  exit code 1 during native teardown. This is recorded separately from capture
  success. Process inspection after the run found no remaining Slicer,
  MoveIt, guard, state-publisher, or simulation-status process.
- The full planner objective remains incomplete: these images diagnose the
  current blocker; they do not prove approach preview, drilling, retraction,
  Home, or repeat-cycle acceptance.
- After removing the temporary capture hook, temporary-cache Python compilation
  and `git diff --check` passed again. `graphify update .` first failed under
  sandbox process restrictions, then succeeded with the required escalation
  and refreshed the repository graph (`5739` nodes, `9202` edges); no semantic
  relabel was run.

## Stable development checkpoint requested before further planner work

- **Operator request:** Commit and push the latest stable version before
  continuing development. The active full-cycle planner goal remains open.
- Fetched `origin`; remote `main` advanced to `e802513` and the local branch was
  five commits ahead and fifteen behind before checkpoint integration. No
  force-push or history rewrite is planned.
- Pre-push static checks passed for 40 changed/new Python files, UI XML,
  `Testing/verification_matrix.json`, four changed Bash scripts, and `git diff
  --check`. The first parallel pytest wrapper ran before its evidence directory
  existed and executed no tests; it was rerun sequentially.
- The focused suite initially returned 105 passed and one failure because
  `Testing/contracts/dentoworkflow_api.json` retained the old
  `step6ApproachPoints(self, parameterNode)` signature. Updated that baseline to
  include the implemented optional `snapshot=None` argument. The complete
  rerun passed **106/106** in 1.49 seconds. Evidence is under
  `/tmp/dentobot-verification/prepush-20260909/`.
- This checkpoint records a stable development state, not full planner-cycle
  acceptance. The exact approach/drilling/retraction/Home goal remains open and
  the FDI21 geometry evidence remains authoritative.

## Windows Docker default and release-line documentation

### Operator decision and evidence boundary

- **Operator decision:** Make Docker Engine inside WSL2 the default Windows
  Docker API provider, retain Docker Desktop WSL integration as an alternative,
  assess whether Windows needs a separate installation guide, and adopt a
  development/stable/lab release structure. The operator will provide the
  required secret-manifest information later.
- **Observed evidence:** The first Windows installation used Docker Desktop and
  has recorded WSLg/CUDA startup evidence. The operator reported that Docker
  Desktop failed on a second machine and direct Docker Engine inside WSL
  succeeded. Exact second-machine versions, identities, and acceptance outputs
  have not yet been supplied, so that install remains operator-observed rather
  than independently accepted.

### Documentation and decisions

- Added `Workspace/docs/WINDOWS_SETUP.md` as the canonical Windows guide. It
  separates the default full WSL2/WSLg profile, the exclusive Docker Desktop
  alternative, and the native Steps 0–5 fallback. It records provider checks,
  release installation, inference/model setup, acceptance evidence, and update
  boundaries without storing credentials.
- Updated `README.md`, `Workspace/docs/SETUP.md`,
  `Workspace/HOST_LAYOUT.md`, `Workspace/docs/AGENT_CONTEXT.md`,
  `Workspace/docs/PROJECT_CONTEXT.md`, and `Workspace/docs/ARCHITECTURE.md` to
  route Windows installations to the canonical guide and use direct Docker
  Engine inside WSL2 as the default provider.
- Recorded in `Workspace/docs/DECISIONS.md` and
  `Workspace/docs/REPRODUCIBILITY_AND_TRACEABILITY.md` that `main` is active
  development, `stable/lab` is a planned moving accepted pointer, and immutable
  `lab/YYYY-MM-DD` tags identify installations and experiments. The moving
  branch is intentionally deferred to the next isolated release checkpoint;
  it was not created from this dirty, divergent checkout.
- Kept SlicerROS2 as an independently pinned repository SHA. A matching stable
  branch is unnecessary. Its runtime image may remain constant across DentoBot
  releases only when all runtime inputs are unchanged and an independent image
  digest is accepted. The current publisher couples image labels to a DentoBot
  revision, so publisher decoupling remains a bounded release task.
- Updated `Workspace/docs/TASKS.md` so `PLAT-U-04` tracks the missing machine
  manifest and accepted checks, isolated source reconciliation, planned
  `stable/lab` creation, and the next immutable lab tag/image identity.

### Commands, verification, and next action

- Read the affected documentation and repository release state with `sed`,
  `rg`, and read-only Git inspection. Applied documentation changes only.
- No installation, network mutation, branch, tag, commit, push, image build,
  container, GUI, simulation, or robot command ran in this documentation step.
- Next: receive the non-secret manifest structure and second-machine evidence,
  then prepare the next accepted release in an isolated clean checkout. Runtime
  acceptance remains subject to the normal DENTOBOT verification approval.

## SlicerROS2/Slicer 5.12 upgrade assessment

### Operator request and interpretation

- **Operator request:** Summarize all changes made to the source SlicerROS2
  container, compare the current DentoBot container/fork with the latest
  SlicerROS2 5.12 source, and recommend whether to upgrade from upstream now,
  postpone, or imitate the newer stack inside the existing container.
- **Interpretation:** This is a serious platform migration assessment. It
  authorizes read-only repository, container-identity, and public-upstream
  inspection plus controlled-document planning updates. It does not authorize
  a source merge, runtime rebuild, container replacement, test campaign,
  release publication, or hardware action.

### Local and upstream evidence

- Graphify routed the affected DentoBot build, launcher, bridge, and controlled
  documentation surfaces. Authoritative files were then inspected directly.
- The adjacent SlicerROS2 checkout is on
  `dentobot/slicer-ros2-step6-20260903` at committed HEAD `49492e0`, one commit
  beyond published fork revision `17f9993`. Four files are additionally dirty:
  `vtkMRMLROS2RobotNode.cxx`, `.h`, `Internals.h`, and
  `ROS2MotionControl.py`.
- Current upstream `main` was read from a complete temporary clone at
  `4f52f1047728894516f2127c2b1aae5b5dcc9ea8`. The DentoBot fork's common base
  is `4ef3d5b427ff2846d3a79246bfc089df9f1cfe8b`, whose message is `testing
  Slicer 5.12`. Upstream is only three commits beyond that base. The delta adds
  reusable CI, changes the Docker default from Slicer 5.10.0 to 5.12.0, and
  updates documentation. A scoped Git comparison found no changes to upstream
  runtime code, `CMakeLists.txt`, or `package.xml` across that delta.
- DentoBot's published fork delta from `4ef3d5b` to `17f9993` changes ten files
  by 612 added and 55 removed lines: explicit-start planning, state validity,
  explicit-state FK, seeded/collision-aware IK, collision-pair diagnostics,
  MRML/ROS lifetime corrections, and a lifetime regression script. `49492e0`
  adds canonical drill-TCP FK. The dirty four-file delta adds position-axis IK
  and residual diagnostics.
- `Workspace/Dockerfile.slicerros2` is a 26-line derivative of upstream's
  `jazzy-slicer-v5.10.0` image. It adds pinned MoveIt config/OMPL and xacro
  packages plus release labels. Compose and launcher files add persistent
  lifecycle/resource controls, ROS network/domain settings, host identity,
  workspace/data/cache/home/backend mounts, X11/DRM, WSLg, CUDA, and DentoBot
  capability/identity checks.
- Read-only inspection of the running `jazzy-moveit-sim-20260903` container
  confirmed Slicer 5.10.0 and the pinned MoveIt/xacro package versions. The
  bind-mounted SlicerROS2 source resolved to `49492e0` plus the four dirty
  files. A direct image-inspect call intermittently lacked Docker-socket
  permission; this did not alter the source/version conclusions.
- The upstream 5.12 image manifest resolves as observed to
  `sha256:5724ea6fdfffb25cb502ecd09cc540ca5eeb4c46196677ec22bef30b99c8aed2`.
  It must be resolved again at an actual release freeze.

### Official guidance and conclusion

- SlicerROS2 1.2 officially lists Ubuntu 24.04, ROS 2 Jazzy, and Slicer 5.10.0
  or 5.12.0 as tested. Its upstream Dockerfile now defaults to Slicer 5.12.0.
- Slicer 5.12.3 is the latest recorded patch release. Slicer 5.12 adds a GPU
  path for linearly transformed segmentation display, DICOM and DICOM SEG
  improvements, rendering/segmentation/markup/transform updates, crash and
  lifecycle fixes, and newer VTK/ITK/CTK/DCMTK dependencies. The documented
  DICOM import performance improvement is Qt-6-specific; the upstream
  SlicerROS2 5.12 image still uses Qt 5.15, so that gain is excluded from the
  first candidate claim.
- **Recommendation:** Start a parallel, isolated Slicer 5.12.0 candidate now;
  retain the accepted 5.10 image as rollback until acceptance. Merge upstream
  into the fork and keep the DentoBot runtime as a thin derivative of the
  upstream image. Do not modify the current container in place, combine 5.12
  libraries with the 5.10 superbuild, or postpone until more native and UI work
  accumulates against the old ABI.
- `Workspace/docs/SLICERROS2_5_12_UPGRADE.md` records the full comparison,
  relevant changes, phased source/build/compatibility/performance/promotion
  plan, and sources. `DECISIONS.md`, `SETUP.md`, `AGENT_CONTEXT.md`,
  `DEVELOPMENT_PLAN.md`, and `TASKS.md` route and track the migration as
  `PLAT-U-06` at `Unprioritized`; no priority was invented.

### Commands and next action

- Used read-only `git status`, `log`, `show`, `diff`, `merge-base`,
  `rev-list`, `ls-remote`, temporary public Git clone, Graphify query, public
  documentation browsing, `docker ps`, `docker exec`, and upstream image
  manifest inspection. Network reads required managed escalation after the
  sandbox could not resolve GitHub.
- No build, test, image pull, container mutation/restart, source branch, merge,
  commit, tag, push, runtime workflow, simulation, robot, or patient-facing
  command ran.
- Next bounded action: prepare an isolated clean upgrade branch/worktree and
  exact patch inventory, then present the concrete candidate diff and staged
  verification commands before running the approval-gated build/tests.


## Cross-platform installation integration

### Operator statement

The operator requested safe integration of the Windows lab WSLg/CUDA branch
and asked that Windows and Ubuntu installation instructions be robust for
others repeating the setup.

### Implementation

- Worked in isolated worktree `/tmp/dentobot-cross-platform-install` from
  `origin/main` at `0b3847e`; the active dirty checkout was not modified.
- Merged the Ubuntu NVIDIA setup branch and Windows WSLg/CUDA setup branch,
  resolving overlapping README, environment, launcher, task, changelog, and
  September 7 logbook content around one combined profile matrix.
- Integrated CPU/CUDA dependency probes, matching cu130 torchvision pin,
  NVIDIA and WSLg Compose overlays, Ubuntu's optional machine override,
  host NVIDIA setup helper, Windows WSL installer/launcher corrections, and
  idempotent TotalSegmentator task 298/115/113 model-cache installation.
- Made Windows WSLg the primary full SlicerROS2 simulation path. Retained
  native Windows Slicer only as the explicitly named Steps 0–5 fallback;
  its launcher profile removes Step 6, suppresses the application shell that
  contains Robot Simulation, and prevents saved Step 6 runtime restoration.
  The ambiguous former launcher exits with profile-selection guidance. Native
  Windows Slicer plus native ROS remains out of scope. Machine-local
  configuration, credentials, model
  cache, data, Conda environments, and generated ROS trees remain untracked.

### Verification and evidence boundary

- `bash -n Workspace/scripts/*.bash`: PASS.
- `python -m pytest -q Testing/test_application_shell.py` could not start
  because this shell has no `python` executable; the check was rerun with
  `python3` below.
- `python3 -m pytest -q Testing/test_application_shell.py`: PASS, 11 tests.
  The profile check proves the exact fallback value disables Step 6 while the
  default profile leaves it enabled.
- Base + CUDA and base + WSLg + CUDA Compose configuration: PASS after the
  final profile changes; no service was created or started.
- PowerShell parsing was not run because `pwsh` is unavailable on this Ubuntu
  host. Windows check-only execution remains part of the real-host gate.
- `git diff --check`: PASS before final commit.
- Ubuntu Mesa+CUDA Compose merge and Windows WSLg+CUDA Compose merge both
  passed `docker compose ... config -q` with placeholder machine paths; no
  service was created or started.
- Installer/launcher help gate initially failed with exit 126 because the new
  model-cache Bash helper lacked its executable bit. The mode was corrected;
  Bash parsing plus all three `--help` entry points then passed.
- Conflict-marker scan across the integrated README, inference README, and
  Workspace tree passed.
- `install-lab-wsl.bash --check-only --skip-docker` passed and parsed
  `lab/2026-09-03`, the published GHCR image, and SlicerROS2 pin without
  cloning, checking out, pulling, or starting Docker.
- Compose configuration and installer self-check results are recorded with the
  final commit below. No Slicer, ROS, MoveIt, model download, Docker image
  build, robot motion, drilling, or hardware operation was run in this
  integration task.
- Existing September 7 Ubuntu and Windows trials remain profile-specific
  runtime evidence. They do not prove a future clean-machine installation or
  graphics performance.
- `graphify update .` was attempted after the script integration and returned
  exit 1 because its watch rebuild could not create a stream file descriptor
  in the sandbox. No graph refresh is claimed; the setup scripts are outside
  the Python/C++ relationship graph used for this change.

### Operator clarification

The operator reported that the similar Windows profile names nearly caused
installation of native Windows Slicer instead of the intended WSLg workflow.
They directed that native Windows Slicer be retired as a primary path and kept
only as a DENTOWorkflow Steps 0–5 workaround without Step 6/SlicerROS2; native
Windows Slicer plus native ROS is explicitly out of scope.

### Next bounded action

Run the documented check-only gate after pulling the integration revision on
each real host. The IITM Ubuntu CPU profile remains open; a second Windows lab
installation should exercise the installer and model-cache helper from clean
machine state before declaring the procedure broadly repeatable.

---

## Tarun-X1 Windows lab bring-up (WSLg + docker-ce + CUDA) — challenges and discrepancies

### Operator observation / request

- Bring up the Windows 11 **lab** profile (WSLg + Docker SlicerROS2), not native
  Windows Slicer, on machine **Tarun-X1** at overlay `G:\IITM\Dentobot`.
- Enable **CUDA** backend (`cuda:0`) and a Desktop launcher.
- Operator later reported GUI not visible (taskbar icon only), then confirmed
  the Slicer window was visible after WSLg/GL fixes.
- Operator asked to summarize challenges vs repo setup instructions and to
  update the discrepancies file **and** the logbook.

### Engineering interpretation

- This is a second Windows-lab trial after the 2026-09-07 PLAT-U-04 notes, on a
  different host (desktop RTX 3060, Ubuntu 26.04 named `Ubuntu`, overlay on
  `G:`). Several README/SETUP assumptions still fail closed for strangers.
- Docker Desktop is unsafe on this host; docker-ce-in-WSL is the workable path.
- “GUI not working” was mostly a **window visibility / WSLg GL** problem: the
  Slicer process and DENTOWorkflow module were already running.

### Work performed / files touched

- Overlay + branch `windows/lab-wslg-cuda`; GHCR image pull; docker-ce in WSL
  after Desktop uninstall; `.wslconfig` memory caps.
- Conda envs: `dentobot` (CPU) and `dentobot-cuda` (Py 3.10 + cu130); model
  cache tasks 298/115/113; `.dentobot.env` set to cuda:0.
- Installed `nvidia-container-toolkit`; launcher `--check-only` green on CUDA.
- Patched `Workspace/compose.wslg.yaml` (mount `/usr/lib/wsl`, fix
  `XDG_RUNTIME_DIR`, software GL env) and `compose.cuda.yaml`
  (`NVIDIA_DRIVER_CAPABILITIES` includes graphics/display).
- Desktop helpers on OneDrive Desktop: `Launch Dentobot (CUDA WSLg).bat`,
  `Show Dentobot Window.bat`; helper `launch-from-windows.sh`.
- Docs: this logbook day file; append to `logbook-windows-history.md`; refresh
  `WINDOWS_LAB_SETUP_DISCREPANCIES.md`.

### Challenges faced (this machine)

1. **Docker Desktop OOM** — `com.docker.backend` ballooned (~49–63 GB private
   RAM) on a 32 GB host → hard reset / Safe Mode. Mitigation: uninstall
   Desktop; run **docker-ce inside WSL**; keep Desktop off; `.wslconfig`
   memory/swap caps.
2. **Wrong defaults vs host** — distro name `Ubuntu` (26.04) not
   `Ubuntu-24.04`; workspace `G:\IITM\Dentobot` not `~/dentobot`; lab bats
   miss the overlay unless env vars are set.
3. **Installer incompleteness** — Conda, TotalSegmentator cache, and a real
   `.dentobot.env` are not created by `install-lab-wsl`; launch fails closed
   until done manually. `--skip-docker` still requires `docker` on PATH.
4. **GHCR / Docker auth** — after Desktop removal, leftover
   `credsStore: desktop` broke pulls until plain auth was restored.
5. **CUDA stack** — Bridge C needs Py 3.10 + cu130 (`dentobot-cuda`); missing
   `pytest` failed the launcher pin probe; container GPU needed
   `nvidia-container-toolkit` (`could not select device driver "nvidia"`).
6. **WSLg GUI visibility** — Slicer reached DENTOWorkflow but window was
   minimized off-screen (`-32000,-32000`) or taskbar-only/blank. Root causes
   included missing `/usr/lib/wsl` in the container, wrong
   `XDG_RUNTIME_DIR`, and OpenGL; fixed with mounts +
   `LIBGL_ALWAYS_SOFTWARE` / llvmpipe / `QT_OPENGL=software`. Black viewports
   remain expected on this path.
7. **Desktop launcher bugs** — cmd.exe mangled bash `${LD_LIBRARY_PATH}` inside
   the `.bat`; fixed by calling `launch-from-windows.sh`. OneDrive Desktop
   (`E:\OneDrive\Desktop`) is the real Desktop on this PC, not
   `C:\Users\tarun\Desktop`.
8. **Noise ≠ conda failure** — Slicer pip-as-root WARNING is Slicer's embedded
   pip; host `dentobot-cuda` was already healthy.

### Discrepancies vs repo setup instructions

| Repo says / assumes | Tarun-X1 reality |
|---|---|
| Docker Desktop + WSL integration | Desktop OOMs; use docker-ce in WSL |
| Distro `Ubuntu-24.04`, `~/dentobot` | `Ubuntu` 26.04, `G:\IITM\Dentobot` |
| README native Slicer vs SETUP WSLg lab | Easy to mix profiles/launchers |
| Stay on tag `lab/2026-09-03` | Work continued on `windows/lab-wslg-cuda` |
| SETUP older image tags in places | `LAB_RELEASE` pin `...-20260903` |
| Post-install Conda/models implied | Not automated |
| WSLg GUI “functional / llvmpipe” | Understated: need `/usr/lib/wsl`, runtime-dir, often software GL |
| GPU via Desktop / compose.cuda | Needs nvidia-container-toolkit on docker-ce |
| PLAT-U-04 unverified | Closer on this box; still not stranger-proof |

### Evidence / verification

- `launch-dentoworkflow.bash --check-only` with `DENTOBOT_BACKEND_DEVICE=cuda:0`
  passed (Torch CUDA health JSON ok; container GPU via nvidia runtime).
- Operator confirmed Slicer GUI visible with DENTO Workflow loaded.
- Black slice/3D panels observed under software GL (lab functional posture).

### Failures / limitations / boundaries

- Does **not** claim Ubuntu-class GUI/render acceptance or clinical validation.
- Does **not** claim Docker Desktop is safe to re-enable on this host.
- Overlay on `/mnt/g/...` remains slower/more fragile than ext4 under
  `~/dentobot`.
- Desktop launchers and compose patches are machine-local lab hardening; still
  need a clean README path for strangers.

### Next bounded actions

1. Fold Tarun-X1 lessons into a stranger-facing Windows lab README (docker-ce
   ban on Desktop, distro/path, CUDA env, WSLg GL mounts, Desktop launchers).
2. Optionally relocate overlay to `~/dentobot` on ext4 for faster mounts.
3. Keep PLAT-U-04 open until a clean second-machine checklist passes without
   agent improvisation.

## Image re-label: jazzy-moveit-sim-20260909

Thin re-label (not a full stack rebuild) of the existing local
dentobot/slicerros2:jazzy-moveit-sim-20260903 image to
jazzy-moveit-sim-20260909 for lab/2026-09-09 OCI identity
(org.opencontainers.image.version / revision). Dockerfile.slicerros2
is unchanged since the 20260903 publish; only LAB_RELEASE / compose image
pins and image labels were updated, then publish-lab-image.bash --push.

## Stable source checkpoint before further Step 6 development

### Operator request

Commit and push the latest stable development state before continuing planner
work. Existing saved case data and engineer-owned records were outside this
checkpoint.

### Repository actions and integration

- Fetched `origin`; local `main` was ahead by five and behind by fifteen.
- Verified and committed the guarded-planning/case-workflow checkpoint as
  `20c5146`, then committed the current agent-policy records as `e318c02` and
  `6676a6d`.
- Merged `origin/main`, preserving both the Step 6 changes and upstream Windows
  lab/platform updates. Resolved navigation labels plus Step 6 gating, combined
  host-UID and WSLg/CUDA launcher behavior, and removed contradictory merged
  release/setup text. No patient/case data or engineer-owned artifact was
  staged.
- The first `git push origin main` was rejected because `origin/main` advanced
  to `354e758` during verification. Fetched it and merged its three
  documentation-only changes, including the lab image publication plan and
  evidence, without rewriting either history.
- A concurrent controlled-document update then appeared for the Slicer 5.12
  migration assessment. It adds `SLICERROS2_5_12_UPGRADE.md` and links it from
  AGENT_CONTEXT, DECISIONS, DEVELOPMENT_PLAN, SETUP, and TASKS as `PLAT-U-05`.
  The checkpoint preserves that internally consistent documentation; this
  commit does not independently re-run its source survey or authorize the
  planned build/runtime work.

### Verification evidence

- Before the merge, the focused planner/workflow suite passed: `106 passed in
  1.49s`. A first invocation raced its output-directory creation and collected
  no tests; rerunning sequentially produced the recorded result. The only
  initial specification failure was the stale `step6ApproachPoints` contract
  signature, corrected to include `snapshot=None`.
- After the merge, all changed Python files compiled, changed shell scripts
  passed `bash -n`, UI XML and `Testing/verification_matrix.json` parsed, and
  the conflict-marker scan passed. `git diff --check` passed when excluding
  the Windows `.bat` files, whose intentional CRLF terminators Git reports as
  trailing whitespace; `file` confirmed all five are DOS batch files with CRLF.
- Post-merge focused regression, including the upstream application-shell
  coverage: `117 passed in 15.49s`. Output:
  `/tmp/dentobot-verification/prepush-20260909/postmerge/pytest-focused.txt`.
- `graphify update .` first failed in the restricted sandbox with `Operation
  not permitted`; the approved rerun succeeded with 5,850 nodes, 9,314 edges,
  and 434 communities. It reported that semantic community labels can be
  refreshed separately; no label refresh was needed for this checkpoint.

### Evidence boundary and next action

This checkpoint establishes source-level integration and focused regression
health. It does not claim that the FDI11 guarded full cycle now completes at
runtime. Complete the documentation-only follow-up merge, push `main`, verify
`origin/main` matches, then resume the approved exact-case simulation from that
published baseline.

### Publication result

`git push origin main` succeeded after integration, advancing the remote from
`354e758` to `ebf5ab0`. A final logbook-only commit records this result; verify
the remote SHA after publishing that record.
## Afternoon: lab update channel, GHCR publish, and GHA lessons

Session on Tarun-X1 (Asia/Calcutta). Goal: document the day-to-day lab update
path, the maintainer-only GHCR publish steps that produced
`jazzy-moveit-sim-20260909`, and the GitHub Actions methodology reviewed for a
future CI publish workflow. **No `.github/workflows` YAML was added in this
docs pass.** Active product work remains on `integration/gui-step6`; this
session is docs-only under `Workspace/docs`.

### Lab update channel (day-to-day)

- **Lab PCs update via** `Workspace/scripts/update-lab-release.bat` /
  `update-lab-release.bash`. The updater pins from `LAB_RELEASE`, syncs
  `slicer_ros2_module`, runs `docker pull` **only if the digest changes**, and
  runs **colcon inside the container**. Lab PCs must **not** rebuild
  `Dockerfile.slicerros2`.
- **`publish-lab-image.bash` is maintainer-only**: tag the local
  `COMPOSE_IMAGE` → GHCR `IMAGE`. Lab operators do not run this.
- **WSL distro autodetection** lives in `resolve-wsl-distribution.bat`: prefer
  `Ubuntu`, then `Ubuntu-24.04` / `Ubuntu-22.04` / `Ubuntu-20.04`, else the
  first name from `wsl -l -q`.
- **Commit `5dbd371`**: `LAB_RELEASE` pin to `lab/2026-09-09` (same underlying
  image `20260903` initially). The updater reads `LAB_RELEASE` from
  `origin/main`, then detaches to the annotated lab tag. Compose WSLg path
  includes D3D12/NVIDIA mounts as previously hardened.
- **Chicken/egg problem**: a frozen lab tag's own copy of the updater cannot
  see newer pins that only exist on `origin/main`. **Fix in `27a12f9`**:
  - `DENTOBOT_REPO` override;
  - `run-update-lab-release-from-main.bash`;
  - the `.bat` fetches the helper from `origin/main` into `/tmp/...` then
    runs it (no cmd-hostile `${}` / `$()` embedded in the bat).
- Tag **`lab/2026-09-09`** was force-moved to `27a12f9`, later to `e802513`
  (final pin after the 20260909 image re-label publish).
- **E2E update check**: detached at `lab/2026-09-03` (`760a8ce`) → bootstrap
  from main → landed on `lab/2026-09-09`; full update OK. Colcon was skipped
  when the container `Running=false`.
- **Desktop entry points on Tarun-X1**:
  `E:\OneDrive\Desktop\Update Dentobot Lab.bat` plus
  `resolve-wsl-distribution.bat`.

### GHCR image publish (phase 2) — thin re-label on Tarun-X1

- `Dockerfile.slicerros2` is **unchanged** since `lab/2026-09-03`. WSL had
  roughly **~11 GiB RAM** available for Docker work, so a full rebuild was
  avoided (prior Docker Desktop OOM on this host was on the order of ~49 GB).
- **Thin re-label**: `FROM` local `jazzy-moveit-sim-20260903` → local
  `jazzy-moveit-sim-20260909` with OCI labels for version `lab/2026-09-09`,
  revision `e802513`, and slicer SHA `17f99931…`.
- **Commit `e802513`**: pin `LAB_RELEASE` + `compose.yaml` to the
  `…20260909` image name; annotated tag force-pushed to that commit.
- **First push failed**: `permission_denied` — token lacked `write:packages`
  (had `read:packages` only). Important lesson: **`docker push` exited 0
  despite the error** — treat that as failed; manifest remained unknown on
  GHCR until a real successful push.
- After `gh auth refresh … write:packages`: login +
  `publish-lab-image.bash --push` **succeeded**.
  - Digest:
    `sha256:544c5b759ccef7ce6c41157bbd7bd8b602657de367f1f6b71352de054c81b019`
  - Pull verified.
  - Old `…20260903` image left on GHCR (not deleted).

### GitHub Actions — what we checked (not implemented)

- Repo currently has **no** `.github/workflows` directory / files.
- Manual PAT push is **not** the recommended long-term methodology for
  publishing the lab image.
- Official tutorial:
  https://docs.github.com/en/actions/tutorials/publish-packages/publish-docker-images
- Recommended shape (for a future implementation, not this commit):
  - Job `permissions`: `packages: write` (+ `contents: read`;
    `attestations` / `id-token` when attesting);
  - `docker/login-action` with `GITHUB_TOKEN`;
  - `docker/metadata-action`;
  - `docker/build-push-action`;
  - pin Actions to commit SHAs;
  - optional artifact attestation.
- Local thin re-label was a **pragmatic RAM workaround on Tarun-X1**. Steady-
  state CI should do **real** `Dockerfile.slicerros2` builds on
  GitHub-hosted runners.

### Open / next

1. Finalize design in
   `Workspace/docs/GITHUB_ACTIONS_LAB_IMAGE_PUBLISH_PLAN.md` (this session),
   then later implement YAML on a narrow `lab/gha-publish` PR — **do not
   implement workflow YAML in this docs commit**.
2. Active product work continues on **`integration/gui-step6`**. Docs commits
   under `Workspace/docs` must not touch GUI / Step 6 product code, scripts,
   `LAB_RELEASE`, or compose pins.

### FDI21 blocker — isolated inferior-view capture

- **Operator observation:** the earlier oblique diagnostic did not visibly
  demonstrate the reported FDI21 collision; a view from below the incisors was
  requested.
- **Implementation:** extended
  `Testing/run_dentobot_step65_exact_case_smoke.py` diagnostic-only capture to
  hide markups and unrelated models, isolate FDI21/FDI11/spindle/burr, render
  inferior, root-apical, and palatal-inferior views, and attempt a yellow VTK
  surface-intersection overlay.
- **Commands / errors:** the first capture launch used `--no-main-window` and
  failed before planning (`Could not find main window`); the corrected launch
  then encountered duplicate ROS processes left by the aborted run. Those
  simulation-only processes were terminated and the clean-stack retry was run
  in `dentobot-slicerros2` against
  `FDI11/dentobot-case-step6x4x2.dentocase`, base local Z offset `-4 mm`.
- **Result:** diagnostic marker
  `DENTOBOT_FDI21_BOTTOM_CAPTURE_PASS`; retained candidate `0` names collision
  pair FDI21 object suffix `71ddde60` / `pneumatic_spindle-Copy`. Captures are
  under `data/dentobot-runs/fdi21-blocker-20260909/`. This was diagnostic-only;
  the harness's generic `DENTOBOT_STEP65_EXACT_CASE_PASS` line is not a full
  cycle acceptance result. Slicer teardown was interrupted after the capture
  marker and exited `139`.
- **Evidence boundary / interpretation:** the root-apical view makes the
  reported contact region visible at the lower-left edge of the orange spindle
  footprint. The other two projections are weak because the tool axis and
  clipping direction collapse or occlude the contact. The capture does not by
  itself establish penetration depth or clinical significance.
- **Next bounded action:** use the root-apical image for operator review before
  changing FDI21 scene geometry, transforms, or collision policy.

### Evidence / disposition (afternoon)

- Update-channel chicken/egg fix landed (`27a12f9`); tag moved; E2E from
  `lab/2026-09-03` → `lab/2026-09-09` succeeded.
- GHCR publish of `jazzy-moveit-sim-20260909` succeeded after
  `write:packages` refresh; digest recorded above; pull verified.
- GHA publish plan written as markdown only; **no** `.github/workflows/*.yml`
  added.

### Mandatory existing-plan-first gate

- **Operator observation:** implementation was started in a new Codex task
  without applying the already recorded plan gate. The operator states that
  failure to prioritize carefully planned and recorded work is detrimental to
  development continuity.
- **Confirmed record:** `TASKS.md` already defines
  `S6-REUSABLE-CASE-SETUP` as Priority 0, with reusable mouth opening,
  base/Task Home setup, per-target trajectory/shell ownership, and a 32 × 3
  registry foundation. Its task-specific promotion does not promote the whole
  Studio roadmap.
- **Change:** added a mandatory existing-plan-first gate to `Workspace/AGENTS.md`,
  placed the lookup rule and current routing at the top of `AGENT_CONTEXT.md`,
  recorded decision `2026-09-09 — Retrieve recorded plans before task scoping
  or execution`, and added task record `AGENT-PLAN-FIRST`.
- **Evidence boundary:** documentation-only change. No product code, test,
  build, Slicer, ROS, MoveIt, robot motion, or simulation was run. Verification
  is limited to instruction readback and diff checks.

## Lab pin advance: lab/2026-09-10

Operator request: update `LAB_RELEASE` to the latest stable checkpoint mentioned
on `main`, and run the lab update on Tarun-X1.

Pinned source freeze: annotated tag `lab/2026-09-10` at the pin commit on
`main` (includes stable checkpoint publication through `ebf5ab0`/`a3f516d` and
subsequent docs through `a231d99`). GHCR image unchanged:
`jazzy-moveit-sim-20260909` (`Dockerfile.slicerros2` unchanged since the prior
pin). Day-to-day update remains `Update Dentobot Lab.bat` / bootstrap from
`origin/main`.

### Git and Google Drive publication — existing-plan-first documentation

- **Operator request:** push the committed documentation checkpoint to Git and
  synchronize the development-controlled records with Google Drive.
- **Git work:** the first push of `414da8a` was rejected because `origin/main`
  had two newer documentation/release commits. Fetched `origin/main`, merged
  it without force-pushing, retained both independent logbook entries, created
  merge commit `d8487b5`, and pushed `main` successfully to `origin/main`.
- **Drive comparison:** retrieved the existing active-development copies of
  `AGENTS.md`, `AGENT_CONTEXT.md`, `DECISIONS.md`, and `TASKS.md`; all were
  last modified on September 2 and lacked the new plan-gate markers. No
  conflicting newer Drive content was found. No matching `2026-09-09.md`,
  `LAB_RELEASE`, or `WINDOWS_SETUP.md` file existed in the established active
  folder; no duplicate was created for the latter two.
- **Drive work:** updated the existing IDs for the four active controls in
  place and created `2026-09-09.md` only in the existing `logbook` folder
  because that dated file did not yet exist. The new logbook ID is
  `1I1-ld2X7MjdHtLZ09Z-8AZBQZ0HW8oGe`.
- **Verification:** fetched all five Drive records after publication and
  confirmed their expected plan-gate or current-routing markers, IDs, and
  September 9 modification timestamps. Active product/test files remained
  unstaged; no runtime, Slicer, ROS, MoveIt, or full-cycle test ran.
- **Boundary:** engineer-owned Drive records were not read or modified.
- **Follow-up:** the audit entry itself was committed as `ea7ba69`, pushed to
  `origin/main`, and the same existing Drive logbook ID was refreshed in place.
  Final verification showed local `HEAD` equal to `origin/main`, an empty
  staging area, and the concurrent product/test edits still unstaged.
