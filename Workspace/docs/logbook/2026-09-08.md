# DENTOBOT Development Log — 2026-09-08

## Resumed FDI11 simulation — combined insertion and warning policy

- **Operator:** Supersedes the blocked geometry disposition for this simulation.
  Requests that the shell contribution be included inside a 6.5 mm maximum
  allowance, with 0.5 mm of the modeled spindle/tool protrusion remaining, and
  that the strict 1 mm guide/template clearance at issue become a recorded
  diagnostic warning. Expects this to permit complete planning and wants the
  warnings carried into the planned diagnostics revamp.
- **Coordinator interpretation:** Preserve the 6.0 mm drilling endpoint and
  represent shell/guide traversal as a provisional 0.5 mm addition, for 6.5 mm
  combined demand against the modeled 7.0 mm protrusion. Apply the warning only
  to positive non-contact distances between non-burr robot links and exact
  configured guide/template objects during phased simulation. The selected
  0.898675 mm spindle/template failure is in this class. Actual collision stays
  rejected; burr-guide keeps 0.1 mm; self/unrelated/unconfigured pairs keep
  1 mm. This does not reinterpret the offline mesh-intersection diagnostic as a
  native collision or discard it.
- **Implementation plan:** Add native warning evidence and bounded re-query,
  bridge parsing, full-chain and persistent diagnostic propagation, preview
  warning accumulation, combined insertion evidence, and guarded reverse-axis
  withdrawal before strict Home. One Luna Max worker owns the bounded code/test
  files; coordinator owns records, integration, build, runtime and acceptance.
- **Verification authority:** The operator's earlier full simulation testing
  approval remains in force. Run focused static/pure checks, rebuild the native
  guard, run its ROS smoke, then run the exact saved FDI11 cycle twice with
  teardown and source-case checksum verification. No hardware command path is
  present or authorized.

## Goal blocked audit — physical geometry prerequisite

- **Previous turn:** No progress toward an executable full cycle; checks and
  stale-status corrections did not change the physical prerequisite.
- **Current readback:** Source-case checksum passes; simulation burr hash
  matches the retained result; exact failure remains spindle/template
  0.898675 mm versus 1 mm. The return proposal remains archived, not active.
- **Threshold:** The same geometry prerequisite remains across the operator's
  diagnostic-record checkpoint and two subsequent goal continuations. No new
  measured geometry or reviewed design change was supplied. Operator direction
  is to address hard constraints rather than make further temporary adjustments.
- **Disposition:** Full goal blocked pending reviewed tool protrusion and
  guide/housing geometry compatible with the requested path. No new runtime,
  geometry change or margin relaxation. All-stage completion, anatomical target
  acceptance, axial withdrawal and repeated Home return remain unproven.
- **Resume condition:** Supply the reviewed physical/design profile or revised
  case/guide geometry; update the authoritative model and feasibility checks,
  complete paired retraction implementation/verification, then run the complete
  guarded cycle and repeat. No narrower success criterion substituted.

## Goal continuation — unchanged physical prerequisite audit

- **Previous turn classification:** Progress: consolidated operator-requested
  hard-constraint records and preserved the incomplete return proposal without
  leaving incompatible active source.
- **Current authoritative checks:** `sha256sum -c` source case returns OK;
  simulation burr SHA256 remains
  `469c4ddc223253c534488fd00b4e31727a8ee6ffcb2be3be87c358b92a968cec`.
  Source still references the 1 mm mesh, 6 mm cap and 0.1 mm pair margin.
  Docker process inspection finds no Slicer/MoveIt/guard/build (grep exit1).
  `result.json` still records exact-case exit1 and the same housing/template
  clearance failure. No new measured profile or revised guide supplied.
- **Record correction:** TASKS previously retained stale “verification active”
  and “return implementation continues” wording from historical updates.
  Corrected current status to awaiting reviewed geometry, no active runtime,
  and unimplemented axial withdrawal with archived proposal.
- **Next:** Resolve measured tool/guide/housing feasibility before repeating
  the unchanged full-case test. No geometry manipulation or additional runtime
  performed. Full objective remains incomplete; this audit does not claim a
  successful simulation or anatomical target.

## FDI11 hard-constraint diagnostic checkpoint — latest operator request

**Operator:** Requests all diagnostics in logs so the actual hard constraints
can be addressed instead of further temporary manipulation. No further
geometry, depth, joint-limit, collision or margin relaxation will be applied
under this continuation. Existing provisional values are recorded below as
current test conditions, not recommended physical specifications.

### Current input and provisional conditions

- Exact saved case: `data/Slicer_Saved/SampleStudy1/FDI11/dentobot-case-step6x4x2.dentocase`.
  Input hash is retained in `artifacts/verification/guide-margin-20260908/package.sha256`;
  verification after runtime returned OK. Original saved case was not modified.
- Manual source trajectory: 7.977207292891484 mm. Confirmed simulation Target
  was capped along the same axis to 6 mm by the operator's later instruction.
  This endpoint is not a verified pulp entry; missing/unlabeled pulp does not
  establish anatomical targeting. The earlier step6x4 trajectory was 17.922 mm
  and was explicitly identified by the operator as old.
- Original CAD burr approximately 2 mm diameter, 7 mm axial extent. Current
  separately named simulation mesh is 1.000005514 mm diameter, 7.000001255 mm
  long; original mesh retained, axial/TCP geometry preserved. Inertia remains
  historical and unvalidated for the variant.
- Saved trajectory-guide bore: 1.5 mm; docking bores 2.2 mm are separate features.
  Ideal concentric radial gap is only 0.25 mm with a 1 mm burr. This arithmetic
  does not include alignment, runout, tolerances or mesh approximation.
- Approved transient burr–guide clearance: 0.1 mm. Other pair margins remain
  1 mm; actual guide collisions remain rejected. Selected-target burr contact
  retains its existing exploratory simulation allowance and must be reported.
- Diagnostic placement: −5 mm base-local Z, applied in memory. J2 axis is
  base-local +Z; the shift restores travel without changing its lower limit.
  This placement is neither saved-case modification nor measured hardware pose.

### Constraints established by the evidence

| Constraint | Evidence / observed limit | Engineering action required |
|---|---|---|
| Tool-only insertion capacity | Fixed 7.0 mm protrusion minus 0.1 mm reserve = 6.9 mm; old 17.922 mm and manual 7.977 mm exceed it | Measure actual usable protrusion and retain an explicit reserve; do not treat trajectory length as attainable insertion |
| Burr versus sleeve diameter | Original 2 mm burr collides with 1.5 mm guide; 1 mm simulation variant passes nominal diameter comparison | Use the actual maximum swept tool envelope and minimum manufactured bore, including runout/tolerance; regenerate incompatible guide geometry |
| Required radial clearance | 1 mm margin cannot fit within a nominal 0.25 mm radial gap; approved 0.1 mm pair policy passes native checks | Establish a reviewed uncertainty/clearance budget from measured tool, sleeve and alignment; provisional 0.1 mm is not a physical validation |
| J2 travel | Unshifted 1 mm/6 mm case stalls at J2 lower bound, Stage3 fraction 0.56, requested pose15/25, residual0.459 mm/0.028° | Select and verify feasible base placement with travel reserve; retain joint bounds |
| Approach guide clearance | With −5 mm base shift and 0.1 mm pair policy, candidates reject at0.080503 mm and0.027368 mm | Validate full swept approach geometry and alignment; nominal bore fit alone is insufficient |
| Housing versus final template | Complete kinematic drilling candidate fails independent guard at composed waypoint402, drilling index12, housing/template0.898675 mm <1 mm | Resolve sleeve/housing envelope and protrusion; burr diameter alone cannot fix it |
| Deep housing interference | Exact saved-mesh triangle tests intersect housing at4,5,6 mm depth in12/12 sampled axial rolls; spindle nose is7 mm behind TCP | Redesign/review guide and tool protrusion together before claiming6 mm achievable; preserve actual collision rejection |
| Return motion | Existing return path requests strict direct Home from Target without explicit axial withdrawal | Require guarded axial withdrawal through the same corridor before free-space return; complete and repeated cycle remains unverified |
| Anatomical endpoint | Current6 mm endpoint is a manual-axis cap, not a labeled-pulp intersection | Separately review target/pulp identification; simulation path completion cannot validate anatomy |

The guide occupies space behind Entry, consuming usable protrusion before the
burr reaches tooth depth. The required dimensional budget is **drilling depth
+ external sleeve/guide stand-off + reviewed housing clearance/reserve ≤ usable
protrusion**, with actual 3D envelopes, axis/registration error and uncertainty
checked throughout motion. This is a design constraint, not a dimension chosen
from the current failed mesh. Measured burr/shank/runout, sleeve bore/length/pose,
housing, TCP, registration and manufacturing tolerances must replace provisional
values together; update fingerprints and invalidate prior plans when they change.

### Verification and evidence boundary

- Diagnostic commands: Graphify scoped query for guarded withdrawal;
  URDF XML/mesh transform reads; `docker start dentobot-slicerros2`; documented
  explicit colcon build; serialized `run-guard.sh` and `run-runtime.sh`;
  `python3 artifacts/verification/guide-margin-20260908/housing-sweep.py`;
  `sha256sum -c .../package.sha256`; process inspection; controlled-doc
  `git diff --check`. Scripts and exact logs are retained with the evidence.
- Corrected tooling errors: initial transform script could not import scipy;
  replaced it with explicit numpy rotation matrices (no dependency installed).
  Initial nested ZIP lookup used percent-encoded MRML fileName; decoded it
  with urllib.parse.unquote before extracting the exact VTK. Initial colcon
  discovery skipped moveit config; explicit documented base path rebuilt it.
  A few relative reads used the workspace prefix from repository cwd and
  failed; corrected to the authoritative path. These errors did not alter
  geometry or produce accepted evidence.

- Earlier focused cap/state/facade checks: 55 passed; description/burr checks:
  47 passed; direct-route metadata regression: facade25 passed. Those prior
  outcomes are recorded earlier in this log; their original host temporary
  raw logs disappeared. Do not imply newly rerun evidence.
- Current guide-margin worker checks: 55 Python tests passed, compilation and
  diff checks passed. Native guard build exit0; native runtime17/17 checks
  passed, including0.2 mm acceptance,0.05 mm rejection and unrelated0.5 mm
  rejection after a nearer configured guide pair passed.
- Exact-case full runner exit1 before preview; Stage3 kinematic fraction1.0 is
  not a guard pass. No approach/drilling/retraction/Home cycle was accepted.
- Offline VTK sweep is a finite roll sample, not a proof over all rotations;
  no surface intersection at0–3 mm is not a clearance or planning acceptance.
- Optional ROS2Tests `psutil` installation failed on permissions; initial module
  loader warning occurred, but ROS/MoveIt later connected and planned. Retained
  VTK objects appeared at shutdown. These are recorded separately from the
  causal Stage3 guard rejection; no clean-shutdown claim.
- Cleanup inspection found no active test Slicer/MoveIt/guard/build. Input hash
  remained unchanged. Durable workspace evidence and runnable diagnostics:
  `artifacts/verification/guide-margin-20260908/README.md`, `result.json`,
  `guard-runtime.log`, `exact-runtime.log`, `exact-diagnostic.json`,
  `housing-sweep.py`, `housing-sweep.json`, and extracted `saved-template.vtk`.
  `prior-container-diagnostic.json` is older recovered evidence, not this run.

**Checkpoint checks:** Restored active façade passed coordinator py_compile
with a temporary cache prefix and scoped diff check. Graphify refresh initially
failed to launch its extractor in the sandbox; authorized host retry extracted
25 uncached files and reported partial-parser warnings in nine existing
SlicerROS2 C++ files (not compiler failures). Output retained in
`graphify-update.log`.

**Software checkpoint:** The return worker reached a Python-only proposal; native
retraction support and verification were not implemented. The unpaired proposal
is retained as `artifacts/verification/guide-margin-20260908/retraction-proposal.patch`
and removed from active source, so no
partially supported return behavior is presented as complete. Explicit axial
withdrawal remains an open requirement.

**Next bounded action:** Work from measured/reviewed protrusion and guide/housing
geometry; retain failed-case evidence. No further temporary geometry/margin
manipulation to obtain a green result. Correct axial-return sequencing is a
separate software requirement; full-cycle acceptance remains open.

## Approved burr–guide margin continuation

- **Exact-case result:** Exit 1, Stage 3 guard rejection. Full kinematic
  fraction 1.0 exists, but selected drilling candidate reports spindle housing
  versus template clearance 0.898675 mm below unchanged 1 mm. Other candidates
  report burr-guide 0.080503 and 0.027368 mm below approved 0.1 mm. No preview
  accepted. Structured evidence: `exact-diagnostic.json`; log `exact-runtime.log`.
- **Geometry diagnosis:** `housing-sweep.py` transforms original URDF housing
  mesh into the provisional TCP frame, then uses committed task rotation and
  Entry + depth × axis. Native URDF nose lies 7.000001 mm behind TCP. Extracted
  exact saved final-template VTK (SHA256
  `309506d69d0806c30862a18a9059be74575cb3f337b4f46e73946a66988a5644`)
  is converted from storage LPS to RAS. VTK triangle-intersection checks at
  12 axial rolls show housing/template intersection for every sampled roll at
  depths 4, 5, and 6 mm; none at 0, 2, or 3 mm. This is offline sampled geometry
  evidence, not a proof over all rolls or a native guard acceptance at shallow
  depths. Exported STL independently gave the same counts.
- **Interpretation:** Shrinking burr diameter cannot resolve housing overlap;
  a 6 mm path needs reviewed protrusion/guide geometry. No housing mesh shrink,
  collision suppression, depth reduction or longer tool was silently applied.
- **Continuation:** Earlier return worker was absent and no return patch
  existed. Assigned one Luna Max worker to accepted-path reversal with explicit
  monotonic retraction guard, preserving all geometry and clearance rules.

- **Native verification:** Explicit guard build exited 0 (16.6s). Serialized
  `run-guard.sh` exited 0 with all 17 report fields true, including 0.2 mm
  guide acceptance, 0.05 mm guide rejection, and unrelated 0.5 mm rejection.
  Collision, bounds, lateral/overshoot and sequence checks also passed.
  Exact FDI11 full runner now started with approved values and the previously
  tested -5 mm in-memory base-Z offset; output is `exact-runtime.log`.

- **Source review:** Optional new guide identity list defaults empty; malformed,
  duplicate, empty and target-as-guide identities reject. Legacy empty retired
  fields remain readable; nonempty retired exemptions reject. Actual collision
  ACM is unchanged; a bounded distance-only requery protects other pairs.
- **Worker checks:** Reported `pytest -q -p no:cacheprovider
  Testing/test_robot_workflow_facade.py Testing/test_ros2_bridge.py
  Testing/test_moveit_config.py`: 55 passed; assigned Python compile and diff
  checks passed. Coordinator inspected the actual protocol/guard diff.
- **Build correction:** General `--packages-select` discovered description only
  and warned that moveit config was unknown. This is not a guard build pass.
  Retried using SETUP.md's explicit `--base-paths
  src/DentoBot/dentobot_moveit_config`; output in `build-guard.log`.

- **Operator:** Explicitly approved the proposed change and requested clean
  records of provisional values and measured-geometry requirements.
- **Decision:** 0.1 mm applies only to configured simulation burr–guide pairs;
  retain actual collision rejection and all other 1 mm margins. Requirements
  for the future measured profile are recorded in DECISIONS.md.
- **Implementation:** One Luna Max worker owns the bounded protocol/guard and
  regression changes; coordinator owns documentation and runtime acceptance.
- **Environment observation:** On continuation, `docker ps -a` shows the test
  container stopped; prior host `/tmp/dentobot-verification` evidence is absent.
  Historical outcomes above remain recorded outcomes, but their temporary
  raw artifacts are currently unavailable. New evidence will be kept in the
  workspace as well as runtime temporary paths. No successful full loop yet.

## 1 mm simulation burr — operator-authorized remote continuation

- **Offset outcome:** Exit1, approach_phase_guard_failed. Saved diagnostic has
  three Stage3 fraction1.0 candidates after -5 mm local-Z base change; guard
  rejects approach at waypoint373, selected clearance0.630296 mm versus1 mm,
  burr↔final template. Other successful kinematic candidates report0.626125
  and0.644861 mm guard clearances. Full kinematic fraction is not a guard or
  executed-preview pass. Source package checksum remains unchanged; Docker
  pgrep confirms no test processes remain. Changed runner/test compilation and
  final diff check pass. Next proposes explicit simulation-only0.1 mm
  burr/guide clearance while retaining real collisions and other1 mm margins;
  this additional policy has NOT been implemented or enabled.

- **Stage-3 evidence captured:** diagnostic.json records fraction0.56, first
  failed requested pose15/25, J2=0 lower bound, residual0.459 mm/0.028 deg.
  Three other viable/direct branches share this joint-limit classification;
  selected failure has no collision pair. No partial drilling path accepted.
- **Bounded simulation-base hypothesis:** Coordinator's URDF transform
  calculation establishes J2 axis in base as (0,0,+1), within numerical
  noise. A -5 mm base-local-Z translation should allow +5 mm J2 at the same
  tool poses. This is agent reasoning under the operator's request to finish
  simulation remotely, not an operator-specified base pose or a hardware move.
  Started one in-memory offset trial using the existing diagnostic nudge/lock
  path, with unchanged case file, Entry/Target, joint limits and collision
  policy. Runtime result remains pending in runtime-offset.log.

- **Corrected runtime:** Exit 1 with truthful approach_full_chain_blocked,
  Stage 3. Report retains 71 free-space/12 fixed-axis Stage-2 checkpoints from
  403 MoveIt samples via workspace sample221, 6.000 mm insertion PASS and
  1.00 mm burr≤1.50 mm bore fit PASS. This is planning/preflight evidence,
  not executed preview or drilling acceptance. Summary omitted exact Stage-3
  cause, so explicit-case smoke now writes task/action details and complete
  MotionDiagnosticSession to container /tmp/dentobot-exact-case-diagnostic.json
  before raising. Launched a justified evidence-capture retry after cleanup;
  no unchanged successful unit suite rerun and no policy relaxation.

- **First 1 mm runtime:** Exit 1 after reaching route evaluation; production
  `planApproachPhase` raised KeyError('clearance') at selected_route indexing.
  Mixed direct/detour candidates can select a direct route without detour-only
  metadata. Corrected lookup to optional `.get('clearance')`; inspected all
  consumers (already handle None). Added executable AST regression exercising
  actual selection block with both direct and detour winners. Focused façade
  suite passed 25/25, exit 0. No geometry/policy relaxation in this fix.
- **Retry:** Verified no live Slicer/MoveIt/guard/build after first run cleanup.
  Started the same approved full-case runner after the correction; output is
  runtime-fixed.log. First shutdown reported retained VTK objects separately
  from the causal planner exception; no clean-exit claim.

- **Operator:** Cannot access host; requests URDF burr diameter 1 mm or a
  workable value and completion of simulation. Existing full testing approval
  remains active. No hardware/tool measurement or hardware action authorized.
- **Geometry:** Graphify query, URDF/launch/loader reads, and binary STL
  inspection found original radial extent 1.0000103 mm and axial extent 7 mm.
  Offline numpy/struct transformation halves radial coordinates about the
  actual drill axis while preserving axial coordinates and URDF origins.
  New STL readback: diameter 1.000005514 mm, length 7.000001255 mm, maximum
  axial change 0.000000237 mm. Original CAD mesh unchanged; original URDF
  checkpoint retained under /tmp/dentobot-verification/burr1-20260908.
- **Implementation:** URDF visual/collision uses named simulation mesh;
  façade fit diameter is 1 mm. Description checksum test includes both
  original and variant; referenced-mesh contract expects the variant.
  No clearance/collision/TCP/joint changes. Historical inertia remains labeled
  unvalidated for variant. Code changes were small and inseparable from this
  serialized geometry inspection/build, handled directly by coordinator.
- **Verification:** First focused run 46 passed/1 expected mesh-reference
  mismatch; updated contract then 47 passed in 0.18s. Description colcon build
  exit 0 (0.89s). Initial py_compile omitted temporary prefix and hit protected
  __pycache__; corrected command with PYTHONPYCACHEPREFIX exited 0. Diff check
  passed. Evidence directory contains mesh.json, build.log and pytest logs.
- **Runtime:** Docker ownership check found no live Slicer/MoveIt/guard/build.
  Started approved exact FDI11 run with owned cleanup and retained guards;
  runtime.log is in burr1 evidence directory. The 1.5 mm bore gives only
  0.25 mm radial gap with this variant, not the existing 1 mm research margin.

## Operator-selected 6 mm simulation cap implementation

- **Guide-fit blocked audit:** Runtime-result turn and two continuations retain
  the same physical guide-fit prerequisite. Previous continuation was no
  progress: result.json readback and unchanged package checksum confirm the
  recorded failure; no replacement dimensions or updated input arrived.
  Marked goal blocked pending compatible guide geometry, not missing testing
  permission. Operator's full testing approval remains active. No further
  identical runtime was launched and no collision exemption was introduced.

- **Exact-case runtime result:** Exit 1, first causal failure at Goal 1 PreEntry
  IK. Converged candidate residuals include 0.00137098 mm/0.00007963 deg,
  0.00567161 mm/0.00642134 deg and 0.00249075 mm/0.00108490 deg; each reports
  final printable template↔burr collision. Other seeds do not converge. This
  is no longer an insertion-preflight failure; no approach/drilling preview
  was accepted. Optional ROS2Tests attempted psutil installation and failed;
  DENTOWorkflow proceeded to real MoveIt planning despite that module warning.
- **Dimensional evidence:** Read nested MRB final-template DockingMetricsJson
  and ParametersJson: trajectoryGuide innerDiameterMm=1.5, targetDocking
  boreDiameterMm=2.2. Final guide's saved trajectory endpoints match the new
  7.977207 mm manual line. Configured burr diameter is 2.0 mm. Thus recorded
  guide geometry is too small for the modeled burr; do not relabel docking
  bore as trajectory-bore clearance or bypass template collision.
- **Cleanup / integrity:** Owned runner trap ended ROS launch. Elevated Docker
  pgrep returned no Slicer/MoveIt/guard/build process, exit 1. Package checksum
  verification passed. Evidence summary result.json retains failure and 55-test
  PASS separately. Next requires reviewed compatible guide/tool geometry before
  retrying the same path; axial withdrawal/Home remains separate unverified work.

- **Approval / execution:** Operator approved full testing until goal completion
  and then explicitly permitted closing the existing saved session. Focused host
  pytest (`Testing/test_step6_state.py Testing/test_robot_workflow_facade.py
  Testing/test_step6_planning.py`, -q -p no:cacheprovider) passed 55/55 in 0.96s,
  exit 0. Changed production/test py_compile and git diff --check exited 0.
  Existing stream-fd startup warnings did not prevent these checks. Host scope
  is recorded explicitly, not misreported as matrix container execution.
- **Runtime ownership:** Docker inspection found Slicer PID311, MoveIt180 and
  guard181. After operator permission, TERM311 allowed launcher cleanup to
  remove the whole session; elevated pgrep returned no matching processes.
  No unrelated processes stopped. Docker sandbox access was intermittent;
  approved escalation succeeded without auto-review rejection.
- **Exact-case runner:** Added explicit DENTOBOT_EXACT_CASE input to existing
  saved-case smoke and rejected historical override/partial-run flags for it.
  Historical default remains opt-in. New runner compilation passes. Saved
  package checksum recorded in cap6 evidence directory. Launched owned ROS
  stack and Xvfb/Slicer via run-runtime.sh with 900-second timeout. First
  readiness loop used unavailable container rg (BrokenPipe errors); corrected
  runner to grep for future execution while observing the existing run.

- **Verification-approval blocked audit:** Source-completion turn proposed
  focused matrix checks; two automatic continuations supplied no operator
  approval. Previous continuation was no progress and confirmed the expected
  cap6 evidence directory is absent (ls exit 2), not an active verification
  process. Marked goal blocked on this third recurrence. Tests/runtime remain
  NOT_RUN; the implementation and full-cycle objective are retained. Resume
  with focused-check approval; no success or safe-retraction claim is made.

- **Source completion:** Luna wrote cap constants/validated helper, confirmation
  integration/provenance freshness check, effective endpoint/depth confirmation
  message and two focused tests. Coordinator stopped the slow worker before
  handoff, inspected actual source/diff, added below-cap and phase-guard snapshot
  assertions, and retained all existing unrelated changes. No tests executed by
  coordinator. Worker had been explicitly forbidden test/runtime execution;
  no worker execution evidence or final handoff received.
- **Readback:** Original build/parse_task_snapshot remains unchanged; old
  payloads remain readable. Explicit confirmation produces capped Target with
  new provenance; old provenance fails freshness. Existing physical 6.9 mm
  preflight remains unchanged. Goal 1, Goal 2, guard config and final FK use
  confirmed snapshot; source-based PreEntry calculation retains the same axis.
  Legacy non-façade motion helper remains outside this guarded-flow cap scope.
- **Graph:** `graphify update .` exited 0, 7135 nodes/11203 edges/634 communities;
  nine native-file partial extraction warnings and retained-node warning remain.
  This is navigation maintenance, not runtime or test acceptance.
- **Prepared approval request:** From ros2_ws/src/DentoBot, run matrix
  pure.step6_planning (container pytest of test_step6_state.py,
  test_robot_workflow_facade.py, test_step6_planning.py), static.step6_pycompile
  for the three edited production files and test_step6_state.py, and
  static.git_diff_check. Expected exits 0; write logs under
  /tmp/dentobot-verification/cap6-20260908. No build required for Python-only
  edits. Exact FDI11 runtime and axial retract remain separate, unexecuted work.

- **New saved input:** `stat` discovered FDI11 `dentobot-case-step6x4x2.dentocase`
  (03:34 IST). Read-only nested zip/json inspection reports a confirmed task
  and two-point line length 7.977207292891484 mm, matching latest screenshot.
  No package changes. Source read of applyTaskHome confirms strict joint-goal
  planning from monitored current state; independent C++ guard currently
  accepts approach/terminal_contact/drilling phases only. Full withdrawal
  acceptance cannot be inferred from the existing Home call.

- **Operator:** Manual trajectory screenshot reports 7.977 mm versus 6.9 mm
  capacity. Initially requested a 10 mm maximum and removal of constraints;
  immediately corrected to 6 mm, stating current spindle reach is about 6.5 mm.
  The 6.5 mm statement is an operator observation, not a new metrology result.
- **Decision:** Cap the effective simulation snapshot Target at 6 mm while
  preserving Entry, direction, shorter lines and original source trajectory.
  Keep the existing 6.9 mm physical insertion preflight and all collision,
  bounds, phase and endpoint checks. No pulp-intersection claim for this cap.
- **Work / commands:** Scoped graph query, rg/sed/head source/context reads and
  git status revealed existing dirty changes, preserved. An earlier context
  concatenation went to /tmp/dentobot-depth-context.txt; scoped document reads
  then established relevant contracts. One native Luna Max worker owns state,
  task-confirmation/façade code and focused tests; no recursive delegation,
  runtime or test execution. Coordinator owns documentation and acceptance.
- **Verification preparation:** Matrix selects static.step6_pycompile,
  static.git_diff_check and pure.step6_planning. Execution approval has not
  been received for this proposed change. Existing historical x4 runtime script
  hardcodes a different package and includes historical audit branches; it is
  not unchanged FDI11 acceptance. Current returnToTaskHome routes to strict
  applyTaskHome; axis-aligned withdrawal must be separately verified against
  the full requested sequence rather than inferred from a Home marker.
- **Next:** Inspect worker diff, obtain focused-check approval, identify saved
  package for the new manual trajectory, then prepare the exact full-cycle
  simulation with serialized runtime ownership. No completion claimed.

## FDI11 immediate guarded-approach rejection

- **Resumed blocked audit:** Following the operator's unlabeled-pulp screenshot, the same unresolved segment-identity prerequisite persisted through the user-triggered turn and two continuations. Previous continuation was no progress: stat showed unchanged packages, with no new segment identity or source mask supplied. Existing parser supports the proposed label once the actual segment is identified; the screenshot alone cannot establish a separate binary mask. Marked goal blocked again pending segment identification/new saved evidence. Full-cycle simulation remains unverified; no source, scene or safety-policy changes.

- **Operator correction / label workaround:** Operator reports visible unlabeled FDI11 pulp and asks for a workaround, with screenshot. Corrected evidence boundary: earlier header inventory proves no recognized FDI11 pulp label, not absence of pulp geometry. Graphify query and scoped rg/sed read of `describeSegmentForReview` show `pulp_fdi11` classifies as pulp/FDI11; generator requires that segment in the selected source segmentation. No code change is needed if a distinct reviewed segment exists. Pending identification of its segment-list row versus an internal tooth surface/cavity; no rename, source-mask edit or runtime action performed.

- **Blocked audit:** Missing reviewed FDI11 pulp input persists across three goal turns. Previous turn made progress by ruling out step5b. Current `stat` confirms saved packages still predate this investigation (step5b 2026-09-07 21:52 IST; step6x4 2026-09-08 02:42 IST); TASKS readback retains the established missing-mask evidence. No new mask location or live-scene confirmation has arrived. Marked the full goal blocked, not complete, pending that input. All requested simulation phases remain unverified; no runtime or hardware operations performed.

- **Continuation / alternate saved input:** `rg --files --hidden --no-ignore data/Slicer_Saved/SampleStudy1/FDI11` finds only step5b, step6x4 and the printable STL. Read-only Python zipfile/io/re inspection of step5b's nested segmentation header confirms the same 54 segment names and no FDI11 pulp label. Both commands exit 0. This rules out the other package in the specified folder as a source of the missing mask. No runtime executed; pending operator identification of a reviewed mask in the live scene or elsewhere.

- **Goal continuation / new evidence:** Previous turn made progress by identifying the preflight return path and exact package. Operator now explicitly confirms the trajectory is old and was not regenerated. Goal is the complete simulation Home→PreEntry→Entry→new pulp-entry Target→retract→Home, not just a pre-entry preview.
- **Saved-mask inspection:** Python zipfile/io read the nested MRB segmentation NRRD header only. The sole segmentation `Post_surgery_seg.seg.nrrd` lists 54 segments including FDI11 tooth, but no FDI11 pulp. Upper pulp labels cover 16/17/26/27; lower labels cover 31–37/41–47. Scoped source read confirms generation requires exactly one selected-FDI pulp mask. No target can be derived from a missing mask without inventing anatomy.
- **Runtime preparation boundary:** Read matrix `runtime.step65_exact_case` and its existing script. It is explicitly a retired historical x4 diagnostic with a different hardcoded package/task fingerprint, so it cannot be run unchanged as FDI11 acceptance. No runtime or tests executed. Next prerequisite is a reviewed FDI11 pulp mask/newer package; then prepare the scoped full-cycle check with explicit runtime approval and preserve operator-owned processes.

- **Operator:** Reports completing preceding steps and immediate Plan Guarded Approach failure; supplies screenshot, backend terminal excerpt, and FDI11 step6x4 package for testing.
- **Read evidence / commands:** Read compact routing, internal README, Graphify/Ponytail skills, verification protocol, today's log and scoped TASKS/source using cat/sed/rg/tail. `graphify query "TRAJECTORY_TOOL_INSERTION_LIMIT_EXCEEDED effectiveToolProtrusionMm" --budget 1800` exited 0. Used file discovery and Python zipfile/json to inspect the identified package and nested MRB without extraction or modification. Matrix is under `ros2_ws/src/DentoBot/Testing/verification_matrix.json`; overlay-relative and parent-relative Testing paths were absent (ls exit 2).
- **Interpretation:** Facade's `_tool_insertion_evidence` computes endpoint distance and compares against fixed CAD-derived 7.0 minus 0.1 mm. `planApproachPhase` returns that rejection before monitored-state/Goal 1 planning. Screenshot gives 17.922 mm, exceeding capacity by 11.022 mm. Terminal excerpt has two generic MoveIt 99999 failures among successful requests, without action correlation or collision detail; these do not establish the popup's cause.
- **Package boundary:** FDI11 `data/Slicer_Saved/SampleStudy1/FDI11/dentobot-case-step6x4.dentocase` contains the complete FDI11 trajectory and matching Step 5B insertion endpoints. Saved step6 confirmedTask is null. Current live state is not inferred from saved confirmation. Whether the line was regenerated using the new first-pulp-hit implementation remains unknown.
- **Implementation / verification:** Updated TASKS with unprioritized active investigation. No production changes, runtime tests, scene mutations, endpoint changes, guard relaxation or hardware operations. Read-only evidence establishes the rejection path; it does not establish anatomically correct depth or full-task viability.
- **Next:** Inspect saved pulp/endpoint relationship, then propose only the needed exact-case runtime check if source/package evidence is insufficient.

## Assisted pulp endpoint, then P0 shell connectivity

- **Operator statements:** Requested that single/dual assisted trajectories change
  only Target to the first selected-tooth pulp-mask intersection, explaining
  that access drilling ends at the pulp and deeper travel burdens Stage 3.
  Subsequently reported failure of automatic/manual Step 5A boundary workflows
  to generate Step 5B shell/unified template and assigned Priority 0. Screenshots
  show FDI11, four support teeth, 50% terminal coverage and two shell components.
  Latest instruction: complete both prompts one after another.
- **Carryover:** Prior turn prepared docs, matrix entries and a pulp test fixture;
  Luna Max worker ended with a usage-limit error and left no owned source edits.
  Coordinator resumed directly; no model escalation or replacement worker.
- **Source interpretation:** Single/dual assistance shares one generator. Preserve
  inferred rootward directions but clip finite lines to FDI-matched binary pulp
  voxel boxes. For P0, building the full collar before clipping terminal coverage
  can remove its end connections; the exact screenshot cause remains unverified.
- **Implementation:** Added first-mask-hit helper and generator integration with
  world/segmentation/image frame conversion; all endpoints calculated before
  creating nodes. Missing/ambiguous/empty/missed pulp and nonlinear parent
  transforms fail explicitly. Existing/manual lines are untouched. Added
  terminal-plane clipping/closure before collar construction; retained final
  coverage, anatomy clearance and connectivity gates. Updated assisted success
  and shell-error wording. Added focused host regressions and extended existing
  Slicer single/dual assisted fixture. Coordinator updated TASKS, DECISIONS,
  ARCHITECTURE and the verification matrix.
- **Commands/read evidence:** `git status --short`, scoped `git diff`, `rg`,
  `sed`, `head`, `cat`, `ls`, Graphify query; edits through `apply_patch`.
  Earlier searches for nonexistent logic_geometry.py, DENTOPatient* and absent
  trajectory tests returned expected missing-path errors. Shell startup also
  printed `Failed to create stream fd: Operation not permitted`; source reads
  and patches still returned successfully. No runtime command was attempted.
- **Verification:** NOT_RUN. Requested approval for focused host pytest,
  changed-file py_compile and git diff check under the project protocol.
  Added a second approval request covering the shell VTK regression and asked
  for the actual FDI11 package path. No approval response recorded yet. The
  prepared tests are not passing evidence. No Slicer/ROS/hardware action,
  Git publication, Drive sync or engineer-owned record access.
- **Next bounded action:** Finish source readback/Graphify refresh; execute the
  proposed host checks on approval. Verify actual single/dual Slicer endpoints
  and the FDI11 shell/unified build in an approved isolated case workflow before
  claiming both requests complete.

- **Source readback / graph:** Scoped diff confirmed the production additions:
  binary-mask endpoint clipping and terminal collar closure. `graphify update .`
  completed, exit 0: 7,114 nodes, 11,168 edges, 625 communities. Existing native
  partial-extraction warnings (nine files) and retained-node warning remain;
  this is navigation maintenance, not test acceptance. Slicer's official
  [segmentation API](https://apidocs.slicer.org/main/classvtkMRMLSegmentationNode.html)
  confirms that `GetBinaryLabelmapRepresentation` returns a copy and a Boolean
  success result; missing representations require explicit creation, so this
  change rejects a missing source mask rather than inventing one from a surface.

## Goal continuation — verification approval still pending

- **Classification:** Previous goal turn made source progress. This continuation
  read the actual scoped diff and shell-cache/geometry-state references using
  `git diff`, `rg` and `tail`; no additional source defect was established.
- **Boundary:** No operator reply approving the proposed checks or identifying
  the FDI11 case has arrived. Automatic goal continuation is not explicit
  approval under the verification protocol. Tests and runtime remain NOT_RUN;
  both requests remain unaccepted. Next action is the pending focused host
  verification once approved, followed by the identified-case runtime plan.

## Approved host verification — pulp endpoint and P0 collar

- **Operator:** Replied `approved` to the proposed focused host checks.
- **Execution:** From `ros2_ws/src/DentoBot`, ran
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-verification/pulp-shell-20260908/pycache python3 -m pytest -q -p no:cacheprovider Testing/test_assisted_pulp_endpoint.py Testing/test_support_boundary_bridge.py`.
  Result: **3 passed in 16.18s**, exit 0. Full output: evidence directory
  `pytest.log`; compact results: `result.json`.
- **Static:** With the same temporary bytecode prefix, `python3 -m py_compile`
  passed for DENTOTrajectoryGeometry.py, DENTOTemplateGeometry.py, workflow
  logic_workflow.py, runtime.py, widget_planning_focus.py, logic_patient_shell.py,
  slicer_tests.py and both new host regression files (nine files); exit 0.
  `git diff --check` passed, exit 0.
- **Findings:** Exact occupied-voxel first-hit regression covers one/two lines,
  separated pulp hits, reversed/diagonal lines, nonzero extent, missing hit,
  Entry in pulp, empty mask and nonfinite input. Synthetic VTK tests establish
  two disconnected regions for the former terminal-cut collar and one
  watertight region with closure before clipping, in original and rotated
  frames. No production-code correction was needed after this run.
- **Documentation:** Updated TASKS/DECISIONS evidence boundaries. Initial combined
  patch failed context matching without changing those files; exact scoped
  Python string replacements completed. Existing shell startup stream-fd
  warnings did not prevent successful commands.
- **Boundary / next:** Tests prove the bounded geometry changes, not Slicer
  integration or the exact FDI11 shell/unified build. Asked for its saved path
  or confirmation it exists only in the active session. No Slicer/ROS/hardware,
  publication, external sync or engineer-owned record operation was performed.

## FDI11 case located; runtime check prepared

- **Operator:** Identified a `stepx4.dentocase` in the FD11 folder.
- **Read-only discovery:** `rg --files --hidden --no-ignore` and scoped `ls`
  found exactly one package in FDI11: `dentobot-case-step5b.dentocase` (62 MB).
  No stepx4 file was present there. Read package membership and selected lineage
  attributes with Python zipfile/json; it retains current support draft,
  visible support and docking records, boundary, plane and insertion direction.
- **Preparation:** Added `Testing/run_dentobot_pulp_shell_smoke.py` and matrix
  row `runtime.pulp_shell_fdi11`. The script runs the existing assisted Slicer
  test, then opens the specified case in its isolated scene, validates FDI11,
  runs unchanged preflight/blockout/shell/fusion methods and verifies the source
  package checksum is unchanged. Explicit phase markers identify the first
  failure. It does not approve stale inputs, mutate saved trajectories or write
  the input package.
- **Approval boundary:** Requested separate runtime approval for ownership
  inspection and, only if free, pinned-container Xvfb/Slicer execution with
  900-second timeout. Existing processes will not be stopped. Runtime NOT_RUN
  until that approval arrives. Host-check approval remains valid.
- **Preparation checks:** New smoke-script `py_compile` and final
  `git diff --check` passed, exit 0. `graphify update .` completed, exit 0;
  full navigation-refresh output retained in the host evidence directory.

## Approved exact-case completion — boundary → preview → patient shell

- **Operator:** Approved the isolated runtime and explicitly requested checking
  the boundary loop, visible preview creation and then patient-shell error.
- **Ownership / environment:** `docker ps` found the pinned container;
  `docker exec ... pgrep -af 'SlicerApp-real|move_group|collision_guard|colcon|simulation.launch'`
  returned no matches (exit 1). Image inspection confirmed
  `sha256:f71da23aaa35161730536530ed18c594ccb7766ed8d3a35cfd68d0f385280faa`.
  Initial sandboxed Docker execution was denied before launch; approved
  escalation ran the already-authorized check. No pre-existing session was stopped.
- **Command:** `docker exec dentobot-slicerros2 sh -lc 'timeout 900s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_pulp_shell_smoke.py'`.
  First log: `/tmp/dentobot-verification/pulp-shell-20260908/slicer.log`.
- **Assisted endpoint evidence:** `DENTOBOT_ASSISTED_PULP_PASS`: actual Slicer
  single/dual generator assertions pass, including first boundary at expected
  voxel face, unchanged Entries, collinearity with original rootward targets,
  shorter length, standard lineage and persistence. Existing source/API
  behavior did not require a further production patch.
- **Saved FDI11 evidence:** Boundary has 50 control points. Regenerated preview
  exactly matches screenshot counts: 3,363 points, 6,028 triangles, all five
  selected teeth, no omissions, 50% terminal coverage and two terminal planes.
  Open preview patch edges are expected at this intermediate surface stage.
  `DENTOBOT_FDI11_SHELL_PASS` and `DENTOBOT_PULP_SHELL_PASS` emitted; process
  exit 0. Shell: one surface region, zero invalid edges. Unified output:
  74,988 triangles, zero invalid edges and one occupied-volume region. Its
  three closed boundary-surface regions are not three disconnected solid
  volumes; the volume-connectivity metric is one.
- **Fresh automatic-loop evidence:** After verifying the first process exited,
  repeated the same serialized command with `DENTOBOT_TEST_AUTO_BOUNDARY=1`
  inside the container command. This skips the unchanged successful assisted
  assertions and regenerates the boundary from the saved support plane before
  preview/shell/fusion. Auto plane intersects 5/5 teeth; output is 50 points.
  Same preview and successful shell/fusion counts. All phase PASS markers and
  final exit 0 recorded in `slicer-auto-boundary.log`.
- **Integrity:** Both runs checked unchanged package SHA-256
  `d3f9dbf07e1e19c317cfa8ba805788c95e5293a3086f6af8f93ffc8981dfb9a5`.
  Source is FDI11 `dentobot-case-step5b.dentocase`; no package was overwritten.
  Final elevated process inspection again returned no matches, exit 1. No
  Slicer/ROS/MoveIt process remained from this work. ROS was not loaded or
  started; its module-unavailable warnings were diagnostic noise. No VTK leak,
  traceback or abort matched either retained log.
- **Acceptance / next:** Both requested development changes are source- and
  runtime-verified in their bounded scope. TASKS/DECISIONS updated; result.json
  includes both runtime summaries. Operator can reload, regenerate assisted
  trajectories as needed, and build from the current Step 5A boundary/preview.
  Anatomical, manufacturing and full Step 6/Track A acceptance remain separate.
  No hardware, publication, external sync or engineer-owned records touched.

## CRD lag / ~2 FPS diagnosis

- **Operator:** Reported CRD very laggy at about 2 FPS.
- **Evidence:** `crd-fix --diagnose` from CRD session; host process/Wi-Fi/display checks. CRD on `:20`, resolution `1875x925` (`CRD_78`), GL `llvmpipe`. Ethernet `enp128s31f6` unavailable (unplugged). Wi-Fi `iitmwifi` RX bitrate oscillating ~8–48 MBit/s HE-MCS 0–2 while TX stays ~200+ MBit/s. CRD logs: estimated bandwidth ~1.12 Mbps below configured min bitrate ~1.24 Mbps; ICE `.local` resolve failures; RTCP RTT timeout. Concurrent load: `SlicerApp-real` ~165% CPU with ~0.95 GiB swap; `codex-switcher` WebKit ~67% CPU; load averages ~4–5.
- **Interpretation:** ~2 FPS matches WebRTC bandwidth starvation on campus Wi-Fi plus continuous dirty frames from software-rendered Slicer, not a CRD service crash. Project policy already treats CRD/llvmpipe as functional-only, not FPS acceptance.
- **Mitigation applied:** Temporary `renice +10` on SlicerApp-real and WebKitWebProcess (CRD host renice denied without privileges). No CRD restart (unsafe from remote). No Slicer stop without operator request.
- **Next (operator):** Prefer wired Ethernet; in the CRD client lower quality / shrink window; pause or minimize Slicer 3D when not interacting; optionally quit `codex-switcher` if unused. If still bad after Ethernet, re-check with `crd-fix`.

## CRD Firefox will not open

- **Operator:** Reported Firefox will not open on CRD.
- **Evidence:** CRD healthy on `:20` (`crd-fix`). Host `xdpyinfo` and non-snap
  `xclock` work. Snap Firefox exits immediately with
  `Error: cannot open display: :20.0`. Same failure for Thunderbird snap.
  Inside `snap run --shell`, `XOpenDisplay` succeeds and the Firefox binary
  launches; `snap run firefox` fails because gnome-platform `desktop-launch`
  sets `GDK_BACKEND=wayland` after detecting stale
  `/run/user/1000/wayland-0` from the local console.
- **Fix verified:** `DISABLE_WAYLAND=1 /snap/bin/firefox` opens a Navigator
  window on the CRD display (`Mozilla Firefox` via `xwininfo`).
- **Durable host changes:** `~/.chrome-remote-desktop-session` exports
  `DISABLE_WAYLAND=1` and syncs X cookies into `~/snap/*/common/.Xauthority`;
  `~/.local/bin/firefox` wrapper; local
  `firefox_firefox.desktop` Exec override. Documented in `SETUP.md`.
- **Boundary:** No CRD restart performed. Menu launches should use the local
  desktop override immediately; full session-wide env applies on the next CRD
  session start.

## FDI11 combined allowance, guide warnings and guarded-return continuation

- **Operator statement:** Keep the confirmed drilling Target at 6.0 mm, count a
  provisional 0.5 mm guide/shell contribution inside a 6.5 mm maximum, and
  accept positive non-contact non-burr-link↔configured-guide distances below
  1 mm as diagnostic warnings. Retain the diagnostics for the later revamp and
  finish the complete approach, drilling, retraction and Home simulation.
- **Coordinator interpretation and boundary:** The 0.5 mm value is an
  operator-specified simulation assumption, not a template measurement. It
  changes the insertion accounting only. Actual mesh collision remains a hard
  rejection; burr↔guide retains 0.1 mm; self and unrelated/unconfigured world
  pairs retain 1 mm. No hardware operation was authorized or attempted.
- **Implementation:** Added the 6.000 + 0.500 <= 6.500 mm combined insertion
  preflight, bounded guide-warning evidence in native acknowledgements/bridge,
  full-chain and persistent motion diagnostics, `CompletedWithWarnings` result
  propagation, and `retraction` as a guarded reverse corridor phase. Return Home
  now reverses only accepted drilling/terminal waypoints axially, reverses the
  accepted approach history, verifies the captured monitored Home state, and
  then uses the existing strict Home application. Partial/stale/mismatched
  histories fail closed. Native collision rejection remains first and
  authoritative. Added signed contact-depth and contact-position evidence to
  native collision rejections.
- **Checks:** Python compilation passed. Focused bridge/facade/state/config/
  planning tests first passed 88/88; final combined focused suite including the
  description package passed 96/96 in 0.88 s. `git diff --check` passed. The
  `dentobot_moveit_config` native package rebuilt twice after the guard changes,
  both exit 0. The serialized native phase-guard smoke passed every reported
  case, including configured 0.05 mm burr-guide rejection, configured 0.2 mm
  acceptance, unrelated 0.5 mm world-pair rejection, actual-collision checks,
  and accepted/reported retraction. The source package hash remains
  `ffc0940899d2396a8ae0c942f0b9d8e9d30a059dc8995807e4998e2f45677f0b`.
- **Exact attempt 1:** Combined insertion passed at exactly 6.500 mm. Route via
  workspace sample 218 accepted four positive spindle/template warnings
  (0.662547 and 0.190292 mm observed) and rejected actual
  final-template↔`pneumatic_spindle-Copy` collision at the last tested Stage 3
  waypoint. No preview began.
- **Exact attempt 2 error:** A diagnostic launch mistakenly used
  `--no-main-window`; the harness stopped at `Could not find main window` before
  planning or state application. The process group was torn down. This command
  error is not planner evidence.
- **Exact attempt 3:** A different planned route rejected burr↔template
  clearance of 0.012463 mm against the retained 0.1 mm threshold before Stage 3.
  No preview began.
- **Exact attempt 4 / quantitative blocker:** Route via workspace sample 218
  accepted four positive guide warnings (minimum accepted distance 0.426370 mm)
  and then rejected signed final-template↔`pneumatic_spindle-Copy` penetration
  of 0.026243 mm at Stage 3 drilling checkpoint 16. Native contact position in
  base metres was `[-0.075091084, -0.005717826, 0.130533654]`. This is an actual
  collision, not the bypassed positive 1 mm clearance condition. It agrees with
  the retained offline saved-mesh sweep, which intersects the housing at 4–6 mm
  for every one of 12 sampled axial rolls. No exact approach preview, Target,
  retraction, Home or repeat cycle was executed or claimed.
- **Integrity and teardown:** `sha256sum -c` passed for the operator case. Every
  test stack used a process-group trap; final container inspection found no
  Slicer, MoveIt, robot-state-publisher, ros2-control or simulation launch from
  this work. The repeated optional ROS2Tests `psutil` install permission error
  and Slicer shutdown leak reports are retained environment noise; neither was
  used as motion evidence.
- **Command-record corrections:** The first checksum verification was invoked
  from the nested source repository, so the manifest's workspace-relative case
  path was not found; repeating the same manifest check from the workspace root
  passed. Final `graphify update .` completed with 5,662 nodes, 9,124 edges and
  409 communities; it reported that semantic community labels can be refreshed
  separately, which is unrelated to planner acceptance.
- **Next bounded action:** Supply measured/reviewed guide and housing geometry
  or explicitly authorize a new simulation-only actual-contact warning policy.
  Under the current recorded rule, the existing 6.0 mm endpoint must remain
  blocked. Once the geometry/policy prerequisite changes, run the already
  approved exact full cycle and repeat gate without changing the saved case.

## FDI11 bounded stationary-housing contact continuation

- **Operator statement:** The robot's ultimate policy need not be entirely
  collision-free in its constrained workspace. Guide-template contact may be
  safe but should be limited to touching the shell; the rotating tool must not
  grind against it because the trajectory is sensitive. Current spindle
  dimensions remain provisional pending teammate review.
- **Coordinator interpretation:** Permit only
  `pneumatic_spindle-Copy`↔configured-guide contact, with at most 0.5 mm
  penetration, during terminal contact, drilling and retraction. Retain 0.1 mm
  positive burr-guide clearance and every unrelated/self collision, joint,
  session, corridor, endpoint, monotonic-drilling and reverse-retraction hard
  gate. Re-run collision evaluation with only the exact admitted pair allowed
  so it cannot conceal another collision. This is a functional contact model;
  it does not establish force, deformation, friction or physical trajectory
  accuracy.
- **Delegation:** Reused one Luna Max implementation worker with ownership only
  of the native guard, bridge/façade diagnostic propagation, focused tests and
  the two runtime harnesses. The coordinator retains controlled documents,
  source review, builds, serialized runtime execution and acceptance.
- **Planned evidence:** Static compile/diff checks, focused pure tests, native
  `dentobot_moveit_config` build, phase-guard regression, unchanged exact-case
  checksum, then the saved FDI11 approach→drilling→retraction→Home cycle and a
  fresh repeat. Record warning kind, penetration, pair, phase and sequence for
  both cycles and verify teardown.
