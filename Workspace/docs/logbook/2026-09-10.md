# 2026-09-10 — PreparedBranch documentation reset

## Operator observation and scope

Operator reports faulty unified-template creation, stale Step 6 import behavior,
scope creep and redundant/conflicting documentation. Requests documentation
cleanup and a clear plan before further implementation. Main workflow must stay
single-target with one trajectory or an explicit pair; multi-target/three-slot
testing is an optional alternative. Step 6 must select a PreparedBranch with its
shell/template/Step 5C identity and preserve unchanged shared setup.

## Reconciliation and changes

- Reopened `S6-REUSABLE-CASE-SETUP`; withdrew the prior source-complete claim.
  TASKS.md now owns one current work order. Stage 3 remains incomplete; Studio,
  batch studies, SQLite and platform upgrade are separate gated work.
- Reduced AGENT_CONTEXT from 416 to 64 lines. Removed repeated historical
  checkpoints, obsolete .dentostudy and automatic reconnect directions.
- Consolidated TASKS and DEVELOPMENT_PLAN, removed duplicate active rows and
  retired queues, and retained legacy task IDs as aliases. Added one detailed
  PreparedBranch correction/acceptance plan in DEVELOPMENT_PLAN only.
- Reduced PROJECT_CONTEXT from 482 to 246 lines by removing repeated dated
  status narratives. DECISIONS records the explicit supersession; ARCHITECTURE
  distinguishes current unaccepted source from intended ownership. Traceability
  now explicitly scopes old test results to their recorded revision/case.
- Archived PROJECT_CHECKPOINT_2026-08-12.md into the existing
  `archive/2026-09-01/` directory; content remains recoverable there. Unique
  dated logs, hashes, failure/retry history and diagnostic artifacts were kept.
  No engineer-owned record was read or changed.
- Corrected the package README's blanket `pytest -q Testing` instruction to
  route through the canonical smallest-sufficient matrix/protocol.

## Inspection and evidence

- Read AGENTS, relevant TASKS entries, controlled routing/architecture/plan/
  decision/evidence sections and package README. Used bounded `rg`, `sed`,
  `cat`, `wc`, Git status/diff and Graphify query. An initial combined read
  truncated output; smaller reads recovered the relevant contracts. A temporary
  in-memory document transformation hit a missing section marker; corrected
  before patch application, with no partial file write from that error.
- Read-only source audit confirms raw trajectory activation, per-slot copied
  guide records, absent explicit pairing intent, and incomplete whole-branch
  Step 5C validation. The legacy three-trajectory parser rejects the guide;
  safe semantic migration requires separate treatment from corrupt packages.
  This does not establish the root cause of every observed geometry regression.
- Documentation edits used apply_patch. `git diff --check` passed. Product
  source/test/robot diff SHA-256 before and after documentation cleanup is
  `d0d6777bee2d518559c9c3c49d19d42bf1325ec38b563c5081a6c66dc42c1702`;
  the only package change in this turn is its Markdown README.
- Final documentation check emitted `DOC_RECONCILIATION_CHECK_PASS`: zero
  duplicate active task rows, zero missing checked local Markdown targets,
  and the historical checkpoint exists only in its archive location.
- No product tests, build, Slicer/ROS/MoveIt runtime, geometry/planner/collision
  edits, commit, push or external sync. Earlier 29/84 test counts do not verify
  the corrected PreparedBranch contract. No fresh runtime/operator acceptance.
- Graphify was used for read-only routing. No code changed in this turn, so
  no code graph update was required; it remains required after implementation.

## Next bounded action

Review DEVELOPMENT_PLAN's PreparedBranch correction. Then audit all ownership,
build/save/load/select/invalidation callers before the smallest product patch.
Gate the normal single-target workflow first, explicit pairing second, optional
testing branch switching third. Do not proceed to Stage 3 or the major revamp
until the recorded dependencies are accepted. AGENT_CONTEXT is the compact
handoff; the conversation itself has not been erased or technically reset.

## Immediate blocker handoff clarification

**Operator request:** Put the immediate development blockers and next task
plainly in the Development Plan and TASKS.

**Documentation change:** Added one ordered blocker table to `TASKS.md` and one
bounded next-implementation package to `DEVELOPMENT_PLAN.md`. The active ID,
priority, dependencies and correction contract are unchanged. `TASKS.md` still
owns live status/order; the plan owns implementation and acceptance boundaries.
No engineer-owned record was read or changed.

**Evidence boundary:** This was documentation-only reconciliation. No product
source, tests, build, Slicer/ROS/MoveIt runtime, robot action, commit, push or
external sync was performed. The next task remains a caller/ownership audit
followed by the smallest single-target PreparedBranch gate; optional testing,
Stage 3 and Studio remain downstream.

**Commands and results:** Used bounded `rg`, `sed`, `readlink`, `git status` and
`git diff` to reconcile `S6-REUSABLE-CASE-SETUP` across TASKS, the Development
Plan, DECISIONS, AGENT_CONTEXT, the package routing map and this logbook. The
first combined `apply_patch` matched neither the wrapped final logbook lines nor
any file, so it exited without a write; the corrected patch applied all three
documentation edits. Section-count/link assertions passed. Final
`git diff --check` exited 0; Git emitted three non-fatal
`Failed to create stream fd: Operation not permitted` warnings while inspecting
the already-dirty documentation files.

## PreparedBranch caller audit and final coding gate

### Operator statement and scope

The operator approved the correction plan for the audit phase, not blind
implementation. The implementation target is limited to three deliverables:
one authoritative selected branch ID, one centralized eligibility function and
one atomic activation of trajectory selection, pairing intent, shell, template,
guide references and matching Step 5C verification. The operator identified
Step 4C/insertion-direction ownership as the dangerous unresolved row and
explicitly prohibited classifying it as shared merely because current storage
is global. The audit output is the final gate before coding. Optional 32 × 3
testing, Stage 3, collision geometry and Studio remain out of scope.

### Agent interpretation

This authorized a read-only caller/ownership audit and controlled-document
updates only. It did not authorize product-source changes, tests, runtime use,
robot action or downstream milestone work. The existing task ID and priority
remain `S6-REUSABLE-CASE-SETUP` / P0.

### Caller evidence

| Operation | Production path inspected | Finding |
|---|---|---|
| Build | Step 5B item-change/build handlers and `_createOrUpdateFinalPrintableTemplate` | The UI stores repeated checked trajectory references, but the actual final-template builder constructs its selection from `parameterNode.trajectoryLine` only. |
| Save / registry | `_createCaseBundle`, `prepareDentoCaseSchema2ForSave`, `syncDentoCaseTrajectoryRegistry` and `DENTOStep6State` registry helpers | Registry sync scans MRML, infers pairing from template references, copies one guide dictionary into each trajectory slot and derives root selection from the raw trajectory pointer. No explicit pairing-intent or Step 5C revision field owns eligibility. |
| Load | `_openCaseBundle`, schema-2 hydration and registry comparison | Package load remains offline as required. Hydration rebuilds/compares registry state but does not resolve and atomically activate one eligible selected branch. |
| Select | target/trajectory/Step 5B selection handlers and `activateDentoCaseTrajectory` | Raw trajectory selection partially changes global target/ROI/trajectory/shell/template/guide pointers. A same-target docking fallback is allowed, pairing and Step 5C identity are absent, and MRML `StartModify` batching supplies no prevalidation/rollback guarantee. Independent selectors can split the active state. |
| Import | `importStep6PlanningContext`, `validate_planning_context` and Step 6 freshness checks | Import checks required global node presence plus scattered Geometry/Verification/target tokens. It does not consume a single branch-eligibility result or bind the complete Step 5C verification identity. |
| Delete / invalidate | `deleteTrajectoryNode`, `_activePlanningDownstreamEntries`, dependency-impact traversal and trajectory observers | Downstream traversal starts from active parameter-node pointers. Inactive stored branch records and their evidence revisions are not governed by one complete invalidation path. |
| Step 4C / insertion | `targetDockingTrajectoriesForTarget`, `createOrUpdateTargetDockingAssembly` and insertion-direction builders | Step 4C collects all complete trajectories for the target tooth and permits up to three. Docking stores ordered trajectory/support provenance; insertion stores its support surface and, when derived, its source trajectory. These are branch-dependent upstream artifacts, reusable only for exact dependency matches. |

### Ownership conclusion

CBCT/anatomy is case-shared. Target identity is case-target state referenced by
the branch. Trajectories, explicit pairing intent, patient shell, unified
template, guides and Step 5C evidence revision are PreparedBranch-owned. Step
4B support/Step 5A visible surface is reusable case-target-derived state only
under exact dependency equality. Step 4C docking and insertion direction are
branch-dependent upstream state, never case-shared by storage accident. Jaw
opening and landmarks/base/Home are reusable shared runtime setup only while
their inputs remain unchanged; saved configuration is not live validity.
Collision acknowledgement, plans, previews, guards and diagnostics are branch-
runtime state and invalidate on branch switch.

### Commands, changes and evidence boundary

- Read the full Graphify and Ponytail skill instructions, then used `graphify
  query` for PreparedBranch ownership/callers. Two bounded `graphify path`
  attempts returned no directed path; this reflects the available graph edges,
  so targeted source reads supplied caller authority.
- Used bounded `rg`/`sed` reads over the registry, case-bundle, guide-build,
  docking/insertion, Step 5C verification, Step 6 import, selection,
  deletion/invalidation and focused-test paths. Used `git status`/`git diff` to
  preserve the already-dirty checkout. The first documentation read used the
  overlay-relative `Workspace/docs` path from the repository parent and failed
  without a write; `rg --files` resolved the physical checkout path.
- Updated only `TASKS.md`, `DEVELOPMENT_PLAN.md`, `DECISIONS.md`,
  `ARCHITECTURE.md`, `AGENT_CONTEXT.md` and this dated logbook using
  `apply_patch`. One DECISIONS patch failed context matching and made no write;
  a smaller patch applied cleanly.
- No product source, test, build, Slicer/ROS/MoveIt runtime, robot operation,
  commit, push or external synchronization was performed. The audit explains
  ownership defects; it does not prove the cause of every reported geometry
  symptom or supply runtime acceptance. Non-fatal Git inspection warnings were
  recorded rather than treated as product failures.

### Next bounded action

Operator reviews this audit and either accepts or corrects the ownership/coding
boundary. If accepted, implement exactly the three recorded deliverables in the
existing registry/workflow, then propose the cheapest focused single-target
checks under the verification protocol. Do not begin optional 32 × 3 testing,
Stage 3, collision/geometry-policy changes or Studio work.

### Documentation verification

Final `git diff --check` over the six controlled records exited 0. Bounded
anchor searches confirmed the audit-complete/final-approval state, exact three-
deliverable boundary, selected-branch/eligibility/atomic-activation wording,
4C/insertion classification and downstream 32 × 3 prohibition across TASKS,
DEVELOPMENT_PLAN, DECISIONS, ARCHITECTURE, AGENT_CONTEXT and this logbook. Git
status showed only those already-dirty/untracked controlled records in the
scoped check. Git again emitted the same three non-fatal stream-fd warnings;
they did not change command exit status or verification outcome.

## Documentation commit and publication gate

The documentation-only commit was created locally as `482bad4` with message
`docs: record PreparedBranch audit and blockers`. Its staged payload contains
11 documentation paths: the controlled Workspace records, package routing
README, the archived checkpoint move and the 2026-09-10 logbook. Product code,
tests, UI and robot-description changes remained unstaged.

An explicit `git push origin main` was attempted. The safety reviewer rejected
the external write because the exact 11-file payload includes substantial
pre-existing documentation cleanup beyond the six latest audit records. No
remote write occurred; the local commit is preserved pending explicit scope
confirmation. No workaround or alternate push path was used.

After the operator confirmed the exact 11-file scope, the current commit was
published with `git push origin main`. The command succeeded:
`8c86e8b..81746fc main -> main`. The remote now contains the documentation
commit; all unrelated product/test/UI/robot-description changes remain
unstaged locally.

## PreparedBranch correction implementation and focused verification gate

### Operator statement and bounded contract

The operator accepted the audited correction plan and authorized exactly three
deliverables: one persisted authoritative selected PreparedBranch ID; one
centralized eligibility result shared by Step 5C, selection, load and Step 6;
and atomic activation of trajectory/pairing, matching Step 4C plus insertion
state, shell, template, guides and Step 5C revision. Failed build/activation
must preserve the last valid branch. The requested acceptance target is the
single-target `4A→4B→4C→5A→5B→5C→6` path. Optional 32 × 3 UI, Stage 3,
collision/geometry-policy changes, Studio, database, batch and unrelated
refactors remain prohibited.

### Implementation

- Bumped the trajectory-registry contract to 2.0 while retaining an in-memory
  1.0 reader. PreparedBranches are stored once under `prepared_branches`; slots
  retain branch IDs only; `selected_branch_id` is the sole persisted selection
  authority. Legacy pairs without explicit intent migrate as stale
  `LegacyUnverified` records.
- Extended the existing guide-set upsert rather than adding another registry.
  Each branch binds ordered trajectory IDs, primary trajectory, `Single` or
  `ExplicitPair` intent, exact Step 4C docking, insertion direction, shell,
  template/guide nodes, dependency revision and verification revision.
- Added `evaluatePreparedBranchEligibility`. Its result vocabulary is the
  approved `VALID`, `MISSING_TRAJECTORY`, `PAIRING_NOT_CONFIRMED`, `STALE_4C`,
  `MISSING_TEMPLATE`, `STEP5C_MISMATCH`, `UPSTREAM_CHANGED` and
  `LEGACY_UNVERIFIED`. Step 5C display/export, hydration, branch selection and
  Step 6 import now route through this result.
- Added rollback-safe `activateDentoCasePreparedBranch`: all destination nodes
  are resolved and validated before mutation; the transaction swaps primary
  and repeated trajectory/pairing selection, target/ROI, 4C plane/assembly/yaw,
  insertion, shell, four guide references, final template and selected branch
  JSON together. Any exception restores every prior pointer/reference/string.
  Raw trajectory choice remains preparation-only when no eligible branch exists.
- Step 4C now consumes the explicit one/pair preparation selection instead of
  scene inventory, and final-template input validation requires exact agreement
  with that Step 4C selection. A new selection builds new final/guide nodes
  rather than staling or overwriting another branch. Failed verification does
  not replace the last selected valid branch.
- The existing Slicer visible-support geometry scenario gained a focused mode
  that stops after one target/trajectory reaches Step 6 import and asserts the
  selected ID, centralized `VALID` result, exact selected references and
  failed-activation preservation. The reusable-case runner invokes only that
  mode; it no longer invokes the old research-template or 32 × 3 scenario.
  No geometry/planner/collision algorithm was changed.

### Verification and errors

- `python3 -m py_compile` first failed before source evaluation because the
  checkout's existing `__pycache__` was unwritable. One retry with
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-pycache` passed for every scoped Python
  source/test/runner. This is one filesystem-cause retry, within policy.
- The first pure registry run found a real legacy-migration defect: a
  tooth-level pair was attached to the empty third slot. Added the missing
  trajectory-membership guard; the one allowed rerun passed 21/21.
- Full host pure selection passed 85/85. Matrix-equivalent container sets
  passed 67/67 for planning/facade/state and 39/39 for case-bundle/placement/
  state. `git diff --check` passed.
- `graphify update .` first failed with `[Errno 1] Operation not permitted`.
  Permission-approved refreshes rebuilt the graph after the final transaction
  refinement; final output reports 6,000 nodes, 9,601 edges and 437
  communities, with `graph.json`, `graph.html` and `GRAPH_REPORT.md` updated.
- Runtime preflight found the serialized container occupied by PIDs 751/769/770
  running `test_DENTOWorkflowSegmentationReviewLogic` for approximately eight
  hours. No overlapping Slicer run, process termination, container restart,
  ROS/MoveIt action or robot motion was attempted. Consequently
  `DENTOBOT_REUSABLE_CASE_PASS` is not yet claimed.
- The checkout was already broadly dirty before implementation. Edits were
  layered only onto the audited registry/case-bundle/docking/guide/Step 6/UI
  seams and focused tests; unrelated existing changes were not reverted. No
  commit, push or external synchronization was requested or performed in this
  implementation turn. No engineer-owned record was read or changed.

### Next bounded action

Leave the existing Slicer process untouched until it exits or the operator
explicitly authorizes clearing it. Then run only matrix target
`runtime.s6_reusable_case`, require `DENTOBOT_REUSABLE_CASE_PASS`, and record
the exact log/result. On failure, diagnose that single target within the
three-attempt ceiling. Do not proceed to explicit-pair/32 × 3 UI, Stage 3,
collision/geometry policy, Studio, database or batch work.

### Runtime blocker resolution and focused acceptance

The operator explicitly authorized terminating the stale Slicer process tree
and immediately running the single focused target. A read-only recheck matched
PIDs 751/769/770 to the same `test_DENTOWorkflowSegmentationReviewLogic`
command at approximately 12 hours 48 minutes elapsed. `SIGTERM` ended all three
processes cleanly; no `SIGKILL` or container restart was required. A follow-up
process listing was empty.

The first evidence-capturing launch was denied Docker-socket access by the
sandbox and exited 1 before Slicer started. The permission-approved retry ran
only `runtime.s6_reusable_case`:

```text
docker exec dentobot-slicerros2 sh -lc 'timeout 300s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_reusable_case_smoke.py'
```

The retry completed in approximately 16.05 seconds, exited 0 and emitted
`DENTOBOT_REUSABLE_CASE_PASS`. The saved log contains no `FAILED`, traceback or
assertion marker, and a post-run process check confirmed no Slicer process was
left behind. Evidence is:

- `/tmp/dentobot-verification/prepared-branch-single-20260910/runtime.log`
- `/tmp/dentobot-verification/prepared-branch-single-20260910/result.json`

This accepts the requested automated single-target
`4A→4B→4C→5A→5B→5C→6` gate, including selected-branch persistence,
centralized `VALID` eligibility, exact activation references, failed-activation
preservation and Step 6 import. Repeated ROS2-module-not-registered messages are
expected for this offline geometry/import target; no ROS/MoveIt activation or
robot action was requested or performed. The plan's normal-window operator
review remains separate and is the next bounded action. Explicit-pair/32 × 3,
Stage 3, geometry/collision policy, Studio, database and batch scope remain
untouched.

## DENTO-NOTE Priority 2 — automatic Step 4B support selection

### Operator observation

The operator proposes automatically selecting four adjacent support teeth,
two on either side of the target, with manual intervention for edge cases. For
FDI31 the expected default is FDI32, FDI33, FDI41 and FDI42. The supplied
Step 4B screenshot also shows the selected lower jaw split across two rows in
the narrow panel; the operator wants each jaw presented as one dental-arch row
during the GUI revamp.

### Triage and bounded contract

- Recorded as `W4B-P2-SUPPORT-AUTO`, Priority 2, backlog. This extends the
  existing Step 4B support-membership owner and does not create another support
  registry or change the current P0 PreparedBranch order.
- Use explicit permanent-tooth arch order. For the lower jaw this crosses the
  midline as `... FDI33, FDI32, FDI31, FDI41, FDI42, FDI43 ...`; the upper jaw
  follows the equivalent `... FDI13, FDI12, FDI11, FDI21, FDI22, FDI23 ...`.
  The suggested set is the nearest two arch positions on each side of the
  target. Only present, reviewed tooth segments are selected automatically;
  the rule never skips a missing position to silently substitute a farther
  tooth, crosses to the opposing jaw, or includes the target.
- Automation is a reviewable initial suggestion, not an automatic lock or an
  anatomical-suitability claim. Missing or unreviewed segments, distal arch-end
  targets, discontinuous dentition, or fewer than two usable neighbors on one
  side must remain visibly incomplete and allow manual add/remove before the
  existing Step 4B lock action.
- The layout increment shows the selected jaw in one arch-ordered row, keeping
  target, support, absent, and unavailable states distinct and usable in
  constrained width. Responsive scrolling or an equivalent presentation may
  be selected during the GUI revamp. This is an acceptance requirement of
  `UI-P3-01`, while the feature retains its operator-assigned Priority 2.

### Evidence and next action

- Evidence is the operator statement and supplied screenshot
  `codex-clipboard-b7112ef9-4ac3-448c-9f43-b5767c3b6e58.png`. It establishes
  the layout limitation, not anatomical correctness.
- Read `TASKS.md` first and followed the Step 4B ownership references in
  `ARCHITECTURE.md` and `DECISIONS.md`. Updated only controlled TASKS and this
  logbook entry. No product source, test, Slicer/ROS/MoveIt runtime,
  engineer-owned record, commit, push, or external sync was used.
- After the current P0 correction, inspect the existing Step 4B arch-button
  model and add the smallest pure arch-neighbor check covering FDI31, a normal
  posterior target, both jaws, missing segments, and terminal teeth. GUI-revamp
  acceptance must verify the one-row layout and manual override in a narrow
  normal Slicer window.

## Immediate P0 — FDI31 assisted target and Step 4A smooth masks

### Operator statement

- The operator supplied an orthogonal CBCT/mask screenshot and a 3D screenshot
  for FDI31. The trajectory Target appears to intersect the pulp in 2D but is
  visibly separated from the pulp in 3D. They requested analysis and an optimal
  fix now.
- The operator also states that smooth masks are not working, explicitly
  promotes restoration to Priority 0, and disallows deferral.
- No operator statement was interpreted as permission for robot motion,
  patient-facing use, mutation of the active scene, termination of Slicer, or
  verification execution.

### Existing-plan lookup and priority delta

- Searched `TASKS.md` first using assisted trajectory, pulp, intersection,
  smooth mask, Step 4A, MPR and backtracking terms. Followed
  `S4A-PULP-ENDPOINT` and `W4-U-02` into the development plan, decisions,
  architecture, reproducibility record and prior logbook evidence.
- Reopened `S4A-PULP-ENDPOINT` as an immediate Priority-0 correction rather
  than creating a parallel task. Promoted `W4-U-02` from Unprioritized to
  Priority 0 exactly as requested. The normal-window portion of `W4-U-02`
  remains required; it is no longer deferred behind `W4-U-01`.

### Read-only evidence and interpretation

- Matched the screenshot's 4.23 mm line to
  `data/Slicer_Saved/SampleStudy1/FDI31/FDI31-step5c.mrb`. The exact saved
  length is 4.242032791 mm. The case contains three FDI31 trajectory nodes;
  current assisted generation supports exactly one or two. The trajectory has
  no current assisted ordinal, analysis JSON, or endpoint-method attributes.
  Therefore the Placement combo visible in the screenshot identifies a
  creation mode, not the provenance of that existing line.
- The markup display had slice projection enabled. That paints an off-slice
  Entry/Target glyph into a 2D view, so apparent overlap is not proof that the
  3D point lies in that slice or anatomy.
- Read the nested `Post_surgery_seg.seg.nrrd`: 0.5 mm isotropic spacing; FDI31
  pulp label 62; 70 occupied voxels. Converted saved Target RAS
  `[-95.0375595093, -38.1458436835, 51.7615283697]` to continuous IJK
  `[190.0751, 76.2917, 103.5231]`. It is about 0.0117 mm from the relevant
  native voxel boundary; the nearest rounded voxel is outside and a small
  rootward step enters the label.
- Read the local Slicer conversion source and reproduced its smoothing factor
  0.5 / 40-iteration / 0.01-passband closed surface with host VTK. The saved
  ray intersects the unsmoothed pulp surface at about 4.484 mm but misses the
  smoothed surface, with about 0.239 mm closest separation. Re-running the
  current root-direction inference on the same FDI31 tooth/Entry gives a native
  boundary at 4.8544 mm and first displayed-surface contact at 6.9479 mm, an
  offset of 2.0935 mm along the same ray.
- Inspected the Smooth checkbox path. The handler returned immediately outside
  active oblique verification, while the control defaulted checked and did not
  synchronize from the actual CBCT and segmentation display modes. The report
  is therefore explained by a checked no-op in ordinary Step 4A, not a failure
  of Slicer's existing native representation switch.

### Decision and implementation

- New assisted lines require a shared interval between the selected tooth's
  FDI-matched binary pulp and the closed surface displayed for that same segment
  on the finite Entry→inferred-root ray. Target is the farther of their entry
  boundaries—the first point contained by both—after explicitly rechecking
  native-mask containment and signed closed-surface distance. Both boundaries
  and their offset remain in assisted analysis. A miss or non-overlap fails
  before any line is created. Entry, direction, atomic one/two-line creation,
  manual verification and no-overwrite behavior are preserved.
- Disabled Markups slice projection in normal trajectory presentation and the
  custom oblique verification view. The longitudinal MPR still contains the
  actual Entry→Target line; unrelated slices no longer display projected points
  as if they were anatomical intersections.
- Made the existing smooth-display switch call the shared native CBCT and
  segmentation display functions in both ordinary Step 4A and oblique
  verification. Outside verification, the checkbox now reflects the actual
  combined CBCT-interpolation/closed-surface state. Oblique verification still
  captures and restores both prior modes. Changed its default from checked to
  unchecked and clarified that it is display-only.
- Added one focused Slicer assertion path for the previously bypassed normal
  Step 4A toggle and extended the existing assisted-generation check to require
  the new endpoint method, recorded boundaries/offsets and non-positive signed
  distance from the displayed pulp surface. Reused the existing serialized pulp/shell runner,
  adding the Step 4A display method and pass marker before its assisted endpoint
  method; updated its matrix pass condition and scoped pycompile file list. No
  new framework, helper layer, dependency or production geometry object was
  introduced.
- Changed product files:
  `logic_workflow.py`, `widget_planning_focus.py`,
  `widget_trajectory_view.py`, `DENTOWorkflow.ui`, `slicer_tests.py`,
  `Testing/run_dentobot_pulp_shell_smoke.py`, and
  `Testing/verification_matrix.json`.
  Updated controlled records: `TASKS.md`, `DEVELOPMENT_PLAN.md`,
  `DECISIONS.md`, `ARCHITECTURE.md`,
  `REPRODUCIBILITY_AND_TRACEABILITY.md`, `AGENT_CONTEXT.md`, and this logbook.

### Commands, results, errors and evidence boundary

- Used `rg`/`sed` to trace all relevant generation, display, UI and test paths;
  used `graphify query` for assisted endpoint and smoothing relationships;
  inspected the saved MRB/NRRD and reconstructed the finite rays with read-only
  host Python/VTK scripts. Read Slicer representation-conversion source in the
  existing container. These inspections did not modify the saved case.
- `docker ps` and a read-only process listing showed the operator's Slicer
  still running in `dentobot-slicerros2` (Slicer process IDs 406/407 at the
  observation). No second Slicer, ROS or MoveIt process was started and no
  running process was stopped or restarted.
- Two initial `apply_patch` attempts failed without writes: one supplied a
  duplicate file target and one used stale context/order. The patches were
  reapplied against the inspected source and succeeded. No destructive command
  was used.
- `graphify update .` completed with exit 0 after each source refinement; the
  final `graphify-out` has 6,016 nodes, 9,617 edges and 454 communities (606
  cross-community edges in the aggregated HTML). It
  emitted non-fatal stream-fd permission messages and reports that saved
  community labels are stale; semantic relabeling is unnecessary for this
  bounded fix.
- Verification status is **PARTIAL — STATIC ONLY**. A scoped
  `git diff --check` was inadvertently appended to a read-only search command
  before the required approval and returned exit 0 with no output. This was a
  protocol error despite having no scene/runtime effect; it will not be treated
  as authorization for or substituted for the pending checks, and it covered
  the intermediate patch before the final shared-interval refinement. No
  pycompile, pure test or Slicer test was run. Runtime serialization still
  prohibits an overlapping Slicer launch. The numeric reconstruction is
  diagnosis evidence, not proof that the edited module behaves correctly in a
  fresh normal window or that the geometry is anatomically acceptable.
- The checkout was already broadly dirty; the correction was layered onto the
  relevant existing changes without reverting unrelated work. No engineer-owned
  record, robot/hardware operation, source CBCT/mask mutation, active-scene
  edit, commit, push or external synchronization occurred.

### Next bounded action

After explicit approval, run the cheapest focused static/pure checks and the
existing Slicer segmentation-display/assisted-generation methods when the
operator Slicer process is clear. Then reload the module, deliberately delete
the legacy FDI31 three-line set, generate one current assisted line, and verify
the target on the visible pulp surface plus truthful non-projected 2D behavior.
Toggle smoothing on/off in the normal Step 4A window and oblique MPR and confirm
both CBCT/masks change visually and captured modes restore on exit. Stop at the
first failure under the three-attempt ceiling; do not proceed to Step 6, ROS,
MoveIt, robot motion or clinical interpretation.

### Approved verification completion

The operator then explicitly requested `finish verification`, authorizing the
recorded verification ladder. No authority for robot/hardware motion or
clinical acceptance was inferred.

- Final `git diff --check` exited 0.
- `static.assisted_pulp_pycompile` exited 0 for the scoped geometry, workflow,
  planning/view, Slicer-test, runner and pure-test Python files.
- `pure.assisted_pulp_endpoint` passed 1/1 in 0.97 s.
- The retained `pure.support_boundary_bridge` prerequisite passed 2/2 in
  0.97 s.
- Docker read-only preflight failed on sandbox socket permission, not product
  behavior. No successful process-list snapshot was captured before the first
  runtime launch; proceeding without that recorded snapshot was a serialization
  evidence gap. The first test process exited before the focused retry, and the
  post-focused-run process listing was empty. Do not infer the pre-run operator
  process state from that post-run evidence.
- The first approved `runtime.pulp_shell_fdi11` launch emitted
  `DENTOBOT_STEP4A_DISPLAY_PASS`, `DENTOBOT_ASSISTED_PULP_PASS`, and
  `DENTOBOT_FDI11_SHELL_PASS`. It then failed at the later Step 5B
  `unified-template` stage: final fusion contained 10 disconnected occupied
  volumes with voxel sizes `[60677, 9, 4, 3, 1, 1, 1, 1, 1, 1]`; process exit
  was 1. Because the Step 4A markers and saved shell completed before the
  independent guide-fusion call, this is evidence under existing `W5-U-03`,
  not a reason to tune the Step 4A endpoint or mask-display correction.
- Reused the same runner with a minimal `DENTOBOT_TEST_STEP4A_ONLY=1` stopping
  boundary and added matrix target `runtime.step4a_pulp_display`. The matrix
  JSON parsed, final diff check and scoped pycompile passed again, and the
  approved focused Slicer run emitted `DENTOBOT_STEP4A_DISPLAY_PASS`,
  `DENTOBOT_ASSISTED_PULP_PASS`, and `DENTOBOT_STEP4A_P0_PASS`; it exited 0 in
  approximately 6.14 s. A post-run process listing was empty.
- Refreshed Graphify after the focused runner/matrix change; final graph output
  reports 6,017 nodes, 9,618 edges and 440 communities. The concluding
  documentation-inclusive `git diff --check` also exited 0.
- Automated Step 4A P0 status is **PASS**. Evidence is
  `/tmp/dentobot-verification/step4a-p0-20260910/result.json`. The exact FDI31
  regeneration, apparent 2D/3D agreement, and visible smooth/native difference
  remain **PENDING OPERATOR NORMAL-WINDOW OBSERVATION**; headless API assertions
  do not manufacture that human/anatomical acceptance.
- No source case was saved. No ROS, MoveIt, robot, hardware, patient-facing,
  engineer-owned, commit, push or external-sync action occurred.

## FDI31 run-2 Step6x5 restore and duplicate docking display

### Operator statement and task routing

- The operator supplied two screenshots and the load traceback for
  `data/Slicer_Saved/SampleStudy1/FDI31/run2-dentobot-case-step6x5.dentocase`.
  Load rolls back at `hydrateDentoCaseStateAfterLoad` with a saved-versus-MRML
  Step 6 environment mismatch. After mouth opening, a separate docking object
  remains at the closed-jaw pose and appears as duplicate docking bores.
- The operator requested immediate load recovery for testing, no extensive
  change because mouth opening will be moved/reworked next, and inclusion of
  this issue in those plan notes.
- `TASKS.md` was searched first. This is acceptance evidence and a blocker
  inside existing Priority-0 `S6-REUSABLE-CASE-SETUP`, not a new task. The
  broader mouth-opening redesign remains downstream.

### Inspection and interpretation

- Read the compact context, package routing map, verification protocol, current
  task/plan/decision/log evidence, Ponytail and Graphify instructions. Graphify
  routed the jaw-opening, case hydration and docking ownership paths.
- The package manifest is schema 2.0 and contains a 63.9 MB MRB. The traceback
  proves both pre/post package-MRML validation stages completed before the later
  derived-environment comparison failed. Source and exact MRML inspection show
  `[Step 6.0A] Opened Target-Attached Geometry` contains the Step 5C final
  template and is parented to the TMJ transform. Its source final template is
  hidden. `[Step 4C] DENTO Four Independent Robot Docks` is separately visible,
  purple and untransformed in world RAS; the actual Step 4C plane and measurement
  markups are hidden. This establishes the duplicate display mechanism without
  claiming mesh corruption.
- The installed robot-resource fingerprint matches the package. The environment
  JSON is redundant derived state; actual package nodes, lineage, registry and
  MRML geometry/matrices already have independent fail-closed validation.

### Narrow implementation

- `logic_case_bundle.py` now retains the MRML-rebuilt environment after an
  environment-only mismatch, logs its changed fields and marks schema migration
  pending instead of rolling back the validated case. The existing post-load
  path still clears live task/collision/diagnostic validity and never reconnects
  ROS. Registry and actual geometry/lineage mismatch failures are unchanged.
- Valid selected-branch hydration reuses
  `refreshStep6CaseTargetAttachedDisplay`. `logic_step6_scene.py` records and
  hides the source Step 4C plane and docking assembly while the transformed
  target-attached proxy is active; reset restores their prior visibility.
- The existing jaw-opening Slicer scenario now asserts source docking hidden
  after apply/reload and restored after reset. No schema bump, package rewrite,
  geometry/planner/collision change, robot action or mouth-opening redesign was
  introduced.

### Evidence boundary and next action

- Used read-only `unzip`, XML/JSON inspection, hashes, bounded `rg`/`sed`, Git
  status/diff inspection and the existing idle container's process listing.
  The container was up with no Slicer/ROS/MoveIt process listed; no application
  process was started or stopped. The source package and screenshots were not
  modified. A later container-path/process recheck failed on sandbox Docker-
  socket permission before execution; it supplied no new product evidence.
  Changes used `apply_patch` and preserved the broadly dirty checkout.
- Verification has not run because the project protocol requires the concrete
  plan below to receive explicit approval. Current status is **Implemented**,
  not Unit/Runtime/Operator Verified.
- Proposed smallest check: first run scoped Python compilation plus
  `git diff --check`; then one isolated headless Slicer invocation that opens
  this exact bundle through `_openCaseBundle`, asserts successful offline load,
  migration-pending rebuilt environment, hidden source Step 4C plane/docks,
  transformed opened proxy and restored source visibility after reset. Stop at
  the first causal failure; do not start ROS/MoveIt, modify the bundle, run a
  full workflow, or continue into the mouth-opening redesign.

### FDI31 Step 6 restore verification — 2026-09-10

#### Commands and results

- Ran `PYTHONPYCACHEPREFIX=/tmp/dentobot-fdi31-restore-pycache-20260910
  python3 -m py_compile` for the three changed workflow Python modules; it
  exited 0. `git diff --check` also exited 0.
- After confirming the existing container had no Slicer/ROS/MoveIt process,
  ran isolated headless Slicer loads of
  `run2-dentobot-case-step6x5.dentocase` through `_openCaseBundle`. The source
  package was read only; no ROS, MoveIt, robot motion or case rewrite occurred.
- The original `CaseBundleError` no longer occurs: exact-package
  `_openCaseBundle` returns successfully offline and sets
  `step6SchemaMigrationPending=True` after rebuilding the redundant environment
  from validated MRML.

#### Evidence and boundary

- The expected transformed Step 6 proxy is not retained after the successful
  load. This is not an environment-rebuild or duplicate-dock failure. The
  package's final template is raw `GeometryState=Current`, its shell/dock/source
  trajectory references all resolve, and no observed loader callback changes
  that raw state.
- The current reader deliberately treats its
  `DENTOBOT.FinalGuideSchemaVersion=1.0` as stale because current source requires
  schema `2.0` for through-bore protection. This makes the PreparedBranch
  ineligible, deactivates the planning context, and clears its transient Step
  6A proxy. The package's `freshnessIssuesAtSave` was empty, so this is a
  software-schema migration boundary after the package was saved.
- Do not bypass this final-guide safety gate or label the V1 final model current
  merely to retain mouth-open presentation. That change would exceed the
  operator's requested immediate recovery and needs explicit review in the
  planned mouth-opening/legacy-template rework.
- The temporary exact-case diagnostic script was removed. The recovery patch
  remains static-verified and restores an error-free offline load, but it is
  **not** accepted as a full Step 6 opened-state restore for this legacy final
  template. Next bounded action: decide whether the next mouth-opening rework
  should support a clearly labelled inspection-only legacy proxy, or require a
  Step 5C schema-2 regeneration before opening.

## Step 5B fusion-shell failure diagnosis

### Operator statement and routing

- The operator asked why fusion-shell generation failed and identified the new
  single-trajectory comparison package as
  `data/Slicer_Saved/SampleStudy1/FDI31/run2-dentobot-case-step6x5.dentocase`.
- Existing-plan lookup routed the failure to `W5-U-03` and the bore/connector
  interaction to `W5-U-04`; no parallel task or geometry policy was created.

### Read-only evidence and interpretation

- The actual recorded failure is from the FDI11 saved-case runner, after its
  shell passed: occupied region sizes were
  `[60677, 9, 4, 3, 1, 1, 1, 1, 1, 1]`. At approximately 0.3 mm sampling, the
  established 0.1 mm³ cleanup ceiling permits at most three voxels. The 9- and
  4-voxel regions therefore remain substantive for the fail-closed check;
  because more than one region is retained, no tiny-region cleanup is applied.
- The same FDI11 package completed shell and unified fusion on 2026-09-08 with
  one occupied region. Current source differs in the relevant path: the
  uncommitted `W5-U-04` through-bore correction lengthens each target-dock
  channel from `depth + 2 * spacing` (5.6 mm for this case) to
  `depth + 2 * (max(reinforcement, connectorThickness) + spacing)` (9.6 mm).
  Channels are subtracted last from shell, reinforcement and docking union.
  FDI11 also uses a 2.2 mm bore and 8 mm pattern radius. This establishes the
  extended bore subtraction as the regression boundary and makes small
  channel-cut attachment/dock slivers the best-supported mechanism; the saved
  exception does not contain spatial labels, so the precise dock/branch remains
  unlocalized.
- The supplied FDI31 run-2 package does not contain a failed fusion. Its saved
  Step 5C model has 57,644 triangles, zero invalid edges and one occupied solid.
  Raw occupied regions were `[45846, 1]`; the one-voxel island was about
  0.0265 mm³ and was validly removed. All four recorded shell-contact branches
  use 2.0 mm endpoint overlap, with surface gaps 0.0066, 0.4967, 1.9073 and
  2.4529 mm. FDI31 uses a smaller 1.5 mm bore, 10 mm pattern radius and 35° yaw,
  so it demonstrates that the extended-channel algorithm can succeed on a
  less aggressive case; it does not clear the FDI11 regression.
- The separate FDI31 package-load rollback and duplicate visible Step 4C docks
  are restore/display defects already recorded under
  `S6-REUSABLE-CASE-SETUP`. The saved final FDI31 mesh itself is connected; the
  duplicate display must not be misreported as fusion topology corruption.

### Conclusion and boundary

- Do not raise the 0.1 mm³ artifact threshold: doing so would hide geometry
  fragments rather than preserve both open bores and one printable solid. The
  next implementation action belongs in the shared bore/attachment geometry:
  localize the two FDI11 slivers, then prevent the final channel cut from
  marooning material while retaining a genuinely through-open bore.
- Inspection used Graphify routing, package/MRB unzip and XML/JSON reads,
  source/diff inspection and existing recorded test evidence. No Slicer/ROS/
  MoveIt process was launched, no case was rewritten, and no production code or
  test was changed during this diagnosis.

## W5-U-04 P0 bore-safe attachment implementation

### Operator request and decision delta

- The operator requested the recommended production correction for both the
  original covered bores and the consequent FDI11 disconnected-volume failure,
  plus actionable feedback for future unified-template failures.
- Existing-plan lookup retained `W5-U-04` for the construction defect and
  `W5-U-03` for broader 5B/5C acceptance. `W5-U-04` is now Priority 0. No
  patient shell, trajectory, robot/planner or collision scope was added.

### Implementation

- Read the FDI11 package without loading Slicer. Its authoritative Step 4C
  parameters confirm 3.0 mm outer diameter, 2.2 mm bore, 3.5 mm connector,
  2.0 mm endpoint overlap, 1.0 mm reinforcement and 0.3 mm processing spacing.
  The 0.4 mm core wall explains why a nearest-point 3.5 mm branch can cross the
  bore before final subtraction.
- `DENTOGuideGeometry.py` now chooses the shortest sampled bore-tangent
  connector for each dock. The centreline clearance is bore radius plus branch
  radius plus one processing voxel; the branch retains the configured overlap
  and joins the reinforced annulus with at least one voxel of ligament. Unsafe
  annulus, route and gap cases fail early with the exact Step 4C/5B controls to
  review. Extended final channel subtraction and schema-2 invalidation remain.
- Final disconnected-volume errors now include voxel sizes, approximate extra
  volumes, the cleanup ceiling, the Step 5B **Shell + Guides** view and relevant
  Step 4C controls. The message warns against increasing the artifact limit.
- `logic_guide.py` supplies the stored dock axes/dimensions, records assembly
  method V3 and requires bore-tangent provenance during Step 5C verification.
  The existing Slicer geometry scenario now asserts four tangent branches,
  minimum clearance/ligament, empty protected channels and open bore axes.

### Commands, evidence and stopping condition

- Used bounded `rg`/`sed`, Graphify query, `git status`/diff inspection,
  `view_image`, and read-only nested ZIP/MRML extraction under
  `/tmp/w5-u04-fdi11.nmF6fd`. Changes used `apply_patch`; unrelated dirty work
  was preserved. No patient-identifying data was recorded.
- No static, Slicer, ROS, MoveIt, robot, hardware or patient-facing execution
  was run. Current status is **Implemented**, not Unit/Runtime/Operator
  Verified. The source case and engineer-owned records were untouched.
- Next, after explicit verification approval, run scoped pycompile and
  `git diff --check`, then the focused synthetic template geometry and existing
  FDI11 saved-case fusion. Stop on the first causal failure; on pass, regress
  FDI31 and request normal-window inspection from both bore faces.

### Continuation self-review

- A first scoped `git diff --check` used the workspace wrapper rather than the
  nested DentoBot checkout and exited 129 without checking files. Re-running
  from `ros2_ws/src/DentoBot` against only the four implementation files and
  five controlled records exited 0 with no whitespace errors.
- Error guidance was tightened to say which Step 4C/5B controls to review while
  requiring any dimension change to use approved geometry. It continues to
  forbid raising the disconnected-artifact threshold.
- `graphify update .` completed from the DentoBot checkout: 6,038 nodes, 9,640
  edges and 454 communities. Graphify reported that semantic community labels
  could be refreshed separately; no label refresh is required for this fix.
- No Python compilation, Slicer, ROS, MoveIt, robot, hardware or patient-facing
  runtime was started. Focused runtime acceptance remains approval-gated.
- Added verification-matrix profile `w5u04-bore-safe-template`, reusing the
  existing synthetic visible-template Slicer method, the existing FDI11 saved-
  case runner and an operator-only normal-window bore inspection. No new test
  harness or dependency was added. Matrix JSON validation is part of the
  pending approved static gate; it has not yet been claimed as valid.

## W5-U-04 approved static and runtime verification

### Approved scope and preparation

- The operator approved the bounded `w5u04-bore-safe-template` sequence. Matrix
  JSON validation, scoped Python compilation and `git diff --check` all exited
  0. The retained pure prerequisites from this date remain valid: assisted-pulp
  endpoint 1/1 and support-boundary bridge 2/2.
- The container preflight found `dentobot-slicerros2` up and no existing Slicer,
  ROS, MoveIt or DENTOBOT process. The runtime source revision was
  `a09beb57046bbbb7272feee5ff3f656d1cf8a9b1`; the scoped code/matrix diff
  digest was `d2f94bbc1127aea22fdb200021c7677ac7d061ecf02038aa1fa024eb9bd14bbe`.
  Logs are retained in `/tmp/dentobot-verification/w5u04-rB7WZr/`.

### Results

- `runtime.w5u04_bore_safe_template` exited 0. Its existing assertions covered
  four bore-tangent branches, one-voxel bore/annular clearance, extended
  channels, zero channel residual, centreline samples through all four bores,
  watertightness, one occupied solid and schema-1 invalidation. The method's
  `delayDisplay` completion text is not emitted in headless stdout, so its
  matrix pass condition was corrected to the authoritative successful exit and
  assertions; no harness was added.
- `runtime.pulp_shell_fdi11` exited 0 and emitted
  `DENTOBOT_STEP4A_DISPLAY_PASS`, `DENTOBOT_ASSISTED_PULP_PASS`,
  `DENTOBOT_FDI11_SHELL_PASS` and `DENTOBOT_PULP_SHELL_PASS`. The final FDI11
  fusion has 62,258 occupied samples, one region, 71,620 triangles, zero
  invalid edges, 3,486 excluded channel samples, zero residual channel samples
  and no removed occupied artifacts. The previous `[60677, 9, 4, ...]`
  fragments did not recur. Post-run process inspection was empty.
- The existing runner was extended only with optional source-path and expected-
  FDI environment variables, preserving its default FDI11 behavior. Its exact
  FDI31 input was `/workspace/data/Slicer_Saved/SampleStudy1/FDI31/run2-dentobot-case-step6x5.dentocase`, SHA-256
  `fad56159e8f4c16dad0b85040c1e4c3f547fac1a8ff65d3850403fd6b0993bcc`.
  FDI31 shell generation passed, but its Step 5B fusion failed with retained
  regions `[45966, 5, 2, 2, 1, 1, 1, 1]`. The five-voxel component is about
  0.1338 mm³, exceeds the three-voxel/0.1 mm³ cleanup ceiling, and correctly
  blocked final generation. No parameter, threshold or saved package changed.

### Evidence boundary and next bounded action

- Added read-only component RAS centre/bounds diagnostics to the existing
  disconnected-fusion error and metrics. Compilation, matrix validation and
  diff checking passed afterward. The next FDI31 diagnostic launch was rejected
  because it is a new runtime attempt after that source change, beyond the
  earlier approval's stop-at-first-failure boundary. It was not rerun and no
  workaround was attempted.
- On fresh explicit approval, run exactly one FDI31 diagnostic rebuild to
  localize the 5-, 2- and 1-voxel components. Use that evidence for one
  construction correction, then rerun FDI31 once. Do not raise the cleanup
  threshold, alter physical dimensions, start ROS/MoveIt, move hardware or
  overwrite either source package.

## Daily glance automation

- Operator request: a short daily review at 06:00 or when the PC is available
  and online, covering yesterday's accomplishments, blockers and direction.
- Existing-plan lookup: searched TASKS.md for daily review/summary/tracker and
  related terms; no matching automation task found. Existing weekly-review
  automation is a separate request and was preserved.
- Configured native thread heartbeat `dentobot-daily-glance`, daily 06:00
  Asia/Kolkata, with a 100–150-word status/accomplishments/blocker/tracker/next
  action contract. Development records only; read-only scheduled reviews.
- Commands/actions: bounded rg/cat/tail reads of task/context/current log and
  existing automation; official scheduling documentation lookup; native
  automation_update. First create returned missing destination; retry with
  destination=thread returned Created, ACTIVE. No runtime checks performed.
- Evidence boundary: creation confirmed; first scheduled delivery and automatic
  catch-up after offline time are not verified. Prompt handles delayed delivery
  if the scheduler provides it; it cannot implement an online trigger itself.
- Next: observe the first daily delivery. No engineer-owned artifact accessed.
