# DENTOBOT logbook — 2026-09-05

## P0/P1 roadmap reconciliation

### Operator direction

- The operator supplied `P0_Tool_Insertion_Depth_and_Manual_Anatomy_Outlier_Handling.md` as the immediate Track-A correction and `P1_DENTOBOT_Case_Platform_and_Simulation_Studio_Master_Plan.md` as the roadmap after current Step-6 acceptance.
- The operator explicitly kept the current base placement, planner, guard, collision policy, endpoint coordinates, and tolerances frozen while the nearest-point evidence is classified.
- P1 work is not authorized before the complete x4 guarded loop is accepted.

### Existing evidence, distinct from interpretation

- The existing template-excluded x4 run retained full Stage-1/2/3 kinematic candidates. Best saved-base branch: FDI15 to `pneumatic_spindle-Copy` minimum geometric distance `0.998041065511 mm` at Stage-3 local waypoint 18.
- The ordinary mesh-collision check passed at that waypoint and retained no collision pair; the independent `1.0 mm` research-margin check rejected it. The gap to the margin is `0.001958934489 mm` (about 1.959 µm).
- The guard stopped at that first margin failure. Therefore current evidence cannot claim that the rest of Stage 3 has no actual mesh contact.

### Agent interpretation and decision

- This is not evidence of an actual FDI15/housing intersection at the recorded waypoint. It is a separate conservative-margin violation.
- The next diagnostic is read-only: with the explicit temporary Step-5C template exclusion still active, inspect all retained Stage-3 waypoint states for ordinary actual collision only. It must not alter base placement, route selection, margin policy, target, or guard acceptance.
- The P0 insertion-capacity rule is an upstream validity condition, not a collision-policy relaxation: `requested depth <= effectiveToolProtrusionMm - 0.1 mm`; failure blocks planning without shortening the line.
- If and only if an operator reviews FDI15 and identifies a local segmentation artifact, a session-local derived collision proxy may be used for research simulation. Source anatomy remains unchanged and the proxy cannot become silently authoritative.

### Controlled-document changes

- Updated `DEVELOPMENT_PLAN.md`, `TASKS.md`, `DECISIONS.md`, `ARCHITECTURE.md`, `REPRODUCIBILITY_AND_TRACEABILITY.md`, and `AGENT_CONTEXT.md`.
- Replaced the redundant planned `S6R-*` Track-B queue with the approved gated P1 `DCP-*`, `DSS-*`, and `DHW-*` roadmap. No P1 source implementation was started.

### Next bounded action

- Implement `S6-P0-DEPTH` only after locating the current CAD/URDF effective tool protrusion source and its existing preflight owner. Before a ROS/Slicer runtime audit, state the exact diagnostic and request confirmation under the verification protocol.

## S6-P0-DEPTH implementation

### Source audit

- Read-only CAD/URDF calculation inspected the `pneumatic_spindle-Copy` collision mesh relative to the fixed `dentobot_drill_tcp` transform. The first upstream housing geometry projects `7.0 mm` behind the tip along the canonical drill axis.
- With the approved `0.1 mm` provisional axial reserve, the current tool-only maximum physical insertion depth is `6.9 mm`.
- The exact x4 Entry→Target distance is `15.760533814 mm`; it exceeds this provisional capacity by about `8.861 mm`. This is a physical-intent block, not a target modification or planner failure.

### Implementation

- Added `PROVISIONAL_EFFECTIVE_TOOL_PROTRUSION_MM = 7.0` and `PROVISIONAL_AXIAL_CLEARANCE_MM = 0.1` in `DENTORobotWorkflowFacade.py`.
- Added the shared `_tool_insertion_evidence()` calculation. `planApproachPhase()` performs it after immutable-task retrieval and before guard setup/IK/MoveIt planning. A failing result is `TRAJECTORY_TOOL_INSERTION_LIMIT_EXCEEDED`, retains requested/max/remaining values, and explicitly states that the line was not shortened or moved.
- Added the insertion result to successful planning details and the Step 6.5 panel; it remains explicitly labeled as provisional tool-only capacity with final guide geometry excluded.
- Added a focused pure façade test covering pass-at-limit and block-over-limit behavior.

### Verification status

- No static, pure, ROS, Slicer, or runtime verification has been run after this implementation. Proposed first check: `pytest -q Testing/test_robot_workflow_facade.py` plus `py_compile` of the three edited Python files and `git diff --check`. This proves only helper arithmetic, source importability, and patch hygiene; it does not validate CAD physics, collision behavior, or x4 runtime. Operator approval is required before running it.
- Attempted required local graph refresh: `graphify update .` — did not refresh; its watcher reported `Operation not permitted`. No code or runtime conclusion relies on this stale graph state.

### Approved focused pure gate

- Operator approved the stated pure verification only. Executed in `/home/light-tarun/dentobot/ros2_ws/src/DentoBot`:
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-verification/20260905-p0-depth/pycache python3 -m py_compile DENTOWorkflow/Resources/Python/DENTORobotWorkflowFacade.py DENTOWorkflow/Resources/Python/DENTORobotSimulationPanel.py DENTOWorkflow/Resources/Python/dentobot_workflow/widget_robot_shell.py && pytest -q Testing/test_robot_workflow_facade.py && git diff --check`.
- Result: PASS, exit 0; façade suite `24 passed in 0.12s`, syntax check passed, and `git diff --check` reported no whitespace errors. The terminal wrapper emitted three `Failed to create stream fd: Operation not permitted` messages before the test output; they did not affect process exit or any test result.
- The first run exposed a truthful pre-existing/current-worktree diagnostic regression: `_goal1_chain_diagnostic_fields()` derived a composed index from a retained Cartesian failure, then overwrote its valid first-invalid RAS point with absent phase-guard fields. Patched it to preserve solver evidence unless actual guard evidence exists. The rerun above includes the existing regression test and passed.
- Evidence level: focused pure/source gate only. It proves insertion arithmetic, importability, existing retained-failure diagnostic behavior, and diff hygiene. It does not prove tool dimensions, guide fit, collision geometry, ROS, Slicer, or x4 runtime behavior.

## S6-P0-CLEARANCE — approved read-only actual-contact audit

### Operator direction

- The operator approved a read-only ROS/MoveIt/Slicer audit to distinguish true
  mesh contact from the independent 1 mm research clearance margin. The
  operator froze base placement, planner behavior, endpoint geometry,
  tolerances, and collision policy during the classification.

### Audit implementation and boundary

- Added `DENTOBOT_AUDIT_ACTUAL_CONTACT_ONLY=1` to the exact-case harness. It
  creates only an ephemeral runtime scene, temporarily bypasses the new
  production insertion hard-stop inside the harness, regenerates historical
  candidate paths, and calls MoveIt's explicit static-state-validity API for
  every retained Stage 1/2/3 waypoint. It starts neither preview nor waypoint
  application. The temporary Step-5C final-template exclusion was active;
  no other object was excluded.
- Executed a clean isolated `dentobot_moveit_config simulation.launch.py` plus
  normal-window Xvfb Slicer harness with
  `DENTOBOT_EXCLUDE_UNRESOLVED_TEMPLATE_COLLISION=1`,
  `DENTOBOT_PLAN_ONLY=1`, and `DENTOBOT_AUDIT_ACTUAL_CONTACT_ONLY=1`; Slicer
  output was retained in container file
  `/tmp/dentobot-stage3-contact-audit-slicer.log`.
- First launch was invalid because `--no-main-window` prevents the workflow
  harness from selecting its module. A second wrapper left duplicate temporary
  ROS processes; the dedicated simulation container was restarted before the
  clean, logged run. These infrastructure corrections changed neither source
  planner behavior nor runtime safety policy.

### Observed result

- Audit marker: `DENTOBOT_STAGE3_ACTUAL_CONTACT_AUDIT`. Slicer returned `1`
  during known VTK/class-loader shutdown cleanup after the marker; the JSON
  report is the runtime evidence, not a clean process-exit claim.
- The previously identified `pneumatic_spindle-Copy`↔FDI15 separation remains
  a margin-only condition at the inspected housing point: best value
  `0.998041065511 mm` (1.959 µm below the 1 mm margin). It is not itself an
  FDI15/housing mesh intersection.
- Six auditable candidate branches all reached Stage 2 and Stage 3
  kinematically. Every one had a real, authoritative non-target FDI15↔`burr`
  contact at Stage 2 waypoint 14 and at Stage 3 waypoint 0. The Stage-3 static
  report also lists expected target-tooth↔burr contact; the concurrent FDI15
  contact remains disallowed. One diagnostic row held no live waypoints, so no
  mesh-contact conclusion was made for it.
- Examples: seed 109/housing roll `74.88235534°` had a 0.507662074815 mm
  guard-margin minimum; seed 128/roll `74.82990524°` had 0.998039141099 mm;
  the retained seed 185 clearance-detour legs had 0.998041065511 mm. Their
  Stage-1 static state sequences were fully valid; Stage 2 first actual contact
  was at index 14; Stage 3 first actual contact was index 0.
- The harness run left ROS launch children after its parent exited. Restarted
  only the dedicated `dentobot-slicerros2` simulation container to clean those
  transient processes; no hardware action was available or called.
- Re-ran `graphify update .` after the harness edit. It again reported its
  watcher `Operation not permitted`, so graph output was not refreshed and no
  conclusion depends on it.

### Interpretation, decision, and next action

- The x4 blocker is therefore not solely an optional 1 mm clearance margin.
  No whole-template, whole-tooth, burr, housing, or margin exemption is
  authorized, and no base/planner change was made.
- `S6-P0-ANATOMY-REVIEW` is now the next bounded Priority-0 decision: the
  operator must assess whether the local FDI15 collision geometry is true
  anatomy or an artifact. Only a confirmed local artifact can justify a
  session-local derived proxy; source anatomy and production collision policy
  remain unchanged. The separate 6.9 mm insertion hard block still prevents
  x4 acceptance until the physical tool/access configuration or trajectory is
  revised upstream.

## S6-P0-ANATOMY-REVIEW source implementation

### Operator need and bounded interpretation

- Following the actual-contact evidence, the next authorized step is not an
  automatic FDI15 exemption. The operator needs to inspect the suspect source
  CBCT/segmentation and decide whether a *local* region is an artifact.
- Exact burr-contact point localization is not yet exposed by the current
  SlicerROS2 static-state API. The implementation therefore opens the selected
  suspect tooth copy for manual review but does not falsely claim an exact
  contact-point marker.

### Implementation

- Added Step 6.5 `Manual non-target anatomy review (session only)` controls:
  select a non-target whole tooth, create a review copy, open it in Segment
  Editor, explicitly confirm a local segmentation artifact, use the reviewed
  proxy for research simulation, or discard it.
- The copy is a disposable `vtkMRMLSegmentationNode` with
  `SaveWithSceneOff`. It references the original CBCT and retains only the
  selected segment. The original segmentation is never modified.
- An inactive copy is harmless. Explicit activation replaces only that
  collision object's prepared mesh while retaining its canonical source/object
  identity, invalidates confirmation and transient phase plans, and requires
  collision-scene re-acknowledgement plus task reconfirmation. Diagnostics and
  Goal-1 messages label the resulting evidence as a research simulation
  anatomy override.
- No generic burr, FDI15, housing, template, or whole-tooth collision exception
  was added. The independent template-only functional override remains
  separate. The 6.9 mm insertion limit remains a hard preflight block.

### Verification boundary and next action

- No compilation, Slicer, ROS, MoveIt, or runtime check was run for this new
  UI/scene-sync source change. `git diff --check` passed after the edit; this is
  formatting evidence only, not functional verification.
- Added (not run) `test_DENTOWorkflowStep6SessionAnatomyReview`. It is scoped to
  the review lifecycle: source-tooth candidate filtering, inactive default,
  `SaveWithSceneOff`, explicit activation, discard, and source mesh fingerprint
  preservation. It neither starts ROS nor plans/previews motion.
- After adding that test source, `git diff --check` passed again. A second
  `graphify update .` attempt had the same `Operation not permitted` watcher
  failure; no graph update claim is made.
- Next proposed approval-gated check: a Slicer-only review lifecycle test that
  creates a copy, verifies `SaveWithSceneOff`, verifies source geometry is
  unchanged, edits/activates/discards the copy, and confirms the collision
  audit changes only when active. It would not start ROS, plan, preview, or
  move hardware.
- `git diff --check` passed after the implementation/documentation update.
  `graphify update .` was attempted as required after code changes, but its
  watcher again failed with `Operation not permitted`; graph output is not
  refreshed and no conclusion relies on it.
