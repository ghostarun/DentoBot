# Logbook — 2026-09-18

## DENTOWorkflow debug session (Cursor agent)

### Operator direction

- Open a debugging/fixes session for DENTOWorkflow issues.
- Changes may live in unstaged/untracked work only; safe to ignore, revert to
  landmark, or keep on a Cursor-agent branch — no requirement to merge to
  `main` without separate review.

### Session landmark (git: `ros2_ws/src/DentoBot`)

- Pre-debug commit: `0f96451` (`main` at session start).
- **Baseline:** branch tip and tag
  `cursor-agent/dentoworkflow-debug-landmark-20260918` (includes `0cc70f2`
  workflow fix plus workspace Drive-sync/logbook files below). Working tree
  clean; Cursor DENTOWorkflow code edits not started yet.
- Checked out branch: `cursor-agent/dentoworkflow-debug-20260918`.
- Branch: `cursor-agent/dentoworkflow-debug-20260918` (same tip as above).
- Annotated tag: `cursor-agent/dentoworkflow-debug-landmark-20260918`.

Revert DENTOWorkflow-only experiments:

```bash
cd ~/dentobot/ros2_ws/src/DentoBot
git checkout main -- DENTOWorkflow/
git clean -fd DENTOWorkflow/   # only if untracked debug artifacts were added
```

Return to landmark exactly:

```bash
git checkout cursor-agent/dentoworkflow-debug-landmark-20260918
# or: git checkout cursor-agent/dentoworkflow-debug-20260918
```

(Landmark tag and debug branch share the same commit — use
`git rev-parse --short HEAD` after checkout.)

### Environment at open

- Live stack already running in `dentobot-slicerros2` (Slicer on DENTOWorkflow,
  simulation launch). `launch-dentoworkflow.bash --check-only` reported session
  conflict — expected while GUI is open.
- No uncommitted changes under `DENTOWorkflow/` at session open.
- Unrelated dirty tree on `main`: Workspace docs/scripts (gdrive sync), not
  part of this debug lane unless explicitly scoped.

### Known backlog context (not yet tied to operator-reported symptom)

From 2026-09-17 recovery checkpoint (`VERIFY-LEARN-01` / restore ownership):

1. Post-restore duplicate visible FDI31 (closed-source + moving-lower proxy) —
   display-state hypothesis for detached ghost anatomy.
2. Assisted trajectory entry: `StartPlaceMode(0)` path fails when active markup
   or placement-valid postcondition mismatches; discriminator not yet logged.
3. FDI21 stale-target warning — valid protection; clean rebind path TBD.
4. P5 duplicate request ID — traceability only.

### Next bounded action

- Operator to name workflow step, case (e.g. FDI14 step6x3, locked r4 FDI31),
  exact status text or screenshot, and first causal failure.
- Agent to fix only evidence-backed defects under existing backlog owners; no
  policy/geometry tuning to force GUI progress.

## PLAT-U-07 — one-time full `Slicer_Saved` upload; ongoing `.dentocase` only

- **Operator request:** Push all existing files under local `data/Slicer_Saved/`
  to Google Drive once; future automated sync remains staged `*.dentocase` only
  per `DRIVE_SYNC_INSTRUCTIONS.md`.
- **Implementation:** Added `gdrive-data-sync.bash bulk-slicer-saved-once`
  (`rclone copy` of entire `Slicer_Saved/` to `IITM Dentobot/Data/Slicer_Saved/`,
  ~5 GiB / 274 files). Removed ongoing instructions-doc staging; watcher now
  monitors only `data/drive-sync/Data/`. Updated env example, `SETUP.md`, and
  `data/Slicer_Saved/DRIVE_SYNC_INSTRUCTIONS.md`.
- **Execution:** Restarted `gdrive-data-sync.service`; started bulk upload
  2026-09-18 (log `data/drive-sync/gdrive-bulk-slicer-saved.log`, marker
  `data/drive-sync/.bulk-slicer-saved-done` on success).
- **Next bounded action:** Confirm bulk log shows completion; optional `rclone ls`
  spot-check under `Slicer_Saved/` on Drive.

## Virtual Open-Mouth Articulator — Phases 2–5 (Cursor)

- **Scope:** Patient condyle ROI extraction, arch-inferred axis, AUTO hinge
  resolution, `.dentocase` articulator provenance, Step 6 status shows
  `hinge_source`.
- **Files:** `virtual_open_mouth_articulator.py` (resolve/solve AUTO),
  `logic_step6_condyle_extraction.py`, `logic_case_foundation.py` (patient-first
  solve), `logic_case_bundle.py`, `widget_robot_scene.py`.
- **Verification:** `PYTHONPATH=DENTOWorkflow/Resources/Python python3 -m pytest
  Testing/test_virtual_open_mouth_articulator.py -q` → **16 passed** (2026-09-18).

## Case Foundation GUI — manual articulator testing

- Step 6 panel: **Force manual TMJ axis**, **Probe virtual articulator (dry run)**,
  diagnostics label, **Copy last provenance JSON**.
- Operator flow: four reviewed landmarks → Probe (inspect `hinge_source` / AUTO path)
  → optional manual override → Commit → slider preview with live diagnostics.
- **Fix (2026-09-18 PM):** articulator GUI used Qt `setText`/`setEnabled` (invalid in
  Slicer PythonQt); use `.text`/`.enabled` so Probe/Commit/override update UI.
- **Fix:** mandible ROI used invalid VTK `GetPoint(index, x, y, z)`; use `GetPoint(index)`.

## Production AUTO landmark policy + Phase 3+4 headless check

- **Operator policy:** `MANUAL` is oracle / debug / optional correction only.
  Production is `AUTO PROPOSE → VISUALIZE → ACCEPT/LOCK → SOLVE`.
  `ARCH_INFERRED` must not require manual condyles.
- **Code:** Probe no longer gates AUTO on four landmarks.
  `resolve_virtual_condylar_axis` compares segmented vs manual centres only when
  manuals exist (None was `np.asarray` size-1 reshape error).
- **Level 1:** `PYTHONPATH=DENTOWorkflow/Resources/Python python3 -m pytest
  Testing/test_virtual_open_mouth_articulator.py -q` → **20 passed**.
- **Level 2 (runtime.auto_open_mouth_test1_post), attempt 1:** FAIL —
  `probeCaseFoundationArticulator` → `validateStep6CaseJawLandmarkAnatomy`
  (“Place the four Step 6 case jaw landmarks.”).
- **Attempt 2:** FAIL — `compare_condyle_centres(..., None, None)` reshape.
- **Attempt 3 (pass):** docker `DENTOBOT_CASE_SOURCE=.../SampleStudy1/test1_post.mrb`
  xvfb Slicer smoke → **DENTOBOT_AUTO_OPEN_MOUTH_PASS** SLICER_EXIT=0.
  Payload: volume `PostDentalSurgery`, seg `Post_surgery_seg`,
  `hingeSource`/`selection` `ARCH_INFERRED`, `landmarkSource` `AUTO`,
  `targetGapMm` 28.0, `achievedGapMm` ~27.93, `thetaDeg` -9.06,
  `openingRotationSign` -1.0. Patient ROI did not pass QA; arch path used.
- **GUI:** Reload DENTOWorkflow, then Probe/Commit AUTO with reviewed
  segmentation only; Edit landmarks remains optional.
- **Operator now:** manual Slicer GUI test on live `dentobot-slicerros2`.
  Stale Case Foundation copy no longer requires four landmarks before AUTO.

## Case Foundation GUI — AUTO primary, landmarks fallback

- **Operator request:** AUTO open as the only primary default; landmark
  placement is a setback when AUTO fails or the user explicitly chooses it.
  One-click run, visual confirm, then next step.
- **Operator observation:** After Reload Module (Dev) on live
  `dentobot-slicerros2`, the AUTO-primary panel was accepted (“perfect”).
- **UI:** Primary **Open mouth (AUTO)** + **Confirm and continue to Step 4A**.
  Landmark/probe/force-manual moved under collapsed
  **Fallback — manual landmarks**. AUTO failure expands that section.
- **Controlled records (this turn):** `DECISIONS.md`, `TASKS.md` (closed
  pointer), `DEVELOPMENT_PLAN.md`, `ARCHITECTURE.md`, `changelog.md`,
  `GUI_ACTION_PARITY.md`, `AGENT_CONTEXT.md` handoff bullet,
  `REPRODUCIBILITY_AND_TRACEABILITY.md`, this logbook. No Drive sync and no
  engineer-owned records. No new backlog row (source-complete; not a P5/gate
  item).
- **TRL-4 remaining (not this GUI contract):** occlusal-plane frame,
  condyle-head ROI vs whole mandible, DW_Derived viz, dentocase §21
  completeness.

## Virtual forehead + approximate robot-base pose

- **Operator request:** Automate base placement for a head-mounted robot given
  opened-mouth Case Foundation; restricted CBCT FOV has no forehead (no DL).
- **Implementation:** `virtual_forehead_mount.py` (`VirtualForeheadPriorV1`),
  Step 6 **Propose virtual forehead + base**. Defaults: URDF-zero joints, 0 mm
  skin offset. After desired-vs-AUTO screenshots, `T_forehead_base` is identity
  (chain extraoral along forehead +Z) and propose does not TCP-slide onto the
  incisor.
- **Verification:** `PYTHONPATH=DENTOWorkflow/Resources/Python python3 -m pytest
  Testing/test_virtual_forehead_mount.py -q` → **6 passed** (2026-09-18 extraoral
  seating).
- **Not closed:** `S6-U-02` physical CAD/normal. No hardware motion.
- **Operator observation:** Forehead/mount plane accepted; AUTO base hung
  through the FOV (S–I) instead of extraoral anterior.
- **Next:** Reload Module, Propose again, confirm the chain hangs extraoral
  of the same plane (toward A, not through the volume). Review/lock still
  required.

## Documentation checkpoint — 2026-09-18 evening

Commands/results already on this date: pytest 20 passed;
`DENTOBOT_AUTO_OPEN_MOUTH_PASS` SLICER_EXIT=0. Next bounded action is
operator continuation into Step 4A on the opened AUTO case, or later TRL-4
articulator work — not a second pending queue.
