# DENTOBOT logbook — 2026-09-04

## Track A guarded-live verification

### Operator authorization

- The operator approved the previously proposed verification sequence after
  implementation: static syntax/import checks followed by the serialized
  simulation-only x4 Slicer/ROS/MoveIt checks. No hardware motion, powered
  drilling, or patient-facing operation was authorized or performed.

### Checks executed

- `PYTHONPYCACHEPREFIX=/tmp/dentobot-verification/20260904-trackA/pycache python3 -m py_compile DENTOWorkflow/Resources/Python/DENTOROS2Bridge.py DENTOWorkflow/Resources/Python/DENTORobotSimulationPanel.py DENTOWorkflow/Resources/Python/DENTORobotWorkflowFacade.py DENTOWorkflow/Resources/Python/dentobot_workflow/widget_robot.py DENTOWorkflow/Resources/Python/dentobot_workflow/widget_robot_shell.py`
  — PASS, exit 0. Evidence: `/tmp/dentobot-verification/20260904-trackA/static.step6_pycompile.log`.
- `git diff --check` — PASS, exit 0. Evidence:
  `/tmp/dentobot-verification/20260904-trackA/static.git_diff_check.log`.
- The conditional `slicer_ros2_module` colcon rebuild was not run: no
  production C++/CMake source changed in this increment.

### Runtime attempt and correction

- The phase-guard runtime reached `STACK_READY` in image
  `sha256:f71da23aaa35161730536530ed18c594ccb7766ed8d3a35cfd68d0f385280faa`
  under ROS domain 73, then failed at the smoke fixture's joint-bound case.
  The fixture sent `[99, 99, 99, 99, 99, 99]`; the new fail-closed policy
  correctly rejected the command first for nonzero external spindle/J6, so the
  fixture could not assert the intended bounds reason. Evidence:
  `/tmp/dentobot-verification/20260904-trackA/runtime.phase_guard.log`.
- Corrected `Testing/run_dentobot_phase_guard_smoke.py` to test spindle motion
  separately and send `[99, 99, 99, 99, 99, 0]` for the independent J1–J5
  bounds assertion. The corrected smoke has not yet been rerun.
- The corrected phase-guard smoke then passed all cases and cleaned up its
  temporary stack. The exact x4 run reached the final Goal 1 waypoint (73/73)
  with an accepted guard status but timed out because the single-shot preview
  timer did not schedule the completion event after the last acknowledgement.
  Patched `DENTORobotWorkflowFacade.previewPhase()` to schedule one final
  event-loop tick at N/N so endpoint FK verification and the finish callback
  always execute.

### Current evidence boundary and next action

- Static source checks pass. Runtime phase-guard acceptance is pending the
  corrected rerun; the exact x4 case check must remain blocked behind that
  prerequisite. No runtime success is claimed.
- Next bounded action: rerun the exact x4 case after the completion-transition
  patch. Record the first causal failure and cleanup status before any further
  change.

### Exact x4 rerun and Stage 3 correction

- The post-completion-transition x4 rerun no longer timed out in Goal 1. It
  reached and completed the guarded approach preview, but Goal 2 remained
  correctly blocked because the full Entry→Target preflight stopped at
  requested pose `10/11`. The retained continuity-IK candidate had an
  authoritative FK orientation residual of `0.514 deg`, just over the strict
  `0.500 deg` limit; no tolerance was relaxed and no partial drilling path was
  promoted. Evidence: `/tmp/dentobot-verification/20260904-trackA/runtime.step65_exact_case.postfix.log`.
- The cause was a coarse saved Step 6 Cartesian sample count being reused as
  the sequential-IK continuation resolution. The fallback now resamples the
  same rigid fixed-frame line at bounded `0.25 mm` spacing (maximum 801 poses),
  preserves the exact requested endpoints/orientation and spindle lock, and
  keeps the existing FK/guard tolerances unchanged.
- Static post-fix checks passed: py_compile for the bridge, facade, and exact
  smoke fixture, plus `git diff --check`. The next bounded action is a fresh
  exact x4 run to establish whether the finer continuation resolves Stage 3;
  its result remains unclaimed until that run completes.

## Track-A pneumatic-spindle model correction

### Operator request and constraints

- The operator stopped further Stage-3 retry/sampling tuning and identified
  `pneumatic_spindle-Copy_Revolute-6` as an uncontrolled, continuously rotating
  air-rotor spindle rather than a robot positioning actuator.
- The requested correction was limited to the Track-A kinematic model: retain
  visual/collision representation, remove J6 from MoveIt planning DOFs, add a
  fixed non-spinning burr-tip TCP upstream of J6, preserve J1–J5 behavior and
  all existing FK/collision/guard tolerances, and report migration impact
  before broad edits. No hardware, powered spindle, drilling, or clinical use
  was authorized.

### Engineering interpretation

The earlier model let an orientation solve use a free spindle roll and then
canonicalize that result to J6 zero. That can make a pose appear reachable even
though the robot cannot command the roll. The minimal truthful boundary is a
five-variable `dentobot_arm` ending at a fixed sibling TCP at the CAD burr-tip
reference pose; the downstream spinning branch may remain for visualization
and collision geometry.

### Implementation

- `dentobot_description/urdf/dentobot.urdf`: added fixed
  `dentobot_drill_tcp` under `pneumatic_spindle-Copy`; retained the burr,
  `dentobot_tool_tcp`, and historical `dentobot_drill_tip_provisional` branch
  downstream of J6.
- `dentobot_moveit_config/config/dentobot.srdf` and `joint_limits.yaml`:
  changed `dentobot_arm` to `base_link → dentobot_drill_tcp`, removed J6 from
  the planning group/state/limits, and kept only adjacent visual links
  excluded.
- `DENTOStep6State.py`, `DENTOStep6Planning.py`, `DENTOROS2Bridge.py`,
  `DENTORobotWorkflowFacade.py`, `DENTORobotPlacement.py`, `logic_robot.py`,
  `widget_robot.py`, `DENTORobotSimulationPanel.py`, and `parameter_state.py`:
  changed planning/state/workspace/IK/FK/trajectory/guard paths to J1–J5,
  retained six-slot display compatibility at zero, and changed the policy
  fingerprint to `external-pressure-spindle-nonplanning-v2`.
- `dentobot_description/scripts/slicer_joint_state_publisher.py` and
  `dentobot_moveit_config/src/collision_guard.cpp`: publish the visual six-link
  tree but forward/check only five planning values; explicit legacy six-value
  motion requests are rejected with an external-spindle explanation.
- `slicer_ros2_module/MRML/vtkMRMLROS2RobotNode.cxx`: canonical TCP FK now
  uses an explicit MoveIt five-variable state. A legacy six-value FK call is
  accepted only by discarding its sixth slot; the visual KDL branch evaluates
  omitted J6 at neutral zero for display compatibility.
- Test fixtures were updated to assert the five-variable planning contract,
  canonical TCP independence from J6, old-home migration, and explicit legacy
  spindle rejection. The existing placement-only jaw-group fallback remains
  intact.

### Verification performed under prior operator approval

- Focused host suite:
  `PYTHONPATH=DENTOWorkflow/Resources/Python pytest -q
  Testing/test_step6_state.py Testing/test_step6_planning.py
  Testing/test_moveit_config.py Testing/test_ros2_bridge.py
  Testing/test_robot_workflow_facade.py
  dentobot_description/test/test_description.py` — **86 passed** in 0.27 s.
- SlicerROS2 C++ build in the isolated container:
  `colcon build --packages-select slicer_ros2_module --cmake-args
  -DCMAKE_BUILD_TYPE=Release --event-handlers console_direct+` — **PASS**,
  package finished in 14.4 s.
- Clean ROS/MoveIt smoke on domain 73 — **PASS, RC=0**. It reported a
  five-value planning state, canonical TCP IK and collision-aware Cartesian
  probe success, 40 strict guard samples with no contacts, and explicit
  rejection of a legacy six-value spindle command.
- Clean phase-guard smoke — **PASS, RC=0**. Target-only contact suppression,
  non-target/self/world collision, bounds, corridor escape, overshoot,
  wrong-task, duplicate sequence, and spindle-motion rejection all passed.
- Slicer façade smoke emitted functional JSON: selected TCP
  `dentobot_drill_tcp`, `joint_count=5`, native Goal/IK/Plan without leaving
  DENTOWorkflow, expert diagnostics with Views and return, hidden Execute,
  coincident robot roots, and workspace acceptance. Its disposable generic
  Cartesian probe remains `diagnostic-only` at 33.3% and is not used as an
  acceptance gate. The Slicer process returned **RC=1 at shutdown** with
  `vtkDebugLeaks` retained-wrapper diagnostics; no façade assertion traceback
  was present. This repeats the known lifecycle defect and is not a clean
  shutdown claim.
- Two wrapper attempts initially produced no visible result because a strict
  readiness-string match held the shell; only the validated transient ROS
  PIDs were terminated. A subsequent bounded run produced the recorded
  passing ROS/phase results and all transient processes were cleaned up.

### Evidence boundary and next action

This is source/build/focused-runtime evidence for the five-DOF boundary, not
acceptance of the complete x4 guarded loop. Old six-value packages will need
the documented migration and explicit re-review of stale evidence. The visual
`FindRootAndTipLinks` result may still name the historical downstream tip;
MoveIt/Step 6 selection is authoritative at `dentobot_drill_tcp`. The known
SlicerROS2 shutdown leak remains open. The next bounded action is a fresh
normal-window x4 trial of Home→PreEntry→Entry→Target→Home, including Return
Home and repeat, after operator review; Track B remains blocked.

## Documentation and graph checkpoint

- Updated the active Step 6 setup and architecture text so it describes the
  five commandable joints, canonical `dentobot_drill_tcp`, and the external
  visual-only spindle slot. Removed obsolete axial-roll planning wording from
  the current Track-A instructions; historical records remain unchanged.
- Refreshed the repository-local AST graph with `graphify update .` from
  `/home/light-tarun/dentobot` under the approved local escalation. The graph
  rebuilt successfully with 6,843 nodes, 10,758 edges, and 629 communities.
  Graphify reported pre-existing parser warnings for several C++ headers and
  retained six unmatched nodes; no semantic/documentation pass was requested.
- Re-ran the approved static syntax check with
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-tracka-pycache python3 -m py_compile`
  over all changed production and smoke-test Python modules — exit 0.
- Re-ran `git diff --check` in both the DentoBot and SlicerROS2 repositories —
  exit 0 in each repository. No full x4 runtime trial was run in this
  continuation, so the normal-window Track-A gate remains pending.
- The final container cleanup inspection found five orphaned
  `simulation_status_publisher` instances (all parented to PID 1, with no
  Slicer, MoveIt, or robot-state-publisher process). These exact validated
  test leftovers were terminated by PID; a follow-up process check found no
  matching simulation/Slicer/MoveIt processes. The ROS 2 daemon was left
  untouched.
- After the final logbook edit, `git diff --check` was run once more in both
  repositories; both returned exit 0.
- Because the final production docstring edit changed a code file, Graphify
  was refreshed once more with `graphify update .`; the incremental rebuild
  completed at 6,844 nodes, 10,759 edges, and 631 communities (the same
  pre-existing C++ parser warnings and six retained unmatched nodes were
  reported).
- The final post-checkpoint `git diff --check` was run in both repositories;
  both returned exit 0.

## Track-A x4 trial after spindle planning-model correction

- Committed before the trial: DentoBot `7a4e7a9`
  (`test(step6): revalidate legacy x4 home profile`) on top of the bounded
  five-DOF correction `afe34c4`; SlicerROS2 `49492e0`
  (`fix(fk): evaluate canonical drill tcp from planning state`). Both
  repositories were clean at trial start.
- Started one serialized simulation-only trial with
  `dentobot-case-step6x4.dentocase`, ROS domain 73, the freshly built
  SlicerROS2 module, and the `setsid` process-group cleanup wrapper. The ROS
  stack reached readiness and Slicer loaded the case; no hardware/controller
  path was used.
- The trial stopped at Goal 1 before producing a preview. The canonical
  non-spinning `dentobot_drill_tcp` had no collision-aware PreEntry IK endpoint
  in the current MoveIt scene. The harness reported the same failure for all
  bounded endpoint attempts; no endpoint, tolerance, collision policy, or
  partial path was substituted or promoted. This is a real Track-A blocker
  introduced/exposed by the truthful five-DOF TCP boundary, not a successful
  full-chain run.
- Cleanup verification found no remaining Slicer, MoveIt, robot-state,
  collision-guard, or simulation-status processes in the container. The
  Slicer shutdown still emitted the known VTK/MRML retained-wrapper leak
  diagnostics; this is a lifecycle defect and not evidence of planning
  success.
- Evidence boundary: ROS readiness and clean teardown passed, but the x4
  Home→PreEntry→Entry→Target→Home acceptance gate remains **blocked at Goal 1
  PreEntry IK**. Track B remains blocked. The next action is focused diagnosis
  of the canonical TCP goal/scene/seed mapping; no further retry or tolerance
  tuning is authorized by this result alone.

## Focused PreEntry axial-roll diagnostic

- Operator instruction: preserve the five-joint/canonical-TCP correction and do
  not start another full x4 rerun. The diagnostic was limited to the exact
  x4 PreEntry XYZ and the Entry→Target drilling axis; J6 was not included.
- Added the read-only fixture
  `Testing/run_dentobot_step6_preentry_roll_diagnostic.py`. It restores the
  package, reconnects the transient simulation stack, and tests the canonical
  `dentobot_drill_tcp` with J1–J5 only at rolls `0, 30, ..., 330` degrees about
  the fixed drilling axis. It records IK result, FK residuals, collision
  pairs/status, and joint-limit margins when a solution exists. If all
  collision-aware requests fail, it repeats the same twelve poses with
  collision checking disabled. It never commands, previews, or persists robot
  motion.
- The first run exposed a fixture-only motion-node lookup error; it was fixed
  to use the ROS2 Motion Control parameter node and the same diagnostic was
  rerun. No production planner, URDF/SRDF, TCP, collision policy, tolerance,
  Entry, Target, or spindle behavior changed.
- Exact case values used: PreEntry RAS `[-74.4336604861,
  -66.8347293003, 47.0575256193]` mm; Entry RAS `[-74.4463499383,
  -66.5457304095, 52.0491504669]` mm; Target RAS `[-74.4863484464,
  -65.6347750511, 67.7832849064]` mm; Entry→Target axis
  `[-0.00253789044, 0.05779977817, 0.99832496951]`.
- Result: all 12 collision-aware requests returned zero J1–J5 values, and
  all 12 collision-disabled requests also returned zero values. Therefore no
  candidate had an authoritative FK residual, collision pair, or joint-limit
  margin to report; those fields are correctly `null`/not evaluated. The
  evidence distinguishes this from a collision-only failure: the current
  exact pose/axis/orientation is not solved by the configured five-joint IK
  even when collision checking is disabled. This is a kinematic/target-frame/
  solver-constraint investigation result, not permission to add a collision
  exemption or revert to J6.
- Functional diagnostic marker: `DENTOBOT_STEP6_PREENTRY_ROLL_DIAGNOSTIC_COMPLETE`.
  The Slicer process exited `1` after the marker with the known VTK/MRML
  retained-wrapper shutdown diagnostics; report this separately from the
  completed diagnostic. Container teardown was verified clean. Evidence:
  `/tmp/dentobot-verification/20260904-preentry-roll/runtime.slicer.rerun.log`
  and `result.json`.

## Five-constraint PreEntry IK implementation

### Operator request

- Diagnose the failed five-joint PreEntry solve and implement the approved
  Track-A correction through drilling preview without reverting J6 removal,
  relaxing tolerances, weakening collision policy, or starting Track B.

### Agent diagnosis

- The fixed canonical TCP matches the former J6=0 tip transform to numerical
  precision. Both collision-aware and collision-disabled coarse full-pose IK
  failed because the API still submitted XYZ plus a full quaternion to the
  five-DOF KDL group.
- A previously accepted x4 J1–J5 endpoint was evaluated statically with the
  current URDF/base: PreEntry error was about `0.099 mm`, drill-axis error
  about `0.0068 deg`, and derived housing roll about `74.603 deg`. This proves
  the required position-plus-axis task is reachable but does not itself prove
  current collision validity.

### Implementation

- SlicerROS2 now exposes bounded position-plus-tool-axis IK using the existing
  MoveIt robot model, Jacobian, joint bounds, and PlanningScene. It returns
  J1–J5 only and retains best residual evidence when it cannot accept a state.
- DENTOWorkflow routes Goal 1 through that operation using Task Home and the
  bounded Home-connected 6.3 seeds. The nominal zero-roll pose is display/input
  scaffolding only, not a planning constraint.
- Accepted PreEntry orientation now comes from authoritative world-RAS FK.
  Stage 2 and Stage 3 copy its full rotation matrix unchanged. The policy
  fingerprint is advanced to `stage1-position-axis-authoritative-fk-v2`.
- Focused façade/bridge contract coverage was updated. No test, build, runtime
  trial, Git publication, Drive sync, or engineer-owned record action was run.

### Commands and evidence used

- `graphify query "Why does five-joint canonical dentobot_drill_tcp PreEntry IK fail all axial-roll candidates, and what code paths define the planning constraints?" --budget 120`
  routed the investigation to the façade, bridge, robot node, and current
  five-DOF correction records.
- A read-only Python URDF transform calculation compared the fixed canonical
  TCP against the former J6=0 chain and evaluated the historical x4 endpoint
  against the saved base transform and exact PreEntry/Entry/Target geometry.
- `rg`, `sed`, `git diff`, and package JSON inspection were used to trace the
  current planning group, IK call, policy, and persisted fixture state.
- `graphify update .` initially failed inside the restricted sandbox with
  `Operation not permitted`; the approved escalated retry completed and
  refreshed the local AST graph to 6,887 nodes/10,830 edges. Existing parser
  warnings for nine SlicerROS2 macro-heavy files remain graph-index evidence,
  not build results.

### Next bounded action

- After explicit verification approval: inspect the source diff, run Python
  syntax/scoped tests, rebuild SlicerROS2, execute the focused x4 PreEntry
  position-axis gate, and continue to full-chain preflight only if at least one
  collision-aware endpoint passes.

## 05:47 IST — Five-constraint IK static integration audit

### Agent implementation/audit

- Traced the current Goal 1 route through `DENTORobotWorkflowFacade`,
  `DENTOROS2Bridge`, `ROS2MotionControlLogic`, and the native SlicerROS2 robot
  node. The source consistently asks PreEntry IK for TCP position plus drill
  axis, validates the returned J1–J5 endpoint, derives the complete accepted
  rotation from canonical-TCP FK, and passes that exact matrix to both Stage 2
  and Stage 3 Cartesian requests.
- Checked the installed MoveIt header signatures read-only in the existing
  container. `RobotModel::getJointOfVariable` accepts variable names and
  `RobotState::getJacobian` provides the overload used by the new solver.
- Added the explicit Eigen Cholesky include required by the LDLT damped
  least-squares solve. This is a build-risk correction only; it does not change
  targets, tolerances, collision rules, the five-joint model, or the fixed-frame
  planning policy.
- Added a second fail-closed check at the façade boundary: the authoritative
  canonical-TCP FK must independently reproduce the exact PreEntry position
  within `0.25 mm` and the Entry→Target drill axis within `0.5 deg` before its
  housing rotation can be committed. The measured authoritative residuals are
  retained with the candidate diagnostic.
- Refreshed Graphify after the source edit. The AST refresh completed with
  6,887 nodes, 10,830 edges, and 638 communities; the same nine macro-heavy
  SlicerROS2 parser warnings and six retained unmatched nodes remain.
- Refreshed Graphify again after the façade/test source edit; the incremental
  result contains 6,890 nodes, 10,833 edges, and 627 communities, with the same
  pre-existing parser-warning class and retained unmatched-node notice.

### Verification boundary / next action

- No Python check, C++ build, Slicer/ROS/MoveIt trial, commit, push, Drive sync,
  or engineer-owned document action was performed in this audit. The source
  remains unverified. After explicit operator approval, the next gate is the
  scoped static check and SlicerROS2 rebuild, followed by the focused x4
  collision-aware PreEntry solve; full-chain planning runs only if that passes.

## 05:54 IST — Exact-case acceptance harness completed through repeat

### Agent implementation

- Extended the existing `run_dentobot_step65_exact_case_smoke.py` acceptance
  path after its first exact Target FK check. A passing marker now also requires
  guarded Return Home, a fresh Goal 1 plan and acknowledged approach preview,
  a fresh Goal 2 plan and drilling preview using the repeated Stage-1 frame,
  a second exact Target FK check, and a final guarded Return Home.
- Updated `Testing/verification_matrix.json` so the exact-case PASS condition
  explicitly requires the complete-preview, Return-Home, and repeat-preview
  report fields. No second harness or alternate acceptance path was introduced.
- Refreshed Graphify after the test-source change: 6,892 nodes, 10,838 edges,
  and 629 communities. Existing macro-heavy parser warnings and retained
  unmatched-node notices remain graph-index limitations, not runtime evidence.

### Evidence boundary / next action

- This was acceptance-harness implementation only. It was not executed, and no
  planner or preview success is claimed. The explicit operator approval gate
  for syntax/tests, rebuild, and serialized x4 simulation remains pending.

## 07:50 IST — Track-A bounded Stage-3 diagnosis and correction checkpoint

### Operator observation

- The x4 Goal 1 preview is useful, but the full-chain diagnostics must explain
  why Stage 3 stops even when the robot has a plausible displayed endpoint.
- The guide bore is currently a separate physical-fit concern (approximately
  1.5 mm guide bore versus an approximately 2.0 mm burr); it must not be
  silently treated as the kinematic cause of a MoveIt/IK failure.

### Agent interpretation

- Stage 1 Home→PreEntry and Stage 2 PreEntry→Entry complete on the exact case.
- Stage 3 Entry→Target reaches requested pose 59 and stalls at pose 60/64 near
  the lower bound of J2. The best retained state is approximately 0.251 mm in
  position and 0 degrees in drill-axis direction, with no named collision pair
  reported. This is a bounded reachability/continuity boundary, not evidence
  that the requested Target may be moved or that collision tolerances should
  be relaxed.
- A deterministic fallback now tries only accepted Home-connected 6.3 postures
  after local continuity failure. It did not produce a complete alternate
  branch for this case.

### Implementation

- Added exact position-plus-drill-axis continuity recovery on the requested
  Entry→Target line, using bounded deterministic J1–J5 perturbations and the
  existing FK residual limits. J6 remains outside planning and fixed at zero
  only for compatibility/visual state.
- Corrected continuation fraction accounting so the requested sample count is
  the dense line count; a failure at pose 60/64 can no longer be reported as
  100% merely because a coarser MoveIt path was returned.
- Added authoritative endpoint FK checks for Stage 2 Entry and Stage 3 Target.
  A partial Stage 3 path is retained for diagnosis only and is never promoted
  to drilling preview.
- Retained Goal-1-only preview as a provisional demonstration path while the
  full chain is blocked. No tolerance, collision policy, endpoint, depth, or
  burr/guide exception was changed.

### Verification and evidence

- Scoped pure suite: `72 passed` across Step 6 state, façade, planning, and
  ROS2 bridge tests.
- Python AST parse and `git diff --check` passed for the DentoBot and
  SlicerROS2 worktrees. Graphify refresh completed after the source/docs edit;
  its existing macro-heavy parser warnings remain indexing limitations.
- Correctly provisioned runtime loaded the x4 case and emitted
  `DENTOBOT_STEP65_EXACT_CASE_PASS` for the Goal-1-only harness. The report
  recorded 55 strict and 21 terminal approach points, 10 suppressed tool
  contacts, five planning joints, and a truthful full-chain `Blocked` status.
  The Slicer process returned exit 1 only during shutdown because of known VTK
  wrapper leaks; the container teardown was clean and this is not planner
  evidence.

### Evidence boundary and next action

- Track-A acceptance is not complete: the exact x4 Stage 3 line still fails at
  pose 60/64, so no complete drilling preview or Return-Home repeat claim is
  made. The next bounded action is to reconcile the exact requested line with
  the approved case/base/trajectory geometry (especially the J2 lower-bound
  configuration) without changing constraints. Track B remains blocked. No
  hardware motion, commit, Drive sync, or engineer-owned record action was
  performed in this checkpoint.

### Runtime correction amendment — 07:58 IST

- The first correctly provisioned Goal-1 harness attempt exposed an undefined
  `target` name in the new façade endpoint check. This was a source defect in
  the diagnostic path, not a MoveIt result. The helper now receives the exact
  snapshot Target RAS explicitly.
- After the fix, the same isolated runtime emitted
  `DENTOBOT_STEP65_EXACT_CASE_PASS`, retained `full_task_status=Blocked` at
  Stage 3 pose `60/64`, and reported Goal-1 provisional preview complete with
  five planning joints. The Slicer process again returned `RC=1` only during
  shutdown with the known VTK-wrapper leak report; ROS/MoveIt startup and
  teardown were clean. Evidence: `/tmp/dentobot-verification/20260904-tracka/goal1only_final4.log`.
- A targeted Release rebuild of `slicer_ros2_module` completed successfully
  (`Finished <<< slicer_ros2_module [1min 4s]`). Only existing CMake Python
  policy warnings were emitted. A post-run process check found no lingering
  MoveIt, guard, robot-state-publisher, or Slicer process in the container.

## 08:46 IST — Focused pose-60 FK boundary and fail-closed classification

### Operator scope

- Approved implementation of the five-joint position-plus-drill-axis plan,
  while preserving exact Entry/Target points, the immutable Stage-1 drill
  axis, J6 exclusion, existing residual limits, collision policy, and the
  Track-A-only boundary.

### Diagnostic evidence

- The focused x4 diagnostic evaluated exact dense Stage-3 pose `60/64` with
  40 deterministic J1–J5 seeds and collision checking disabled, so guide/burr
  collision could not be the cause. No seed returned an accepted solution.
- The best finite state was consistent across the successful branch seeds:
  `link-2_Slider-2 = 0.0 m` (the URDF lower mechanical bound), drill-axis
  residual `0.0 deg`, and authoritative FK position residual
  `0.2510335 mm`. The residual vector was effectively `[0, 0, +0.2510335]`
  mm in the robot base frame.
- A diagnostic-only FK probe with J2 set to `-0.0003 m` reduced the residual to
  `0.0489665 mm`, confirming that the exact target lies approximately
  `0.251 mm` beyond the valid lower J2 limit. This out-of-bounds probe was not
  submitted to MoveIt, the guard, persistence, or preview.
- The result is therefore a mechanical reach boundary for the saved base and
  trajectory, not a spindle-roll, IK-seed, orientation, or template-bore
  collision problem. The correct physical remedy is to reposition the
  provisional base or revise the case placement, followed by normal
  invalidation/replanning; silently moving the target, relaxing tolerances, or
  changing URDF limits would be incorrect.

### Implementation

- Added a pure `position_axis_joint_limit_blockers` helper and native-limit
  evidence reader in `DENTOROS2Bridge.py`.
- Position-axis continuity failures now classify a finite near-limit result as
  `sequential_position_axis_joint_limit` and explain the exact pose, limiting
  joint, and required operator remedy. The evidence is persisted in the
  planner-leg diagnostic without promoting any partial path.
- Added the corresponding diagnostic field to `MoveItCartesianResult` and the
  façade's retained candidate records. No joint bound, residual threshold,
  endpoint, collision exception, or spindle policy changed.
- Removed the one-off FK probe code from the smoke harness after collecting
  the evidence; the harness remains the normal Track-A path.

### Verification and boundary

- Focused runtime log: `/tmp/dentobot-verification/20260904-tracka/stage3_pose60_diag7.log`.
- Pure Step-6 suite: `73 passed`.
- Read-only AST parse and `git diff --check` passed. Graphify refreshed after
  the source edit (6,914 nodes, 10,874 edges, 615 communities); its known
  macro-heavy C++ parser warnings remain indexing limitations.
- The exact x4 full chain remains correctly `Blocked` at Stage 3 pose 60/64;
  Goal 1 provisional preview remains available. Full-chain acceptance,
  Return-Home repeat, commit, Drive sync, and Track B work remain pending
  until the operator adjusts the provisional base/case geometry and reruns
  the approved Track-A loop.

- A follow-up Goal-1-only wrapper was started to surface the new diagnostic in
  the façade, but it was stopped after the Slicer startup/planning wrapper
  exceeded its bounded run without emitting a functional marker. It is not
  counted as planner evidence; the completed pose-60 diagnostic above remains
  authoritative. All transient ROS processes were subsequently absent.
