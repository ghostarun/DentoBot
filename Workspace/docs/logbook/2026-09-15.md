# DentoBot logbook — 2026-09-15

## Campaign-1 P2 source-only handoff continuation

This is the date-boundary continuation of the 2026-09-14 Campaign-1
source-only launcher diagnosis. No new runtime authorization was granted and
no Docker, ROS, Slicer, native build or native diagnostic was run.

- Final source-only checks remained: handoff stubs `3 passed`; relevant
  launcher-contract checks `5 passed`; Bash syntax and `git diff --check`
  passed. The full launcher test file retains one pre-existing unrelated
  ordering failure recorded in the 2026-09-14 logbook.
- The canonical recovery report contains the recovered wrapper evidence,
  unproven-cause classification, minimal helper diff, local test evidence and
  one exact unexecuted native P2 recheck proposal.
- Graphify refresh attempts through the approved wrapper, `--no-cluster`, and
  repository-root `graphify update .` all stopped with local
  `[Errno 1] Operation not permitted` before rebuild completion. No graph
  refresh is claimed; no runtime or source state was changed by those attempts.
- Current disposition remains P1 saved-input/machine checks PASS for completed
  checks; P1 overall scene correctness/operator review INCOMPLETE; P2
  INCONCLUSIVE; operator acceptance NOT_RECORDED; P3/P4 unauthorized.

Next bounded action: obtain explicit approval for the exact native recheck in
`Workspace/docs/diagnostics/FDI31_PLANNER_RECOVERY_REPORT.md` before any
Docker/ROS/Slicer/native execution.

## Campaign-1 P2 bounded runtime recovery — preflight and input-path correction

The operator subsequently requested immediate bounded Slicer-runtime recovery
within the existing offline P2 envelope. A fresh read-only inventory in
`dentobot-slicerros2` found the container up, ROS domain `73` with subnet
discovery, and no matching DentoBot/ROS/Slicer/MoveIt process to preserve or
replace. The frozen package was present at
`packet-a-fdi31-20260914-r7/FDI31-step5c.dentocase` with SHA-256
`5440961c2f1464df10adec3dfb6ab59f8a0e412c6074cae7852d8150babe4fff`.

The first read-only identity check showed that the proposed dated R13 path was
missing. A read-only search found the authoritative frozen R13 at
`/workspace/data/Slicer_Saved/SampleStudy1/FDI31/packet-a-fdi31-r7/diagnostics/FDI31-stage6-exact-r13.json`,
SHA-256 `0e4683c8def9584abddface6655c49451e1a642e41f0568c08fe342dad09c32b`,
matching the host and retained copies. The canonical report command was
corrected to this path before any runtime launch; no command was run with the
missing path. This is input provenance correction only and does not change the
frozen case, retry history, geometry, task or collision policy.

Next bounded action: execute the corrected single P2 handoff with the existing
simulation-only stack/helper, inner diagnostic timeout, outer container
timeout, owned-process cleanup and post-run evidence/identity checks.

## Campaign-1 P2 handoff attempt 1 — Slicer entry argument defect

The bounded handoff was run with the corrected undated R13 path, ROS domain 73,
the existing simulation-only startup, the helper-owned process group, and
inner/outer timeouts. Readiness was observed on attempt 1 (`rc=0`,
`ready=true`, 175 bytes); the helper emitted `slicer_launch_request` and
`diagnostic_entry`. The diagnostic then failed before case loading because the
invocation included `--no-main-window`, while
`run_dentobot_fdi31_recovery_diagnostic.py::_open_case` calls
`slicer.util.selectModule("DENTOWorkflow")`.

The durable result was `RuntimeError: Could not find main window`; no native
scene acknowledgement, static-state result, transition result, contact
measurement, image or inspection scene was produced. The Slicer log also shows
the non-fatal existing `psutil` installation attempt and `ROS2Tests.py` module
load warning; neither is the observed failure branch. The helper preserved
diagnostic status `1`, sent `SIGINT` to its own stack group, and completed
cleanup. The run-created ROS CLI daemon was verified by PID, command and
domain, then stopped gracefully; no matching runtime process remained.

Attempt-1 evidence was archived as `runtime-handoff-attempt1-no-main-window.log`,
`runtime-readiness-wrapper-attempt1-no-main-window.log`, and
`rejected_state-attempt1-no-main-window.json` alongside the pre-existing r6
artifacts. This is a Slicer handoff correction, not planner/full-flow retry
evidence and does not change the frozen inputs or P2 status.

Next bounded action: one evidence-backed P2 recheck with the same command and
inputs, removing only `--no-main-window` so the diagnostic can use the main
window it explicitly requires.

## Campaign-1 P2 bounded runtime recovery — attempts 2 and 3, final disposition

The operator then requested immediate Slicer-runtime repair with available
diagnostics, within the existing Campaign-1 P1/P2 envelope. The frozen run
remained `c1-p1p2-recheck-20260914-r6`; no new run name, retry-history reset,
P3/P4 action or full-flow retry was used.

### Attempt 2 — corrected Slicer entry, native packet obtained

The only invocation change from attempt 1 was removal of `--no-main-window`.
The existing simulation-only stack/helper reached readiness, launched Slicer,
entered the diagnostic and produced native static/transition records, five
labelled captures and a rotatable MRB. The first remaining runner defect was
then observed directly: the live goal robot was visible, but
`display_evidence.goal_robot_fk.displayed_model_names` was empty, so the
displayed robot placement and burr/TCP/spindle pairing were not yet trusted.
The complete attempt was copied to
`corrected-p2/attempt2-live-goal-refresh/`; the canonical current packet was
not overwritten by that archive operation. The run-created domain-73 ROS CLI
daemon was identified by PID and exact command and stopped with
`ros2 daemon stop`; no matching runtime process remained.

### Source-only display repair and focused verification

No native source, build, dependency, geometry, task, policy or collision
semantic change was made. The diagnostic runner was minimally repaired to
reacquire the live goal robot after the display call, catalog both model and
transform references, and compare available TCP/spindle/burr transforms by
known names. The focused diagnostic suite passed `11 tests`; isolated runner
`py_compile`, helper/launcher `bash -n`, and `git diff --check` passed. The
source runner used for the final attempt has SHA-256
`95ccc30e3cdd50414a9d6499c82fb20a6e610c2339a085c89afcfa8086fb2fab`.

### Attempt 3 — final bounded runtime recheck

The read-only preflight found the container up, ROS domain 73/subnet discovery,
the frozen package and authoritative undated r13 input, with no conflicting
runtime process to preserve. The helper observed readiness on attempt 2 after
one non-ready poll, emitted `slicer_launch_request` and `diagnostic_entry`,
and the runner wrote the current corrected packet before returning status 1.
The handoff log records diagnostic exit and graceful helper cleanup with the
initiating status preserved. The final attempt therefore proves the corrected
readiness-to-Slicer handoff branch; status 1 is the diagnostic gate result,
not a wrapper startup failure.

Final native/display evidence:

- The static predicate is recorded separately as phase-aware and invalid for
  the requested candidate state, with the native pair `[Step 5C] DENTO Final
  Printable Template` ↔ `pneumatic_spindle-Copy` and world count 31.
- The validate-only transition is separately recorded from setup Home
  `[0, 0, 0, 0, 0]`; candidate 0 first rejects interpolated sample 1/134 at
  fraction `0.00746268656716` for corridor violation. This is not a Target
  static verdict and not the r13 insertion edge.
- The final scene acknowledgement is trusted: expected 31, observed 31,
  matching IDs/poses/bounds/policy, and unchanged before/after scene
  fingerprint. The earlier r6 `expected 31, observed 0` discrepancy remains
  historical and preserved, not silently rewritten.
- Native contact count, depth, onset, nearest points and penetration remain
  unknown where the guard did not expose them.
- Display TCP/spindle FK now matches native FK within tolerance. The goal
  catalog exposes a matrix-bearing `burr_goal_transform`, but the runner's
  normalized-name classifier does not yet pair it; burr FK remains
  unavailable. This is the precise remaining evidence gap and is not a
  geometry/contact conclusion.

Current artifacts and hashes are recorded in the canonical report Section R:
`rejected_state.json` (`f7eb06286909d979440d236b8a35696d361b1f79e0564937e95550518aa0b4dd`),
`state_identity.json` (`fb7957580fbd0f40796cbfbaee2516b5f189ded1925f68943c69f0996aec722a`),
`contact_records.json` (`e857dfca6ae3e2e31f44a811a89431dda36ee4cc2237db2ad8ab708d53526f60`),
and `inspection-scene/fdi31-p2-rejected-state.mrb`
(`5f36343b8a2608fb6d98dad44cbbbaa7a4cc90de39dac192746fb41b63b4ed4c`).
The five screenshots remain labelled display-only review evidence; they do not
record operator acceptance of the 15 selections or geometry.

### Cleanup and gate

Only the helper-owned simulation process group was signalled. The run-created
ROS CLI daemon was stopped after ownership verification, and the post-cleanup
inventory was empty for matching DentoBot/ROS/Slicer/MoveIt processes. The
container was not restarted, no GUI/operator session was touched, and no
controller, hardware, trajectory, spindle or motion path was activated.

Final distinction: P1 saved-input/machine checks are **PASS for completed
checks**; P1 overall scene correctness/operator review is **INCOMPLETE**; P2
is **INCONCLUSIVE** because burr display FK pairing is missing;
`operator_acceptance` is **NOT_RECORDED**. The existing three-attempt runtime
ceiling is reached. No further native launch is made in this turn; stop for
operator review and do not start P3/P4.

### Documentation verification note

After the source edits, `bash ros2_ws/src/DentoBot/Workspace/scripts/graphify-update.bash`
was retried from the repository root. It exited 1 before rebuild completion
with `[Errno 1] Operation not permitted`; no graph refresh is claimed. The
final read-only container inventory confirmed `dentobot-slicerros2` remained
up and found no matching DentoBot/ROS/Slicer/MoveIt descendants.

## Campaign-1 P2 continuation — burr alias repair and stopped native recheck

The operator requested continuation to finish remaining P1/P2 items and
blockers. This was treated as a new bounded decision for the deterministic
source defect and one same-run native verification attempt, while preserving
the run name, frozen inputs, retry history, P1 operator-review gate and all
P3/P4 exclusions.

The remaining defect was confirmed in the retained `state_identity.json`:
the live transform catalog contained matrix-bearing `burr_goal_transform`,
but `_goal_display_node_kind` normalized that name to `burr-goal-transform`
without recognizing it. The smallest alias-only source repair was applied to
`Testing/run_dentobot_fdi31_recovery_diagnostic.py`, with one focused test
assertion. Verification passed:

- `PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-burr-alias-pycache python3 -B -m pytest -q Testing/test_fdi31_recovery_diagnostic.py` — `11 passed`;
- isolated `py_compile` — PASS;
- `git diff --check` — PASS;
- repaired runner SHA-256:
  `adfec14b0b89774a006829c850f030a739a6b2bf12a5c7346650a0025bae0238`.

A fresh inventory found the container up and no matching runtime process. The
versioned output directory `corrected-p2/burr-alias-recheck/` was created. The
first constructed shell invocation was malformed: helper continuation
arguments were placed on separate shell lines, so the helper received no
arguments, printed usage, and the subsequent bare Slicer command started
Slicer/Xvfb without the diagnostic. No native case or evidence artifact was
written.

The exact task-owned process group was read back as PGID `18306`, with
`xvfb-run`, `Xvfb`, `Slicer` and `SlicerApp-real` descendants. The original
task-owned terminal session was interrupted; the descendants were then
confirmed absent. The shared container reported `Exited (143)` with
`OOMKilled=false`. No restart was performed because the standing Campaign-1
boundary excludes restarting the shared container. No operator-owned process
or GUI session was intentionally touched.

Result: the alias repair is **Implemented / Unit Verified**, but not Runtime
Verified. The prior corrected P2 packet remains the latest valid native
evidence; the burr FK result was not overwritten or promoted. P2 remains
**INCONCLUSIVE**, P1 operator acceptance remains open, and a specific
authorization to restart the shared container is required before another
native verification attempt.

The required post-source graph refresh was retried with
`bash ros2_ws/src/DentoBot/Workspace/scripts/graphify-update.bash` from the
repository root and again failed before rebuild completion with
`[Errno 1] Operation not permitted`; no graph refresh is claimed.

## Campaign-1 P2 — authorized container restoration and final native recheck

### Operator authorization

The operator explicitly authorized restarting the existing
`dentobot-slicerros2` simulation-only container to finish P1/P2, while
preserving the frozen run, inputs, retry history, P1 review gate and all
P3/P4/hardware/motion exclusions. This entry records the operator scope
separately from agent interpretation and verification.

### Inspection and cause

Read-only Docker inspection found `Exited (143)`, `OOMKilled=false`, no
container error and no container log output. The configured command was
`sleep infinity` with `tty=true`, `stdin=true` and restart policy `no`.
The Docker event stream recorded the task cleanup command
`/bin/kill -TERM -18306` immediately before the task exec and container both
returned 143. The evidenced cause is task-owned cleanup signal propagation
after the malformed no-diagnostic attempt, compounded by the intentional
no-restart policy; it is not evidence of an OOM, missing native API or
simulation-stack self-crash.

### Restoration and execution

The existing container was restored in place with `docker start
dentobot-slicerros2`; it was not recreated or dependency-modified. Preflight
showed zero conflicting DentoBot/Slicer/MoveIt/guard processes. The corrected
handoff used one `bash -lc` command through non-TTY `docker exec`, preserved
ROS domain 73/subnet discovery and frozen r7/r13 inputs, and enforced outer,
helper and Slicer timeouts of 180/150/120 seconds. Readiness was observed on
attempt 2, followed by `slicer_launch_request` and `diagnostic_entry`.

The diagnostic produced a PASS packet, five nonblank labelled captures,
`contact_records.json`, `state_identity.json`, `rejected_state.json` and a
rotatable MRB. The Slicer process returned status 1 after writing the packet;
the wrapper preserved that status and recorded the existing extension-dir,
class-loader and VTK shutdown warnings. No traceback or failed diagnostic
packet was emitted, and no exit-code suppression was added. The run-created
ROS CLI daemon was identified by its exact domain-73 command and stopped with
`ros2 daemon stop`; the final process inventory was empty for helper,
simulation stack, Slicer, MoveIt, guard and ROS CLI processes. The container
remained Up with `OOMKilled=false` after cleanup. No operator-owned process or
GUI session was touched.

### Evidence and interpretation

Final packet directory:
`data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/`.

- Packet status is machine **PASS** and campaign gate is
  **USER_REVIEW_REQUIRED**; operator acceptance remains `NOT_RECORDED`.
- Scene acknowledgement is trusted at 31/31 matching IDs, poses, bounds and
  policy; before/after scene fingerprints are equal.
- The phase-aware static predicate is separately recorded as invalid for the
  requested endpoint with `[Step 5C] DENTO Final Printable Template` ↔
  `pneumatic_spindle-Copy`.
- The Home-to-requested-endpoint validate-only transition is separately
  recorded from `[0,0,0,0,0]`; candidate 0 rejects sample 1/134 at fraction
  `0.00746268656716` for corridor violation. It is not a Target static
  verdict or the r13 insertion edge.
- TCP, spindle and burr display transforms now match native FK within the
  existing 0.25 mm/0.5 degree tolerances. Contact count, depth, onset,
  nearest points and penetration remain unknown where the native guard does
  not expose them.
- The close-up keeps the 31 collision-audit copies in the native payload and
  hides surrounding anatomy only for display. No geometry/task/policy was
  changed.

The canonical report Section T and
`corrected-p2/runtime_handoff_status.json` were updated with the root cause,
restart boundary, hashes and final disposition. P1 remains saved-input/
machine PASS for completed checks with overall scene/operator review
INCOMPLETE. P2 bounded native diagnostic evidence is PASS, but the campaign
stops at operator review; no P3/P4 or later phase is authorized.

### Final verification record

- Focused diagnostic test: `PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-final-pycache python3 -B -m pytest -q Testing/test_fdi31_recovery_diagnostic.py` — **11 passed**.
- Runner/test isolated `py_compile` — **PASS**.
- Handoff-helper and launcher `bash -n` — **PASS**.
- JSON parse validation for the final packet and
  `corrected-p2/runtime_handoff_status.json` — **PASS**.
- Final packet SHA-256 verification — **PASS** for the four hashes recorded
  in the canonical report Section T.
- An initial `git diff --check` from `/home/light-tarun/dentobot` returned
  `Not a git repository`; this was a command-path error only. The same check
  was rerun from the actual source checkout
  `/home/light-tarun/dentobot/ros2_ws/src/DentoBot` and passed.

No graph refresh is claimed; the previously recorded Graphify attempts remain
blocked by local `[Errno 1] Operation not permitted` before rebuild. This does
not affect the focused source, runtime, packet, or container verification
reported above.

## Campaign-1 operator-review handoff — retained P2 packet only

**Operator instruction:** Preserve the completed native P2 packet, do not
repeat the launcher/native run, correct the P2 summary, present the 15
automatic selections and derived outputs, review screenshot attribution, and
reconcile superseded `P2 INCONCLUSIVE` summaries. P1/P2 acceptance and P3/P4
authorization were not granted.

**Coordinator action:** Read-only inspection of the canonical recovery report,
retained final packet metadata, static/transition records, collision-policy
source and r13 comparison record, saved PNGs, and the rotatable MRB. No Docker,
ROS, Slicer, native diagnostic, process, geometry, policy or implementation
operation was performed in this handoff.

**Evidence interpretation:** The handoff now distinguishes phase-aware static
rejection of both retained endpoints for final printable template ↔ spindle,
generic static contacts including FDI31 tooth ↔ burr and tooth ↔ spindle, and
Home-to-endpoint corridor rejection at the first interpolated sample. The
static endpoint result is present; `guide_clearance_warning=false` is recorded
as no configured guide warning/admitted guide contact for that query, not as
absence of collision. The existing bounded spindle-guide allowance and r13
warning evidence were kept separate from the non-approved template ↔ spindle
result. Unknown contact fields remain unknown.

**Review limits:** The context, target-state, visible-vs-collision-payload and
burr/spindle views are useful for their limited roles, but the first-rejected
image does not visibly show the evaluated robot state and the burr/spindle
close-up does not explain the template ↔ spindle pair. A two-frame,
display-only saved-scene recapture was proposed but not performed; it would
not rerun native diagnostics.

**Controlled-record updates:** Canonical recovery report Section U was added;
backlog, TASKS, DEVELOPMENT_PLAN and DECISIONS now point to the retained
bounded P2 evidence as current with `USER_REVIEW_REQUIRED`, while earlier
zero-object, launcher and container-exit statuses remain historical. P1
overall scene correctness/operator review remains INCOMPLETE, operator
acceptance remains NOT_RECORDED, and P3/P4 remain unauthorized.

**Next bounded action:** Await the operator's decisions on the frozen source,
selected geometry/bore roles, and whether the proposed display-only recapture
is needed. No native rerun, policy/geometry change or downstream phase follows
automatically.

## Campaign-1 final P2 packet — coordinator read-only review

**Operator input:** Supplied Luna's final bounded-P2 summary distinguishing
completed diagnostic collection from unrecorded P1/P2 operator acceptance.
No additional execution or acceptance was authorized in that message.

**Review performed:** Read the active backlog/campaign contracts, report
Section T, latest decision and dated evidence, and used `jq` to inspect
`corrected-p2/burr-alias-recheck-corrected/rejected_state.json`. Inspected
the saved target-state, first-rejected and FDI31/burr/spindle close-up PNGs.
Commands were read-only `cat`, `rg`, `sed`, `tail`, `wc`, `ls`, `command -v`
and `jq`, plus image viewing. An initial `.states[0]` query failed because
`states` is an object; the corrected query selected
`.states.first_guard_rejected`. No tests, native checks, process operations,
Slicer launch or implementation edits were performed.

**Findings:** Both retained candidates have authoritative phase-aware static
results `valid=false`, reporting template/spindle as the non-approved pair
and `guide_clearance_warning=false`. The separate generic static predicate
also lists target/burr and target/spindle contacts; it is not a substitute
for phase-specific authorization. The Home-to-endpoint drilling transition
rejects its first interpolated sample for corridor violation and remains
distinct from both static results and the original r13 insertion edge.
Machine checks record trusted scene acknowledgement and FK display matches;
these do not establish upstream scene/operator correctness.

**Remaining review limits:** The first-rejected PNG shows anatomy and a
marker, not a visually identifiable evaluated robot configuration. The
FDI31/tool close-up does not visibly include the template needed to explain
the reported phase-aware static pair. The saved first-rejected record has
outer `available=false`/`joints_si=null`, while its nested evaluated sample
is available with a vector; its consumption/label semantics need explanation
before treating that image as rejected-state placement evidence. No runtime
defect is inferred from that representation alone. Active top summaries in
backlog/TASKS still contain superseded P2-INCONCLUSIVE text, although report
Section T and updated task rows record bounded evidence PASS.

**Advisory only:** Preserve the recovered diagnostic packet; request a
concise saved-evidence review of the separate predicates, the existing
spindle-guide warning mode versus the original r13 policy, and the 15
input/output selections with explicit unresolved questions. Reconcile active
status summaries without erasing history. No policy change, geometry change,
P3/P4 authorization or acceptance follows from this advisory. Do not rerun
the native stack merely to restate already saved results.

## Campaign-1 bounded offline display-only recapture — 2026-09-15

**Operator scope.** The operator authorized one offline Slicer display-only
recapture using the retained inspection MRB and matching final P2 JSON packet.
The scope explicitly excluded ROS/MoveIt/native diagnostics, planning,
controllers, spindle activation, trajectory execution, container restart and
operator-owned session changes. The operator did not accept the inputs,
geometry, clinical intent or P1/P2 gates.

**Read-only preparation.** Inspected the retained `rejected_state.json`,
`state_identity.json`, scene audit and source geometry roles. Confirmed the
endpoint vector, Home vector, evaluated transition sample 1/134 and fraction
`0.00746268656716`. Confirmed the four draft support IDs versus the separate
five-tooth visible support, the absent independently materialized
`FinalizedTemplateShell` role, the retained `FinalPrintableTemplate`/STL, and
the separate robot-dock versus trajectory-guide parameters.

**Bounded execution.** Ran one temporary display-only Slicer process with the
existing offline FK path. The process loaded the retained MRB, used
`DENTORobotPlacement.py` / `DENTOWorkflowLogic.createOrUpdateRobotPlacement` /
`updateRobotJointPoses`, wrote the endpoint view first, then the transition
sample view, and saved separate MRBs and a JSON manifest. No ROS, MoveIt,
native diagnostic, planner, controller, spindle or trajectory path was
started, and the already-running container was not started, restarted or
recreated. A preliminary probe process created during this session was checked
by exact command/process ownership and gracefully terminated; the final
inventory showed no Slicer/Xvfb recapture process remaining. No operator-owned
process was stopped and the original MRB was not overwritten.

**Outputs.** Output directory:
`data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915/`.

- `01-static-endpoint.png`, SHA-256
  `33d2552ba0702c08bc50766b24728f6d93c77911e92466a23f3c7f5614c7ec88`;
  static endpoint display scene SHA
  `eda5526c773cacfa55960903dccf76997ebef34a74f4fd22d47b059357a1cf12`.
- `02-transition-sample-1-of-134.png`, SHA-256
  `55b42c909b0a0d0d0cf84bbd09b4308ef096f00cd8cbc7c04eed1f1bec985a19`;
  transition display scene SHA
  `3f298cdd71651fc6ef9acaa4dd3723b87283423780be1370b4d93fca88f41c75`.
- `display-only-recapture.json` records source hashes, exact vectors, runtime
  boundary flags, output hashes, `operator_confirmed_in_this_run=false` and
  `operator_acceptance=NOT_RECORDED`.

**Evidence limits.** The endpoint image visibly shows FDI31, final printable
template, burr and spindle with the phase-aware static rejection and reported
template–spindle pair. Native contact point/depth/clearance remain unknown and
were not drawn. The transition image shows the recorded sample attribution in
the banner and saved manifest; its 3-D numeric labels are partly clustered,
so the manifest is required for exact numeric re-derivation. This is
display-only evidence, not native contact or onset evidence.

**Disposition.** Canonical report Section V records the recapture and
supersedes Section U's proposal that it had not yet been performed, while
Section U remains preserved as historical handoff context. P1 overall scene
correctness/operator review remains INCOMPLETE; the current bounded native P2
packet remains `USER_REVIEW_REQUIRED`; operator acceptance is
`NOT_RECORDED`; P3/P4 remain unauthorized.

## Campaign-1 recapture inconsistency — coordinator diagnosis

**Operator observation:** The supplied recapture views did not appear to
represent the labelled states; the template was separated from the tooth and
the TCP did not appear near the collision. Asked for explanation, not another
run, implementation or geometry change.

**Inspection:** Read the pasted handoff, active backlog/campaign contracts,
report Section V and manifest. Ran `graphify query 'offline display FK robot
joint state recapture base transform goal transforms' --budget 900` for source
routing, then scoped `rg`/`sed` reads of the placement and diagnostic helpers.
Used `unzip -l`, scoped `jq`, and inline read-only Python (`zipfile`,
`xml.etree.ElementTree`, `struct`, `math`) to inspect both saved MRBs, decode
their legacy binary VTK points with storage RAS/LPS metadata, apply the saved
jaw matrix, and compare label positions to manifest state centres. No Slicer,
Docker, ROS, native query, test suite or build ran. Some initial file queries
used overlay-relative paths from the physical checkout and failed; corrected
queries used the appropriate root. No failed read changed state.

**Findings:** Recorded exact evidence in recovery report Section W. The tooth
review mesh omitted the jaw-opening transform (approximately 40.89 mm mean
vertex displacement), while the displayed template's transformed bounds
match the native audit. Static-scene Burr/Spindle labels equal transition
sample coordinates rather than endpoint coordinates (64.05/70.17 mm centre
differences). Both MRBs omit the seven pose-transform nodes referenced by
their robot models. The recapture is not valid visual acceptance evidence;
these are technical reconstruction defects, not unresolved operator intent.
The exact screenshot command ordering is unknown because the temporary
driver was not retained. Native packet results remain distinct and preserved.

**Documentation/disposition:** Updated report Section W and the existing
backlog campaign review requirement. No implementation was performed or new
runtime authority inferred. Luna owns the bounded reconstruction correction;
no source anatomy/template/tool/policy change or P3/P4 advancement is justified
by the misleading recapture.

## 2026-09-15 source-only Section-W recapture correction

**Operator scope.** The operator directed a source-only correction after
rejecting the prior recaptures as unfaithful state/geometry evidence. The
operator prohibited Slicer/ROS/Docker/native execution, actual template or
anatomy/tool changes, collision-policy changes, planning, and P3/P4. No
operator-confirmed flag was set.

**Implementation.** Added and retained
`ros2_ws/src/DentoBot/Testing/run_dentobot_c1_display_only_recapture.py` plus
`ros2_ws/src/DentoBot/Testing/test_dentobot_c1_display_only_recapture.py`.
The driver now loads the retained inspection MRB separately for endpoint and
transition states; copies the selected tooth and final printable template in
prepared world RAS/mm using existing offline display helpers without moving
either source node; keeps endpoint state and evaluated transition sample
`1/134` separate; compares display geometry fingerprints/bounds before any
image; compares displayed robot mesh transforms to offline FK; derives and
compares displayed TCP/burr/spindle link frames to saved native endpoint FK;
and compares the same model/link transforms across save/reopen. Only the
review-copy robot nodes/pose attributes are made persistent in the separate
review MRB, preserving the production transient-node policy.

**Verification.** Source-only commands run from the DentoBot checkout:

```text
PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-display-corrected-pycache python3 -m py_compile Testing/run_dentobot_c1_display_only_recapture.py Testing/test_dentobot_c1_display_only_recapture.py
PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-display-corrected-pycache python3 -m pytest -q -p no:cacheprovider Testing/test_dentobot_c1_display_only_recapture.py
```

Result: `8 passed`. Graphify overlay refresh then completed successfully with
`Code graph updated` / `[graphify watch] Rebuilt: 8250 nodes, 12930 edges,
723 communities`; existing extraction warnings for unrelated C++ files were
reported by Graphify. No Slicer, ROS, Docker, MoveIt, build, planning or
native diagnostic ran in this turn.

**Evidence boundary.** The retained P2 packet has endpoint native FK matrices,
but only the transition joint vector for sample `1/134`. The corrected driver
therefore fails closed before a transition image until evaluated-sample native
FK matrices are saved; it consumes an explicitly saved evaluated-sample FK
record if one is later present, but does not substitute offline FK, endpoint
FK or a marker. No new image or MRB was created, and the prior recapture artifacts
remain historical/unaccepted. P1 overall scene correctness remains
`INCOMPLETE`, P2 remains under the existing review gate,
`operator_acceptance=NOT_RECORDED`, and P3/P4 remain unauthorized.

## Campaign-1 endpoint-versus-transition continuation advisory

**Operator context:** Supplied the two source-only continuation reports; both
retain missing transition-sample native FK and no display-runtime approval.
This review does not reinterpret those reports as runtime authorization.

**Read-only checks:** Read backlog/contracts, report Section X, the current
verification protocol and the retained recapture driver's state extraction,
readiness, capture and `run`/`main` paths. Used scoped `sed`/`rg` and
`graphify query 'run_dentobot_c1_display_only_recapture endpoint transition
native FK prerequisite capture run' --budget 750`. The matrix search found
no recapture entry. No tests, code edits, Slicer/container/native actions or
new captures occurred.

**Finding:** `run()` invokes endpoint `_capture_one` first, then
unconditionally invokes the transition capture and reports overall PASS only
if both pass. Missing transition FK is therefore not a precondition that
prevents the endpoint call in the inspected source; it is a guaranteed
transition failure and aggregate completion limitation for this retained
packet. Source-only approval independently explains why neither continuation
executed the corrected endpoint path. Its runtime parity remains unproven.

**Recommendation, not adopted authority:** Explicitly narrow the next
deliverable to endpoint-only review, retain all endpoint geometry/native-FK/
save-reopen checks, and defer the unsupported transition image with a visible
reason. Use a focused orchestration test to show endpoint success can be
reported independently without entering the transition capture. Do not add
more parsers for nonexistent sample FK or launch native diagnostics to obtain
it solely for the context image. A separately explicit, bounded offline
endpoint display approval is required; P1/P2 campaign acceptance and P3/P4
authorization remain unchanged.

## Campaign-1 endpoint-only review — pre-execution authorization and invocation

**Operator authorization.** The operator explicitly superseded the previous
two-image requirement for the next attempt with ENDPOINT-ONLY REVIEW. The
transition sample remains numerically preserved but is deferred as `NOT_RUN`
because saved native sample FK is unavailable. No new native FK or speculative
transition parsing is authorized for this session. One bounded offline Slicer
endpoint session is authorized after source checks; ROS/MoveIt/native
diagnostics, planning, hardware, geometry/policy changes and P3/P4 remain
excluded.

**Source change and checks.** Added the explicit
`DENTOBOT_C1_RECAPTURE_ENDPOINT_ONLY=true` mode so `run()` reports endpoint
status independently and never enters `_capture_one` for the transition. The
transition record remains embedded in `state_separation` and in the explicit
`transition_sample.status=NOT_RUN` report. `py_compile` and the focused test
file passed `9 tests`.

**Inputs and output boundary.** The three frozen inputs were hash-checked
before runtime: inspection MRB
`8ded0a8237aa220ff51cb4ea733ff950fad459f5b9ae9e883e3222b1c9a9aa47`,
`rejected_state.json`
`b185dc8caf3153c524df368c70cbf5395eada198e22d40e1763f5f29bb15a7f1`, and
`state_identity.json`
`05f1f2328a066b1b48878519bae299e7fce69e08658761cb714f848f5a9a9d20`.
The new host output directory was absent before execution:
`data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only/`.

**Exact invocation recorded before execution:**

```bash
timeout 180s docker exec dentobot-slicerros2 bash -lc 'set -euo pipefail; export DENTOBOT_C1_RECAPTURE_ENDPOINT_ONLY=true DENTOBOT_C1_RECAPTURE_SOURCE_ROOT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31 DENTOBOT_C1_RECAPTURE_INSPECTION_MRB=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/inspection-scene/fdi31-p2-rejected-state.mrb DENTOBOT_C1_RECAPTURE_REJECTED_STATE=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/rejected_state.json DENTOBOT_C1_RECAPTURE_STATE_IDENTITY=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/state_identity.json DENTOBOT_C1_RECAPTURE_OUTPUT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only DENTOBOT_C1_RECAPTURE_MANIFEST=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only/endpoint-review.json; timeout 150s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_c1_display_only_recapture.py'
```

This uses the existing `dentobot-slicerros2` display host only. It does not
start/restart/recreate the container or launch ROS/MoveIt/native diagnostics.
The inner timeout is 150 seconds and the outer Docker timeout is 180 seconds;
post-run inventory will verify that only this task's Slicer/Xvfb processes
exited, without touching operator-owned processes. The runtime result is
recorded in the following entry.

## Campaign-1 endpoint-only review — bounded runtime result and source compatibility repair

**Operator scope.** The approved deliverable was endpoint-only review. The
transition image was explicitly `NOT_RUN` because saved native FK for sample
`1/134` is unavailable. The frozen MRB/JSON inputs, collision policy,
geometry, task, native interfaces and retry history remained unchanged. No
ROS/MoveIt/native diagnostic, planner, controller, spindle, trajectory,
geometry/policy change or P3/P4 action was authorized.

**Preflight and execution.** `docker ps` showed the existing
`dentobot-slicerros2` container already `Up 3 hours`. The exact process
inventory was empty for Slicer/Xvfb, this recapture, ROS launch, MoveIt,
collision guard, inference and trajectory processes. The first host-side
Docker invocation was denied by the local Docker-socket permission boundary
before starting anything. The identical recorded invocation was then run
through approved elevated access. It used the existing container, inner
`timeout 150s` and outer `timeout 180s`; no container restart/recreate or
operator-owned session action occurred.

Slicer loaded the retained inspection MRB in approximately 1.04 seconds. The
driver stopped before endpoint capture with the exact compatibility error:

```text
'MRMLCorePython.vtkMRMLModelDisplayNode' object has no attribute 'SetPropertiesLabelVisibility'
```

The separate output directory contains only
`endpoint-review.json` (347 bytes, SHA-256
`25b6a9b113902967234b5edc2e875e72d40c06aae21b942e6df3ac4f5175594a`). No
endpoint image or review MRB was written. This is an early runtime
compatibility stop, not an endpoint geometry/FK parity failure: the endpoint
status is `NOT_ESTABLISHED`, and no prepared-world geometry, displayed
TCP/burr/spindle/native-FK or save/reopen result exists. Transition status is
`NOT_RUN`; its saved numerical evidence remains preserved and no new native FK
or speculative parser was used.

**Cleanup evidence.** The container remained `Up 3 hours` after the session.
Post-run process inventory was empty for the checked Slicer/Xvfb/ROS/MoveIt/
guard/inference/trajectory classes. No operator-owned process was touched.

**Source-only continuation after the stop.** Added the smallest optional-API
guard for `SetPropertiesLabelVisibility` and preserved endpoint-only
`review_type`/`capture_mode` in early error manifests. Compile and focused
verification then passed:

```text
PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-endpoint-only-pycache python3 -m py_compile Testing/run_dentobot_c1_display_only_recapture.py Testing/test_dentobot_c1_display_only_recapture.py
PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-endpoint-only-pycache python3 -m pytest -q -p no:cacheprovider Testing/test_dentobot_c1_display_only_recapture.py
10 passed in 0.02s
git diff --check
```

Graphify refresh completed with `Code graph updated` and
`[graphify watch] Rebuilt: 8258 nodes, 12948 edges, 732 communities`.
Known unrelated C++ extraction warnings and six fail-closed retained nodes
remain as Graphify warnings; they are not caused by this Python change. No
second Slicer session was run. A replacement endpoint runtime requires a new
specific approval. P1 overall scene correctness and operator review remain
`INCOMPLETE`, operator acceptance remains `NOT_RECORDED`, and P3/P4 remain
unauthorized.

## Campaign-1 endpoint-only repair rerun — pre-execution authorization and invocation

**Operator authorization.** The operator authorized a new bounded endpoint-only
offline Slicer session and permitted a container restart only if needed to
recover a Slicer-testing blocker. ROS/MoveIt/native diagnostics, planning,
controllers, spindle, trajectory execution, geometry/policy changes and P3/P4
remain excluded. The previous error output is preserved; this is a separate
output location under the same frozen Campaign-1 run/evidence inputs.

**Preflight.** Source py_compile passed and the focused suite passed 10 tests.
The existing dentobot-slicerros2 container was Up 3 hours; the checked
Slicer/Xvfb/recapture/ROS/MoveIt/guard/inference/trajectory process inventory
was empty, so no restart was needed. The inspection MRB, rejected_state.json
and state_identity.json hashes remained respectively
8ded0a8237aa220ff51cb4ea733ff950fad459f5b9ae9e883e3222b1c9a9aa47,
b185dc8caf3153c524df368c70cbf5395eada198e22d40e1763f5f29bb15a7f1, and
05f1f2328a066b1b48878519bae299e7fce69e08658761cb714f848f5a9a9d20.
The new output directory
display-only-recapture-20260915-endpoint-only-repair1/ was absent.

**Exact invocation recorded before execution:**

~~~bash
timeout 180s docker exec dentobot-slicerros2 bash -lc 'set -euo pipefail; export DENTOBOT_C1_RECAPTURE_ENDPOINT_ONLY=true DENTOBOT_C1_RECAPTURE_SOURCE_ROOT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31 DENTOBOT_C1_RECAPTURE_INSPECTION_MRB=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/inspection-scene/fdi31-p2-rejected-state.mrb DENTOBOT_C1_RECAPTURE_REJECTED_STATE=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/rejected_state.json DENTOBOT_C1_RECAPTURE_STATE_IDENTITY=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/state_identity.json DENTOBOT_C1_RECAPTURE_OUTPUT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only-repair1 DENTOBOT_C1_RECAPTURE_MANIFEST=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only-repair1/endpoint-review.json; timeout 150s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_c1_display_only_recapture.py'
~~~

The runtime result will be appended after cleanup. Endpoint parity remains
fail-closed; the transition remains NOT_RUN and no new native FK will be
obtained.

## Campaign-1 endpoint-only repair1 result — second runtime API blocker

The repair1 invocation passed the previous optional display-label API and
loaded the retained MRB in approximately 1.20 seconds. It then stopped before
the endpoint parity gate with:

~~~text
'MRMLCorePython.vtkMRMLModelNode' object has no attribute 'GetMatrixTransformToWorld'
~~~

The separate repair1 directory contains only endpoint-review.json (361 bytes,
SHA-256
1ec4cb53791b7ece4aef5b9ef4f324e7e1871819407a725e1747cfb7f432750). No image, review
MRB or parity record was created. Endpoint remains NOT_ESTABLISHED due to a
second runtime API compatibility stop, not a geometry/FK mismatch. Transition
remains NOT_RUN; no native FK was requested or obtained.

Cleanup again showed the container Up 3 hours and no checked Slicer/Xvfb/
ROS/MoveIt/guard/inference/trajectory process. No operator-owned process was
touched and no restart was needed.

**Source-only repair.** Added a direct transform-node world-matrix path and a
fail-closed parent-transform fallback for transformable model nodes; parentless
review models use identity. No source geometry, display placement, collision
policy, native interface or transition parsing changed. The focused suite now
passes 12 tests and Graphify refreshed successfully with
Code graph updated (`8266 nodes, 12962 edges, 731 communities`).

## Campaign-1 endpoint-only repair2 — pre-execution authorization and invocation

The current operator authorization covers this needed source repair and one
further bounded endpoint-only Slicer attempt. The preflight found the existing
container Up 3 hours, no matching Slicer/Xvfb/ROS/MoveIt/guard/inference/
trajectory process, and the repair2 output directory absent. No restart is
currently needed. This is the last display-runtime attempt under the existing
three-failure ceiling; all previous error artifacts and frozen input hashes
remain preserved.

**Exact invocation recorded before execution:**

~~~bash
timeout 180s docker exec dentobot-slicerros2 bash -lc 'set -euo pipefail; export DENTOBOT_C1_RECAPTURE_ENDPOINT_ONLY=true DENTOBOT_C1_RECAPTURE_SOURCE_ROOT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31 DENTOBOT_C1_RECAPTURE_INSPECTION_MRB=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/inspection-scene/fdi31-p2-rejected-state.mrb DENTOBOT_C1_RECAPTURE_REJECTED_STATE=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/rejected_state.json DENTOBOT_C1_RECAPTURE_STATE_IDENTITY=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/state_identity.json DENTOBOT_C1_RECAPTURE_OUTPUT=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only-repair2 DENTOBOT_C1_RECAPTURE_MANIFEST=/workspace/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/c1-p1p2-recheck-20260914-r6/corrected-p2/burr-alias-recheck-corrected/display-only-recapture-20260915-endpoint-only-repair2/endpoint-review.json; timeout 150s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_c1_display_only_recapture.py'
~~~

The inner and outer timeouts remain 150s and 180s. Endpoint parity remains
fail-closed and transition capture remains explicitly NOT_RUN.

## Campaign-1 endpoint-only repair2 result — endpoint parity blocker

The third and final scoped display-runtime attempt reached the endpoint
pre-save parity gate and stopped as required. Its manifest is
endpoint-review.json in
display-only-recapture-20260915-endpoint-only-repair2/ (26,492 bytes,
SHA-256
3362594236be3227729b5dd59dde7ebcd4cc592b971644465a174b12075ebd3c). No
endpoint image or review MRB was written.

Measured results: the final printable-template fingerprint and bounds matched
exactly (0.0 mm error versus 0.05 mm tolerance). The prepared FDI31 target
fingerprint mismatched, and its maximum bound error was 40.74010467529297 mm
versus the 0.05 mm tolerance. Robot mesh and model/link transforms matched.
Saved native-vs-offline FK matched for TCP, burr and spindle; displayed burr
and spindle matched native FK with approximately 1.07e-13 mm and 1.22e-13 mm
translation error. Displayed TCP-versus-native FK was unavailable because the
seven displayed models contain link-1 through link-5, spindle and burr but no
TCP model; the retained catalog exposes TCP as a transform node only. The
driver rejected a marker/transform-only substitution. Save/reopen was not
reached because the pre-save gate failed.

Read-only source tracing attributes the target mismatch to the review driver's
use of the segmentation-parent world helper without the Case Foundation/
jaw-opening frame represented in the native prepared-target record. The final
template path uses the target-attached world helper and matches. This is an
evidence-contract finding, not authorization to move anatomy/template/tool
geometry or change collision policy.

The output status is BLOCKED, endpoint review is not accepted, and transition
remains NOT_RUN with its saved numerical evidence preserved. The container
remained Up 3 hours and the post-run process inventory was empty for Slicer,
Xvfb, ROS, MoveIt, guard, inference and trajectory classes. No operator-owned
process was touched. The display retry ceiling is reached; no further runtime,
geometry correction, tolerance change or speculative TCP substitution was
performed. P1 overall scene correctness/operator review remain INCOMPLETE,
operator acceptance remains NOT_RECORDED, and P3/P4 remain unauthorized.

## Endpoint parity blocker — read-only orchestration review

**Operator request:** explain the parity blocker and provide one bounded Luna
repair prompt; the operator reports already authorizing needed container
restarts, blocker fixes, and testing reruns. No implementation or runtime was
requested from this reviewer.

**Inspection:** read the attached repair2 report and routed campaign records;
used Graphify query and scoped `sed`, `rg`, `jq`, and `tail` reads of the
recapture driver, geometry helpers, URDF, manifest, and verification protocol.
The driver omits the native moving-jaw preparation step after segmentation
world extraction. Production applies `_step6CaseJawPolydataWorld` and
triangulation before recording prepared target geometry. Separately, the URDF
defines `dentobot_drill_tcp` as a meshless frame fixed to
`pneumatic_spindle-Copy`; requiring a physical TCP mesh is an incorrect review
criterion. The displayed spindle already passes the saved native comparison.

**Recommendation, not executed:** reconstruct only the review target using
the unchanged saved native preparation path; derive observed TCP from the
actual displayed spindle-link pose and frozen URDF fixed transform, then
compare independently with saved native TCP. Do not use a free-standing marker
as proof or change geometry, TCP definition, tolerances, or collision policy.
Provide a revised endpoint-only implementation/testing envelope for the
operator to send to Luna; preserve the previous failed attempts and retry stop.
Endpoint review, campaign acceptance, transition evidence, and P3/P4 authority
remain unchanged. Only this advisory log entry was added. An initial patch
context mismatch made no changes; the corrected append succeeded. No tests,
Slicer/container operations, native diagnostics, or source changes were run.

## Campaign-1 endpoint-only parity repair — pre-execution record

The operator explicitly authorized the two source corrections in the attached
endpoint-review repair request and at most three targeted endpoint-only offline
Slicer attempts. Prior repair1/repair2 artifacts, frozen inputs, retry history,
operator acceptance status and all exclusions remain in force. The production
trace showed one native Case Foundation jaw-opening transform followed by the
native triangle filter for a moving target. The driver now reproduces that
path on the review copy exactly once and checks the retained source/prepared
fingerprints/bounds. The retained URDF's canonical `dentobot_drill_tcp` is
meshless and fixed to `pneumatic_spindle-Copy`; the driver now derives TCP from
the actual displayed spindle mesh and its visual-origin-aware fixed relation,
with robot-profile identity verification.

Compilation without bytecode writes passed; focused tests passed `18/18`.
The host/container driver hash is
`75953b2faf9567f4c90d254dba3509a6e4a3236b2de31f9400911fc37184bb14` and the
focused test hash is
`cdf50314409460fd58fa55fecbbb4b8b13e528e3aea07d754104769e51ea5c8a`.
Graphify refreshed successfully with `Code graph updated` (`8292 nodes`,
`13026 edges`, `720 communities`). The existing container was `Up 3 hours`
with no matching Slicer/Xvfb/ROS/MoveIt/DentoBot/guard/inference/trajectory
processes; no process was stopped and no restart was needed. The repair3
output directory was absent. Frozen input hashes were unchanged: inspection
MRB `8ded0a8237aa220ff51cb4ea733ff950fad459f5b9ae9e883e3222b1c9a9aa47`,
rejected state `b185dc8caf3153c524df368c70cbf5395eada198e22d40e1763f5f29bb15a7f1`,
identity `05f1f2328a066b1b48878519bae299e7fce69e08658761cb714f848f5a9a9d20`.

The exact command recorded before execution in recovery report Section AE is:
`timeout 180s docker exec dentobot-slicerros2 bash -lc 'set -euo pipefail;
export DENTOBOT_C1_RECAPTURE_ENDPOINT_ONLY=true ... DENTOBOT_C1_RECAPTURE_OUTPUT=.../display-only-recapture-20260915-endpoint-only-repair3 ...;
timeout 150s xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer
--no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow
--python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_c1_display_only_recapture.py'`.
The ellipsis is only for this logbook line; the recovery report contains the
full literal invocation. The endpoint gate remains fail-closed and transition
capture remains `NOT_RUN`.

## Campaign-1 endpoint-only repair3 result and repair4 preflight

The first revised Slicer attempt loaded the retained inspection MRB, then the
unchanged native jaw helper stopped at its freshness gate because the MRB did
not contain the transient fixed-upper and moving-lower Case Foundation display
volumes. The saved jaw transform was present. Repair3 wrote only the preserved
325-byte error manifest at
`.../display-only-recapture-20260915-endpoint-only-repair3/endpoint-review.json`
with SHA-256
`9af0d39f8e94363d81192df25e93040d41084120145c1221ea18a335fd23643d`.
No image/MRB/parity record was produced; cleanup found no matching process.

The next source correction restores only those missing transient display
volumes in the isolated loaded scene through the existing offline helper,
checks the saved Case Foundation preparation fingerprint against retained
native evidence, then uses the unchanged one-time jaw transform plus native
triangulation. It passed compilation and `20/20` focused tests; Graphify
refreshed successfully. Host/container hashes match: driver
`4668e5b3207c5dfa92947529fad329efcf6e498d707a03d47febd93eb69cf9ed`, tests
`6a32b4ee3c4245b5afc78dbedc27db8176c3e635802dbc63d2b3b278091c127e`.

The existing container was up with no matching Slicer/Xvfb/ROS/MoveIt/
DentoBot/guard/inference/trajectory processes and no repair4 output existed.
The full repair4 command is recorded in report Section AF. It keeps the
150-second inner and 180-second outer timeout, endpoint-only capture, separate
output location, no native diagnostics and transition `NOT_RUN`.

## Campaign-1 endpoint-only repair4 result and repair5 preflight

Repair4 progressed through target preparation and pre-save endpoint parity but
failed closed after reopening the separately saved review MRB. Pre-save
prepared-target geometry, one-time jaw-preparation fingerprint, robot-profile
identity, seven link/mesh transforms, and derived spindle/burr/TCP FK all
matched retained evidence. After reopen, the workflow rebuilt transient robot
links from the old parameter-node joint fields; link 2 onward and the spindle,
burr and derived TCP no longer matched the retained endpoint. Manifest:
`.../display-only-recapture-20260915-endpoint-only-repair4/endpoint-review.json`,
SHA-256
`4a87c8a7acc08d90c915c51f605e60b8b8d37e957de6314e760554be3f473047`.
The separate `01-static-endpoint.mrb` was preserved; no PNG was captured.
Repair4 status is `BLOCKED`, not endpoint PASS.

The minimal driver repair writes the frozen J1-J5 endpoint vector into the
existing workflow parameter-node display fields (degrees/mm), retains J6 at
canonical zero, reapplies offline FK, and records the review-only values before
save. No production node policy or frozen input changed. Focused tests pass
`22/22`; compile checks pass; Graphify refreshed (`8307/13064/719`). Host and
container hashes match: driver
`b47e47983a87c76e4083846c98b38adc3b71c47f8b8a948bce32f1f1852950c2`, tests
`edac74fc61a644c4caee52c11b2a148bcbbed7b02fecbaf8b24777526a1ccbbd`.

The read-only preflight found `dentobot-slicerros2` `Up 4 hours`, no matching
Slicer/Xvfb/ROS/MoveIt/DentoBot/guard/inference/trajectory process, and no
repair5 output. No process was stopped or restarted. The full repair5 command
is recorded in recovery report Section AG. It is the final authorized
endpoint-only attempt, with a 150-second inner and 180-second outer timeout;
transition remains `NOT_RUN`, and no native FK or planning is authorized.

## Campaign-1 endpoint-only repair5 result

The final authorized endpoint-only offline Slicer attempt passed the complete
machine parity gate. Manifest:
`.../display-only-recapture-20260915-endpoint-only-repair5/endpoint-review.json`,
SHA-256
`d02ee16dbedd6c193670c4e99f7f26f7070393e9b4d644f05b1beefd1dca5fcf`.
Saved review MRB:
`.../display-only-recapture-20260915-endpoint-only-repair5/01-static-endpoint.mrb`,
SHA-256
`04a5f62a455205d83820d7dfab7b9923ef6914d2f78124e71c35f65524c5fcdb`.
The endpoint manifest status is `PASS`; transition is `NOT_RUN` because native
sample-1/134 FK was not retained.

Prepared target and final template fingerprints/bounds matched native evidence
exactly (0.0 mm errors); the jaw-preparation fingerprint matched and the
application count was one. Robot-profile identity, all seven link/mesh and
model/link transforms, and post-reopen pose persistence passed. Derived
meshless TCP, spindle and burr matched retained native endpoint FK with 0.0
degree rotation error; TCP translation error was `0.000014754 mm`.

Images were saved separately and passed nonblank checks:
`.../01-static-endpoint-context.png` SHA-256
`89871ad31287c2d8b8e43726507f99c65b45c4fd668cbaaaecbffa2e154eb211` and
`.../01-static-endpoint-closeup.png` SHA-256
`2bab8ec2fd8f2d7956ce4292b213ebe1c6c310ced43dc244161dde69871224fc`.
They remain display-only evidence; target scale/label overlap still requires
operator visual review. No operator acceptance was recorded.

The container stayed `Up 4 hours` and the post-run matching process inventory
was empty. No process was stopped/restarted and no operator-owned session was
touched. No ROS/MoveIt/native diagnostic/planning/controller/spindle/trajectory
activity occurred. P1 overall scene correctness/operator review remains
`INCOMPLETE`; P3/P4 remain unauthorized. Historical repair3/4 manifests and
the repair4 blocked MRB remain preserved.

## Endpoint screenshot approval and proposed next Luna handoff

**Operator observation/request:** "yes finally! i could see screesnhots which
passes, and can be approved to proceed. give next prompt as required, hopefully
P1 and P2 blockers are all solved?" Recorded as review acceptance of the
corrected endpoint images, not all 15 inputs or clinical suitability.

**Read-only review:** read the new attached report, backlog/routed contracts,
Campaign-1 P1/P2/P3 criteria, verification protocol, and repair4/5 evidence.
Graphify query located the display/reopen path; scoped source reads confirm
the corrected parameter values are written before save and the reopened
display is compared rather than reset by the capture path. `jq` reports PASS
for repair5 pre-save, post-reopen and persistence gates; `sha256sum` matches
`d02ee16dbedd6c193670c4e99f7f26f7070393e9b4d644f05b1beefd1dca5fcf`.
Inspected both endpoint PNGs. No tests or Slicer/ROS/container runs occurred.

**Interpretation/handoff:** the visual parity blocker is solved for the retained
endpoint. Overall P1 scene/input acceptance is not automatically solved.
Prepare one prompt for retained-evidence closeout and conditional P3 under the
existing 128-solve bound; do not require missing historical transition imagery
for static Target feasibility. P4 remains unauthorized. Updated the same
canonical report (AI), task, plan, decision and backlog records; no new task
queue or source implementation. Documentation changes verified by readback.

## Campaign-1 retained-evidence closeout after endpoint approval

**Operator decision recorded:** The repair5 endpoint screenshots are approved
as understandable visual evidence of the retained static endpoint only. This
does not accept the fifteen automatic selections, scene/geometry, clinical or
physical fit, native contact interpretation, collision-free motion or full
workflow. No generated acceptance flag was changed.

**Read-only evidence review:** The repair5 manifest remains `PASS` for endpoint
geometry identity, robot/link/model transforms, derived meshless TCP,
burr/spindle FK parity and save/reopen pose persistence. The retained native
P2 packet separates phase-aware static rejection of both endpoint candidates
(final printable template ↔ spindle), generic static contacts (including tooth
↔ spindle and tooth ↔ burr), and Home-to-endpoint corridor rejection at
sample 1/134. The last item is transition-only, not a Target static verdict or
original r13 insertion/onset. The later native scene acknowledgement matches
31/31 IDs, poses/bounds and policy; the earlier zero-object audit remains
historical. Technical bookkeeping resolves the finalized-shell row to the
retained final STL and the P1 null payload to historical audit state, without
changing geometry or policy. Transition FK remains unavailable and its image
remains `NOT_RUN`.

**Gate result:** P1 saved-input/machine checks are `PASS`; P1 overall
scene/input correctness and operator review are `INCOMPLETE`; P2 static
diagnostic evidence is `PASS` with `USER_REVIEW_REQUIRED`. No P3/P4 runtime,
planning, source implementation or hardware action was performed. The two
remaining grouped operator questions are recorded in report Section AJ:
confirm the exact frozen case/task inputs, and confirm the exact selected
scene/workflow objects and robot-dock-bore versus guide-bore role. Campaign 1
stops here pending those answers.

**Repository checkpoint:** The pre-closeout working tree was committed as
`b94a201` (`checkpoint: Campaign 1 P1/P2 diagnostics and runtime repair`) and
pushed to the new remote branch
`codex/campaign1-checkpoint-b94a201`; `origin/main` was not changed. The
closeout documentation added in this entry is pending its own verification
and checkpoint.

## Campaign-1 current Step-4A–5B operator observations and source-only correction

**Operator statements, not agent inference:** After reopening the immutable
`dentobot-case-13sept.dentocase`, the operator sees FDI31 preselected and an
apparent duplicate target display in original/closed and opened poses, does
not recall saving the target, and says downstream navigation still proceeds.
The operator confirms the four adjacent Step-4B supports shown in their image.
They request the pictured Step-4C dimensions as defaults, question whether
the oblique plane is visual versus geometric/open-jaw misplacement, request a
4-mm Step-5A plane-depth default, and report that Step-5A reset changed the
appearance. At Step 5B they see a wrong-looking plane, grey fields and a
saved 1.5-mm trajectory-guide hole blocked by the 2.0-mm builder minimum.
No blanket scene, input, clinical or template acceptance was spoken.

**Read-only diagnosis:** Backlog-first lookup reused
`S6-REUSABLE-CASE-SETUP` (priority 0), with separate linked
`W4B-P2-SUPPORT-AUTO`, `W5-U-01` and `W5-U-05` slices. `rg`/`sed` traced the
parameter schema, UI defaults, Case Foundation opened-world surface copy,
4C target-crown docking constructor, 5A five-tooth plane reset, 5B panel,
builder/preflight and shared geometry normalization. A scoped overlay
`graphify query` located those components. `unzip -l` and a read-only
`zipfile`/JSON/MRML extraction showed the immutable source has a saved FDI31
target ROI/segment but empty trajectory and no 4C/5A plane; source and r7
Foundation planning-pose fingerprints match. The source's saved radius is
15.0 mm, robot-dock bore 1.0 mm, 5A depth 3.0 mm, trajectory-guide hole 1.5
mm and research shell channel 1.5 mm. R7 world plane values and crown-cap
normal/tilt metrics are in report AK; they establish retained r7 geometry, not
the operator's current unsaved viewport. The operator's before/after 5A
screenshots show visible-preview mesh counts changing from 1584/2727 to
1898/3326 points/cells, but provide no plane origin/normal readback. Source
trace found no 5A alias to the 4C plane; retained r7 centres differ 3.8367
mm and normals differ 14.8413°, explaining possible visual overlap without
proving the current live state. One 3.0-mm programmatic 5A method default
remained after the schema/UI repair; all production callers passed depth.
5B status computed but failed
to show a dimension `inputError`; the inexpensive preflight did not call the
same normalizer before cached work. The screenshot's grey undercut controls
are output fields; source does not explicitly disable the editable section-2
guide-hole control. UI clamping in the current live session remains unproven.
`rg --files` under the saved SampleStudy1 data found only retained Campaign-1
review MRBs/JSON for 2026-09-15, not a newly saved operator 4C/5A scene.
Further read-only XML inspection of the source MRB found one closed source
segmentation plus fixed-upper/moving-lower Case Foundation derived displays.
The FDI31 ID is represented in the closed source and opened moving-lower
display, not twice in the target-selection lineage. The source display was
aggregate-visible at save, both derived displays aggregate-hidden; the
current source's opened-view enforcement is intended to invert that display
relationship. Which live callback/toggle left two poses visible is unknown.

**Decision/implementation, separate from operator acceptance:** The pictured
4C/5A defaults apply only to new parameter nodes; frozen source/r7/r13/r14
inputs are not changed. Edited
`DENTOWorkflow/Resources/Python/dentobot_workflow/parameter_state.py`
(4C robot-dock bore 1.0 mm; 5A depth 4.0 mm),
`DENTOWorkflow/Resources/UI/DENTOWorkflow.ui` (4C radius 10.0 mm; 5A depth
4.0 mm), `dentobot_workflow/widget_template_build.py` (shared early Step-5B
normalization and actionable status),
`dentobot_workflow/logic_guide_support.py` (programmatic 5A default and
tilted-plane docstring), and `Testing/test_step6_planning.py`
(default/UI parity and local-stub invalid-hole preflight). Updated this
logbook, recovery report AK, DECISIONS, TASKS and backlog under the existing
task IDs. The 2.0-mm trajectory-guide floor, collision policy, anatomy,
transforms, TCP/tool and frozen geometry are unchanged. Copied the five
operator PNGs without moving originals into separate versioned
`data/dentobot-runs/fdi31-operator-steps4a-5b-20260915/`; `sha256sum`
verified each copy. No Slicer/ROS/MoveIt/native, container or hardware run and
no interaction with the operator's GUI occurred.
Each original/copy PNG pair also passed byte-for-byte `cmp -s` (5/5).

**Commands/check results/errors:**

- Before source repair,
  `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_planning.py -k 'new_case_dock_and_support_defaults or legacy_guide_hole_fails_5b_preflight'`
  produced the expected `2 failed` (old bore default and missing early
  preflight). After repair the same selection passed `2/2`; the complete
  focused file passed `24/24`. The continued 5A API-default assertion first
  failed against its remaining 3.0-mm default, then the focused file again
  passed `24/24` after the one-line repair.
- First direct `python3 -m py_compile` could not write the checkout's
  `__pycache__` (`Errno 13`); the same changed-file compilation with
  `PYTHONPYCACHEPREFIX=/tmp/fdi31-campaign-pycache` exited 0. This was a
  host write-path error, not a source syntax failure.
- `git diff --check` exited 0; the overlay-only
  `Workspace/scripts/graphify-update.bash` exited 0 and printed
  `[graphify watch] Rebuilt` / `Code graph updated`. Its unrelated existing
  C++ parser/fail-closed corpus warnings are retained as warnings, not a
  graph-update failure.
- After the continued 5A API default change, the overlay-only graph wrapper
  again exited 0 with `Rebuilt` and `Code graph updated`; final changed-file
  compilation (including `logic_guide_support.py`), the focused `24/24` tests
  and `git diff --check` all exited 0.
- `sha256sum` confirmed unchanged frozen source
  `14ed8f0f...`, r7 case `5440961c...`, and r7 STL `ccee3e59...`, matching
  the Campaign-1 manifest. The five screenshot copy checksums are in the
  shell output and source originals remain in `/tmp`.

**Evidence boundary/next action:** The operator's manual Step-4B membership
confirmation narrows one P1 review item. P1 saved-input checks remain PASS for
those previously verified, but overall current scene correctness/operator
review remains INCOMPLETE. Retained P2 static diagnostic evidence remains PASS
with USER_REVIEW_REQUIRED; no new P2 observation is claimed. The precise
missing evidence for a visual-versus-geometric 4C/5A verdict is a separate
saved current world-MRML scene with plane/dock/trajectory/display state.
Pre-reset numerical state was not retained, so its exact cause cannot be
retroactively proved by the two camera screenshots. P3/P4 and all motion,
controller, spindle, patient-facing and full-flow actions remain paused.

## Separate 15Sept test `.dentocase` saved after perceived FDI31 artifact deletion

**Operator statement:** The operator says they deleted the FDI31 artifact and
saved `dentobot-case-15sept.dentocase` as a new test case. No statement of
FDI31 mask, target, plane, scene or Campaign-1 acceptance was made.

**Read-only inspection and interpretation:** Reused priority-0
`S6-REUSABLE-CASE-SETUP` and its restore overlap; report Section AL contains
the detailed comparison. ZIP and all outer-manifest member hashes passed;
whole-file SHA-256 is `d084f0ce...d6a`. The nested MRB retains byte-identical
source FDI31 mask data and derived segment files, equal jaw/base matrices,
Foundation pose `VALID`, and an unlocked base. The empty FDI31 trajectory
member was removed, active FDI31 registry slot 1 became `Empty`, but MRML
`targetToothSegmentId` and target ROI remain; Step-4A `_updatePlanning` reads
that MRML field for combo preselection. The saved source and moving-lower
display nodes both show FDI31 3D-visible. Repeated Subject Hierarchy virtual
branches/segment refs grew systemically, not just for FDI31. This is a
saved-display/bookkeeping mismatch, not evidence of a new binary mask or proof
of its precise callback cause. The separate opened-lower planning-surface
`.vtk` bytes differ and were not geometrically compared, so full scene
identity is not claimed. The target-ROI control point also differs from the
13Sept source; its 15Sept world-RAS point is
`[-95.607559204,-76.015136719,28.572851181]` mm. The new partial fixture
has no 4C/5A plane,
draft, final template or PreparedBranch; its persisted dimensions remain old
saved values. The 4B screenshot's four selected supports are not represented
by this case's three saved support IDs.

**Commands and results:** `rg` backlog/task/source routing; `unzip -Z1` and
`unzip -t` on the new package (ZIP PASS); `sha256sum` on 15Sept; read-only
`python3` inline `zipfile`/`hashlib`/`json`/`ElementTree` comparisons of all
manifest members, lineage, MRML display/parameter nodes and nested MRB
member hashes (six manifest entries match; 43 nested members in each case;
one removed, one added, seven changed; source/derived segment files equal).
`graphify query "Step 4A target tooth selection restore: registry target_id
versus saved targetToothBoundsRoi and target segment parameter, case bundle
load" --budget 80` guided scoped reads of `widget_planning.py`,
`widget_case_backend.py`, `widget_views.py`, `logic_case_bundle.py` and
`logic_step6_scene.py`. `git status --short --branch` at the checkout showed
the prior uncommitted Campaign-1 source/docs diff. An initial `git status`
from the overlay and an overlay-relative matrix search failed because those
paths are not the Git checkout/matrix root; the checkout readback corrected
the former. No application tests, Slicer/ROS/container/native execution,
artifact edit, geometry or policy change occurred this turn.

**Controlled-record changes/check:** Appended report Section AL and updated
this logbook, `TASKS.md` and `backlog.md` under the existing
`S6-REUSABLE-CASE-SETUP` owner; no source/test or case file changed. Final
`unzip -t` exited 0 with “No errors detected”; `git diff --check` exited 0;
`rg -n` readback found all four new record sections. `sha256sum` confirmed
the frozen 13Sept source and r7 case still match `14ed8f0f...` and
`5440961c...` respectively. Documentation-only checks do not establish a
runtime/display fix or operator acceptance.

**Evidence boundary and next action:** The operator-created case is a
separate test fixture, not a replacement for frozen Campaign-1 source/r7. It
does not capture the current 4C/5A plane issue, nor certify removal of the
two-layer FDI31 display. Need a bounded save/reopen display-state trace and a
later saved current-world plane scene before P1 overall review can close.
Retained P2 is unchanged; P3/P4 and hardware/full-flow remain paused.

## Operator clarifies reusable foundation and supplies saved Step-5B MRB

**Operator statements, not agent inference:**
`dentobot-case-15sept.dentocase` was supposed to be a clean reusable test
foundation with only mouth opening and robot-base placement, avoiding repeated
landmark/base placement. `FDI31/2026-09-15-Scene.mrb` is the requested current
Step-5B saved scene for testing. The operator did not accept its scene/plane,
foundation eligibility, contact geometry or Campaign-1 inputs.

**Read-only saved evidence:** The MRB exists, SHA-256
`43bbeeacd6ecb738aa116b0076797f67f361acc2de1cccb0434e53f5c2adca9c`,
and `unzip -t` reports no compressed-data error. Its authoritative source
segmentation bytes and saved jaw-opening/base matrices match 15Sept exactly.
The 15Sept bundle saves these placements but is `PartialOrInspectable`, not
eligible `FoundationOnly`: base placed yet `robotBaseMountLocked=False`, with
persisted target ID/ROI, three old supports and both closed/opened FDI31
3D-visible. The Step-5B MRB instead records the four operator-confirmed
supports FDI42/41/33/32, four 4C docks, two planes, Entry→Target and current
patient shell, with closed-source FDI31 3D-hidden and opened proxy visible.
It has no final printable template, and the 1.5-mm trajectory-guide hole
remains below the unchanged 2.0-mm builder floor. This MRB supersedes the
earlier “no current plane scene” evidence boundary; the older before-reset
plane remains unrecorded.

**Numerical cause evidence:** MRML/JSON readback gives 4C world normal
`[0.1062008629,-0.8351349009,-0.5396953530]`, matching its `FrameJson`
within rounding; the point-normal plane's displayed X/Y handles each differ
`14.736°` from the docking `FrameJson` X/Y, so the display plane is not used
as proof of the separate dock model's yaw/fit. 5A metadata records crown-cap fit normal
`[0.0462882197,-0.7354511789,-0.6759947959]` at `11.432482°` from the
trajectory axis; saved plane world normal is
`[0.064557,-0.997913,-0.00134685]`, `42.454376°` from the fit and
`31.289385°` from the axis. Saved jaw rotation times the fit normal matches
the displayed normal within `4.7e-7` component error. The 5A world origin
agrees with Entry+4 mm within `0.000044 mm`. This discriminates an orientation
reparent/frame defect from a depth or camera-angle issue, without judging
clinical correctness. `rg` traced all callers of
`_reparentCaseFoundationNodePreservingWorld`: the shared helper preserved
markup world positions but not plane world normal, while 4C/5A constructors
both call `SetNormalWorld` and the lifecycle ownership refresh can attach a
new plane afterward. The saved r7/13Sept cases were not changed.
The exact callback sequence for the historical 5A plane was not retained; the
helper's contribution to that MRB is inferred from the exact jaw-rotation
match and independently reproduced reparent defect, not a traced event.

**Source implementation and verification:** Applied a six-line change only
to `dentobot_workflow/logic_case_foundation.py`, snapshotting/restoring a
plane markup's world normal around existing jaw reparenting. Added one local
AST/stub regression in the already touched `Testing/test_step6_planning.py`.
Before the repair the focused command
`python3 -m pytest -q -p no:cacheprovider Testing/test_step6_planning.py -k
case_foundation_plane_reparent_preserves_world_normal` failed as expected
(`1 failed`, 24 deselected) on rotated normal; afterward it passed (`1
passed`). The complete focused file passed `25/25`.
`PYTHONPYCACHEPREFIX=/tmp/fdi31-sept15-pycache python3 -m py_compile
DENTOWorkflow/Resources/Python/dentobot_workflow/logic_case_foundation.py
Testing/test_step6_planning.py` and `git diff --check` both exited 0.
`Workspace/scripts/graphify-update.bash` from the checkout exited 0 with
`[graphify watch] Rebuilt` and `Code graph updated`; the pre-existing C++
parser/fail-closed graph warnings did not invalidate refresh. Updated
recovery report AL/AM, TASKS, DECISIONS, backlog and this logbook under
`S6-REUSABLE-CASE-SETUP`. A first suffix-based MRB comparison raised
`StopIteration` because Slicer sanitized segmentation member filenames;
using MRML node names and the actual NRRD member corrected that read-only
comparison. No Slicer/ROS/native/container execution or saved-file rewrite.

**Evidence boundary and next bounded action:** The source/pure regression
cannot prove the Slicer API/save-reopen path or retroactively fix the saved
MRB. Preserve it as a rejected plane-orientation fixture. A separately bounded
offline Slicer plane-recreation/readback on a copy, including world normal/
origin parity and save/reopen, is the next verification gate; never change
the original scene or Campaign-1 frozen geometry. The 15Sept foundation
requires deliberate cleanup/eligibility review before universal fixture use;
saved placement does not imply base lock or target-state removal. P1 overall
and operator review remain INCOMPLETE, retained P2 PASS with
USER_REVIEW_REQUIRED; P3/P4, planning, motion and patient-facing actions stay
paused.

**Final readback:** `rg -n` found report AM, the linked TASKS/DECISIONS and
today's evidence entry; `git diff --check` again exited 0. Final
`sha256sum` preserved frozen 13Sept/r7 hashes (`14ed8f0f...`,
`5440961c...`) and confirmed unchanged 15Sept/Step-5B evidence hashes
(`d084f0ce...`, `43bbeeac...`). `git status --short --branch` shows this
source/test/controlled-doc change is still uncommitted alongside the prior
Campaign-1 source-only corrections; no commit, push or external sync occurred.

## Assisted Step 4A crown-entry placement QoL correction

**Operator request:** In assisted trajectory placement, each activation of
`Place / Replace Crown Entries` must accept one crown point and leave marker
placement mode. For a two-trajectory assist, the operator must activate the
button once for Entry 1, again for Entry 2, and then explicitly generate the
assisted trajectory nodes. Additional live markers such as F1/F2 are not
wanted.

**Source finding:** `startAssistedTrajectoryEntryPlacement` used Slicer's
persistent `StartPlaceMode(1)`, while `onPlaceAssistedTrajectoryEntries`
reset the assisted-entry markup on every button activation. The existing
manual trajectory placement already uses one-shot `StartPlaceMode(0)`.

**Implementation:** Changed the assisted placement helper to
`StartPlaceMode(0)`. The button handler now reuses an incomplete entry set
only when its expected count, target segment and segmentation reference still
match the current selection; completed or mismatched sets retain the existing
confirmation-before-replacement behavior. The incomplete status message and
button tooltip now state the two-click sequence. No case, geometry, policy,
planner, runtime, hardware or operator-confirmed state changed.

**Verification:** `python3 -c` AST parsing of the two source files and the
focused test passed. `python3 -B -m pytest -q Testing/test_assisted_pulp_endpoint.py
-p no:cacheprovider` passed `2/2`, including a stubbed two-click callback
sequence and two one-shot placement activations (`StartPlaceMode` arguments
`[0, 0]`). The existing pure planning check
`python3 -B -m pytest -q Testing/test_step6_planning.py -p no:cacheprovider`
passed `25/25`; `git diff --check` passed. The initial test attempt failed
only because its fake widget bound a function-local variable in a class body;
the test harness was corrected and the same focused command then passed.

**Evidence boundary:** These are source/pure and stub checks only; no Slicer,
ROS, Docker, native diagnostic, planning, controller, spindle or robot action
was run. The one-shot argument is verified against the existing Slicer API
usage pattern, but a live Slicer interaction remains runtime evidence for a
separate authorized check. Operator acceptance of entries, geometry or the
campaign remains unrecorded; P3/P4 and later phases remain unauthorized.

**Final readback:** After the test-harness correction, the focused assisted
placement file passed `2/2`, the existing Step 6 pure file passed `25/25`, the
safe modular-structure subset passed `3/3`, AST parsing passed, and
`git diff --check` passed. The full modular-structure file was also sampled;
its two failures are unrelated pre-existing worktree conditions (verified API
manifest mismatch and `widget_template_build.py` at 1520 lines), so neither
was changed for this QoL request.

## Step 4B automatic four-neighbor support suggestion

**Operator request:** The Step 4B interface was automatically selecting three
support teeth for an ordinary target even though the requirement is two
adjacent same-jaw teeth on each side. Edge targets and missing-tooth cases
remain under the existing manual-review exception path.

**Source finding:** The current Step 4B refresh decoded and displayed persisted
support IDs but had no canonical empty-selection suggestion. The existing
same-jaw filter already limited candidates to reviewed, eligible whole-tooth
records; the missing behavior therefore needed to be exact arch-position
lookup rather than a farther-neighbor search.

**Implementation:** Added the private shared logic helper
`_automaticTemplateSupportSelection` and invoked it only when the current
target has no persisted support selection and has not yet received its
initial suggestion. It uses permanent-tooth arch order across the midline and
checks exactly two positions on each side. Present eligible records are
persisted as the initial, still-editable selection; a missing position is not
replaced by a farther tooth. The selector/status now reports four immediate
same-jaw neighbors for a complete suggestion and an explicit incomplete
warning for edge/missing cases. Existing non-empty saved selections, locked
packages, manual toggles and collision/geometry policy are unchanged. No
operator-confirmed flag is set.

**Changed files:**

- `DENTOWorkflow/Resources/Python/dentobot_workflow/logic_guide_support.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_bootstrap.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_guide_support_setup.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_guide_support.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/widget_panels.py`
- `Testing/test_step4b_support_auto.py`
- `Testing/verification_matrix.json`
- `Workspace/docs/TASKS.md`, `Workspace/docs/backlog.md`

**Verification:** The selected matrix check
`pure.step4b_support_auto` ran
`PYTHONPYCACHEPREFIX=/tmp/dentobot-w4b-pycache python3 -m pytest -q -p no:cacheprovider Testing/test_step4b_support_auto.py`
and passed `5/5`. Cases cover FDI31, a lower posterior target, an upper
target, a missing immediate position without substitution, and a terminal
arch edge. The three changed Python modules passed `py_compile` with the same
temporary cache prefix; `python3 -m json.tool Testing/verification_matrix.json`
and `git diff --check` exited 0. The required overlay-only graph refresh
`/home/light-tarun/dentobot/ros2_ws/src/DentoBot/Workspace/scripts/graphify-update.bash`
completed with exit 0 and `Code graph updated`; retained parser warnings are
unrelated C++ extraction warnings.

**Evidence boundary and next action:** This is source/pure evidence only. No
Slicer, ROS, Docker, planning, native diagnostic, controller, spindle or
robot action was run. The code now supplies four IDs for a normal complete
arch, but a normal-window Slicer review is still needed to verify the visual
selector and manual/lock interaction. `W4B-P2-SUPPORT-AUTO` remains pending
that acceptance, and the one-row GUI revamp remains under `UI-P3-01`.

## Durable Step-4A–5B testing baseline and trajectory-guide bore confirmation

**Operator request/confirmation:** The operator asked that the supplied
Step-4A through Step-5B screenshots and the pictured default parameters be
recorded in a static context so future automated tests do not lose them, and
reconfirmed that the drilling trajectory-guide bore must never be below
`2.0 mm` because of the drill-burr diameter hard constraint.

**Recorded context:** Added the compact new-case/automated-test baseline and
the required source/UI/docs change-control rule to
`Workspace/docs/AGENT_CONTEXT.md`. It records the FDI31 fixture, four
immediate same-jaw support positions (`FDI42`, `FDI41`, `FDI32`, `FDI33`), the
Step-4C dock/plane defaults, the Step-5A support-plane defaults and the
Step-5B undercut/shell/guide defaults. It explicitly separates the 1.0-mm
Step-4C robot-dock bore from the Step-5B trajectory-guide hole and links the
earlier screenshot provenance rather than treating screenshots as acceptance.

**Source/configuration readback:** `parameter_state.py` has
`templateSleeveInnerDiameterMm=2.0` and `templateChannelDiameterMm=2.0`.
`DENTOWorkflow.ui` sets the trajectory-guide hole and shell-channel minima to
`2.0`. `DENTOGuideGeometry.py` defines
`MINIMUM_TRAJECTORY_BORE_DIAMETER_MM=2.0` and rejects a smaller guide hole
with the configured-burr error. The Step-5B preflight and existing pure
tests preserve the same fail-closed behavior. The historical immutable case
may still contain `1.5 mm`; it remains rejected evidence and was not edited.

**Controlled-record decision:** Added the corresponding technical
reconfirmation to `Workspace/docs/DECISIONS.md`. No workflow source, UI,
case, MRB, geometry, collision policy, runtime, container or operator state
was changed in this documentation-only update.

**Verification:** Read-only `rg`/`sed` source/UI checks confirmed each
recorded value and the distinct parameter names. `git diff --check` passed.
No Slicer/ROS/MoveIt/container/native diagnostic or hardware action was run;
no operator acceptance was recorded. The baseline change rule requires future
default/minimum changes to update source, UI, normalization/preflight,
focused tests and these controlled docs together.

## Documentation baseline archival and repository checkpoint preparation

**When:** 2026-09-15, after the operator's renewed request to preserve the
Step-4A–5B screenshots/defaults and to make the 2.0-mm trajectory-guide rule
durable for future automated testing.

**What:** The static agent context now contains the operator-supplied FDI31
test fixture and the complete photographed/default parameter set for 4A, 4B,
4C, 5A and 5B. It records the four expected immediate same-jaw supports
(`FDI42`, `FDI41`, `FDI32`, `FDI33`), the 4C plane/dock dimensions, the 5A
support-plane inputs, and the 5B undercut, shell and guide inputs. It labels
which values came from the screenshots, which were read from source/UI, and
which appearances are unresolved observations rather than accepted geometry.
The exact static record is `Workspace/docs/AGENT_CONTEXT.md`.

The context also now states the change-control procedure: a future default or
minimum change must update the persisted parameter schema, visible UI, owning
normalization/preflight/generator path, focused automated fixture and
verification matrix when applicable, and the linked controlled documents
together. This prevents a workflow-only edit from leaving automated tests or
agent routing on stale values.

**Why:** The screenshots contain useful operator observations and intended
new-case defaults, but they are easy to lose in conversational history and do
not by themselves prove live MRML state, geometry correctness or acceptance.
The direct Step-5B trajectory-guide hole was also at risk of being confused
with the separate Step-4C robot-dock bore. The durable record therefore makes
the roles explicit: the robot-dock bore remains `1.0 mm`; the drilling
trajectory-guide hole (`templateSleeveInnerDiameterMm`) must be at least
`2.0 mm` because of the drill-burr hard diameter constraint. The shell channel
(`templateChannelDiameterMm`) is likewise defaulted and UI-bounded at
`2.0 mm`. Historical saved `1.5 mm` values remain rejected evidence, not
defaults to be clamped or silently rewritten.

**How:** Read-only source and UI inspection mapped the displayed labels to
their canonical parameter names. The readback found `2.0` defaults in
`parameter_state.py`, `2.0` UI minima for both guide-hole/channel controls in
`DENTOWorkflow.ui`, and
`MINIMUM_TRAJECTORY_BORE_DIAMETER_MM=2.0` plus the fail-closed configured-burr
error in `DENTOGuideGeometry.py`. Existing Step-5B preflight/pure-test
evidence was retained; no new runtime was needed for this documentation
change. The five supplied PNGs remain in their existing `/tmp` locations and
their preserved evidence copies are referenced by the recovery report and
this logbook; no screenshot was modified.

The matching technical decision was added to `Workspace/docs/DECISIONS.md`.
The existing source, focused tests, verification-matrix entry, recovery report,
TASKS/backlog updates and prior evidence remain in the same working tree and
are being checkpointed together. No frozen case, MRB, geometry, collision
policy, runtime/container state or operator-confirmed flag was changed.

**Verification and evidence limits:** `git diff --check` passed after the
documentation edits. Existing relevant pure checks remain recorded as
`5/5` for `Testing/test_step4b_support_auto.py` and `25/25` for
`Testing/test_step6_planning.py`; the assisted-placement focused check is
recorded as `2/2`. No Slicer, ROS/MoveIt, Docker/container, native diagnostic,
controller, spindle, motion or patient-facing action was run for this update.
P1 overall review and operator acceptance remain open; P3/P4 remain
unauthorized.

**Checkpoint scope:** At the operator's explicit request, the next repository
checkpoint will include every current tracked modification and the new
`Testing/test_step4b_support_auto.py` file shown by `git status`. No reset,
checkout, selective deletion or unrelated cleanup is authorized; the existing
uncommitted work is preserved as-is until the commit and push complete.

**Checkpoint-time verification:** On 2026-09-15, immediately before staging,
the focused commands were rerun with
`PYTHONPYCACHEPREFIX=/tmp/dentobot-checkpoint-pycache` and
`python3 -B -m pytest -q -p no:cacheprovider`. Results were `2 passed` for
`Testing/test_assisted_pulp_endpoint.py`, `5 passed` for
`Testing/test_step4b_support_auto.py`, and `25 passed` for
`Testing/test_step6_planning.py`. The changed workflow/test Python modules
passed `python3 -B -m py_compile`; `python3 -m json.tool
Testing/verification_matrix.json >/dev/null` and `git diff --check` also
exited successfully. These are source/pure checks only and do not establish
Slicer, ROS, native, clinical or operator acceptance.

**Repository checkpoint result:** The complete current worktree described
above was committed as `11f4c88` (`checkpoint: preserve Campaign 1 workflow
fixes`) on the local `main` branch. It was pushed explicitly with
`git push origin HEAD:codex/campaign1-checkpoint-b94a201`, which completed
successfully as `2619dca..11f4c88`; the protected/default `origin/main` branch
was not changed. This publication preserves the source corrections, focused
tests, verification-matrix entry, recovery/task records and the durable
Step-4A–5B context as one reviewable checkpoint. The current logbook
publication amendment is the only follow-up change after that checkpoint.

## P0 modular-structure gate repair — API manifest and context budget

**When:** 2026-09-15, immediately after the operator asked to address the two
modular-structure failures that had previously been left as unrelated
pre-existing worktree conditions.

**What was observed:** The full `Testing/test_modular_structure.py` gate
reproduced `2 failed, 3 passed`. The API-contract failure was exact: the
current `Step6SceneLogicMixin._hideStep6SourceJawSegments` implementation has
the intentional optional argument
`baselineVisibility: dict[str, bool] | None = None`, while
`Testing/contracts/dentoworkflow_api.json` still described the older
three-argument signature. The context-budget failure was exact:
`widget_template_build.py` was `1,520` lines while the active test still
required `<= 1,500`.

**Why this was fixed:** The API manifest is meant to detect accidental
signature drift, so leaving it stale would make the modular gate report a
false regression after the visibility-preservation repair. The 1,500-line
ceiling was also too narrow for the current cohesive template-build module,
but removing the gate or exempting that module would lose the purpose of the
modularization check. The operator therefore authorized a small bounded
relaxation to `1,600` lines, enough for the current `1,520`-line module while
preserving an explicit inspection budget and headroom for future maintenance.

**How it was repaired:**

- Updated the API manifest's `_hideStep6SourceJawSegments` entry to the exact
  current AST-unparsed signature, retaining strict class/method/annotation/
  return/decorator comparison.
- Added the named `ROUTINE_MODULE_LINE_BUDGET = 1600` constant to
  `Testing/test_modular_structure.py` and used it in the existing assertion.
- Updated the active package routing rule in
  `DENTOWorkflow/Resources/Python/dentobot_workflow/README.md` and the current
  architecture statement in `Workspace/docs/ARCHITECTURE.md` from 1,500 to
  1,600 lines.
- Added the active rule and current 1,520-line status to
  `Workspace/docs/AGENT_CONTEXT.md`, so future agents see the same bound.
- Added the superseding technical decision to `Workspace/docs/DECISIONS.md`
  and the current evidence-routing note to
  `Workspace/docs/REPRODUCIBILITY_AND_TRACEABILITY.md`. Historical entries
  that accurately reported the former 1,500-line rule/failure were retained.

**Preserved invariants:** The public `DENTOWorkflow.py` entrypoint remains
limited to 500 lines. `runtime.py` and `slicer_tests.py` remain the only
existing module exemptions. CMake-install coverage, no-public-entrypoint
back-imports, no process/thread/socket/network boundary, duplicate-method
detection and strict manifest comparison remain active. No planner, MRML case,
geometry, collision policy, runtime/container, hardware or operator-acceptance
state was changed.

**Verification plan and evidence boundary:** The full modular test is the
authoritative recheck for both defects. The focused Campaign-1 pure suites are
also rerun after the gate repair to detect regression in the current source
worktree. This remains source/pure evidence only; no Slicer, ROS/MoveIt,
Docker/container, native diagnostic, controller, spindle, motion or
patient-facing operation is authorized or implied. P1/P2 acceptance and P3/P4
gates remain unchanged.

### P0 final recheck and graph refresh — 2026-09-15

The first recheck after the bounded rule/API edits exposed one formatting detail
in the checked contract: the comparison uses Python AST-unparse output, which
renders the optional union default as `| None=None`. After that exact spelling
was corrected, the next recheck exposed the second stale manifest entry for
the already-implemented correlated `syncStep6MoveItPlanningScene` keyword-only
options. That entry was updated to the exact current signature. No behavior,
planner, guard policy or runtime path was changed.

Authoritative commands and results:

- `PYTHONPYCACHEPREFIX=/tmp/dentobot-modular-p0-pycache python3 -B -m pytest -q -p no:cacheprovider Testing/test_modular_structure.py` — **5 passed**.
- `PYTHONPYCACHEPREFIX=/tmp/dentobot-modular-p0-pycache python3 -B -m pytest -q -p no:cacheprovider Testing/test_assisted_pulp_endpoint.py Testing/test_step4b_support_auto.py Testing/test_step6_planning.py` — **32 passed**.
- `PYTHONPYCACHEPREFIX=/tmp/dentobot-modular-p0-pycache python3 -B -m py_compile Testing/test_modular_structure.py Testing/test_assisted_pulp_endpoint.py Testing/test_step4b_support_auto.py Testing/test_step6_planning.py` — exit 0.
- `python3 -m json.tool Testing/contracts/dentoworkflow_api.json >/dev/null && python3 -m json.tool Testing/verification_matrix.json >/dev/null` — exit 0.
- `git diff --check` — exit 0.
- `wc -l` measured `widget_template_build.py` at **1,520** lines and the public `DENTOWorkflow.py` entrypoint at **119** lines.
- `Workspace/scripts/graphify-update.bash` — exit 0 with `Code graph updated` and `graphify-out` rebuilt. The extractor emitted the repository's known C/C++ syntax-warning summary; this did not fail the refresh.

Files changed by this P0 repair are the modular test, the strict API contract,
the active package/architecture/agent routing rules, the superseding decision,
the reproducibility note, the current S6 task evidence and this logbook. The
old 1,500-line reports remain dated historical evidence; the active rule is
1,600 lines and the gate remains strict. No Slicer, ROS/MoveIt, Docker,
container, native diagnostic, controller, spindle, motion or patient-facing
operation was run. P1/P2 acceptance, operator review and P3/P4 authorization
are unchanged.

**Publication:** The repair was committed as `4bfad26` (`fix: close modular
verification gate`) and pushed successfully to
`origin/codex/campaign1-checkpoint-b94a201` (`bff1590..4bfad26`). The local
worktree was clean after publication; `origin/main` was not changed.

## Revised Campaign 1 activation and P0 saved-trajectory extraction

**Operator direction:** The operator supplied the revised Campaign 1 contract,
then instructed implementation to start. This supersedes the prior
Astra/design-only/P4 campaign limit within the existing
`S6-REUSABLE-CASE-SETUP` / `S6-LIVE-01..04` lane. It does not authorize hardware
connection, physical motion, powered spindle, patient action, policy relaxation
or alteration of frozen anatomy/task/base/TCP/limit/tool evidence.

**P0 inspection:** Read the campaign attachment, active backlog/contracts,
verification protocol, current recovery report, exact case inventory and the
saved 15 September MRB. The source checkout is clean at
`48c0c82c0455eeb7a1557b4a6f186813adf69f91`; immutable 13Sept and r7 case
hashes remain `14ed8f0f...` and `5440961c...`. The 15Sept `.dentocase` remains
`PartialOrInspectable`, not silently promoted. `unzip -p` found the exact
locked Step-4A FDI31 trajectory member in `FDI31/2026-09-15-Scene.mrb`:
archive SHA `43bbeeac...`, member SHA `61dac396...`, LPS/mm Entry
`[95.06119545332044, 36.948910276519285, 55.86918917410314]`, Target
`[94.63312182009788, 38.39889044842043, 50.85265351160811]`, length
`5.239400689721231 mm`. The Step-5B insertion member is coordinate-identical
but is not used as the authoritative construction input.

**Diagnosis and implementation ownership:** Existing
`Testing/run_dentobot_stage6_target_generation.py` clears derived trajectories,
regenerates FDI31 and sets `targetDockingBoreDiameterMm` to `1.5`; that conflicts
with the revised preserved-trajectory/1.0-mm-dock contract. Dispatched one
`gpt-5.6-luna` worker at `max`, with editable scope limited to that runner and
focused pure test(s). Its required correction is opt-in archive parsing,
strict two-point LPS→RAS import through existing trajectory APIs, provenance
diagnostics, no generated duplicate in the opt-in path and the 1.0-mm default.
No runtime, Docker, Slicer, ROS/MoveIt, build, saved-case rewrite or controlled
gate acceptance has been performed by this entry.

**Worker result and coordinator acceptance:** The worker changed only
`Testing/run_dentobot_stage6_target_generation.py` and added
`Testing/test_fdi31_saved_trajectory.py`; no production module, case/MRB,
geometry or controlled record was edited by the worker. The runner's opt-in
`DENTOBOT_STAGE6_FDI31_TRAJECTORY_MRB` path validates the exact archive member,
LPS/mm two-point/length contract and member hash, converts LPS→RAS once,
creates/configures the existing DENTOBOT line, writes `SavedStep4A15Sept`,
uses `getTargetPulpAssociation`, and does not create an assisted duplicate.
It changes the runner's FDI31 dock-bore default from 1.5 to 1.0 mm while
preserving the 2.0-mm guide/channel floor. I inspected the actual diff.

**Commands/results:** The worker and coordinator both ran the focused pure
set; coordinator command
`PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-coordinator-pycache python3 -m pytest -q
-p no:cacheprovider Testing/test_step6_planning.py Testing/test_fdi31_saved_trajectory.py`
passed **29/29**. Coordinator `py_compile` for both changed files and
`git diff --check` exited 0. An initial review command incorrectly used
`git -C ros2_ws/src/DentoBot` while already in that checkout and failed with
path-not-found; it changed nothing and the corrected command above passed.
The first sandboxed `Workspace/scripts/graphify-update.bash` failed with an
operation-permitted error; the approved local-overlay retry exited 0 with
`Code graph updated`. Existing Graphify C/C++ syntax warnings were reported
but did not fail the refresh.

**Evidence boundary / next action:** The correction is **Implemented / Unit
Verified**. It has not run Slicer, ROS, Docker, MoveIt, a build, case creation,
save/reopen, planner, guard, motion or any Gate-A acceptance. The next bounded
action is one serialized offline Slicer Gate-A construction/reopen from the
15Sept foundation with the explicit archive input, followed by artifact/image
review; it needs the normal runtime preflight and records its first failure
without changing frozen inputs or retrying blind.

**Runtime authorization and plan:** The operator explicitly authorized this one
Gate-A construction/reopen run. The question is whether the corrected generator
can create a new FDI31 production case from the 15Sept foundation while retaining
the exact saved Step-4A trajectory, four reviewed supports, 1.0-mm dock bores
and >=2.0-mm trajectory-guide/channel values. The only runtime resources are
the existing `dentobot-slicerros2` container, one Slicer process, its MRML scene,
display and (if the existing facade needs it) the installed offline SlicerROS2
module. The run ID is `c1-gatea-fdi31-20260915-r1`; outputs are new versioned
paths beneath `SampleStudy1/FDI31/` and `SampleStudy1/central-incisors/`.
Preflight must stop for an operator-owned Slicer/ROS/MoveIt process or a missing
container/source/archive. The single command opens the 15Sept bundle, passes
`DENTOBOT_STAGE6_FDI31_TRAJECTORY_MRB` pointing to the retained scene, runs only
`run_dentobot_stage6_target_generation.py` for FDI31, writes one package/STL/
diagnostic/save-reopen packet and exits. It excludes planner/guard/insertion,
hardware/controller connection, motion, powered spindle, patient action,
policy/geometry invariant change and retries. PASS requires the runner's FDI31
generation marker plus the saved package/reopen diagnostic; otherwise preserve
the first failure and stop.

## Campaign 1 Gate-A construction/reopen — first-failure record

**Operator authorization / scope:** The operator explicitly authorized one
offline, serialized Slicer Gate-A construction/reopen run, then instructed the
campaign to continue. The scope excluded planner, insertion, guard, hardware,
motion, powered spindle and patient work; it also prohibited blind retries.

**Preflight observed:** The initial sandboxed Docker query was denied access to
the Docker socket; the approved container-scoped retry succeeded. It found
`dentobot-slicerros2` up, no active Slicer/ROS/MoveIt/DentoBot process, absent
new `c1-gatea-fdi31-20260915-r1` output directories, source package SHA-256
`d084f0ce733f35bc73eaccf805113a267dbf3ea7b295315c4b5da3fb67136d6a`, and MRB
SHA-256 `43bbeeacd6ecb738aa116b0076797f67f361acc2de1cccb0434e53f5c2adca9c`.

**Execution:** One `xvfb-run` Slicer process opened the 15-September package
and ran `Testing/run_dentobot_stage6_target_generation.py` for FDI31 with the
explicit retained MRB input. It exited 1 after about two minutes. The first
causal runner failure is at the existing trajectory-validity assertion:
`STAGE6_TARGET_FAIL FDI31 {"error": "FDI31 trajectory is invalid"}` followed
by `STAGE6_TARGET_GENERATION_COMPLETE {"passed": 0, ...}`. The runner's FAIL
trace points to line 868, `logic.getTrajectorySummary(trajectory)["isValid"]`.
The contemporaneous ROS2Tests `psutil` installation permission warning is an
environment/module-load warning; the runner continued to load the source and
the recorded trajectory assertion is the first construction failure.

**Evidence retained:**

- Full log: `/tmp/dentobot-verification/c1-gatea-fdi31-20260915-r1/runtime.log`
  (SHA-256 `ea00448978da1b09b2c47577fd9f4bb77c1bb86d5325173d3f7d0be81f738270`).
- FAIL diagnostic: `data/Slicer_Saved/SampleStudy1/FDI31/c1-gatea-fdi31-20260915-r1/diagnostics/FDI31-generation-diagnostic.json`
  (SHA-256 `d505726dd6647906190baa06d8a7c861d90dae36436c41cf6f1e38a1f43f2295`).
- Per-target report: `.../FDI31/c1-gatea-fdi31-20260915-r1/report.json`
  (SHA-256 `1f50e342333d2a0905dc422e30e06305ba70da4cd20ac7d22f154e375c16beaa`).
- Campaign report: `.../central-incisors/c1-gatea-fdi31-20260915-r1/generation-report.json`
  (SHA-256 `220f30394b90122bbc71f9509baab304435fd82310f392e071d67f577359087b`).
- Slicer UI and viewport failure screenshots were captured successfully and
  visually inspected; they show the loaded FDI31 Step-4A scene and no produced
  template artifact.

**Disposition:** Gate A is **FAIL / shared construction blocker**. No STL,
saved corrected case, reopen verification, endpoint/insertion/approach/guard,
withdrawal/Home or fresh-process check was attempted. r7/r13 and all frozen
inputs remain untouched. No runtime retry was made. The next bounded action is
to diagnose the exact imported-MRML trajectory-state mismatch in source, add a
minimal regression check, review the correction, and obtain a separate runtime
authorization before another Gate-A attempt.

**Record consistency check:** A first final `git diff --check` was accidentally
issued at `/home/light-tarun/dentobot`, the non-Git overlay root; Git returned
exit 129 and made no change. The corrected physical-checkout command at
`/home/light-tarun/dentobot/ros2_ws/src/DentoBot` returned exit 0. Its known
`Failed to create stream fd: Operation not permitted` prefix is exec-wrapper
noise; the source/docs diff has no whitespace error.

## Campaign 1 Gate-A source correction after r1 failure

**Operator direction:** The operator explicitly authorized diagnosis and repair
of the recorded Gate-A blocker in order to achieve success. This authorization
does not relax frozen trajectory, anatomy, base, tool, policy or safety
invariants.

**Diagnosis / minimal correction:** The saved-line branch locked its newly
created trajectory before running the same validity sequence used by the
established assisted-generation path. The runner now leaves that line unlocked
until `getTrajectorySummary()` has validated it, then locks it exactly as the
shared flow does. On an invalid line the error now includes the existing
summary's defined-point count, Entry/Target world-RAS values and measured
length; no coordinate, parser, transform, geometry or policy value changed.

**Checks:**

- `PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-fix-pycache python3 -m pytest -q -p no:cacheprovider Testing/test_step6_planning.py Testing/test_fdi31_saved_trajectory.py` — **29 passed**.
- `PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-fix-pycache python3 -m py_compile Testing/run_dentobot_stage6_target_generation.py Testing/test_fdi31_saved_trajectory.py` — exit 0.
- `git diff --check` — exit 0.

**Second-attempt preflight:** The approved container-scoped check again found
`dentobot-slicerros2` up, no active Slicer/ROS/MoveIt/DentoBot process, absent
r2 output directories, and unchanged locked package/MRB hashes. The proposed
single r2 Slicer construction/reopen launch was then rejected by the execution
safety gate: the current operator phrase authorized diagnosis/fix but did not
explicitly authorize the separate second state-changing Slicer/case run that
the r1 record required to be newly approved. No r2 runtime process, case,
STL, planner/insertion/guard/hardware path, or retry was started. Await an
explicit second offline Gate-A construction/reopen authorization.

## Campaign 1 Gate-A r2 result and r3 source boundary

**r2 authorization / preflight:** The operator explicitly authorized one new
isolated offline Gate-A construction/reopen attempt
`c1-gatea-fdi31-20260915-r2`. Its container/process/fresh-output/hash preflight
passed with the same locked source package and MRB identities as r1.

**r2 execution / first causal result:** The one Slicer process exited 1. The
added summary established that the line had no accepted points:
`definedPointCount: 0`, `entryRas: null`, `targetRas: null`, and `lengthMm:
null`. Thus moving the final lock after validation did not fix the defect; the
failure is point insertion before any geometry/template/planner/guard/hardware
path. Retained hashes are:

- Runtime log: `6a66169876a41eef8b0e4dde1ff6703b43700709c96e399344f256eef29a08c9`.
- FAIL diagnostic: `b68f7bcea53752d1653b3ef53190cefbaad4757206f5e691a8ee43c6ad8d9389`.
- Target report: `b6ba6a23d9333f646ab61b1198f6f2551183b3c7873fbb6bd6434bbd4861e497`.
- Campaign report: `2c868a076a574b1836b155363a19d5b55c0a3cf1e98cadc0b38f1dce8e0676c6`.

The screenshots were retained. The repeated optional ROS2Tests `psutil` install
warning was non-fatal and did not obscure the recorded runner failure.

**r3 source-only correction:** Before adding either saved point, the runner now
explicitly calls `trajectory.SetLocked(False)` and immediately fails closed with
the existing summary if two defined points are not accepted. This is the
smallest evidence-backed, distinct hypothesis: a stage/UI inherited lock was
preventing Markups insertion. It does not change saved coordinates, transforms,
anatomy, package bytes, dimensions, policy or safety scope. The focused command
`PYTHONPYCACHEPREFIX=/tmp/dentobot-c1-r2-fix-pycache python3 -m pytest -q -p
no:cacheprovider Testing/test_step6_planning.py Testing/test_fdi31_saved_trajectory.py`
passed **29/29**; `py_compile` and `git diff --check` exited 0.

**Retry ceiling / next action:** r1 and r2 are two failures against the same
causal blocker. Under the verification protocol, exactly one third attempt may
test this separately stated live-lock hypothesis because r2 added diagnostic
evidence. Do not run it implicitly. It requires one explicit authorization for
`c1-gatea-fdi31-20260915-r3`, with the same isolated offline construction/reopen
scope and first-failure stop condition. A third failure closes autonomous
runtime retries and returns the blocker to the operator with its three-attempt
evidence packet.

## Campaign 1 Gate-A r3 final runtime disposition and source root cause

**r3 execution:** The operator explicitly authorized the final permitted
isolated offline attempt `c1-gatea-fdi31-20260915-r3`. Preflight again passed
exclusive container ownership, fresh output paths, and the locked source/MRB
hashes. The one Slicer process exited 1 with the same final summary as r2:
zero defined points and null Entry/Target/length. r3 therefore disproves the
inherited-live-lock hypothesis and closes autonomous runtime retries for this
blocker. No planner, insertion, guard, motion, hardware or patient path ran.

**Root-cause trace:** r3's immediate post-insertion two-point assertion passed,
so Slicer initially accepted both saved points. The later removal was the
existing Step-4A `_enforceTrajectoryBounds()` path: it removes an out-of-bounds
point when that line has no prior accepted point to restore. The saved branch
fed its closed-scene LPS→RAS values directly to the opened lower-jaw planning
scene. The established assisted path instead maps lower-jaw points through the
frozen `logic._step6CaseJawMatrixWorld(parameter)` once. The saved branch
omitted that mapping.

**Source repair / evidence boundary:** The runner now maps both saved Entry and
Target through that existing Case Foundation matrix exactly once for a
`MovingLower` target, records the resulting
`OpenedCaseFoundationWorldRAS` planning points in saved-source diagnostics, and
then inserts/validates/locks the line. The original parsed LPS/RAS values remain
recorded and unchanged. This is source/unit evidence only: focused tests pass
29/29, `py_compile` and `git diff --check` pass. It has not received a fourth
runtime attempt and must not be reported as runtime-verified or Gate-A PASS.

**Final r3 evidence:**

- Runtime log SHA-256: `32216c2f1ab9ac07929390c8f1c43d06e89258d73b25a022a65f3fd85b49c5a2`.
- FAIL diagnostic SHA-256: `1209c971603ddc8ec4c95ef55aa0c8cc97bbaf50b103b103db14baa20ee2b451`.
- Target report SHA-256: `ed7ce79ef32aa1e9e265233a65d1a45c1a619682dff3d40afd3161c06a75bb41`.
- Campaign report SHA-256: `a21d5feb0299a6bce1dc7c866b9f3e9b7426726fbf2de48c0797ed2fd4e41f0e`.

**Disposition:** Gate A remains **FAIL / runtime retry ceiling reached**;
Gates B–D and other teeth remain NOT RUN. Any future r4 construction/reopen
requires an operator-approved revised verification plan that explicitly records
why this source correction is an exception to the three-failure ceiling, how
the final Case Foundation mapping will be checked before UI bounds enforcement,
and why another state-changing runtime attempt is warranted.

## Campaign 1 Gate-A r4 revised verification and completion

**Operator authorization and scope:** The operator authorized revised
verification and completion of the Gate-A construction/save/reopen gate. This
was the documented r4 exception after r1-r3, limited to one serialized offline
Slicer process and the existing `dentobot-slicerros2` container. It excluded
planner, insertion, approach, guard, hardware/controller connection, robot
motion, powered spindle, patient action, frozen-input changes and any Gate B-D
work.

**Preflight and source evidence:** The container was up with no active
Slicer/ROS/MoveIt/DentoBot process; r4 output paths were absent. The source
package SHA-256 was unchanged at
`d084f0ce733f35bc73eaccf805113a267dbf3ea7b295315c4b5da3fb67136d6a`; the retained
MRB SHA-256 was unchanged at
`43bbeeacd6ecb738aa116b0076797f67f361acc2de1cccb0434e53f5c2adca9c`. Before the
run, the focused source suite passed 29/29, `py_compile` passed and
`git diff --check` passed. The revised source maps the saved lower-jaw LPS/RAS
points through the existing frozen Case Foundation matrix once, asserts two
inserted points and their bounds before the template path, and preserves the
original saved coordinates and hashes in diagnostics.

**Execution and functional result:** One `xvfb-run` Slicer process ran
`Testing/run_dentobot_stage6_target_generation.py` for FDI31 using the exact
15-September source and retained Step-4A MRB. It emitted
`STAGE6_TARGET_PASS FDI31` and
`STAGE6_TARGET_GENERATION_COMPLETE {"passed": 1, ...}`. The saved diagnostic
status is `PASS`: the exact saved length is `5.239400689721231 mm`, two
planning-frame trajectory points are within bounds, supports are FDI42/41/32/33,
the reopened PreparedBranch is `Current` and verified, and the final STL is
verified. The new saved case is 84 MB. Both post-reopen screenshots were
captured and visually inspected; the UI labels are densely overlaid, but the
scene and target/template artifacts are visibly present after reopen.

**Shutdown outcome and evidence boundary:** The outer Slicer command returned
exit 1 during shutdown after the functional PASS markers, accompanied by the
known nonfatal ROS2Tests `psutil` install warning and VTK leak warnings. This
is recorded as a process-health warning, not as a functional Gate-A failure.
No physical/clinical fit, planner, insertion, approach, guard, motion,
hardware or patient conclusion follows.

**Evidence retained:**

- Runtime log: `/tmp/dentobot-verification/c1-gatea-fdi31-20260915-r4/runtime.log`
  (SHA-256 `8d806a37a8d146a4f74e2682e894deea0e85068cc52f3fed9641934222ad1d3c`).
- Saved case: `data/Slicer_Saved/SampleStudy1/FDI31/c1-gatea-fdi31-20260915-r4/FDI31-step5c.dentocase`
  (SHA-256 `6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`).
- PASS diagnostic: `.../FDI31/c1-gatea-fdi31-20260915-r4/diagnostics/FDI31-generation-diagnostic.json`
  (SHA-256 `5cda22b0eb98f71e9d3b04ab78c38f9e07c70da29fcb411952e5ed22b9d95f7b`).
- Target report: `.../FDI31/c1-gatea-fdi31-20260915-r4/report.json`
  (SHA-256 `e24be4bb62937e4540ef7971da4167d5fb2a2257308b7e6209a979a77c9aa08e`).
- Campaign report: `data/Slicer_Saved/SampleStudy1/central-incisors/c1-gatea-fdi31-20260915-r4/generation-report.json`
  (SHA-256 `e1b4f1b881deba6fbb81e195fe609340683072925fe6ca3464ae683b6f9381b0`).
- Post-reopen captures: `.../screenshots/post-reopen-ui.png` and
  `.../screenshots/post-reopen-viewport.png`.

**Disposition:** **Gate A PASS — corrected-case construction/save/reopen**,
with a separate nonzero-shutdown warning. Gates B-D, other teeth and all
physical/clinical/hardware work remain NOT RUN and separately approval-gated.

## Campaign 1 post-Gate-A next-dependency reconciliation

**Operator direction:** The operator said “Proceed to next.” I treated this as
authority to inspect and prepare the next bounded dependency, not as a silent
waiver of the Campaign 1 P1 scene/input-review prerequisite for native endpoint
or insertion testing.

**Read-only r4 reconciliation:** The r4 PASS diagnostic supplies current
numerical construction evidence absent from the older P1 packet: FDI42/41/32/33
supports; a 35-degree Step-4C dock frame with zero trajectory-axis deviation;
a Step-5A plane at 4.0 mm from Entry with recorded opened-world origin/normal;
four independent 1.0-mm robot-dock bores; 2.0-mm sleeve/channel values; and
PASS checks for dock provenance/orientation, support, guide, topology and
current PreparedBranch eligibility. Its final-template gate retains the
separate `Clinical collision and fit review: WARNING`; this is not a clinical
or physical-fit decision.

**Gate-B safety/identity stop:** The existing P3/endpoint diagnostic runner
`Testing/run_dentobot_fdi31_recovery_diagnostic.py` is explicitly frozen to
the historical r7 package and its `6.6719049312 mm` Entry-to-Target line. The
r4 case has the approved immutable 5.239400689721231-mm Step-4A trajectory and
different planning-frame Entry/Target coordinates. Reusing that runner would
silently substitute historical task geometry, so no Slicer, ROS, MoveIt,
planner, insertion, Docker or hardware process was started. The retained
Campaign 1 contract also requires P1 technical reconciliation plus recorded
operator review before P3/endpoint feasibility and P4/insertion.

**Next bounded action:** Obtain an explicit operator decision that the r4
case's recorded 4C/5A/5B/5C geometry and saved trajectory are the intended
Gate-B baseline, then issue a new r4-specific endpoint-only verification plan
with a runner that accepts those exact identities rather than the r7 constants.
No source change or new runtime authorization is implied by this read-only
reconciliation.

## Campaign 1 r4-only endpoint diagnostic — implementation and execution plan

**Operator delta:** The operator clarified that r4 is the sole active case;
historical r7/r13 outputs must not be used as current execution data. This
supersedes older r7-based endpoint-runner assumptions, while retaining those
files as non-operational history only.

**Implemented boundary:** The existing explicit-case smoke runner now accepts
`DENTOBOT_ENDPOINT_ONLY=1` only with `DENTOBOT_EXACT_CASE`. Before any
approach-planning call it loads that case, reconstructs the simulation-only
ROS/MoveIt state needed for a native static query, derives the exact r4 Target
pose from its current Entry/Target snapshot, and evaluates direct
position-axis IK from Task Home plus at most 13 persisted Home-connected
workspace seeds. Each unique solution receives existing generic static and
phase-aware drilling-state validation. The branch writes one diagnostic and
returns before Goal 1/Goal 2, preview, insertion, route locking or any command
path. It does not enable historical overrides. The focused test set passed
29/29; `py_compile` and `git diff --check` passed.

**Approved runtime plan:** One serialized, offline Slicer process in the
existing `dentobot-slicerros2` container will open only
`c1-gatea-fdi31-20260915-r4/FDI31-step5c.dentocase`, require FDI31 identity,
write only under a fresh `c1-endpoint-fdi31-20260915-r4/` directory, and run
the endpoint-only mode. Question: whether at least one exact r4 Target
position-axis IK candidate satisfies the existing 0.25-mm / 0.5-degree
residuals plus generic and phase-aware static validity. Expected decisive
result: `DENTOBOT_ENDPOINT_ONLY_COMPLETE` with either `PASS` or
`NO_VALID_TARGET_SOLUTION_FOUND_IN_BOUNDED_SEARCH`. Stop after that one batch;
do not enter insertion, approach, guard-preview, hardware or any historical
case path. Preflight stops for an active Slicer/ROS/MoveIt/DentoBot process,
missing r4 case, changed r4 checksum or existing output directory.

**Runtime attempts and ceiling:** Preflight passed: `dentobot-slicerros2` was
up, no conflicting DentoBot/Slicer/ROS/MoveIt process was identified, and the
r4 case checksum matched `6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`.
The first launch ended in `sh: Illegal option -o pipefail`. The next launch
could not register the ROS2 Slicer module; the third reached the module loader
but stopped because `libqSlicerROS2Module.so` could not find `librclcpp.so`.
All three failures occurred before the r4 case or endpoint runner opened, so
they produced no case output and did not invoke planner, preview, insertion,
motion, hardware or any historical case. The next fully configured launch was
rejected at the protocol's three-failure retry ceiling. The runtime lane is
therefore **NOT EXECUTED / environment initialization blocked**. A new run
requires a specifically informed, explicit operator-approved exception.

**Post-disposition source verification:** After correcting the completion
marker to `DENTOBOT_ENDPOINT_ONLY_COMPLETE`, the focused source suite
`python3 -m pytest -q -p no:cacheprovider Testing/test_step6_planning.py Testing/test_fdi31_saved_trajectory.py`
passed **29/29**. `python3 -m py_compile
Testing/run_dentobot_step65_exact_case_smoke.py` and `git diff --check` also
passed. These are source checks only; they do not change the runtime status.

## Campaign 1 P3 r4-only authorization and environment preflight

**Operator instruction:** “yes so i authorize to proceed as per the planners
clear instructions.” Interpreted against the immediately preceding plan
clarification: execute the Campaign 1 P3 endpoint-feasibility contract using
r4 only. The operator's earlier r4-only directive remains controlling: no
historical output may be opened, read, compared, used as an anchor or fallback.

**Scope and stop condition:** Convert the preliminary r4 endpoint probe into
the plan's one premanifested deterministic P3 batch of at most 128 J1–J5
position-axis IK solves. Use r4 Task Home/current workspace anchors plus active
runtime joint-limit coverage only; apply generic and phase-aware static
validity to every converged result before deduplication. Stop after the batch
with either an admissible Target, a complete bounded zero-candidate result, or
an interruption/scene-drift error. P4 remains unstarted unless P3 passes. No
planner, preview, insertion, route locking, controller/hardware connection,
motion, powered spindle or patient action is permitted.

**Read-only environment preflight:** The authorized container inspection found
`dentobot-slicerros2` up. With `/opt/ros/jazzy/setup.bash` and
`/workspace/ros2_ws/install/setup.bash` sourced, `ROS_DISTRO=jazzy`,
`librclcpp.so` resolves at `/opt/ros/jazzy/lib/librclcpp.so`, and the installed
`libqSlicerROS2Module.so` exists. This directly addresses the earlier loader
failure without starting Slicer, ROS, MoveIt or a robot process. The planned
runtime batch remains serialized and will use this exact setup.

**P3 implementation and static verification:** The endpoint-only runner now
implements the r4-only P3 manifest: a maximum of 128 Task-Home/current-workspace
and deterministic Halton joint-limit seeds; active J1–J5 ranges fail closed;
every converged candidate receives generic plus phase-aware static checks before
post-validation deduplication; all raw candidates and no more than eight ranked
representatives are retained. `runtime.c1_p3_r4_endpoint` was added to the
matrix with the same serialized Slicer/ROS/MoveIt resources and an explicit
offline/static pass condition. `python3 -m json.tool
Testing/verification_matrix.json`, the focused pytest command, `py_compile`,
and `git diff --check` passed; pytest remained **29/29**.

**Runtime dispatch boundary:** The exact preflight then passed for r4 checksum,
fresh output path and absence of conflicting process. The requested Slicer/ROS
batch was rejected before dispatch because it would be the fourth initialization
attempt after the three recorded failures, and the user's general P3
authorization did not explicitly name that retry risk. No container process,
Slicer, ROS/MoveIt stack, r4 case, planner, output or hardware action started;
this rejection is not a fourth execution failure. The concrete authorization
needed is one fourth initialization attempt using the already verified source
setup, one temporary simulation stack, one r4-only <=128-seed native static
batch and cleanup—without planner, insertion, motion, controller/hardware,
spindle, patient or historical input.

## Campaign 1 P3 r4-only batch — interrupted at no-planning boundary

**Execution:** The operator explicitly authorized the fourth Slicer/ROS
initialization attempt. Its in-command checksum/process/fresh-output preflight
passed; it sourced Jazzy plus the workspace install, launched one temporary
simulation stack and opened the exact r4 case. The optional `ROS2Tests`
`psutil` install warning recurred but DENTOWorkflow loaded the case.

**Stop observation:** Before the runner emitted its P3 marker or candidate
JSON, `runtime.log` showed repeated MoveIt `Planning request accepted` /
`Planning request complete` entries. That conflicts with the operator's
explicit no-planning condition, so the coordinator terminated the owned Slicer
process immediately. The parent cleanup removed the temporary launcher; one
orphaned run-owned `collision_guard` was then terminated and its absence
verified. There was no preview, trajectory execution, controller/hardware,
spindle, patient or historical-data action. The only retained artifacts are
`data/Slicer_Saved/SampleStudy1/FDI31/c1-p3-fdi31-20260915-r4/runtime.log` and
`simulation-stack.log`; no `endpoint_candidates.json` exists. Result:
**execution INTERRUPTED; P3 endpoint result NOT TESTED/INCONCLUSIVE**.

**Diagnosis and correction:** The exact-case runner's generic restore path
called `facade.generateWorkspaceCloud()` before its endpoint-only return. That
production helper causes the planning requests; it is unnecessary because P3
uses the saved r4 anchors plus deterministic active-limit coverage. The P3
return now occurs immediately after live Task Home/task confirmation and before
`generateWorkspaceCloud()`. A focused source assertion enforces that ordering.
`pytest` passed **29/29**, `py_compile`, matrix JSON validation and `git diff
--check` passed. This source repair does not authorize a replacement runtime
batch. A new explicit authorization must name a fresh output directory and the
same no-planning/no-preview/no-motion/no-hardware/no-historical-input boundary.

**Read-only solver attribution:** Inspection of the installed/source
`vtkMRMLROS2RobotNode::ComputeMoveItPositionAxisIK` implementation confirms it
iterates a `moveit::core::RobotState` and Jacobian directly; it does not use the
MoveGroup planning interface. The observed MoveGroup planning-request records
are therefore attributable to the now-bypassed workspace-generation helper,
not to the required native position-axis IK query itself. This supports one
replacement P3 batch after the operator authorizes it, but does not convert the
interrupted run into endpoint evidence.

## Campaign 1 P3 r2 wrapper interruption and r3 dispatch stop

**r2 outcome:** The authorized replacement was prepared in fresh
`c1-p3-fdi31-20260915-r4-r2`. Its `setsid` watchdog wrapper exited after the
temporary simulation stack became ready but before Slicer launched; consequently
there is no `runtime.log`, no P3 marker and no candidate JSON. The retained
`simulation-stack.log` identifies the launcher PID 2535 and guard PID 2559;
both were terminated and their absence verified. This is a harness interruption,
not a P3 candidate/result or a no-planning violation.

**Source correction and verification:** Before a replacement can assess P3,
the native `ComputeMoveItPositionAxisIK` call now uses `avoidCollisions=False`.
That preserves every kinematically converged result for the existing generic
and phase-aware predicate instead of allowing a generic prefilter to hide an
otherwise reportable candidate. No collision policy, tolerance or guard changed.
Focused pytest passed **29/29**, with `py_compile`, matrix JSON validation and
`git diff --check` passing.

**r3 dispatch stop:** A new r3 command was rejected before launch because the
operator had authorized one replacement batch and r2 had already consumed that
attempt at the harness/stack stage. The proposed cleanup also used a broad
matching-process pattern rather than proving each PID run-owned first. No r3
directory, Slicer, stack, planner, motion or hardware process was created. The
next required operator decision is a specific additional r3 authorization after
the r2 harness interruption, with cleanup restricted to PIDs recorded as
created by that exact run.

## Campaign 1 P3 r3 interruption and retry-ceiling closure

**Operator authorization:** The operator authorized the exact requested r3
envelope. r3 created only `c1-p3-fdi31-20260915-r4-r3/simulation-stack.log`
and `owned-pids.txt`; it did not reach Slicer, `runtime.log`, the endpoint
runner, candidate JSON or the MoveGroup watchdog boundary. Its recorded launcher
PID was 3775. The stack log recorded guard PID 3800; the coordinator verified
both command lines, appended the guard PID to r3's owned-PID record, terminated
only those two PIDs and verified neither remained. No historical input, preview,
insertion, controller/hardware, spindle, patient or robot action occurred.

**Stop condition:** P3 now has three runtime/setup interruptions: r1 stopped
at the explicit no-planning boundary after workspace generation, r2 stopped at
the watchdog-wrapper/stack stage before Slicer, and r3 stopped at the
pre-Slicer stack/readiness stage. In accordance with the three-failure ceiling,
the runtime lane is closed. P3 is **NOT TESTED / INCONCLUSIVE**, not a
candidate PASS, bounded-negative result or P4 entry condition. Further r4 P3
execution needs an operator-approved revised plan that first resolves the
launcher/readiness behavior with the cheapest non-Slicer check; a simple
additional runtime authorization is insufficient.

## Campaign 1 P3 launcher/readiness diagnosis and repair

**Cheapest discriminating check:** A no-Slicer stack-only readiness run isolated
the failure: `planning_ready:true` but `ready:false` because the status node
observed four `/joint_states` publishers. Process IDs from the r1/r2/r3 retained
stack logs matched twelve orphaned children (robot-state publisher,
`dentobot_slicer_joint_state_publisher`, MoveIt and status publisher) from
those prior temporary stacks. After command-line validation, those exact PIDs
were terminated. The baseline returned to zero publishers.

**Minimal repair:** Added `Testing/run_c1_p3_r4_batch.bash` and routed
`runtime.c1_p3_r4_endpoint` through it. The driver sources Jazzy/workspace
setup before enabling strict-unset mode (fixing the `AMENT_TRACE_SETUP_FILES`
shell error), launches the temporary stack with `setsid`, writes the process
group ID, waits for status and always terminates that exact process group. It
preserves r4 checksum/fresh-output checks and performs no workspace generation.

**Verification:** `bash -n` passed; matrix JSON parsed; focused pytest passed
**29/29**; `py_compile` and `git diff --check` passed. Fresh readiness-only run
`c1-p3-launcher-readiness-20260915-r3` emitted
`C1_P3_STACK_READINESS_PASS` on attempt one with one publisher and
`ready:true`. After exit, read-only verification found zero `/joint_states`
publishers and no `ros2 launch`, MoveIt, guard or Slicer process. This fixes the
launcher/readiness blocker only. P3 still has no endpoint candidates/result and
remains NOT TESTED/INCONCLUSIVE under its runtime ceiling; a revised explicit
authorization is required before this driver can open r4.
