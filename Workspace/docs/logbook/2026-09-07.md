# DENTOBOT logbook — 2026-09-07

## Track-A resumption — anatomy-review geometry ownership

## Emergency Step 3 consecutive-segmentation usability fix

### Scan/run context implementation continuation

- **Implementation:** Added persisted inspection references (`inspectedVolume`, `inspectedSegmentation`) alongside the existing planning references. Steps 0–3 now use a fixed scan/result context bar and footer. Inspection selection updates the source-paired slice background and run visibility without changing the committed Step 4+ planning segmentation or trajectory. Explicit `Use for Planning → Step 4` remains the handoff that can invalidate dependent downstream evidence after confirmation.
- **Workflow behavior:** Loaded scalar volumes are marked as case scans; derived bridge volumes remain out of the scan selector. Segmentation results are filtered to the teeth bridge operation and grouped by their exact `DENTOBOT.SourceVolume` reference. New inference results stay attached to their launch volume even if another scan is inspected before completion.
- **Comparison:** Added same-source run comparison and cross-source comparison across separate slice composites, with an exit action restoring backgrounds, run visibility, opacity, and the inspection context. Recommended View/Restore are guarded from reintroducing planning anatomy while Steps 0–3 are active. Step 3 label actions now explicitly restore whole-run display visibility before changing segment visibility.
- **Verification:** The first post-patch host suite exposed a missing CMake registration for the new module (`57 passed, 1 failed` in `test_all_internal_modules_are_installed_by_cmake`). Added `widget_scan_context.py` to `DENTOWorkflow/CMakeLists.txt`. The focused Slicer scenario then passed with `DENTOBOT_SEGMENTATION_RUNS_PASS`, exercising preDental Run 1, preDental Run 2, postDental Run 1, actual slice-composite background IDs, run isolation, planning-context preservation, and comparison enter/exit. UI XML parsing, Python compilation, and `git diff --check` passed.
- **Evidence boundary:** The runtime check uses synthetic source volumes with distinct voxel values and synthetic segmentation runs. It validates the state transition and viewer IDs, not the operator's real DICOM anatomy or visual registration. Operator reload and visual confirmation remain the next action.

- **Operator observation:** The 3D Slicer sample case contains preDental and postDental CBCT volumes. Running teeth segmentation consecutively for both in one DENTOWorkflow session produced two opaque segmentation-run entries. Step 3 could neither identify their source scans nor intentionally show one versus both, and exposed no reliable rename action.
- **Root cause:** Segmentation output names contained only `DENTOsegmentation_Teeth_<run-id>`. Selecting a result changed the authoritative review node but did not manage the display visibility of sibling DENTOBOT segmentation runs. The selector did not expose a working rename path in this Slicer build.
- **Implementation:** New results are named `[Teeth] <source-volume> — run <short-id>` using Slicer's unique-name generator. Completing a run isolates that result by default. Step 3 now has `Show Selected Run Only`, `Show All Runs Together`, and `Rename Selected Run` controls. Only nodes produced by the `segment-teeth` bridge operation participate, so unrelated segmentation nodes are not hidden or renamed.
- **Completion ownership:** A run that finishes while another scan is selected stays attached to its launch scan, remains hidden from the active inspection, and reports `Completed for <scan> — ... Review result` without stealing focus.
- **Verification:** Temporary-cache Python compilation passed for the affected backend/bootstrap/review modules and focused smoke script; UI XML parsing and `git diff --check` passed. The first `--no-main-window` smoke stopped before feature execution because DENTOWorkflow requires a main window. An isolated Xvfb-backed Slicer rerun then exposed that this build ignored the selector's declarative `renameEnabled` property; that attempt failed specifically with `selector rename action is disabled`. The unreliable property was replaced by the explicit rename button. The final isolated Slicer run exited successfully with `DENTOBOT_SEGMENTATION_RUNS_PASS` and preserved both source-based names while proving selected-only and all-runs visibility behavior. The operator's existing Slicer/ROS/MoveIt session was not stopped, reloaded, or mutated.
- **Evidence boundary / next:** This is synthetic Slicer UI/MRML evidence, not yet observation of the operator's actual preDental/postDental nodes. Reload DENTOWorkflow (or restart Slicer) before checking the live case. Existing old nodes keep their old names but can now be renamed; newly completed runs receive source-based names automatically.

### Final Steps 0–3 implementation checkpoint

- **Documentation:** Added the source-paired inspection workflow to `DEVELOPMENT_PLAN.md` and `DECISIONS.md`. The plan now explicitly separates inspection state from planning authority and records the real preDental/postDental reload as the remaining acceptance boundary. `TASKS.md` retains one non-duplicated `S3-U-01` row for that operator check.
- **Focused runtime rerun:** With the pinned container idle (only its init/sleep and ROS 2 daemon were present), reran `run_dentobot_segmentation_runs_smoke.py` under Xvfb. The first strengthened comparison assertion exposed a test-fixture mistake (the reference was accidentally chosen from the already active postDental scan); the fixture was corrected to use preDental as the reference, and the final run exited `0` with `DENTOBOT_SEGMENTATION_RUNS_PASS` for pre run 1, pre run 2, and post run 1. The scenario now verifies independent cross-scan slice backgrounds, selected-run isolation, unchanged planning references while browsing, and reversible comparison. No ROS/MoveIt planning or robot motion was started.
- **Evidence boundary:** The test remains synthetic MRML/UI evidence. It does not establish that the operator's real DICOM scene has correct source references or visual registration. The next action is a module reload and manual visual check in the real preDental/postDental scene.
- **Static regression:** The focused host contract suite passed `58 tests in 0.57s` (`test_step6_state.py`, `test_robot_workflow_facade.py`, `test_step6_planning.py`, and `test_modular_structure.py`). Python compilation, UI XML parsing, `git diff --check`, and the Graphify refresh also passed; Graphify reports 7,082 nodes and 11,126 edges with only the repository's existing C/C++ extraction warnings.
- **Final bounded edits:** Step 0's fixed-footer action is disabled until a scan is selected. Segmentation completion status now names the launch scan and explicitly says when the current inspection was preserved. The final local check passed Python compilation, UI XML parsing, `git diff --check`, and the same 58-test host suite (`58 passed in 1.09s`). Graphify refreshed to 7,087 nodes / 11,132 edges; its existing C/C++ parser warnings remain non-blocking.

### Follow-up — review session and preview freshness

### Follow-up — native serialization gate prepared

### Native anatomy-review regression — PASS

- A fresh `docker top` showed the operator Slicer/MoveIt processes had exited; only container init/sleep and the pre-existing Python daemon remained. This external-state change cleared exclusive runtime access. No operator process was terminated.
- Ran the previously approved focused synthetic test, without ROS planning or robot motion: `docker exec dentobot-slicerros2 bash -lc 'source /opt/ros/jazzy/setup.bash; source /workspace/ros2_ws/install/setup.bash; QT_QPA_PLATFORM=offscreen timeout 180s /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --no-main-window --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/data/dentobot-runs/anatomy-review-check.py > /workspace/data/dentobot-runs/anatomy-review-check.log 2>&1'`.
- Evidence: `data/dentobot-runs/anatomy-review-check.py` invokes only `test_DENTOWorkflowStep6SessionAnatomyReview`; the adjacent log contains `DENTOBOT_ANATOMY_REVIEW_RESULT {"success": true}`. Process exit **0**. No Error/Traceback match. Post-run process inventory returned to the same init/sleep/pre-existing daemon; the test Slicer exited.
- Source baseline `529626df3db483531bf477e884dc8b5d4a1ff8a8` plus current dirty edits; container image `sha256:f71da23aaa35161730536530ed18c594ccb7766ed8d3a35cfd68d0f385280faa`.
- Proven scope: synthetic source-copy transform alignment, source geometry preservation, non-target eligibility, prior-session/accepted-fingerprint/source-transform rejection, active-proxy/display exclusion from actual MRB XML, and proxy discard. This is archive-content evidence, not a complete reload trial, real artifact classification, live collision publication, or full x4 acceptance.
- Remaining physical input: valid insertion capacity/trajectory and operator assessment of actual FDI15/burr contact. No trajectory shortening, CAD changes, collision exemptions or x4 rerun occurred.

- Inspected `_saveSceneSnapshotToMrb`: DentoCase delegates to native `slicer.util.saveScene` after existing ROS-node sanitization. No alternate serializer was introduced.
- Extended the focused anatomy-review regression to save an active review into a temporary MRB and inspect its MRML XML. It requires the source segmentation to remain present, proxy/display IDs to be absent, and no active-review attribute to be serialized. The temporary archive is removed afterward; the current test scene is not cleared.
- Regression compilation and `git diff --check` passed. The new archive assertion is **not executed** and is not save/load acceptance evidence. Graphify refresh requested. Existing operator runtime remains untouched.
- Next runtime action requires exclusive access to the existing simulation container. A single focused native regression is sufficient here; a full x4 rerun remains blocked by unresolved tool-depth and anatomy inputs.

- Read the complete active goal attachment again; the full single-trajectory loop remains the definition of done. The preceding turn was progress (source corrections and passing host checks).
- Added a module-local review session identity. Old MRML review copies fail activation/publication after module reload rather than inheriting authority. Added `step6AnatomyReviewFreshnessIssues` to shared confirmed-task validation and guarded-preview waypoint/final-completion checks. A stale review consumes the preview session, reports `task_stale`, and retains the away-from-Home recovery requirement.
- Extended the native regression with prior-session and accepted-fingerprint mismatch cases. No native test executed. This implements the boundary but does not prove Slicer runtime behavior or save/load exclusion.
- Verification: temporary-cache `py_compile` for scene sync, robot logic, façade and native regression passed; the same four-file host pytest selection passed **58 tests in 0.60s**. Graphify update requested after source edits. No runtime process or physical input changed.
- Remaining: native review lifecycle/roundtrip verification, local edit provenance/contact-region evidence, then operator resolution of insertion capacity and actual adjacent-tooth contact before x4 acceptance.

- **Operator instruction:** Resume implementation after quota exhaustion; preserve the approved Track-A/P0 scope.
- **Inspection:** Preserved the existing dirty tree, including today's changed-target Step 6A fix. The review copy omitted its source parent transform/reference geometry, and collision replacement retained live Home/workspace validation.
- **Implementation:** `logic_robot_scene_sync.py` now copies source transform/reference geometry, rejects duplicate review nodes, records source and accepted-proxy world-surface fingerprints, and rejects changed source/transform, newly targeted review anatomy, or post-acceptance edits before proxy publication. The transient display is excluded from scene saving. `DENTORobotWorkflowFacade.py` clears live Home/workspace validation when changing active review geometry. No collision policy, endpoint, tool length or base pose changed.
- **Regression source:** Expanded `test_DENTOWorkflowStep6SessionAnatomyReview` with translated-source world-geometry equality, target-change rejection and source-transform-change rejection; welded synthetic cube vertices. This Slicer test is unrun.
- **Executed verification:** `PYTHONPYCACHEPREFIX=/tmp/dentobot-review-20260907/pycache python3 -m py_compile` on the two production files above plus `dentobot_workflow/slicer_tests.py` passed. `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_state.py Testing/test_robot_workflow_facade.py Testing/test_step6_planning.py Testing/test_modular_structure.py` passed **58 tests in 1.21s**. `git diff --check` passed. These are host syntax/contracts, not native MRML acceptance.
- **Graph:** `graphify query` and `graphify update .` succeeded; update reports 7,008 nodes/11,022 edges with C/C++ extraction warnings and retained out-of-corpus nodes.
- **Runtime ownership:** Docker socket read initially failed; escalated read-only `docker inspect` and `docker top` succeeded. The container now contains an existing Slicer/MoveIt/domain-73 session. No process was started, stopped, reloaded or commanded. Focused Slicer execution is deferred to preserve it.
- **Open boundaries:** Operator review still determines local edit extent. Module-reload deactivation, preview-time detection of post-acceptance edits, package roundtrip exclusion, and exact contact-region localization remain pending. Nonempty watertight geometry alone does not prove a local artifact correction. Anatomy review is not fully accepted.
- **Physical blockers:** Prior x4 evidence remains 15.760533814 mm requested depth versus 6.9 mm provisional capacity, plus actual non-target FDI15/burr contact separately from the 0.998041065511 mm housing clearance. No new x4 trial or physical-input change was made.
- **Next:** Finish session freshness/lifecycle, then execute the focused Slicer regression with exclusive runtime access. Full Track-A acceptance and P1 remain pending. No commit/push, Drive sync or engineer-owned record access occurred.

## Step 6A unexpected geometry after changing target — active investigation

- **Operator observation:** After completing the workflow on a different target tooth and applying the open-mouth transform, remnants apparently associated with the earlier FD14 case appear attached/fused. Operator requested immediate cleanup, then asked to continue. Screenshot supplied in the task. The attribution to FD14 is the operator's hypothesis, not yet established by a geometry audit.
- **Source inspection:** `Step6SceneLogicMixin._createStep6DerivedAnatomy` clears all previous proxy segments before copying the explicit current source segment IDs. `createOrUpdateStep6CaseJawOpening` rebuilds upper/lower proxies. `_updateStep6CaseTargetAttachedDisplay` separately copies the referenced final printable template (or draft support/docking models) for a lower-jaw target. The screenshot alone cannot distinguish upstream template/segmentation contamination from stale visible scene objects.
- **Commands/read evidence:** Read compact context, package routing map, verification protocol, relevant architecture/traceability passages, and scoped source methods. `graphify query "Step 6A open mouth derived segmentation stale previous case target tooth remnants"` completed and located the jaw-preparation methods. Initial `git status --short` at the host overlay failed because it is not the development repository; the same inspection at `ros2_ws/src/DentoBot` succeeded and showed pre-existing source/document edits, which were preserved. Today's log did not previously exist.
- **Access failure:** `unzip -l /home/light-tarun/dentobot/data/Slicer_Saved/SampleStudy1/FDI44/dentobot-case-step6x4.dentocase` failed with permission denied both normally and with escalated host execution. No archive contents were read or modified. This was an OS access failure, not an automatic-approval rejection. The approved container inspection was then attempted; the configured container was stopped, its command was confirmed as `sleep infinity`, and `docker start` failed before process creation with an unsupported Docker-shim protocol.
- **Additional evidence:** The readable historical `test1_6_FD14.mrb` contains a `[Step 5C] DENTO Final Printable Template` plus `[Step 6] MoveIt obstacle` references and explicit FDI14 lineage. This confirms that Step 5C geometry is a separate source that can be carried into a Step 6 scene, but it does not prove the unreadable FDI44 package contents.
- **Implementation:** Fixed the target/segmentation invalidation path in `widget_planning.py` and `widget_segmentation.py`. Before a changed target or authoritative segmentation is accepted, the transient Step 6A opening is reset, removing the jaw transform, opened-jaw proxies, gap line, opened trajectory, and target-attached display proxy while restoring source visibility. `logic_step6_scene.py` now excludes target-attached models whose persisted lineage belongs to another target, and the shared lineage helper hides/marks those stale models. Existing downstream Step 4/5 deletion still runs afterward. The change preserves all unrelated dirty edits.
- **Verification boundary:** `PYTHONPYCACHEPREFIX=/tmp/dentobot-step6a-pycache-20260907 python3 -m py_compile` passed for `logic_lineage.py`, `logic_step6_scene.py`, `widget_planning.py`, and `widget_segmentation.py`; `git diff --check` passed; the matrix-routed pure command `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_state.py Testing/test_robot_workflow_facade.py Testing/test_step6_planning.py` passed `53 tests`, and the same run with `Testing/test_modular_structure.py` passed `58 tests` including the 1,500-line context budget. Direct host `py_compile` was attempted first but could not write the repository's owner-only `__pycache__`; no source was modified by that attempt. `graphify update .` succeeded after escalated local-index runs, with expected C/C++ extraction warnings. No Slicer execution, live MRML access, ROS/MoveIt process, or robot action was performed.
- **Runtime limitation:** The configured native Slicer executable is absent on the host. The configured container is an idle `sleep infinity` shell, but `docker start dentobot-slicerros2` failed before process creation with an unsupported Docker-shim protocol. The focused Step 6A Slicer regression therefore remains unrun; no functional runtime PASS is claimed.
- **Next bounded action:** Reload the updated module or restart the scene in the operator's Slicer session, use Reset Case Jaw Closed once if the old opening is still visible, then change the target and re-apply Step 6A. Capture the visible result and run the focused Slicer regression when the pinned runtime is available. Do not delete or rewrite saved anatomy based on the screenshot alone.

### Follow-up — FDI44 package replaying prior-target planning objects

- **Operator clarification:** The FDI44 lower-incisor case opens with the target-tooth bounds box and Entry-to-Target trajectory from the earlier FD14 development case. This is a workflow-lineage mismatch, not merely a disabled Reset button.
- **Root-cause boundary:** Case-package MRML integrity validation compared each recorded node to its manifest but did not require the active target, trajectory association, bounds ROI, docking assembly, and final template to share one target lineage. The restored planning widget could therefore adopt a persisted FD14 trajectory and silently switch the active target during UI hydration. The view catalog also allowed hidden-from-editor DENTOBOT-owned display nodes to escape the managed Recommended preset.
- **Implementation:** `step6PlanningPackageFreshnessIssues` now rejects cross-target trajectory/ROI/docking/template chains; Step 6A opening refuses to run while that chain is inconsistent; restore deactivation clears disposable opened-jaw/target-attached proxies before disabling the stale imported context; and target-attached visibility restoration keeps mismatched source artifacts hidden. DENTOBOT-owned phase/trajectory nodes are included in managed Step 6 view isolation even when hidden from editors.
- **Association hydration correction:** Planning UI hydration no longer adopts a saved trajectory's target over an already persisted active target. A mismatch remains visible as a repair warning and is rejected by the Step 6 package freshness gate, preventing FD14 from silently replacing FDI44 during restore.
- **Reset-gate correction:** Step 6A Reset remains enabled when disposable opening refs exist even if restore has deactivated the imported context; it remains disabled while ROS motion control is active. This makes stale-scene cleanup reachable without weakening the disconnect-ROS safety check.
- **Verification:** Temporary-cache compile passed for the four changed Python modules. `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_state.py Testing/test_robot_workflow_facade.py Testing/test_step6_planning.py Testing/test_modular_structure.py` passed **58 tests**; `git diff --check` passed; the active scene module remains exactly 1,500 lines; Graphify refreshed successfully after an initial sandbox permission failure. No native Slicer/MRML runtime was started or modified.
- **Evidence boundary:** The specific FDI44 `.dentocase` remains owner-only (`nobody:nogroup`, mode 600) and was not read or changed. The operator's visible report motivates the lineage guard, but the archive's exact internal FD14/FDI44 records remain unconfirmed until it is opened in the operator's Slicer session or provided through an authorized read path.
- **Next bounded action:** Reload the module in the operator's Slicer session and reopen the FDI44 package. If the restored package reports a cross-target planning-chain error, regenerate Step 4A/4C/5C for FDI44 before 6.0A; do not apply the old FD14 trajectory. If old opened geometry remains visible after reload, disconnect ROS and use Reset Case Jaw Closed once; the stale imported context now clears disposable Step 6A proxies during package revalidation.

### Operator hypothesis — New Empty Case may have preceded FDI44 work

- **Operator observation:** The operator may have used the Step 0 “New Empty Case” action to clear the prior FD14 session before starting FDI44, and will verify by cleanly closing/reopening the container and repeating the complete FDI44 workflow.
- **Source check:** `onNewCase()` confirms the action and calls `slicer.mrmlScene.Clear(0)`. The scene-start-close handler releases the façade, clears transient state, disconnects DENTOBOT ROS motion control when present, and shuts down the Slicer adapter before the scene is removed. Scene-end-close then creates only the default ROS singleton and a fresh workflow parameter node. No intentional path preserves prior MRML trajectory, ROI, template, or opened-jaw geometry.
- **Interpretation boundary:** A clean FDI44 rerun with no FD14 artifacts would support session carry-over as the cause. Reproduction after the clear would shift the investigation to package/import contents or a remaining restore path. This is not yet runtime evidence; the operator's clean rerun is the next bounded verification.

### Package-level inspection — relocated FDI14/FDI44 archives

- **Read-only inspection:** The relocated FDI14 and FDI44 `dentobot-case-step6x4.dentocase` archives are now readable. Both contain the expected `manifest.json`, `scene/case.mrb`, `workflow/lineage.json`, robot profile, save report, and checksum files.
- **Lineage result:** FDI14's recorded bounds, trajectory, support draft, and jaw transform are labeled FDI14. FDI44's corresponding records are labeled FDI44; its nested `case.mrml` contains FDI44 labels and zero `FDI 14`/`FDI14` strings. This is evidence against direct FD14 text/lineage contamination in the saved FDI44 archive, but binary mesh identity is not established by labels alone.
- **Runtime boundary at inspection time:** The pinned container initially owned an existing Slicer/ROS/MoveIt session, so a second Slicer process or replacement would have violated serialized runtime ownership without an explicit takeover decision. The operator later explicitly authorized takeover after confirming that session had been forcibly closed; the bounded round trip is recorded below. No source archive was modified.

### Runtime verification — FD14 load → New Empty Case → save

- **Operator authorization:** The operator explicitly authorized taking over after the prior session was forcibly closed. The container was checked first; no Slicer, `xvfb-run`, or test process remained. No robot motion, drilling, ROS motion command, or patient-facing operation was performed.
- **Command:** `timeout 180s docker exec dentobot-slicerros2 sh -lc "xvfb-run -a /opt/slicer/Slicer-SuperBuild/Slicer-build/Slicer --no-splash --additional-module-paths /workspace/ros2_ws/src/DentoBot/DENTOWorkflow --python-script /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_fd14_newcase_save_smoke.py"` with output redirected inside the container to `/tmp/dentobot-fd14-newcase.log`.
- **Runtime:** Pinned `dentobot-slicerros2` container image `dentobot/slicerros2:jazzy-moveit-sim-20260903` (`sha256:f71da23aaa35161730536530ed18c594ccb7766ed8d3a35cfd68d0f385280faa`), Slicer 5.10 build, virtual X display. The first no-main-window attempt correctly failed at the UI lookup boundary (`Could not find main window`); it did not modify the case archive. The rerun with a virtual main window exited `0` and emitted the explicit marker below. A post-run process check found no Slicer/test process remaining.
- **Result marker:** `DENTOBOT_FD14_NEW_CASE_SAVE_PASS {"afterNodeCount":99,"savedLineageNodeCount":0,"savedPackageHits":{"lineage_FDI14":0,"mrml_FDI14":0},"source":{"fd14_nodes":5,"package_hits":{"lineage_FDI14":4,"mrml_FDI14":5}}}`. The source FD14 bundle was loaded as FD14, New Empty Case removed all FD14-named nodes, and the newly saved bundle contained no FD14 text/lineage hits.
- **Interpretation:** This directly supports session carry-over as the mechanism behind the observed contamination when New Empty Case is used successfully. It does not prove that the separate FDI44 archive's binary meshes are correct, nor does it replace a clean end-to-end FDI44 regeneration and visual review. The original FD14 archive was not overwritten; the temporary post-clear bundle was deleted by the test after validation.
- **Implementation/evidence:** Added `Testing/run_dentobot_fd14_newcase_save_smoke.py` as a repeatable regression for this exact load/clear/save boundary. The test validates the saved `.dentocase` before inspecting nested `scene/case.mrb` MRML and `workflow/lineage.json`.
- **Follow-on verification:** The matrix-routed host selection `python3 -m pytest -q -p no:cacheprovider Testing/test_step6_state.py Testing/test_robot_workflow_facade.py Testing/test_step6_planning.py Testing/test_modular_structure.py` passed **58 tests in 0.58s**. Temporary-cache `py_compile` passed for the new Slicer regression script, `git diff --check` passed, and `graphify update .` refreshed the code graph to 5,528 nodes / 8,930 edges / 394 communities.

## Chrome Remote Desktop weekend outage — diagnosis

- **Operator report:** Remote access stopped randomly Saturday; lost access over weekend despite PC staying on.
- **Current state (2026-09-07 ~14:45 IST):** uptime ~1h (rebooted ~13:41 today). `chrome-remote-desktop@light-tarun` is **inactive** and **disabled**. `crd-fix --diagnose`: Host STOPPED; X display :20 down; session file and host config present.
- **Timeline:**
  1. Sep 2 19:31 — unit enabled.
  2. Sep 2 20:17 — `toggle-crd` stop path ran via pkexec and **disabled** CRD (`systemctl disable --now`), then CRD was started again later that evening **without re-enable**.
  3. Sep 1–Sat Sep 5 09:14 — long boot with live CRD heartbeats (pid family from Sep 2 21:15 start).
  4. Sat ~09:14:56 — **unclean reboot** (journal ends mid-snapd-desktop-integration spam; no clean shutdown, no OOM/panic line found). CRD last heartbeat 09:14:34.
  5. Sat 09:16–13:56 and Sat 13:59–Mon 13:39 — machine was up (matches operator “PC stayed on”), but CRD **never auto-started** because the unit stayed disabled.
  6. Mon ~13:39 — another reboot; still disabled/inactive.
- **Root cause (access loss):** CRD was not boot-persistent after `toggle-crd` stop/disable; the Saturday host interruption killed the running session and nothing brought it back. Secondary: intentional policy in `~/.local/bin/toggle-crd` keeps CRD disabled at boot to avoid GDM races.
- **Not primary cause:** WiFi (`iitmwifi`) had periodic CONNECTED_SITE flaps; heartbeats continued until the unclean reboot. No evidence remote “randomly” stopped while CRD stayed up.
- **Next (awaiting operator):** From local console, `crd-fix --fix --enable --restart` (or `toggle-crd` start) to restore remote access and decide whether boot-enable is acceptable vs. manual start after each reboot.

## CRD conditional boot-guard — implemented

- **Operator ask:** Keep CRD after unclean shutdowns and remote-initiated reboots; do not auto-start after clean/manual host shutdowns when on the physical machine; provide a remote reboot path.
- **Design:** `chrome-remote-desktop@light-tarun` remains disabled at boot. Enabled `crd-boot-guard@light-tarun` starts CRD only when `~/.config/chrome-remote-desktop/armed` or `reboot-with-crd` is present. Clean-stop/shutdown hooks (`ExecStop` drop-in + `crd-boot-policy-shutdown@`) disarm unless `reboot-with-crd` is set. Unclean power loss leaves `armed` in place.
- **Tools:** `~/.local/bin/crd-boot-policy`, `crd-boot-guard`, `crd-reboot`; installer `~/.local/share/crd-boot-guard/install.bash`; polkit `49-crd-reboot.rules` for passwordless reboot. `toggle-crd` / `crd-fix` updated to arm/disarm and prefer boot-guard over always-on enable.
- **Verification:** installer completed; boot-guard + shutdown hook enabled; stock CRD unit disabled; policy sims (disarm→skip, arm→start, mark-reboot→start) passed; `systemctl start chrome-remote-desktop@light-tarun` left host STARTED with `armed=yes`. No intentional reboot test yet.
- **Operator use:** From host start with `toggle-crd` (arms). From CRD reboot with `crd-reboot`. From host end remote work with `toggle-crd` stop (disarms).

## data/ permissions — root ownership from SlicerROS2 container

- **Operator report:** Most files under `/home/light-tarun/dentobot/data` felt read-only; could not add folders or change contents.
- **Diagnosis:** Not a filesystem remount or immutable flag. Top-level `data/` remained `light-tarun:light-tarun` mode `775` and was writable. ~223 paths were `root:root` (dirs often `755`), so host user could read but not create/modify inside them. Affected trees: `dentobot-runs/`, `model-cache/`, `fixtures/`, `Slicer_Saved/SampleStudy1/` (incl. FDI44), plus several QA PNGs and STL/MRB artifacts.
- **Culprit:** `Workspace/compose.yaml` bind-mounts host `data` → `/workspace/data` for `dentobot-slicerros2`. The service has no `user:` mapping and runs as container root (`slicer-user` → `/root/.config/slicer.org`). Files created/saved by Slicer/tests inside the container land on the host as `root:root`. Newest root-owned write today: `Slicer_Saved/SampleStudy1/FDI44/dentobot-case-step6x4-markup.dentocase` (~14:49 IST). Pattern dates back at least to 2026-07-31 (`model-cache`).
- **Fix:** Host `sudo chown` failed (askpass timeout). Ran `docker exec dentobot-slicerros2 chown -R 1000:1000 /workspace/data` (container root). Then host `chmod -R u+rwX,g+rwX` on the formerly root-owned trees. Post-check: `0` root-owned paths under `data/`; `13319` owned by `light-tarun`; mkdir/touch verified in `dentobot-runs`, `SampleStudy1`, `model-cache`, `fixtures`.
- **Recurrence note:** Any later save from the same root container will reintroduce root-owned files until compose/`user:` or an exit-time chown is added. No compose change made in this turn.
- **Evidence boundary:** No case archive contents were modified beyond ownership/mode; no Slicer workflow logic changed.

## SlicerROS2 host-UID mapping (lasting data/ ownership fix)

- **Operator request:** Wire the lasting fix so container writes stop creating root-owned files under `data/`.
- **Implementation:** `Workspace/compose.yaml` now runs as `DENTOBOT_HOST_UID:DENTOBOT_HOST_GID` with `group_add: [DENTOBOT_RENDER_GID]`, `HOME=/home/dentobot`, and bind `slicer-home` → `/home/dentobot`. Retired `/root/.config/slicer.org`. Launcher exports host UID/GID and render GID, migrates legacy flat `slicer-user/` into `slicer-home/.config/slicer.org/` (compat symlink retained), grants X11 to the host desktop user, and reclaims ownership of `data/`, `slicer-home/`, and `ros2_ws/{build,install,log}` via `docker exec -u 0` after bring-up / on GUI-session exit.
- **Verification:** `launch-dentoworkflow.bash --check-only` passed after recreate. Container `User=1000:1000`, render group 990, `dri_ok`. Container-created test files under `data/dentobot-runs` and `slicer-home` appear as `light-tarun` on the host. `0` root-owned paths under `data/` and `slicer-home/`. First check-only hit a root-owned `ros2_ws/log` PermissionError during colcon; reclaim scope was widened and the rerun passed (description + moveit_config build).
- **Docs:** `DECISIONS.md`, `SETUP.md`, `HOST_LAYOUT.md`, `changelog.md`, `install-lab-wsl.bash`, `update-lab-release.bash`, manual RViz launcher X11 grant.
- **Next:** Ordinary GUI launch via the daily launcher; new saves should remain host-owned without manual chown.
## Step 6 baseline cleanup implementation — 2026-09-07

### Operator request

The operator supplied the case-reset/cleanup directive and asked to review which
Step 6 collision workarounds had become obsolete before continuing the revamp.
The explicit course correction is to retire the abnormal pre-surgery/x4 case as
the acceptance baseline, preserve generic diagnostics and the guarded planning
architecture, and start implementation of that cleanup.

### Interpretation and scope

The existing production path had two broad exceptions that could hide the
geometry problem instead of exposing it: the burr-proximity helper returned all
target/anatomy and guide objects, and the façade passed guide objects as
clearance exemptions. A separate template-collision bypass and session anatomy
proxy were useful for historical investigation, but neither is valid default
acceptance state. The accepted J1–J5 canonical TCP, J6-outside-planning model,
strict guard, endpoint checks, and full-chain promotion rules are not being
reworked in this slice.

### Implementation

- `step6BurrProximityCollisionObjectIds` now returns only the selected target
  tooth collision object. Adjacent teeth, jaw anatomy, and guide/template
  geometry are no longer folded into the burr proximity set.
- `_configure_phase_guard` sends an empty guide-clearance exemption list. The
  guide/template remains authoritative in the MoveIt scene.
- `DENTOROS2Bridge.configure_task_phase_guard` rejects non-empty guide
  clearance exemptions, preventing an old caller from silently reintroducing
  the production bypass.
- The historical final-template exclusion is process-gated by
  `DENTOBOT_ENABLE_HISTORICAL_TEMPLATE_OVERRIDE=1`; the normal UI keeps it
  disabled and labels it retired. Session anatomy-review collision proxies are
  effective only with `DENTOBOT_ENABLE_HISTORICAL_ANATOMY_REVIEW=1`; source
  segmentation remains authoritative otherwise.
- Updated the historical exact-case smoke entry point and source-contract test
  to use the new explicit legacy boundary. No case package or engineer-owned
  record was modified.

### Evidence boundary and next action

No Slicer, ROS, MoveIt, robot motion, or hardware process was run in this
implementation slice. Source inspection and patching completed; focused
static/pure verification is the next bounded check and must be reported before
execution. A clean post-surgery case review is required before Track-A runtime
acceptance. The retired x4 fixture must not be made to pass by relaxing policy,
tolerances, base placement, or guide/anatomy collisions.

### Commands and result record

- `graphify update .` completed and refreshed the repository-local graph. It
  reported the repository's existing C/C++ parser warnings and fail-closed
  retained nodes; no semantic/API pass was requested.
- `git diff --check` passed after the implementation patch.
- No Python compilation, C++ build, pytest, Slicer, ROS 2, MoveIt, or runtime
  verification was run in this slice. The next proposed check is the scoped
  pure/static contract gate; runtime acceptance still requires a clean case and
  the verification protocol's approval.


## CUDA workstation documentation publication and device handoff

- **Operator statement:** Requested the Ubuntu CUDA workstation README/logbook
  changes on origin; development will alternate between two devices, one active
  at a time.
- **Scope/implementation:** Imported README, September 7 laptop logbook, TASKS,
  and changelog from `origin/plat/ubuntu-nvidia-cuda-workstation` (`5dee4da`)
  in isolated worktree `/tmp/dentobot-ubuntu-cuda-docs`, branch
  `integration/ubuntu-cuda-docs`. Added an alternating-device commit/push →
  fetch/fast-forward routine and explicitly marked CUDA code integration pending.
- **Commands/results:** Initial overlay-root Git inspection failed because the
  overlay is not a repository. Sandboxed fetch failed DNS; escalated
  `git fetch origin` succeeded. `git log`, `git diff`, and `git merge-base`
  identified six branch commits/eight files. `git worktree add`, scoped
  `git restore`, `git add`, and `git commit` succeeded. Commit `0b3847e`
  contains four documentation files. `git push origin HEAD:main` from the
  isolated worktree succeeded: `ea50434..0b3847e HEAD -> main`.
- **Preservation/evidence:** Original checkout remains at `529626d` with all
  pre-existing local changes preserved. No runtime, build, or test was run;
  imported laptop acceptance remains historical evidence. This entry records
  publication locally and is not itself included in `0b3847e`.
- **Next:** Reconcile the five local commits plus uncommitted source/docs with
  updated origin before switching devices. CUDA launcher/dependency changes
  remain on the platform branch under `PLAT-U-05`; this is a documentation
  import, not full source synchronization. Engineer-owned records untouched.
# Step 6 baseline cleanup continuation — focused verification

- **Operator request:** Continue and complete the approved anomaly-reset cleanup;
  preserve general planning/diagnostics and stop optimizing the retired x4 case.
- **Interpretation:** Complete the bounded source cleanup, not claim a successful
  clean-case simulation without runtime evidence. No source segmentation was edited.
- **Implementation:** Python bridge and native guard now reject non-target
  clearance allowances; native guard also restricts the allowed contact link to
  `burr`. Guard-policy fingerprint changed to stale previous validation evidence.
  Disabled historical anatomy overrides reject activation in shared logic;
  existing review state can still be discarded without enabling that override.
  Revoked template authorization fails closed and requires full scene resync.
  The x4 runner requires an explicit historical diagnostic flag. The phase-guard
  smoke now expects rejection, rather than success, for non-target exemptions.
- **Verification:** From `ros2_ws/src/DentoBot`,
  `PYTHONPYCACHEPREFIX=/tmp/dentobot-cleanup-pycache python3 -m pytest -q -p no:cacheprovider Testing/test_ros2_bridge.py Testing/test_moveit_config.py Testing/test_robot_workflow_facade.py Testing/test_step6_state.py`
  returned **63 passed**, exit 0. `git diff --check` passed. Scoped `compileall -q`
  of the changed bridge/facade/panel/scene-sync/widget/native-test and smoke Python
  files passed, exit 0. These checks do not establish native C++ or runtime validity.
- **Runtime boundary:** Initial Docker socket access was sandbox-denied; approved
  elevated read-only `docker ps` and `docker exec ... pgrep -af
  'Slicer|move_group|collision_guard|colcon'` found an existing Slicer process
  (PID 2252). Its owner was not this cleanup run. No process was stopped, no build
  or second Slicer was launched, and no robot commands were issued.
- **Next bounded action:** Release the shared runtime, build/verify the native
  guard, then use a reviewed clean post-surgery case for the complete guarded
  loop. Track A acceptance remains pending; Studio remains gated. No Git/Drive
  publication or engineer-owned record changes were performed by this continuation.

## Astra light model/delegation policy revision

- **Operator statement:** Astra has been released; reconsider task/load routing using new OpenAI docs, revise AGENTS.md, and never select anything above Astra light because of quota consumption.
- **Agent interpretation:** Use `gpt-6-astra` / `low` as the primary and maximum route; conservatively constrain all optional model routes to low effort. This is a quota preference, not a measured cross-model cost ordering.
- **Read evidence/commands:** Used `rg --files`, `ls -ld`, `cat`, `head`, `sed`, scoped `rg -n`, and `git -C ros2_ws/src/DentoBot status --short` to inspect active instruction symlinks, compact context, today's log, package routing, verification protocol/matrix, controlled policy passages, installed Sol Advisor skill, OpenAI docs skill/reference, Ponytail, and model-only config fields. The overlay AGENTS.md resolves to Workspace/AGENTS.md; the user-global AGENTS.md is empty. Existing unrelated dirty work was preserved.
- **Official research:** Searched and opened OpenAI Astra model guidance, Codex subagents, and configuration reference; URLs are retained in AGENTS.md and DECISIONS.md. Astra guidance is current; the subagent page still illustrates GPT-5.6 routes. No universal latest optimization schema or subscription quota equivalence was inferred.
- **Finding:** Saved Codex configuration already selects `gpt-6-astra`, `model_reasoning_effort = "low"`, and default service tier. Sol Advisor 0.6.0 conflicts by requiring Sol High and fixed higher-effort workers.
- **Implementation:** Ran `/tmp/update_dentobot_model_policy.py`; changed Workspace/AGENTS.md, docs/AGENTIC_VERIFICATION_PROTOCOL.md, docs/DECISIONS.md, docs/TASKS.md, and this log. Added a durable project override, low-only task routing, one normal auxiliary, no recursive delegation, and bounded verification/context rules. No global settings, cached plugin, robot code, engineer-owned record, or external destination was changed.
- **Verification boundary:** Documentation/config inspection only; no subagents, model benchmarks, builds, tests, Slicer/ROS processes, robot actions, or paid model comparisons were launched. This does not prove runtime model selection for an existing task or quantify quota savings. Final readback results follow below.
- **Next bounded action:** Apply explicit low model/effort on the next authorized auxiliary; if unavailable, continue solo rather than escalating.

- **Final readback:** `sed`/`head`/`tail` of all five edited records and `rg -n` of the saved model settings exited 0. The policy contains the low-only ceiling, explicit legacy-role override, task routing, and one-worker default; the protocol links to it. Model remains `gpt-6-astra` / `low`. Service tier changed externally during this task from `default` to `priority`; no config write was made, and its quota impact was not verified. DECISIONS.md was corrected to preserve that observation. No automated verification campaign was required or run for this prose-only revision.

## Steps 0–3 scan/run inspection completion checkpoint

- **Operator request:** Continue the approved Steps 0–3 source-paired workflow implementation after the focused smoke passed.
- **Implementation:** Added explicit cross-source comparison display scoping: each inspected segmentation is bound to its own slice viewer, and the prior display-node view bindings are restored on exit. Step 0 remains disabled until a scan is selected; segmentation completion names its launch scan and preserves a different active inspection.
- **Verification:** Python compilation of all affected workflow modules and the smoke passed; UI XML parsing and `git diff --check` passed; the focused host suite returned **58 passed**. The focused Slicer smoke had already returned `DENTOBOT_SEGMENTATION_RUNS_PASS` for two preDental runs and one postDental run before this final display-only edit. No ROS/MoveIt or robot action was run.
- **Graph:** `graphify update .` completed with 7,090 nodes, 11,138 edges, and 632 communities. Existing C/C++ parser warnings and fail-closed retained nodes remain non-blocking.
- **Evidence boundary / next action:** Real preDental/postDental operator reload and visual acceptance remain pending. The implementation preserves downstream planning authority until explicit `Use for Planning → Step 4`; no commit, Drive sync, or engineer-owned record change was performed.
- **Final hardening:** View-node capture now handles both wrapped `vtkStringArray` and iterable Slicer bindings, and comparison falls back safely when an older display node exposes no writable view-ID setter. Static compilation and `git diff --check` were rerun after this guard; `graphify update .` refreshed the graph to 7,096 nodes, 11,145 edges, and 621 communities.

## Model routing correction — Astra ceiling is model-specific

- **Operator correction:** The low/light ceiling is for Astra only. Avoid Sol and use Astra instead. Use Terra High for mechanical tasks/testing and prefer Luna Max for coding once Astra has laid out the plan and theory in detail; quickly review OpenAI recommendations online.
- **Agent interpretation correction:** The earlier blanket low-only restriction was too broad and is superseded. Low effort is not established to be useless; exact cross-model quota savings are unmeasured. Keep requested presets and optimize whether delegation is needed, specification quality, and context size.
- **Research:** Searched official OpenAI domains, then opened `https://learn.chatgpt.com/docs/agent-configuration/subagents` (model/effort and parallel-write guidance) and `https://developers.openai.com/api/docs/guides/latest-model` (delegation/testing guidance). These support bounded task-specific roles, not a proven optimal preset combination.
- **Implementation/commands:** Read active records with `sed`, `head`, and `tail`; ran `/tmp/revise_dentobot_routing.py` using Python stdlib. Updated Workspace/AGENTS.md (including rule 15), docs/AGENTIC_VERIFICATION_PROTOCOL.md, docs/DECISIONS.md, docs/TASKS.md, and this log. Luna may edit only assigned code/test files under a complete Astra plan; controlled docs remain coordinator-owned and verification workers remain read-only. This resolves the prior rule conflict rather than leaving Luna unable to implement.
- **Boundaries:** No subagents, model runs, settings changes, cached plugin changes, tests/builds, robot/runtime commands, external writes, or engineer-owned record access. Existing unrelated edits preserved. Historical first-pass log retained with this explicit correction.
- **Next:** Apply Astra low / Luna max / Terra high assignments on the next substantial in-scope task, with one normal auxiliary and no automatic review chain. Final documentation readback follows.

- **Readback result:** `sed`/`head` displayed the corrected AGENTS rule 15, routing table, protocol and active decision/task records. A scoped `rg -n` search for `all allowed routes use|low-only|only the coordinator|sole production-code` in AGENTS.md and the protocol returned no matches (expected exit 1). Correct presets and the implementation/verification distinction were visible. Follow-up prose cleanup wrapped long lines and clarified that defect-only reporting applies to verification workers. No runtime or test acceptance is claimed.
# Native cleanup guard verification — approved continuation

- **Operator:** Explicitly approved terminating the identified headless diagnostic
  PIDs 2232/2251/2252 and continuing verification. Revalidated command identities,
  sent TERM only to those PIDs; no interactive case was loaded or modified.
- **Build:** Initial `colcon build --symlink-install --packages-select
  dentobot_moveit_config` found zero packages (not a successful build). Repeated
  from `/workspace/ros2_ws` with `--base-paths
  /workspace/ros2_ws/src/DentoBot/dentobot_moveit_config`: one package built,
  exit 0. Full log retained in the evidence directory below.
- **Runtime:** Sourced Jazzy and workspace install; used ROS domain 73 and
  `ros2 launch dentobot_moveit_config simulation.launch.py` (hardware execution
  disabled). Ran `timeout 180 python3
  /workspace/ros2_ws/src/DentoBot/Testing/run_dentobot_phase_guard_smoke.py`.
- **First failures and fixes:** Native rejection of a non-target allowance was
  correct, but the smoke helper retried it as a startup race until timeout.
  Added an explicit expected-invalid-config test option. The next run reached
  a fixture error: required target was deleted before the self-clearance probe.
  Kept the synthetic target at (2,2,2) metres for that probe. No production
  constraints, margins or collision rules changed during these corrections.
- **Result:** Final run exit 0, all 14 JSON checks true: selected burr-target
  contact accepted/reported; strict target contact, non-target configuration,
  unconfigured contact, non-tool world contact, spindle command, bounds,
  corridor escape, overshoot and wrong task rejected; sequence continuity and
  duplicate rejection passed; existing 1 mm research margin probe passed.
- **Evidence:** `/tmp/dentobot-verification/cleanup-guard-20260907/` contains
  build/stack/first-failure/retry/final logs and `result.json`. Source base
  `529626df3db483531bf477e884dc8b5d4a1ff8a8`, dirty working tree; image
  `sha256:f71da23aaa35161730536530ed18c594ccb7766ed8d3a35cfd68d0f385280faa`.
  No patient-case checksum applies to this synthetic test.
- **Teardown:** SIGINT sent only to owned launch group 2484; launcher exited 0.
  Final pgrep found no SlicerApp-real, move_group, collision_guard or simulation
  launch processes. `git diff --check` and smoke `py_compile` passed. Graphify
  AST update completed (known nine native-header partial-extraction warnings).
- **Boundary/next:** Native baseline cleanup is verified, not the full Track A
  drilling milestone. Next is a reviewed clean post-surgery case full guarded
  loop; no retired x4 rerun, Studio, hardware, Git publication or Drive sync.


## Windows lab first install (WSLg + Docker + CUDA) — imported evidence

Branch `windows/lab-wslg-cuda` records the first successful Windows 11 lab
installation: WSL2 Ubuntu 22.04, Docker Desktop, WSLg, RTX 4060 CUDA, the
published SlicerROS2 container, and the `lab/2026-09-03` source/dependency
pins. The check-only gate passed, CUDA health reported `status=ok`, and the
full GUI opened DENTOWorkflow with the MoveIt simulation stack. The operator
closed Slicer manually; no robot hardware motion occurred.

The trial exposed and corrected the Windows-specific setup gaps: actual WSL
distro-name selection, safe `wsl --exec` quoting, detached lab-tag checkout,
GHCR authentication, WSLg without `/dev/dri`, optional `xhost`, Docker Desktop
restart syntax, rebuilding `slicer_ros2_module` under the bind mount, separate
CPU/CUDA dependency probes, tracked CUDA device requests, and editable
inference installation. A later failure due to absent model weights led to the
idempotent model-cache installer for TotalSegmentator tasks 298, 115, and 113;
its check-only mode passed after downloading about 637 MB on that host.

Machine-local `.dentobot.env`, credentials, Conda environments, model caches,
and generated workspace files remain untracked. WSLg FPS/hardware OpenGL,
native Windows SlicerROS2, and robot hardware operation were not accepted.

## Integration handoff: Ubuntu NVIDIA CUDA workstation (`PLAT-U-05`)

**Branch (do not merge blindly onto busy `main` without rebase):**
`plat/ubuntu-nvidia-cuda-workstation`

**Machine:** Ubuntu 22.04 dual-GPU laptop (AMD iGPU Mesa + NVIDIA RTX 4060
Laptop). Overlay at `~/dentobot`. Active product development continues on
`origin/main` on the IITM workstation; this branch only carries platform
setup + launcher adaptations discovered while bringing that laptop online.

### Why this branch exists

A clean laptop setup hit several gaps that the CPU/Intel-only Ubuntu docs did
not spell out:

1. NVIDIA kernel module 550 vs userspace 580 → `nvidia-smi` NVML mismatch until
   conflicting 550 packages were removed and the machine rebooted onto 580.
2. Official Ubuntu launcher refused anything except `cpu` / Python 3.12 CPU
   pins; Bridge C CUDA uses Python 3.10 + `torch==2.10.0+cu130`.
3. Explicit `docker compose -f Workspace/compose.yaml` did **not** auto-load
   overlay `compose.override.yaml`, so `gpus: all` never reached the container
   (`DeviceRequests=null`, no `/dev/nvidia*`, CUDA false inside docker exec).
4. Launcher colcon only built `dentobot_description` + `dentobot_moveit_config`,
   so GUI launch failed with
   `package 'slicer_ros2_module' not found`.
5. `numpy` (and later matplotlib/filelock) had been satisfied from
   `~/.local` on the host; the Conda env bind-mount into the container did not
   include user-site, so metadata probes failed until packages were reinstalled
   with `PYTHONNOUSERSITE=1`.

### What landed on this branch

| Path | Change |
|---|---|
| `Workspace/scripts/launch-dentoworkflow.bash` | Accept `cpu` or `cuda:0` with matching dependency probes; merge `${workspace}/compose.override.yaml` when present; build `slicer_ros2_module`; set `PYTHONNOUSERSITE=1` on backend probes/exec |
| `Workspace/scripts/install-host-nvidia-docker.bash` | New helper: Docker Engine + NVIDIA Container Toolkit + write local override |
| `Workspace/.dentobot.env.example` | Document `cpu` vs `cuda:0` and dual-GPU render-node guidance |
| `README.md` | Ubuntu first-run checklist + Ubuntu+NVIDIA section; drop hardcoded IITM-only launch as the sole path |
| `Workspace/docs/TASKS.md` | New `PLAT-U-05` for integration on the other development machine |
| This logbook | Handoff evidence |

Untracked on the laptop (must **not** be committed): `~/dentobot/.dentobot.env`,
`~/dentobot/compose.override.yaml`, `data/`, `slicer-user/`, Conda env.

### Acceptance already observed on the laptop

- `nvidia-smi` → Driver 580.173.02, CUDA 13.0, RTX 4060 Laptop.
- Host:
  `python -m dentobot_inference health --json --require-device cuda:0` →
  `status=ok`.
- `docker run --rm --gpus all nvidia/cuda:12.6.0-base-ubuntu22.04 nvidia-smi`
  works.
- After override merge + recreate:
  `docker exec … torch.cuda.is_available()` → True.
- `./scripts/launch-dentoworkflow.bash --check-only` passed (CUDA health in
  container; `dentobot_description` / `dentobot_moveit_config` / later
  `slicer_ros2_module` build).
- First GUI attempt failed only on missing `slicer_ros2_module` install; fixed
  by building that package and updating the launcher package list.

Interactive OpenGL identity (AMD vs NVIDIA vs llvmpipe) and a full operator
GUI session were not closed as formal acceptance on this date; `--check-only`
and CUDA-in-container are the platform bar for this handoff.

### Instructions for the agent on the other development machine

1. **Do not force-push or rewrite `main` history.** Fetch the branch:
   `git fetch origin plat/ubuntu-nvidia-cuda-workstation`.
2. Rebase onto current `origin/main` (or merge) in a worktree / PR after
   reviewing launcher diffs for conflicts with any parallel launcher edits.
3. Keep IITM default `.dentobot.env` on **`cpu`** unless that workstation also
   gains a verified CUDA Conda stack; the launcher still supports both.
4. If IITM remains Intel DRM-only, no `compose.override.yaml` is required; the
   launcher skips a missing override.
5. Close `PLAT-U-05` only after: rebase/merge without regressing
   `--check-only` on IITM CPU, and (optional) a short note in SETUP.md pointing
   at the README NVIDIA section.
6. Product Priority-0 Step 6 work (`S6-P0-*`) stays authoritative on `main`;
   this branch must not reorder those tasks.

### Operator / engineer notes

- TotalSegmentator CLI `-t teeth` / `craniofacial_structures` is not in the
  argparse choices for 2.16.0; download task IDs 115 then 113 via
  `totalsegmentator.libs.download_pretrained_weights`.
- Overlay Compose build context can resolve incorrectly if only the symlink
  `compose.yaml` is used without override; the host install script pins
  `build.context` to `…/DentoBot/Workspace`.

### Follow-up: torchvision NMS failure (same day)

First `segment-teeth` on `cuda:0` failed with
`RuntimeError: operator torchvision::nms does not exist`. Cause: a generic
`torchvision==0.25.0` wheel was present alongside `torch==2.10.0+cu130`
(pulled transitively by `timm`). Fix: install matching
`torchvision==0.25.0+cu130` from the cu130 index; pin both in
`Inference/requirements/pytorch-cu130.txt` and require the `+cu130` tag in the
launcher CUDA dependency probe. Retry segmentation after the env fix.

## DENTO-NOTE — Step 5B unified template / dock guiderails (operator)

**When:** 2026-09-07 (laptop session after CUDA segmentation path came up).
**Tasks:** `W5-U-03` (detailed 5B testing), **`W5-U-04`** (major geometry defect).

### Operator observation

Step 5B unified template creation needs **detailed** testing, not only a smoke
pass. The major defect: **dock guiderails / unified-template branch connectors
into the docks are covering the dock bore holes themselves**. The solid that
should link shell↔dock is eating the bore lumen the dock is supposed to keep
open.

### Engineering interpretation (for the primary `main` agent)

- Treat this as a **fusion / connector routing** problem, not a display toggle.
  Likely ownership: `DENTOGuideGeometry.py` target-docking + Step 5B unified
  fusion path (connector diameter/thickness vs bore diameter; boolean order
  with rails).
- Acceptance must prove each of the four docks retains an **open bore** after
  unified fusion (through-check or equivalent), and that guiderail approach
  does not seal or bridge across the bore axis.
- Prefer a smarter connector path (offset, neck, keep-out cylinder around bore,
  or dock-first boolean subtract after connector union) over ad-hoc diameter
  shrinks that break mechanical strength.
- Coordinate with existing `W4C-U-01` (support-aware docks/rails) but keep
  `W5-U-04` as the 5B fusion-specific bore-occlusion fix so it is not lost
  inside support-pack validation.

### Integration

Recorded on branch `plat/ubuntu-nvidia-cuda-workstation` together with the
platform handoff so the other development machine can pick it up when
rebasing `PLAT-U-05` / reading TASKS. Product fix work should land on
`main` after rebase, not stay stranded on the platform branch.

## DENTO-NOTE — Step 5B UX: dimensions, reset, viewing, Advanced panel

**When:** 2026-09-07 (continued operator pass).
**Task:** **`W5-U-05`**.

### Operator observation

- Step 5B **template dimensions interact badly with previous steps** (upstream
  geometry/params produce awkward or conflicting 5B sizing without a clear
  recovery path).
- There is **no Reset option** for the unified-template / 5B path when the
  operator needs to discard a bad generation and start clean.
- There is **no interactive viewing** while working dimensions / fusion —
  inspection is awkward for a geometry-heavy required step.
- The **Advanced** tab/section must be **manually collapsed every time** the
  operator enters the step. That is wrong for Step 5B: it is a **crucial
  workflow stage**, not an optional advanced detour. Primary actions should
  not live behind a collapsed Advanced chrome that fights the operator.

### Engineering interpretation (for the primary `main` agent)

- Panel IA: promote the primary unified-template generate/inspect/verify
  controls to the always-visible Step 5B body; reserve Advanced only for
  true research overrides (staged shell-only refresh, research ROI knobs).
- Add an owned **Reset Step 5B / unified template** that clears owned outputs,
  marks stale correctly, and restores safe defaults without deleting upstream
  4A/4C/5A authority.
- Add interactive view assistance on Step 5B entry (preset + optional isolate)
  so dimension edits can be judged in 3D without hunting Scene checkboxes.
- Dimension coupling: when 5B params conflict with upstream docks/rails/shell
  bounds, fail closed with an explicit lineage message rather than silently
  producing unusable solids (pairs with `W5-U-04` bore occlusion).

### Integration

Same handoff branch / TASKS path as `W5-U-04`. Fix on `main` after rebase.

## DENTO-NOTE — Viewer missing mask 2D/3D opacity sliders (plan deeper UX)

**When:** 2026-09-07 (continued operator pass).
**Task:** **`VIEW-U-02`** (also annotates `VIEW-U-01`).

### Operator observation

The viewer **no longer has 2D and 3D opacity sliders for masks**. Mask
visibility without fine opacity control is insufficient for CBCT +
segmentation review. Do **not** slap controls back without a plan — this needs
a **deeper UI/UX plan** for good viewer interaction (where controls live, Legacy
vs New GUI parity, interaction with stage presets / View Controls).

### Engineering interpretation (for the primary `main` agent)

- Legacy segmentation UI still defines global 2D fill/outline and 3D closed-
  surface opacity sliders (`segmentation2DOpacitySlider` /
  `segmentation3DOpacitySlider` in `DENTOWorkflow.ui`, synced in
  `widget_segmentation.py`). Operator loss implies New GUI / application shell
  / View Composition path no longer surfaces them.
- Plan first (short written UX contract): always-reachable display-only mask
  opacity (2D + 3D), scene-persistent, non-destructive to masks, compatible
  with stage presets and Restore Previous View, parity Legacy↔New.
- Then implement; acceptance is a normal-window trial on a real CBCT+mask case
  proving both presentations expose and persist the sliders.

### Integration

Same handoff branch. Product UX work on `main` after rebase; coordinate with
`UI-P3-01` so New GUI refinement does not drop display authority again.


## Documentation integration and alternating-device handoff

### Operator statement

The operator requested the new Ubuntu CUDA workstation README/logbook changes
be brought onto origin and stated that development will alternate between two
devices, with only one device active at a time.

### Interpretation and implementation

Imported README, this logbook, TASKS, and changelog from remote branch
`plat/ubuntu-nvidia-cuda-workstation` at `5dee4da` into isolated branch
`integration/ubuntu-cuda-docs`, based on `origin/main` at `ea50434`.
The initial request named documentation; the separate CUDA code integration
remains pending under `PLAT-U-05`. README explicitly marks that boundary and
records the commit/push → fetch/fast-forward device handoff.
The primary checkout at `/home/light-tarun/dentobot/ros2_ws/src/DentoBot`
remains on local `main` at `529626d`, with five unpublished commits and 50
pre-existing modified/untracked paths. Those local changes, including its
independent September 7 logbook, were not included or overwritten.

### Commands, results, and evidence boundary

- Initial Git inspection at the overlay root failed: it is not a Git repository.
  Inspection continued in the authoritative nested checkout.
- Sandboxed `git fetch origin` failed DNS resolution; the authorized network
  retry succeeded and discovered the workstation branch.
- `git log`, `git diff`, and `git merge-base` showed six branch commits and
  eight affected files based on current `origin/main` (`ea50434`).
- `git worktree add -b integration/ubuntu-cuda-docs
  /tmp/dentobot-ubuntu-cuda-docs origin/main` succeeded.
- `git restore --source=origin/plat/ubuntu-nvidia-cuda-workstation -- README.md
  Workspace/docs/logbook/2026-09-07.md Workspace/docs/TASKS.md
  Workspace/docs/changelog.md` imported the four documentation files.
- Manual text inspection preserves the laptop observations as historical
  evidence and leaves CPU integration/runtime acceptance open. No build,
  automated test, installer, container, Slicer, ROS, or hardware action ran.
- Publication will use a normal non-force push to `origin/main`; its result
  and remote commit identity are reported in the task response.

### Next bounded action

Reconcile and publish the primary machine's five local commits and uncommitted
work before moving active product development to the other device. Integrate
the remaining CUDA code with the local launcher ownership changes, then
propose the CPU-profile check under the verification protocol. Do not treat
this documentation import as a complete cross-device source handoff.
