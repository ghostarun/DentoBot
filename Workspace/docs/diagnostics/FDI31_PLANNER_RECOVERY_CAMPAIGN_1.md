# FDI31 planner recovery — Campaign 1 architecture and Luna handoff

## 2026-09-16 P3 fourth static revalidation — corrected endpoint verdict

The operator expressly authorized one fourth fresh r4-only P3 static batch
after the repaired deferred-audit guide-ID path. It consumed only the immutable
21 saved J1–J5 vectors from old input SHA-256
`8853f5208a87368d2611fd9c0a9737fe6b73b4da43de6851f4a428adee3f734c`; it did
not run new IK, planning, workspace generation, preview, motion, hardware,
policy change or P4. The locked Gate-A r4 SHA-256 remained
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`.

The fresh raw static artifact (SHA-256
`d5a3ef8857b518a293d1ed7c8a9c750d0bfe0922110150242c4a4107729acd4f`) retained
all 21 accepted native replies but was falsely classified inconclusive because
`collision_guard` emits status doubles with `std::setprecision(12)`. The
repaired bounded echo attribution and companion artifact (SHA-256
`ec6ad4febcf202de3469303f53ceb4d8056f14d2d6e03272a467ba5938fce362`) preserve
the raw requests/responses/FK/generic diagnostics and establish **21
phase-static accepted, 0 rejected, 0 unknown and 21 admissible**. Each request
has a unique ID and sequence 1–21; every reply proves one requested/starting/
evaluated state with a matching 31-object scene, task, policy and guard session.

All generic-static checks still reject only as diagnostic
Template↔visual-spindle / target-tooth↔burr contact evidence. The authoritative
guard accepts every candidate with its configured non-rotating
template/spindle warning (`0.326643468686`–`0.406108573858 mm`, below
`0.500000 mm`); the native result reports self, unrelated-world and corridor
checks passed. This is not literal guide/housing clearance. No candidate was
applied or displayed, so no screenshot is claimed. Focused checks pass 80 tests
and the owned runtime is cleanly gone.

**Campaign consequence:** P3 endpoint evidence is **PASS** and conditionally
qualifies P4 as the next feasibility question: whether a permitted static
Target yields a valid bounded insertion path. P4 remains NOT RUN and requires
separate authorization. A fifth P3 batch, all motion/hardware work and all
frozen-invariant changes remain unauthorized. Detailed evidence is in recovery
report Section AU and the 2026-09-16 logbook.

## 2026-09-16 P4/P5 explicit-operator supersession and bounded result

The operator subsequently authorized the scoped P4 diagnostic, P4 evidence
visuals, and the work necessary to reach P5. That current decision supersedes
only this document's historical stop-after-P4/architecture-review instruction:
it permits the one r4-only P5 approach diagnostic below, not P6, P7, a full
cycle, withdrawal/Home, repeat, geometry/policy/configuration change, hardware,
powered spindle or patient action.

P4 wrote `c1-p4-fdi31-20260916-r4/insertion_branches.json`, SHA-256
`3cc1759d1dc5042748d729d5e2a1ae784a94a60d33cb7c8350695b321492bc72`, with six
complete diagnostic insertion witnesses. P5 wrote
`c1-p5-fdi31-20260916-r4/approach_branches.json`, SHA-256
`6455a52eb170ca304f33bf1b4e76bcb583bc552cc5991971fa3e9593546b7f9b`.
It is a `SAMPLED_PASS` from a fresh monitored zero-vector simulation state via
one saved P4 source-6 Entry witness: one 5-second RRTConnect Stage-1 request,
208 accepted Phase-`approach` edges, a complete fixed-axis Stage 2 with 12
accepted Phase-`terminal_contact` edges, and one accepted P4 Entry join. The
222 requests are distinct/increasing and `validate_only`; native positions and
the raw stream remained unchanged/off. Stage 2's actual FK residual is
`0.0004285879962906579 mm / 0.0002018339679783406°`. Its existing Cartesian
interpolator returned no points, while its existing bounded position-axis
continuity fallback completed the diagnostic line; this does not alter OMPL,
the guard policy or any frozen invariant. P5 is not physical guide-fit or
housing-clear evidence. The authoritative report is Section AV.

## 2026-09-16 P3 diagnostic-correctness correction — current supersession

The old r4 P3 artifact remains immutable Campaign-1 input, but its completed
bounded-negative result is superseded as a phase-static verdict: it evaluated a
Home-to-endpoint transition and allowed generic static collision diagnostics to
be an independent veto. The repaired r4-only runner reuses exactly its 21 saved
vectors and issues only correlated drilling `static_state` requests; generic
contacts remain evidence, and authoritative phase-aware static validation alone
can admit a candidate after unchanged residual/bounds checks.

Corrective runtime attempts r1–r3 all stopped before a candidate request or new
evidence JSON. r1 exposed an ordinary raw state/readback acknowledgement route;
r2 exposed absent offline Task Home; r3 exposed a deferred-audit guide-ID lookup.
The narrow r3 repair is source-verified (79 focused tests), but the required
three-attempt causal ceiling is reached. The corrected P3 endpoint status is
**INCONCLUSIVE; accepted/rejected/unknown counts are not measured**. Do not run
P4 or a fourth revalidation batch without a fresh explicit ceiling exception.

## 2026-09-16 P3 terminal execution record

The authorized repaired r4-only endpoint batch
`c1-p3-fdi31-20260916-r4-r4` completed the P3 contract: exactly 128
premanifested J1–J5 seeds, 21 native position-axis IK convergences, six unique
solution clusters and zero generic-static/phase-static admissible Target
states. Its fresh `endpoint_candidates.json` SHA-256 is
`8853f5208a87368d2611fd9c0a9737fe6b73b4da43de6851f4a428adee3f734c`; the
result is **NO VALID TARGET SOLUTION FOUND IN BOUNDED SEARCH**. All converged
candidates were rejected by the existing phase guard because the provisional
tip left the approved Entry-to-Target corridor. The generic static records also
retain Template↔visual-spindle and target-tooth↔burr contacts.

No planning request, preview, joint command, controller/hardware motion,
spindle/patient action or historical-data input occurred; the owned temporary
stack left no matching process. This is a bounded-search discriminator, not a
claim of global, physical or clinical infeasibility. In accordance with P3,
Campaign 1 stops here: P4 is NOT RUN, no more seeds/solver tuning or
geometry/policy alteration is authorized, and the next action is the required
architectural review for any later campaign.

## 2026-09-15 revised active-execution supersession

The operator has superseded this document's former design-only/Astra wording
and authorized Campaign 1 implementation under the existing
`S6-REUSABLE-CASE-SETUP` and `S6-LIVE-01..04` owners. The main orchestrator
owns diagnosis, gate decisions, runtime resources, evidence review and
controlled records; one `gpt-5.6-luna` worker at `max` may implement only a
fully specified, bounded work order. The worker neither changes a hypothesis,
geometry/policy invariant, acceptance decision nor delegates recursively.

The active outcome is a **new corrected FDI31 case**, never a retroactive
claim about r7/r13. It uses the 15 September opening/landmark/robot-base
foundation, the operator-specified latest saved FDI31 trajectory (approximately
5.2394 mm), and supports FDI42/FDI41/FDI32/FDI33. The 1.0 mm robot-dock bores
and the distinct >=2.0 mm trajectory-guide/channel floor remain fixed. P0 must
bind the rounded trajectory statement to an exact saved artifact, IDs and
coordinates before construction; it must not invent or regenerate a trajectory
to fill a missing identity.

Gates now continue, after their stated acceptance, from A (corrected production
case and scene) through B (bounded endpoint/insertion feasibility), C (approach
and production-path reconciliation), and D (guarded insertion, withdrawal,
Task Home and fresh-process reproduction). Hardware/controller connection,
physical motion, powered spindle, patient action, collision-policy relaxation
and changes to frozen anatomy, base, TCP, limits, depth or tool geometry remain
forbidden. The three-failure causal ceiling and serialized Slicer/ROS/MoveIt
runtime lane remain in force. Historical sections below retain their evidence
but are superseded where they limit Campaign 1 to design-only or P4.

Issued 2026-09-14. **DESIGN ONLY; no execution authorized by this document.**
Owner: existing `S6-REUSABLE-CASE-SETUP` and `S6-LIVE-01..04`; upstream
dependencies remain `W5-U-04`, `S3-P0-DENTAL-SEMANTICS`,
`S4A-PULP-ENDPOINT` and the existing crown/support/fit review owners.
This is a campaign contract, not a new task or pending-work queue.

## 1. Scope, roles and supersession

The operator accepts the preceding causal review and requests Astra architecture
with one Luna Max implementation owner. Astra specifies gates and reviews the
result; Luna performs source inspection, scoped implementation, tests, tooling,
approved offline runtime, evidence collection and development-document updates.
No recursive delegation, mandatory extra reviewer, model auto-switch, hardware
motion, powered spindle, patient action, dependency upgrade, external sync or
publication. Select the actual Luna model/Max effort explicitly at handoff;
role names in Markdown do not switch a running model.

This campaign-specific instruction supersedes coordinator-only document editing
and stop-after-every-packet implementation rules for this scope. Luna may fix
directly evidenced, unambiguous defects within the owned files and approved
execution envelope. Astra retains architectural and final acceptance; the
operator retains anatomical, task-intent and physical decisions. The repository
three-failure ceiling, runtime ownership, approval and manual review rules remain.
The orchestration skill's Sol-only primary-model and automatic parent-rerun
requirements do not override these operator/project instructions. Astra reviews
actual diffs and decisive artifacts; it does not duplicate Luna's routine runs.

**Campaign 1 ends at P3's unresolved negative result or, after P3 passes, P4's
insertion result.** P5–P7 are later-campaign skeletons, not authorized code or
runtime work. An earlier architectural/invariant blocker also ends Campaign 1.
User review pauses are not automatically architecture failures; Luna can finish
independent audit/instrumentation work but cannot pass the dependent gate.

Logical order:

`scene correctness -> endpoint -> insertion -> approach -> integration -> full cycle`

Implementation order:

`P0 -> P1 audit + P2 supporting diagnostics -> P1/P2 accepted -> P3 -> P4 -> R1`

P2 may repair the evidence needed to finish P1; that is not permission to test
endpoint/insertion feasibility in an unreviewed scene. Consistency checks apply
at every gate; the final integration gate is not a reason to defer obvious
scene/state disagreements.

## 2. Frozen invariants

Preserve the source package bytes, anatomy/masks and selected FDI31 tooth;
Entry/Target coordinates, clinical intent, trajectory axis and requested depth;
source and generated-case identities; guide/support/dock parameters and actual
geometry; burr/spindle physical assets and dimensions; URDF/SRDF kinematic
structure, joint limits, TCP definition and base/opening matrices; RAS/mm and
base_link/metre conventions; active collision/clearance/phase authorization;
J1–J5 planning and J6 excluded/zero compatibility state. Preserve the independent
final guard and all existing staleness/identity checks.

### Gate-A execution result — 2026-09-15

Run `c1-gatea-fdi31-20260915-r1` is the single explicitly authorized offline
Slicer construction/reopen attempt for this revision. Its preflight passed: the
isolated `dentobot-slicerros2` container had no active Slicer/ROS/MoveIt owner,
the new output directories did not exist, and the source package and exact MRB
matched their locked hashes. The runner loaded the 15-September source but
stopped at the existing `getTrajectorySummary(trajectory)["isValid"]` assertion
after constructing the saved-trajectory line. It emitted
`STAGE6_TARGET_FAIL FDI31 {"error": "FDI31 trajectory is invalid"}` and
`STAGE6_TARGET_GENERATION_COMPLETE {"passed": 0, ...}`.

This is **Gate A: FAIL / shared construction blocker**, not evidence about
endpoint, insertion, approach, guard, withdrawal, Home, planner feasibility or
hardware. No case, STL or reopen PASS was created; no such downstream path ran.
The retained evidence is the runtime log SHA-256
`ea00448978da1b09b2c47577fd9f4bb77c1bb86d5325173d3f7d0be81f738270`, FAIL
diagnostic SHA-256
`d505726dd6647906190baa06d8a7c861d90dae36436c41cf6f1e38a1f43f2295`, and the
failure UI/viewport screenshots in the versioned run directory. Preserve this
first failure and do not retry Gate A or enter Gates B–D. The next action is a
minimal source-level diagnosis of the trajectory-state mismatch plus a focused
regression check, followed by a separately approved runtime attempt if needed.

### Revised Gate-A r4 completion — 2026-09-15

The operator approved the exception plan after r1-r3: map the immutable
closed-scene FDI31 points through the already frozen lower-jaw Case Foundation
matrix exactly once, assert the two accepted points and their bounds before
template construction, and rerun the same isolated construction/save/reopen
scope only. Run `c1-gatea-fdi31-20260915-r4` passed its container/process/hash
and fresh-output preflight. It emitted `STAGE6_TARGET_PASS FDI31` and
`STAGE6_TARGET_GENERATION_COMPLETE {"passed": 1, ...}`, then wrote and reopened
`FDI31-step5c.dentocase` (SHA-256
`6235772e2d14d74b5417d42ed8a5e0cc160df82c77efd3014f589dd264113d83`). Its PASS
diagnostic (SHA-256
`5cda22b0eb98f71e9d3b04ab78c38f9e07c70da29fcb411952e5ed22b9d95f7b`) records
the unchanged 5.239400689721231-mm saved path, two bounded planning-frame
points, FDI42/41/32/33 support selection, a current/verified reopened branch,
and an STL SHA-256 `5e0b3c3ff4e6491daa591108ab94fc646f61b35a0fdf01051bffa6fa69fd085e`.
Post-reopen UI and viewport captures are retained in the versioned run
directory.

The outer Slicer command returned exit 1 during shutdown after producing those
PASS artifacts, with nonfatal ROS2Tests package-install and VTK leak warnings.
This is a process-health warning, not a functional construction/reopen failure.
Therefore **Gate A is PASS for corrected-case construction/save/reopen only**.
It is not evidence about endpoint, insertion, approach, final guard,
withdrawal/Home, fresh-process full-cycle reproduction, physical/clinical fit,
robot motion or hardware. Gates B-D and every other tooth remain NOT RUN and
need separate authorization and acceptance.

Correctly applying an already frozen transform once, fixing a serializer that
misreads an existing field, or removing a proven duplicate transient publication
is permitted software correction. Changing a transform value, source mesh,
segmentation, guide design, TCP offset, tolerance or allowed-collision entry is
not. If correction has competing geometric interpretations, stop before editing.
No smoothing, shrinkage, padding removal, depth cap, omitted obstacle or changed
target may be used to manufacture PASS.

Use diagnostic copies and new versioned outputs; never overwrite source/r7/r13
or r14 evidence. Diagnostic snapshots/ghost robots must not restore executable
routes, live ROS validity or operator approval. No joint commands, ExecuteTrajectory
actions or real controller connection in Campaign 1, including static probes.

Five task constraints (position three, axis two) approximately consume five DOFs.
Unspecified task roll is not an independently controllable housing-roll joint.
Explore realizable configurations, not an artificial roll sweep or added J6 DOF.

## 3. Baseline to freeze in P0

All data paths below are relative to `/home/light-tarun/dentobot`.

| Retained item | Identity / boundary |
|---|---|
| Source | `data/Slicer_Saved/SampleStudy1/dentobot-case-13sept.dentocase`; SHA-256 `14ed8f0f61e0c791d31b9582af7969ce712dd80d8cfd3a33319da63d0a996a15` |
| r7 case | `data/Slicer_Saved/SampleStudy1/FDI31/packet-a-fdi31-20260914-r7/FDI31-step5c.dentocase`; SHA-256 `5440961c2f1464df10adec3dfb6ab59f8a0e412c6074cae7852d8150babe4fff` |
| r7 STL | Same r7 directory, `DENTO_Final_Printable_Template.stl`; SHA-256 `ccee3e597b7e579b0224a31f00308ee1d65e42bc89ac17bf9c68330d5bcec425` |
| r13 diagnostic | `data/Slicer_Saved/SampleStudy1/FDI31/packet-a-fdi31-r7/diagnostics/FDI31-stage6-exact-r13.json` (different directory spelling is intentional) |
| r13 runtime | `/tmp/dentobot-verification/step65-fdi31-20260914-r13/runtime.log` |
| r14 probe | `/tmp/dentobot-verification/step65-fdi31-20260914-r14-runtime.log`, stdout marker `DENTOBOT_ENDPOINT_POSITION_AXIS_DIAGNOSTIC`; requested sidecar was overwritten by planner fallthrough and is not authoritative probe evidence |
| Historical source HEAD | `3af2d864449679ead25398899d6325aefb50595a`; HEAD alone does not identify the dirty sources/binary used |
| Task | TCP `dentobot_drill_tcp`; depth `6.67190493116205 mm`; exact coordinates and joint vectors come from r13, not rounded prose |
| Rejection | Stage-3 local index 27; composed index 255/direct and 295/seeded; selected tooth versus `pneumatic_spindle-Copy` |
| Earlier search | 22 seeds, eight kinematic solutions, no admissible alternate evidenced; not exhaustive IK or infeasibility proof |

P0 verifies availability and hashes rather than asserting they are current.
Copy available ephemeral logs into the durable campaign evidence directory with
provenance; missing files remain missing, never recreated from recollection.
Do not run a full failure reproduction simply to re-establish this baseline.

## 4. Results and exact acceptance contract

Use separate fields so execution, evidence and the requested decision cannot be
confused:

- `execution`: `NOT_TESTED`, `COMPLETED`, `INTERRUPTED` (`NOT_RUN` is the legacy alias).
- `result`: `PASS`, `FAIL`, `INCONCLUSIVE`, `NO VALID SOLUTION FOUND`.
- `decision`: `CONTINUE`, `USER DECISION REQUIRED`,
  `ASTRA ARCHITECTURAL REVIEW REQUIRED`.
- `contact_class`: `HOUSING_CLEAR`, `AUTHORIZED_GUIDE_WARNING`,
  `FORBIDDEN_CONTACT`, `UNKNOWN`.
- `evidence_level`: static/unit/native/sampled-insertion/integration/full-cycle/
  operator-reviewed, scoped to case/revision.

FAIL means a specific evaluated state/output violates a requirement. It does
not prove the entire task infeasible. INCONCLUSIVE means evidence or coverage
cannot decide. A completed bounded search with zero valid endpoints must say
**NO VALID TARGET SOLUTION FOUND IN BOUNDED SEARCH**. An interrupted search is
INCONCLUSIVE, not a completed negative experiment. Untested gates have no PASS.

| Gate | PASS requires |
|---|---|
| Scene | All relevant input selections, source-to-derived geometry, units, transforms, joint ordering, TCP, robot collision assets, object ownership, native scene contents and active policy reconcile. Native snapshot acknowledgement, not publication success alone. Deterministic saved fields use existing serialization tolerances; mesh conversions use documented precision, with numerical residuals. No invented clinical tolerance. Operator reviews each decision-critical input/output; unreviewed anatomy/Entry/dock is not silently accepted. |
| Endpoint | At least one J1–J5 configuration at the unchanged Target has TCP error <=0.25 mm and axis error <=0.5 degrees (the existing runtime limits; any contrary active value is escalated), joint bounds satisfied, and native static phase validity PASS for all self/world/clearance rules. This does not establish path or execution validity. |
| Insertion | At least one continuous joint interpolation over the complete unchanged Entry–Target task passes endpoint, axis/corridor, bounds, continuity, progress and phase-aware native edge checks at the recorded resolution; every forbidden pair fails. Report this as sampled simulation validation, not mathematical swept-volume proof. |
| Approach | From the actual validated start state, a complete Stage-1 route reaches a PreEntry configuration connected by a valid Stage 2 to the chosen insertion Entry branch. No join jump, start substitution or unrelated IK branch. |
| Guard consistency | The same named joint vectors and interpolated edges, scene content/version, task/phase/policy and timestamps receive consistent geometric verdicts; final guard additionally enforces session, sequence, current start, progress and staleness. Explain intentional differences between kinematic candidates and guarded acceptance. |
| Full workflow | Reviewed source/preparation/save/reopen and fresh Step 6 reconstruction, then actual simulated start/Home -> PreEntry -> Entry -> Target (the drilling insertion) -> guarded axial withdrawal to Entry/PreEntry -> guarded Home. Target is not reached and then inserted again. Ordered acknowledgements, endpoint, full-chain checks and shutdown result retained. No hardware action. |
| Reproduction | Close the first process; open the exact saved output in a fresh process, reconstruct and revalidate live scene/start/guard, freshly plan and repeat the complete guarded cycle. Saved trajectories are diagnostic replay data, never restored execution authority. Geometry/task identity and acceptance repeat; stochastic Stage-1 waypoints need not be byte-identical. |

Report existing burr–target authorization and every bounded spindle–guide
warning separately. A policy-valid warning route is not housing-clear, clinically
safe or physically validated. Retain existing margin thresholds, including
non-contact clearance rules; absence of penetration alone is insufficient.
Operator acceptance of simulation inputs does not replace clinical/physical fit.

## 5. Luna ownership and implementation limits

Checkout: `/home/light-tarun/dentobot/ros2_ws/src/DentoBot`.
Read-only inspection may follow callers throughout this checkout and the sibling
`../slicer_ros2_module`; exclude engineer-owned records. Read the routing docs,
all backlog matches and linked contracts first. Use the live overlay Graphify
graph for navigation and inspect authoritative source before editing.

Potential **owned production files**, only for the phase purposes below:

- `DENTOWorkflow/Resources/Python/DENTORobotWorkflowFacade.py`
- `DENTOWorkflow/Resources/Python/DENTOROS2Bridge.py`
- `DENTOWorkflow/Resources/Python/DENTOStep6State.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/logic_case_bundle.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/logic_case_foundation.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/logic_robot_scene_sync.py`
- `DENTOWorkflow/Resources/Python/dentobot_workflow/logic_step6_scene.py`
- `dentobot_moveit_config/src/collision_guard.cpp`

Potential **owned tests/tooling**:

- Existing `Testing/run_dentobot_step6_geometry_diagnostic.py`,
  `run_dentobot_step65_exact_case_smoke.py`, `run_dentobot_case_reopen_diagnostic.py`,
  `run_dentobot_phase_guard_smoke.py`, and `run_dentobot_stage6_target_generation.py`.
- Existing `Testing/test_ros2_bridge.py`, `test_step6_state.py`,
  `test_robot_workflow_facade.py`, `test_step6_planning.py`, `test_case_bundle.py`.
- If existing entrypoints cannot cleanly stop at the selected diagnostic mode,
  one `Testing/run_dentobot_fdi31_recovery_diagnostic.py` and one focused
  `Testing/test_fdi31_recovery_diagnostic.py`; reuse existing helpers. No framework.
- `Testing/verification_matrix.json`: only actual recovery checks, resource/
  approval/dependency/timeout/marker metadata; no automatic whole-suite runner.

Luna records its exact selected write set before each edit; this list is an
allowlist, not a request to modify every file. Guard code changes are additive
diagnostics/read-only exposure of the existing predicate, not policy changes.
P3/P4 production planner/IK algorithms are **read-only**; experiments live in
diagnostic tooling. SlicerROS2 native solver source, URDF/SRDF, meshes, OMPL YAML,
dependencies and build-system changes are not in the initial write set. If an
essential missing native API requires those edits, deliver its exact minimal
interface proposal to Astra rather than inventing a second FK/IK/collision stack.

Preserve public APIs and existing case schemas. Extend diagnostic JSON
additively with a schema-versioned recovery section; preserve old fields/readers
and label their semantics. No forced legacy rewrite. Audit every consumer of a
changed diagnostic field, not just the current runner.

Luna owns updates to the canonical recovery report, today's logbook, backlog,
TASKS, DEVELOPMENT_PLAN and DECISIONS for this campaign; update SETUP only for
an actual approved environment change, traceability/architecture for actual
changes. Do not alter general AGENTS policy. Pending work remains only in
backlog. Source-complete changes remain pending while acceptance is open.

## 6. Campaign 1 phase contracts

### P0 — baseline and evidence freeze

**Objective/tasks:** Inventory the retained baseline, current dirty checkout,
native binary/build identity, actual matrix/check entrypoints and case identities.
Write the invariant manifest, exact write set and execution envelope. Identify
all current automatic choices from retained reports without replaying generation.

**Files/allowed:** report, logs/manifests and scoped development-doc registration;
read-only source/config/case inspection. Capture HEAD, tracked diff and untracked
source hashes for DentoBot and relevant sibling source; distinguish original
baseline from current modifications. No reset, checkout, commit or source edit.

**Tests/evidence:** checksum and JSON/package structure inspection; `baseline.json`,
`invariants.json`, source-state/diff manifest, command/approval/resource plan,
report sections A/B/M. Retain r13/r14 raw evidence and limitations.

**PASS/abort/decision:** PASS when baseline identity is reproducible from the
record, missing evidence explicitly identified and required current inputs intact.
An optional lost log need not block if independent retained evidence suffices;
an unexplained case/hash discrepancy or missing essential state is INCONCLUSIVE
and USER/ASTRA decision, not a substitute case. PASS -> P1/P2. No baseline
full-planner rerun. Execution approval must precede tests/runtime in the envelope.

### P1 — upstream scene and automatic input/output audit

**Objective/tasks:** Build one sequential operator-readable audit, not a ceremonial
checklist. At each step show input IDs/hashes/values, selection rule and code
location, whether saved/automatic/manual, actual derived output, warning/fallback,
machine checks, visual, operator review record and downstream dependencies.

Audit in this order:

1. Source scan/masks, jaw, Foundation opening/base transforms, frame/unit chain,
   object ownership and duplicates; upper/lower transformations exactly once.
2. Tooth/pulp segment IDs, FDI/spatial association and all fallback rules.
3. Step 4A Entry heuristic, Target rule, direction/depth; geometric estimate is
   not anatomical approval. Preserve the actual intended endpoints.
4. Supports, guides and docking: dimensions, selected teeth, yaw/position,
   automatically supplied defaults and who actually confirmed them. Distinguish
   trajectory guide bore from robot-dock bore.
5. Steps 5A–5C support extraction, blockout, clearances/thickness, invalid-edge
   fallback and rebuilt surface, fusion/bore subtraction, final STL/geometry gates.
   A valid STL or open support-surface boundary count alone does not establish fit.
6. Save/reopen and Step 6: exact geometry/selection/TCP/collision-asset identity,
   derived transforms, padding and complete collision exceptions. Compare native
   acknowledged contents with displayed outgoing payloads; bounds alone are weak.

**Files/allowed:** audit/reopen/generation tooling and the allowlisted
case/foundation/scene-sync modules. Fix proven software bookkeeping/duplicate/
transform-application defects only when the frozen correct result is unambiguous.
Add diagnostic extraction/captures without regenerating or overwriting the source.
If a historical intermediate is missing, record missing evidence; a scratch
reconstruction must prove the same inputs/output identity and cannot replace it.

**Forbidden:** arbitrary anatomy/Entry/default/guide corrections; inferred user
signoff; dropping difficult obstacles; geometry/model edits to get clearance.

**Tests/evidence:** smallest focused failing test then fixed test; saved-copy
reconstruction where needed; `scene_audit.json`, transform/object manifests,
step-by-step audit visuals and a saved inspection scene free of live ROS handles.
Case field tolerances come from current contracts; unexpected precision/shape
disagreement is quantified, not dismissed by a new tolerance.

**PASS/abort/decision:** technical reconciliation plus recorded operator review
of each decision-critical step. Present gates in order and wait for the operator
at those review points; work on independent evidence tooling may continue.
No P3 on an unreviewed or failed scene. Straightforward fixes iterate within
budget, rerunning only the affected gate/dependents. Unclear scene, anatomy or
geometry requirement -> USER DECISION REQUIRED; competing software meaning ->
ASTRA REVIEW. Classification SCENE_CONSTRUCTION, TRANSFORM_OR_SCALE,
TOOL_COLLISION_MODEL, COLLISION_POLICY or UNKNOWN only as evidence supports.

### P2 — trustworthy state/contact diagnostics (supports P1)

**Objective/tasks:** Make the baseline explainable without an expensive planning
loop. Reuse saved joint states and native validation, with a ghost/display robot
only. A real joint command is not a diagnostic state-setting API.

- Separate `last_kinematically_valid`, `last_guard_accepted`,
  `first_guard_rejected` and `target_state`; retain `last_housing_clear` separately
  where observed. Guard-accepted can contain authorized guide warnings, so avoid
  implying geometrical clearance through the name `last_guard_clear`.
- Record stage/index/edge/interpolation fraction, joint names and SI units, FK
  pose and axis, axial depth, phase, guard acceptance and reason for every retained
  state. No prior accepted sample means null/unknown, not the rejected endpoint.
- Preserve native contact pair, position/frame/unit, penetration/native depth
  definition and sign convention, available normal and clearance; one contact
  point is not a pair of nearest points. Unavailable values stay null with reason.
  Record contact-count limits/truncation; do not claim an incomplete list exhaustive.
- Bind evaluated states to native object/shape/pose content, robot/TCP assets,
  policy/ACM, build/source and task fingerprints. Detect scene change during a
  probe. A matching publisher label without native acknowledgement is insufficient.
- Reconstruct context, Target, last accepted, first rejected, collision close-up/
  section and visible-versus-collision-payload views. Label reconstructions, depth
  and identifiers. Review nonblank content and correct framing; file existence or
  `capture_errors=[]` is not acceptance. No image-generation tool for evidence.
- Recover onset only from actual saved preceding states/edges or an explicitly
  labelled new bounded reconstruction. Report a bracket at the checked resolution,
  not an exact onset from a screenshot. Missing path stays unknown until P4.
- Ensure diagnostic entrypoints write atomically and return; no fallthrough into
  full planning after `slicer.util.exit`. Do not repeat the r14 sidecar overwrite.

**Files/allowed:** façade/bridge/state JSON plumbing, native collision_guard.cpp
additive result fields/read-only same-predicate access, owned diagnostic/tests.
Guard policy and geometric acceptance cannot change. Use one format/helper owner.

**Tests/evidence:** fixtures for first-sample rejection, preceding accepted state,
kinematic-versus-guard disagreement, authorized burr contact, guide warning versus
housing-clear, simultaneous authorized+forbidden contact, missing/truncated
contacts, stale scene, legacy field compatibility and no planner/command fallthrough.
One appropriate native guard check after C++ changes; no rebuild for Python-only
edits. Retain `rejected_state.json`, contact records, scene identity, images and
inspection scene. If current scene changed by a P1 correction, compare before/after
and never claim old r13 was captured contemporaneously in the new scene.

**PASS/abort/decision:** machine and visual evidence distinguish all four state
semantics and reproduce/explain the retained collision with matching identity, or
establish a specific scene-correction delta. Unexpected native disagreement,
missing essential API or unsupported attribution -> INCONCLUSIVE/ASTRA REVIEW.
Unknown optional nearest-point fields alone do not fail if native pair/contact and
verdict evidence are sufficient. P1 and P2 PASS -> P3 after gate authorization.

### P3 — bounded endpoint feasibility discriminator

**Objective:** Find an actual admissible configuration for the unchanged Target
position/tool axis, or characterize what the bounded search did not establish.

**Experiment contract:** one predeclared batch, at most **128 IK solves**.
Manifest exact seeds before solving. Use up to 32 distinct known anchors: current
validated simulation start/Home, retained r13/r14 solutions, existing candidate
states and deterministic one-joint lower/upper/centre perturbations within limits.
Fill remaining slots with a deterministic five-dimensional low-discrepancy
joint-limit sample (e.g. Halton bases 2/3/5/7/11, fixed indices). Reuse installed
utilities or a minimal stdlib implementation, no new dependency. Record all joint
ranges/topologies and seed provenance. Unknown/unbounded joint ranges require
resolution, not invented bounds. The batch has joint-space coverage, not a proof
of reaching every IK basin; no automatic second larger batch.

Use the existing native position-axis solver and existing tolerances/iteration
limits, position <=0.25 mm, axis <=0.5 degrees; record their active values. Solve
kinematics without generic collision rejection if needed, then evaluate **every
converged candidate** with authoritative phase-aware static native validity.
Generic `isStateColliding` with the wrong ACM cannot reject intended burr contact
on behalf of the task guard. No solver or tolerance tuning in this experiment.

Static endpoint validity must use the same geometric/clearance predicate as the
guard at terminal depth, including self/world collisions and the exact configured
exceptions. It must not send motion or pretend to have validated approach,
sequence/history or full guard session continuity. Report those as untested.

Retain all raw seed results. Validate candidates before deduplication. Deduplicate
postures using per-joint comparisons (revolute 1e-4 rad, prismatic 1e-5 m; these are
diagnostic clustering thresholds, not motion tolerances); wrap only genuinely
continuous joints. Preserve each raw candidate/contact outcome and any valid
representative within a cluster. Label clusters as candidate branches, not proved
topological branches or just different J5 values. Record near-singular solutions
using an appropriately unit-scaled Jacobian metric where available; no new
singularity acceptance rule.

Rank valid representatives deterministically: housing-clear before authorized
guide warning; measured clearance when comparable; normalized joint-bound margin;
residuals; lexicographic vector tie-break. Unknown clearance cannot outrank a known
positive value as infinity. Retain at most eight representatives for P4; preserve
all others and disclose truncation, not exhaustive branch coverage.

**Files/allowed:** diagnostic runner/tests/manifest/matrix/report only; reuse
existing IK/guard APIs. Production IK/path/policy is frozen. P2-level plumbing
bug fixes allowed only with an isolated reproducer and revalidation of P2.

**Tests/evidence:** deterministic seed manifest, joint-unit/topology and dedup
tests, mock no-motion/no-Stage1 checks, native endpoint batch in a stable reviewed
scene. `endpoint_candidates.json` retains every vector, residual, limits, allowed/
warning/forbidden contacts, policy result and cluster/seed provenance. Report
seeds planned/completed, convergence, TCP-valid, axis-valid, bound-valid,
guard-valid and unique-posture counts separately.

**PASS/abort/decision:** >=1 native admissible Target -> PASS -> conditional P4.
Zero after complete batch -> NO VALID TARGET SOLUTION FOUND IN BOUNDED SEARCH ->
ASTRA ARCHITECTURAL REVIEW REQUIRED; classification IK_COVERAGE versus
TASK_GEOMETRY_FEASIBILITY remains tentative unless stronger evidence separates
them. A runtime interruption, scene drift or broken solver/API -> INCONCLUSIVE,
not infeasibility. Stop before geometry changes, more seeds or a new solver.

### P4 — bounded insertion feasibility across retained Target candidates

**Entry condition:** P1/P2/P3 PASS, operator review complete and P4 execution
authorized. This is diagnostic task construction, not production branch selection.

For each retained valid Target representative (maximum eight), use the existing
position-axis solver in backward continuation Target -> Entry to determine an
Entry on that candidate branch. Keep the original drilling-axis direction while
walking positions backward. Reverse the recovered array and validate the actual
forward Entry -> Target motion with drilling phase/progression semantics; reverse
construction is not retraction validation.

Use uniform axial position samples at no more than 0.25 mm, or the stricter
existing task sampling limit. Seed each sample from the neighboring solved
configuration; no unrelated restart or branch jump masquerading as continuation.
At a failed/ambiguous edge allow deterministic midpoint subdivision to three
additional levels (0.03125 mm if starting at 0.25 mm), with **1024 total IK solves
across all retained candidates**, counted and recorded. Reaching the computation
cap leaves uncovered branches INCONCLUSIVE. Do not add a global branch graph,
beam search or alternative solver in Campaign 1.

Validate **edges**, not only Cartesian waypoints, using the native guard's actual
joint interpolation and no coarser guard resolution. Recompute FK at interpolated
states for axis/corridor/depth, joint bounds, progress and configured collision/
clearance rules. Do not assume a linear joint edge follows a Cartesian line.
Retain continuity/joint increments and existing jump limits; unresolved sharp
changes/singular behavior are reported, not accepted through a new tolerance.
Record minimum clearance only when the native metric and pair exclusions are
known. Finite sampling cannot prove absence of arbitrarily narrow contact.

**Files/allowed:** the same diagnostic runner, tests, JSON and visuals. Production
Stage-2/3 branch choice, IK, OMPL and all invariants remain unchanged.

**Tests/evidence:** neighbor-seeding/reversal/monotonic-depth tests, an edge with
valid endpoints but forbidden middle, stale-scene rejection and no-motion tests;
then one saved-scene insertion experiment. `insertion_branches.json` records each
Entry/Target, all evaluated joints/depths, interpolation/resolution, rejected edge/
fraction, last guard-accepted and housing-clear state, first rejected pair/contact,
clearance, near-singularity evidence and sample counts. Never call constructed
diagnostic joints an accepted executable plan.

**PASS/abort/decision:** one whole native-admissible insertion -> sampled PASS,
save its witness; all tested paths rejected -> NO VALID SOLUTION FOUND within
the stated continuation method (not all insertions impossible); incomplete
coverage -> INCONCLUSIVE. In **every** case end Campaign 1 with R1 and ASTRA
ARCHITECTURAL REVIEW REQUIRED. No automatic P5/P6/full-flow run.

## 7. Later phase skeletons — NOT Campaign-1 authorization

| Phase | Objective / likely owners | Allowed work only after later approval | Tests / artifacts / PASS | Abort, classification and next decision |
|---|---|---|---|---|
| P5 approach | Connect actual start through PreEntry/Stage 2 to a P4 witness; façade/bridge and existing MoveIt pipeline | Inspect start freshness, branch joins, scene, constraints and current RRTConnect first; later plan specifies bounded attempts and any config write set | Start/scene manifest, Stage-2 witness, Stage-1 attempts, full joint edges and guard output; PASS only complete connected approach | Invalid start -> scene/state gate; invalid Stage 2 -> insertion/construction review; no route -> NO VALID SOLUTION FOUND, not proven free-space infeasibility. Persistent supported search limitation -> Astra before OMPL changes. PASS -> integration after approved correction |
| P6 responsible-layer correction | Smallest evidence-backed fix; earliest failing subsystem owns it | If insertion limitation is demonstrated: consistent position-axis generation, multiple realizable candidates, whole-insertion screening/ranking, exact phase validity; retain final guard. If approach-only failure demonstrated: bounded existing-planner/pipeline evaluation. No new framework by default | Frozen-case A/B test plus known valid/forbidden regression; unchanged task/resources/policy; corrected gate and dependents pass | Competing architectures, changed invariants, guard-policy ambiguity, exhausted hypothesis -> Astra. P6 can precede P5 when fixing P4; numeric labels do not override dependencies |
| P7 integration/full cycle | Prove production reconstruction/planning, final guard, simulation and fresh-process repeat; existing exact/restore/phase-guard runners | Later-approved simulation-only sequence and acknowledgement/withdrawal/Home verification; no live validity restored from case | Candidate/native/final-guard comparison, complete approach/insertion/axial withdrawal/Home, save-close-reopen-reconstruct-replan-repeat, regression and cleanup record | First failure returns to earliest affected gate. A PASS marker with abnormal shutdown is reported separately. Full pass -> Astra final acceptance; physical/clinical acceptance remains separate |

Decision tree:

- A: no admissible Target -> inspect task/tool/scene evidence and IK coverage;
  Astra decides a stronger coverage experiment or a user-owned geometry question.
- B: Target valid, insertion not found -> inspect continuation/branch choices and
  Stage-2/3 construction. Do not start with OMPL. A stronger insertion experiment
  or small production correction requires its own Campaign-2 contract.
- C: valid insertion, approach unresolved -> P5 becomes justified; only then can
  free-space planner configuration/replacement become an evidence-backed question.
- D: a component claims geometric validity but identical states fail the final
  guard -> first compare scene/state/interpolation/policy. A kinematic candidate
  from collision-off construction is not a validity claim and is not this bug.
- E: a complete production route passes -> P7 approved simulated execution,
  withdrawal/Home and fresh-process reproduction; not hardware execution.

## 8. Execution approval, economy and bounded iterations

This response does not launch Luna or grant runtime approval. At P0 Luna proposes
one concrete Campaign-1 envelope, mapping existing matrix checks and any new
diagnostic command, exact case, modes, resources, timeouts and output paths. One
operator approval may cover its bounded P1–P4 checks conditionally on the gates;
do not request permission for every command. Manual input review and architecture
stops remain explicit. `AUTO-CONTINUE` below means eligibility to authorize the
next phase without Astra, not permission to run an unapproved phase.

Check ladder: read/hash/source -> focused pure test -> saved-scene reconstruction
-> endpoint static validity -> insertion replay -> later Stage 1 -> later
integration -> later full cycle. These are evidence levels, not eight mandatory
test runs; native endpoint/reconstruction may already require the pinned container.

Matrix reuse: `static.git_diff_check`, `static.step6_pycompile`,
`pure.step6_planning`, `pure.step6_restore`, and, only after relevant native edits,
`runtime.phase_guard`. New recovery modes need explicit entries/commands before
execution. `runtime.step65_exact_case`, target matrix, broad shell smoke and full
workflow are not Campaign-1 defaults. Avoid historical FDI31 matrix entry defaults
that point at a different package. Use the exact frozen case in every command.

Build only changed compiled owners; native guard uses the documented explicit
package base path. Do not rebuild SlicerROS2 for a Python edit or install/update
packages. Verify loaded source/binary identity. Serialize `dentobot-slicerros2`,
ROS domain 73, Slicer/MRML/display and install tree. Check for operator-owned
processes; do not kill or replace them. Reuse a stable reviewed session when safe;
before each probe compare native scene identity and prohibit editing/builds during
the run. Otherwise reload the diagnostic snapshot, not the entire generation flow.
Persist checkpoint artifacts and release resources before operator/architect waits.

A code bug may be fixed and its cheapest failing test rerun without Astra when
the cause and correct behavior are unambiguous and inside ownership. Preserve the
project ceiling of three failed executions of the same causal blocker across
turns; new names/parameters do not reset it. The already exhausted full-FDI31
retry history stays exhausted. A newly approved diagnostic protocol is a distinct
experiment, not another blind full-flow attempt. Individual rejected seeds/samples
in its fixed batch are observations, not 128 retry permissions. After a broken
harness run, repair only with evidence; stop at the ceiling or earlier for no new
information. Wall-clock timeouts protect resources; a timeout is INCONCLUSIVE,
not a feasibility verdict or sole architecture stopping rule.

## 9. Canonical R1 recovery report

Luna creates and maintains **one**
`Workspace/docs/diagnostics/FDI31_PLANNER_RECOVERY_REPORT.md`.
This design document stays the specification; the report is the evidence handoff.
Do not create another task queue. R1/R2 sections may be preserved within that
report with immutable revision-specific artifact links; never replace old results.

Durable artifacts:
`/home/light-tarun/dentobot/data/Slicer_Saved/SampleStudy1/FDI31/planner-recovery/c1/<run-id>/`.
Temporary execution logs may begin under `/tmp/dentobot-verification/<run-id>/`
but copy all decisive evidence to the durable directory before handoff. Use plain
JSON and existing diagnostics; no database/dashboard/schema framework. Link all
artifacts by relative path plus SHA; keep large joints/logs out of prose/context.

Required report sections:

| Section | Required content |
|---|---|
| A Executive | Overall result/decision, furthest proven and blocking gates, evidence-supported class/confidence, whether another bounded Luna attempt is justified, precise review required |
| B Baseline | Immutable source/r7/r13/r14 identities, exact trajectory/Entry/Target/depth, robot/TCP/dimensions/base, pair/stage/state, baseline HEAD + dirty diff/binary identity; missing evidence and old sidecar caveat |
| C Changes | File/function, diagnostics-only versus behavioral, reason/hypothesis, before/after evidence, tests, and unchanged invariants |
| D Gates | Scene, endpoint, insertion, approach, consistency and full workflow: execution/result/decision, exact test/input, quantitative evidence, artifact/hash, interpretation; untested gates explicit |
| E Endpoint | Planned/completed seed counts, IK/TCP/axis/limit/guard counts, unique posture clusters versus proved branches, coverage/solver limits, all vectors and contacts, rejection reasons and raw-to-dedup mapping |
| F Insertion | Each retained candidate: Entry/Target, actual joints/edges, method/resolution/count, continuity/singularity metrics, accepted/rejected states and depth, pair/contact and available clearance; uncovered candidates explicit |
| G Approach | Only if later authorized: actual start, chosen insertion/PreEntry/Entry, pipeline/planner/settings/seeds/attempts/path/joins and rejection; otherwise NOT_TESTED |
| H Guard consistency | Per-state/edge geometric candidate checks versus native static and full guard results; policy, sequence, interpolation, scene/build identity, differences and whether final guard was actually exercised |
| I Visuals | Context, Target, last accepted/housing-clear, first rejected, close-up/section, display/collision payload comparison; exact reconstructed-state ID, scene SHA and useful inspection scene |
| J Tests | Exact command/script/config, input hashes, result/marker/exit, duration, native/source identity, cleanup and artifact; expensive tests deliberately skipped and why |
| K Classification | Evidence-supported SCENE_CONSTRUCTION, TRANSFORM_OR_SCALE, TOOL_COLLISION_MODEL, TASK_GEOMETRY_FEASIBILITY, IK_COVERAGE, IK_BRANCH_SELECTION, INSERTION_CONSTRUCTION, FREE_SPACE_PLANNER, COLLISION_POLICY, PLANNER_GUARD_INCONSISTENCY or UNKNOWN; confidence and competing explanations |
| L Failed approaches | Intervention/hypothesis, observation, why unsuccessful/rejected/reverted, evidence link and retry count; mandatory, including earlier relevant r13/r14 limitations |
| M Repository | Both relevant source revisions, baseline/current dirty diff summaries and hashes, exact modified/untracked files, new tools, runtime/build identity, temporary artifacts and ownership-specific cleanup/revert recommendation; no automatic reverts |
| N Recommendation | One bounded next action or ASTRA ARCHITECTURAL REVIEW REQUIRED with exact question, alternatives and deciding evidence; no independently invented next architecture |

Each JSON artifact records schema version, run/phase/case/task/scene IDs, source/
binary/policy hashes, command/parameters, frames/units, status and completeness.
Use null plus reason for unavailable evidence; never zero as unknown clearance.
Publish report/artifacts atomically; ensure linked files exist and hash correctly.

## 10. Copy-ready Luna prompts

Use one Luna Max thread. Send the bootstrap once, then only the relevant phase
prompt. A resumed/fresh thread reads this specification and canonical report,
not a pasted history. No new agent is launched by these prompts appearing here.

### Bootstrap — paste once

```text
LUNA MAX — FDI31 RECOVERY CAMPAIGN 1
OBJECTIVE: Execute only the authorized phase of the campaign specification at
Workspace/docs/diagnostics/FDI31_PLANNER_RECOVERY_CAMPAIGN_1.md in checkout
/home/light-tarun/dentobot/ros2_ws/src/DentoBot. Read it completely. Follow
AGENTS, backlog-first matching owners, routed context, verification protocol
and checkout Testing/verification_matrix.json. Do not request repeated history.
FILES/OWNERSHIP: You are the sole campaign implementer and development-document
editor under the operator's campaign-specific override. Use the specification's
exact phase allowlist; record the selected write set. Preserve concurrent/pre-existing
changes. No recursive delegation, unrelated refactor or other-tooth run.
INTERFACES: Reuse existing MRML, facade, bridge, native IK/guard and diagnostic
schemas. Additive diagnostic compatibility only; no parallel FK/IK/collision stack.
CONSTRAINTS: All section-2 invariants apply. No motion commands, hardware,
geometry/intent/tolerance/policy changes or OMPL rewrite. Do not implement P5-P7.
VERIFICATION: Cheapest matrix-routed check first; run only within the explicitly
approved execution envelope, preserve retries and serialize runtime resources.
RETURN: Maintain FDI31_PLANNER_RECOVERY_REPORT.md and durable hashed artifacts
per sections 9/11. Send short gate deltas; on a campaign stop send the prescribed
Astra review request. Evidence PASS is not operator or full-cycle acceptance.
Begin only the phase explicitly assigned next.
```

### P0 prompt

```text
CURRENT PHASE P0 — BASELINE FREEZE
OBJECTIVE: Freeze r7/r13/r14 and current source/build identities without rerunning
the planner. Inspect campaign section 3, retained reports, dirty checkout and
matrix entrypoints; verify essential hashes/fields and previous failure history.
FILES: Documentation and evidence manifests only; production is read-only.
INTERFACES/CONSTRAINTS: Preserve all frozen inputs and existing work. Missing
evidence is explicit; do not substitute packages, reset Git or reconstruct facts.
VERIFICATION: Read-only package/hash/JSON checks. Produce baseline.json,
invariants.json, initial canonical report and one concrete P1-P4 execution envelope
with commands/modes, case, resources, bounds, output paths and approval classes.
ACCEPT: Essential baseline is attributable and intact; every uncertainty recorded.
STOP: Essential identity mismatch -> USER DECISION REQUIRED or ASTRA REVIEW.
No runtime or implementation begins without the applicable authorization.
EXPECTED OUTPUT: Baseline manifest/report links, selected write set, execution
envelope and concise P0 result. CONTINUATION: AUTO-CONTINUE on PASS and approved
next scope; no Astra needed for routine P1/P2 handoff.
```

### P1 prompt

```text
CURRENT PHASE P1 — INPUT/OUTPUT AND SCENE AUDIT
OBJECTIVE: Audit all six upstream groups in section 6/P1 in order; expose actual
automatic choices, outputs, fallbacks and confirmation provenance. Use the frozen
case, not a new generated task. P2 may supply missing audit instrumentation.
FILES: Owned audit/reopen tooling and allowlisted case/Foundation/scene-sync files.
ALLOWED: Diagnose and fix only proven software representation/ownership defects
whose correct frozen result is unambiguous; add the smallest regression test.
FORBIDDEN: Anatomy, target, base values, tool/guide geometry, policy or tolerance
changes; inferred user signoff; hidden/omitted collision objects to obtain PASS.
VERIFICATION: Focused source/unit checks then approved saved-scene reconstruction;
compare native acknowledged geometry/transforms/policy to source and display.
EVIDENCE: scene_audit.json, per-step input/output/provenance/metrics, useful images
and inspection scene, exact changes/tests and operator decisions in the report.
ACCEPT: Technical scene agreement AND decision-critical operator review recorded.
STOP: Unclear anatomy/geometry -> USER DECISION REQUIRED; ambiguous software
meaning/exhausted fixes -> ASTRA REVIEW. While awaiting review, only independent
audit/P2 work may continue; no P3. EXPECTED OUTPUT: Ordered review gates and scene
verdict. CONTINUATION: ASTRA REVIEW IF BLOCKED; PASS enables P3 only with P2 PASS
and its approved execution scope. Routine operator signoff does not require Astra.
```

### P2 prompt

```text
CURRENT PHASE P2 — DIAGNOSTIC TRUST
OBJECTIVE: Repair state/contact/scene evidence per section 6/P2 and explain r13
with saved-state reconstruction, not repeated full planning. Inspect every caller
of the legacy last_valid field and native forbidden-contact result path.
FILES: Owned diagnostic tests/tooling, facade/bridge/state diagnostic plumbing,
collision_guard.cpp additive telemetry/read-only existing-predicate access only.
INTERFACES: Preserve legacy readers; separate kinematic-valid, guard-accepted,
housing-clear and first-rejected states, with unknown explicit and schema version.
FORBIDDEN: Acceptance-policy/geometry/planner changes, joint-command publication,
or treating zero-length endpoint validation as full sequence validity.
VERIFICATION: Focused semantic/legacy/no-fallthrough/known-contact/edge/stale-scene
tests; smallest native build/check only if needed, then approved reconstruction.
EVIDENCE: Native pair/position/depth/frame where available, state/depth/resolution,
scene/build/policy identity, correct nonblank views and rotatable inspection scene.
ACCEPT: Trustworthy distinct fields and attributable visual/native verdicts;
no post-exit planner fallthrough or overwritten prior artifact.
STOP: Essential native API absent, unresolved display/native disagreement,
policy ambiguity or retry ceiling -> ASTRA REVIEW. EXPECTED OUTPUT: Diagnostic
diff/tests, rejected-state packet and limitations. CONTINUATION: ASTRA REVIEW IF
BLOCKED; successful P1+P2 enable the authorized P3 experiment without redesign.
```

### P3 prompt

```text
CURRENT PHASE P3 — ENDPOINT FEASIBILITY
OBJECTIVE: Only after P1/P2 PASS and operator review, run the section-6/P3
experiment: <=128 premanifested deterministic seeds, known anchors plus joint-space
coverage; existing native position-axis IK, unchanged Target and tolerances.
FILES: Owned diagnostic runner/tests/matrix/report only; production solver/path
selection and invariants are read-only. Reuse phase-aware native static validity.
CONSTRAINTS: J1-J5 only, no arbitrary housing-roll/J6 sweep, no generic ACM rejection
of authorized burr contact, no more seeds/solver tuning after this bounded batch.
VERIFICATION: Seed/dedup/unit/no-motion tests, then one authorized stable-scene
native batch. Validate every converged candidate before deduplication.
EVIDENCE: All seed/joint vectors, TCP/axis errors, limits, native contacts and
verdicts, candidate clusters, coverage/completeness; retain <=8 valid representatives
for P4 while preserving all raw candidates and rejected attempts.
ACCEPT: At least one endpoint satisfies all section-4 endpoint criteria.
STOP: Completed batch with zero -> NO VALID TARGET SOLUTION FOUND IN BOUNDED SEARCH,
ASTRA ARCHITECTURAL REVIEW REQUIRED. Interrupted/stale experiment -> INCONCLUSIVE.
EXPECTED OUTPUT: Endpoint witness(es) or R1 negative/uncertain evidence packet.
CONTINUATION: ASTRA REVIEW IF BLOCKED; PASS enables the explicitly authorized P4
diagnostic, never P5 or a production planner rewrite.
```

### P4 prompt — conditional diagnostic only

```text
CURRENT PHASE P4 — INSERTION FEASIBILITY
OBJECTIVE: Only with P1/P2/P3 PASS, test complete insertion for <=8 retained valid
Target candidates using section-6/P4: neighboring-seeded Target-to-Entry construction,
then forward Entry-to-Target native phase/edge validation, same fixed drilling axis.
FILES: Owned diagnostic runner/tests/evidence only. No production branch-selection,
IK or OMPL edits, invariant changes, random restarts or motion commands.
VERIFICATION: Tests for reversal/progression/continuity and colliding edge interiors;
approved saved-scene native experiment. Max axial step 0.25 mm or stricter existing
limit; <=3 midpoint refinements; <=1024 total IK solves. Preserve all counts and
native joint-edge resolution; incomplete coverage is INCONCLUSIVE.
EVIDENCE: Per-candidate Entry/Target and joints, all edges, depth/resolution/counts,
continuity/singularity indicators, guard-accepted/housing-clear/rejected states,
pair/contact/clearance and usable views; immutable witness path if found.
ACCEPT: At least one complete insertion passes section-4 sampled acceptance.
STOP: After this experiment regardless of outcome; finish R1, verify report links
and cleanup, and emit ASTRA ARCHITECTURAL REVIEW REQUIRED. No Stage-1/OMPL/full-flow
attempt. EXPECTED OUTPUT: Insertion witness or bounded failure plus complete R1.
CONTINUATION: ASTRA REVIEW REQUIRED even on PASS because Campaign 1 is complete.
```

## 11. Astra escalation and paste-back

Stop for an architecture choice, exhausted bounded strategy or three-failure
ceiling; required invariant change; suspected physical/task infeasibility; major
planner replacement; collision-policy/intent conflict; materially invalidated
assumptions; unresolved native/display/guard disagreement; essential out-of-scope
API; or completion of P4. Package evidence before stopping. Routine deterministic
implementation defects and operator visual signoffs need not consume Astra.

```text
ASTRA REVIEW REQUEST — FDI31 PLANNER RECOVERY
Previous Astra campaign: Campaign 1
Report revision / evidence manifest SHA:
Overall state:
Furthest proven gate:
Blocking gate:
Leading classification / competing explanation:
Confidence and evidence limit:
Architectural review required: YES/NO

What changed:
[Small diff summary; diagnostics-only vs behavioral; invariants unchanged/exception]
Strongest new evidence:
[Decision-relevant facts, metrics, witness IDs; not log excerpts]

Scene correctness: PASS/FAIL/INCONCLUSIVE
[Technical + operator review evidence]
Endpoint feasibility: PASS/FAIL/NO VALID SOLUTION FOUND/INCONCLUSIVE/NOT TESTED
[Seeds completed/planned; kinematic, bound and guard-valid counts; unique clusters]
Insertion feasibility: PASS/FAIL/NO VALID SOLUTION FOUND/INCONCLUSIVE/NOT TESTED
[Candidates; interpolation/coverage; witness or first rejected depth/pair]
Approach feasibility: PASS/FAIL/INCONCLUSIVE/NOT TESTED
[Evidence or deliberately outside Campaign 1]
Guard consistency: PASS/FAIL/INCONCLUSIVE/NOT TESTED
[Static verdicts vs full guard actually tested; scene/policy/edge identity]
Full workflow / fresh-process repeat: PASS/FAIL/INCONCLUSIVE/NOT TESTED
Housing-clear or authorized-guide-warning result:

Current blocker:
[One precise assertion without claiming exhaustive infeasibility]
What Luna already tried:
[Hypothesis -> result -> why not repeat; retry counts]
Architectural question for Astra:
[Specific alternatives and evidence that would decide; proposed bounded next action]

Detailed evidence:
Canonical report: Workspace/docs/diagnostics/FDI31_PLANNER_RECOVERY_REPORT.md
Machine-readable diagnostics: [durable paths + hashes]
Screenshots / inspection scene: [paths + state IDs]
Relevant source changes: [diff / exact files / current+baseline revisions]
Runtime ownership and cleanup: [status; anything left running and whose]
```

Keep this paste-back approximately one page; the canonical report carries the
detail. Fresh Astra reviews actual decisive artifacts and scoped diffs, challenges
classification/coverage, and issues a separate Campaign 2 if justified. No automatic
architect/model loop or background campaign is created.

Final effort outcome is either reproducible full simulation acceptance or an
operator-reviewed task/mechanical decision supported by the evidence. A bounded
negative search alone is neither proof of infeasibility nor full task completion.
